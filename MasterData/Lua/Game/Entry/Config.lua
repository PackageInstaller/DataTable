---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yajieli.
--- DateTime: 2019-08-16 15:41
---
--
--local kTool = CS.Engine.Lib.KTool
--local isShare = kTool.IsMobile() or CS.UnityEngine.Application.isEditor

---@class Config
local Config = {
    DEBUG = false,
    WriteAccount = true,
    LastLoginUserPasswordPreferenceKey = "LastLoginUserPasswordPreferenceKey",
    CHANNEL_IOS_TEST = 1001, ---iphone 测试1月10号的用的渠道id
    CHANNEL_REVIEW   = 2001, --- 审核服渠道id
    CHANNEL_FONDANT  = 1004, --- 测试服android的id
    CHANNEL_FONDANT_WINDOW  = 1005, --- 测试服WINDOW的id
    CHANNEL_FONDANT_IOS  = 1003, --- 测试服ios的id
    DEBUG_UPDATER = false,
    SHOW_INVITE_CODE = false,
    DEBUG_ADJUST = true,--- adjust库是否是调试状态
    DEBUG_QUICK_START = false,  -- 快速启动模式（关闭登录影片，屏蔽弹出公告，自动登录账号，跳过首次主界面表现等，开发人员的友好开关）
    SHOW_FPS = false,---是否显示fps
    SHOW_FIGHT_DEBUG = false,---是否显示fps
    REVIEW = false,
    UPLOAD_URL = 'http://tracker.dddwan.com/Api/gameLogUpload',
    SHOW_SHARE = false,
    IS_SAVE_BATTLE_RECORD = 1, --- 控制保存录像按键开启状态
    BATTLE_TCP_LOG = true,     -- 是否把战中tcp日志写到本地
    MAIN_TCP_LOG = false,       -- 是否把主tcp日志写到本地
}

if Config.DEBUG or CS.UnityEngine.Application.isEditor then
    CS.UnityEngine.Debug.unityLogger.filterLogType = CS.UnityEngine.LogType.Log
end
return Config
