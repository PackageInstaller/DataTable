---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by liuzhipeng.
--- DateTime: 2024/5/31 16:04
--- Description: 
---

--region import
--endregion import

--region define
local CoWait = CS.Engine.Lib.CoWait
local UIModule = CS.Engine.UI.UIModule
--endregion define

--- from: Assets/BundleResources/Prefabs/OverseaMall/OverseaMallChargePopup.prefab > name: OverseaMallChargePopup
---@class OverseaMallChargePopup
---@field Env                           	OverseaMallChargePopup                  
---@field controller                    	Engine.UI.UILuaDialog                   
---@field BtnClose                      	UnityEngine.RectTransform               	@ 0    
---@field Txt2                          	UnityEngine.RectTransform               	@ 1    
---@field BtnClose1                     	UnityEngine.RectTransform               	@ 2    
---@field FontStyleButton               	UnityEngine.RectTransform               	@ 3    
local OverseaMallChargePopup = Class("OverseaMallChargePopup")

--region lifecycle
function OverseaMallChargePopup:__init()

end

function OverseaMallChargePopup:__delete()

end

function OverseaMallChargePopup:Awake()
    self.controller:SetSortOrder(2006)
end

function OverseaMallChargePopup:OnInitialize()
    self:InitHandler()
    self:InitData()
    self:InitView()
end

function OverseaMallChargePopup:OnFinalize()
    self.controller = nil --删除lua层引用到的c#对象 否则不会产生c# gc回收到对象
    self:Delete()
    self = nil --lua层的gc需要
end
--endregion lifecycle

--region init
function OverseaMallChargePopup:InitHandler()
    SetButtonAction(self.BtnClose1, Bind(self, self.BtnCloseOnClickHandler))
    SetButtonAction(self.FontStyleButton, Bind(self, self.BtnCloseOnClickHandler))
end

function OverseaMallChargePopup:InitData()
    local parameters = checkTable(self.controller.Argument.parameters)
    self._leftAmount = checkInt(parameters.left)
end

function OverseaMallChargePopup:InitView()
    self:RefreshView(self._leftAmount)
end
--endregion init

--region handler
function OverseaMallChargePopup:BtnCloseOnClickHandler()
    CfUtils.DialogBack()
end
--endregion handler

--region public
---@param leftAmount number @剩余金额
function OverseaMallChargePopup:RefreshView(leftAmount)
    if checkInt(leftAmount) > 0 then
        CfUtils.SetActive(self.Txt2, true)
        CfUtils.FillText(self.Txt2, localize('(本月还可以充值_num_元)', { _num_ = leftAmount }))
    else
       CfUtils.SetActive(self.Txt2, false) 
    end
end
--endregion public

return OverseaMallChargePopup