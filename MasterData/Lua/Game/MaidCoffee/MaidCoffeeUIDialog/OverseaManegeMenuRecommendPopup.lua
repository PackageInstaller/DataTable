---
--- Generated by suixin
--- Created by suixin.
--- DateTime: 2023/6/13 10:58
---
local util = require 'XLua.util'
local CoWait = CS.Engine.Lib.CoWait
local KTool = CS.Engine.Lib.KTool
local KCookie = CS.Engine.Lib.KCookie
local UIModule = CS.Engine.UI.UIModule
local cs_coroutine = require 'XLua.cs_coroutine'
local Yielders = CS.Engine.Lib.Yielders

--- from: Assets/BundleResources/Prefabs/OverseaManage/OverseaManegeMenuRecommendPopup.prefab
---@class OverseaManegeMenuRecommendPopup
---@field Env                           	OverseaManegeMenuRecommendPopup
---@field controller                    	Engine.UI.UILuaDialog
---@field ScrollView                    	SuperScrollView.LoopListView2
---@field BtnClose                      	UnityEngine.RectTransform

local CellPath = "Game.MaidCoffee.MaidCoffeeUIDialog.Cell.OverseaManegeMenuRecommendPopupCell"
local MenuCount = 3
local OverseaManegeMenuRecommendPopup = Class("OverseaManegeMenuRecommendPopup")
local ItemCell = "Cell"
function OverseaManegeMenuRecommendPopup:__init()
end

function OverseaManegeMenuRecommendPopup:Awake()
    self:AddUIClick()
end

function OverseaManegeMenuRecommendPopup:AddUIClick()
    SetButtonAction(self.BtnClose,Bind(self,self.OnBtnCloseClick))
end

function OverseaManegeMenuRecommendPopup:OnFocus(focus)
end

function OverseaManegeMenuRecommendPopup:OnShow()
    ---@type Engine.Lib.CoWait
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        xTry(function()
            self:RefreshShow()
        end)
    end))
    return coWait
end

function OverseaManegeMenuRecommendPopup:RefreshShow()
    if not self.ScrollView.IsListViewInit then
        self.ScrollView:InitListView(MenuCount, Bind(self, self.OnRefreshCellIndex))
    else
        GameUtils.ReloadData(self.ScrollView, MenuCount)
    end
end

function OverseaManegeMenuRecommendPopup:OnRefreshCellIndex(listView,index)
    if index < 0 or index >= MenuCount then
        return
    end
    local cell = listView:NewListViewItem(ItemCell)
    local tableCellEnv = CfUtils.GetLuaScr(cell, CellPath)
    tableCellEnv:SetCallBackAndIndex(Bind(self,self.OnBtnGoToClick),index + 1)
    ---@type MaidCoffeeComponent
    local component = GameUtils.GetOrAddEntityCompByType(ECSComponentType.Constants.MaidCoffeeComponent)
    local doubleData = component:GetDoubleMenuData(self.curSelectIndex,index + 1)
    local isMatch = self:IsMatch(doubleData,index + 1)
    tableCellEnv:RefreshShow(doubleData,isMatch)
    return cell
end

function OverseaManegeMenuRecommendPopup:IsMatch(doubleData,i)
    if self.curData and self.curData[i] then
        if self.curData[i].refId == doubleData.recipeId then
            return true
        end
    end
    return false
end

function OverseaManegeMenuRecommendPopup:OnBtnGoToClick()

end

function OverseaManegeMenuRecommendPopup:OnInitialize()
    ---@type Engine.Lib.CoWait
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        xTry(function()
            self.parameters = self.controller.Argument.parameters
            if  self.parameters.curIndex then
                self.curSelectIndex = self.parameters.curIndex
            end
            if self.parameters.curData then
                self.curData = self.parameters.curData
            end
        end)
    end))
    return coWait
end

function OverseaManegeMenuRecommendPopup:OnFinalize()
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        xTry(function()
            self.controller = nil --删除lua层引⽤到的c#对象否则不会产⽣c# gc回收到对象
            self:Delete()
            self = nil --lua层的gc需要    
        end)
    end))
    return coWait
end

function OverseaManegeMenuRecommendPopup:__delete()
    self.controller = nil
end

function OverseaManegeMenuRecommendPopup:OnBtnCloseClick()
    UIModule.BackDialog()
end

return OverseaManegeMenuRecommendPopup