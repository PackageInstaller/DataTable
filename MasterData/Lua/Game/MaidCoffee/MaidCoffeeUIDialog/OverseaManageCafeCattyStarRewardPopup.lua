---
--- Generated by suixin
--- Created by suixin.
--- DateTime: 2023/9/25 15:51
---
local util = require 'XLua.util'
local CoWait = CS.Engine.Lib.CoWait
local KTool = CS.Engine.Lib.KTool
local KCookie = CS.Engine.Lib.KCookie
local UIModule = CS.Engine.UI.UIModule
local cs_coroutine = require 'XLua.cs_coroutine'
local Yielders = CS.Engine.Lib.Yielders

local OverseaManageCafeCattyStarRewardPopup = Class("OverseaManageCafeCattyStarRewardPopup")
local StarRewardCellPath = "Game.MaidCoffee.MaidCoffeeUIDialog.Cell.OverseaManageCafeCattyStarRewardPopupCell"
local Cell = "Cell"
function OverseaManageCafeCattyStarRewardPopup:__init()
end

function OverseaManageCafeCattyStarRewardPopup:__delete()
    self.controller = nil
end

function OverseaManageCafeCattyStarRewardPopup:Awake()
    self:AddUIClick()
end

function OverseaManageCafeCattyStarRewardPopup:AddUIClick()
    SetButtonAction(self.BtnClose,Bind(self,self.OnBtnCloseClick))
end

function OverseaManageCafeCattyStarRewardPopup:OnFocus(focus)

end

function OverseaManageCafeCattyStarRewardPopup:OnShow()
    ---@type Engine.Lib.CoWait
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        xTry(function()
            self:RefreshShow()
        end)
    end))
    return coWait
end

function OverseaManageCafeCattyStarRewardPopup:RefreshShow()
    self.allVos = CfUtils.GetCf(AutoIds.IdSetting3044, "MaidCoffeeStarUpVo", true)
    self.count = table.count(self.allVos)
    if not self.ListView.IsListViewInit then
        self.ListView:InitListView(table.count(self.allVos), Bind(self, self.OnGetItemByIndex))
    else
        GameUtils.ReloadData(self.ListView, table.count(self.allVos))
    end
end

function OverseaManageCafeCattyStarRewardPopup:OnGetItemByIndex(listView, index)
    if index < 0 then
        return nil
    end

    if self.count < (index + 1) then
        return nil
    end

    local cell = listView:NewListViewItem(Cell)
    if not isNull(cell) then
        local env = CfUtils.GetLuaScr(cell,StarRewardCellPath)
        if env then
            env:RefreshShow(self.allVos[index + 1],index + 1)
        end
    end
    return cell
end

function OverseaManageCafeCattyStarRewardPopup:OnInitialize()
    ---@type Engine.Lib.CoWait
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        xTry(function()

        end)
    end))
    return coWait
end

function OverseaManageCafeCattyStarRewardPopup:OnFinalize()
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        xTry(function()
            self.controller = nil --删除lua层引⽤到的c#对象否则不会产⽣c# gc回收到对象
            self:Delete()
            self = nil --lua层的gc需要    
        end)
    end))
    return coWait
end

function OverseaManageCafeCattyStarRewardPopup:OnBtnCloseClick()
    UIModule.CloseDialog(self.controller)
end

return OverseaManageCafeCattyStarRewardPopup