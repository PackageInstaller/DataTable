---
--- Generated by suixin
--- Created by suixin.
--- DateTime: 2023/7/26 10:19
---
local KTool = CS.Engine.Lib.KTool

--- from: Assets/BundleResources/Prefabs/OverseaManage/OSManageReportDetailsDialog.prefab
---@class OverseaManageCafeRecommendRoot
---@field Env                           	OverseaManageCafeRecommendRoot          
---@field controller                    	Engine.Modules.LuaBehaviour             
---@field ImgLine                       	UnityEngine.RectTransform               
---@field PackageNode                   	Engine.Modules.LuaBehaviour             
---@field Content                       	UnityEngine.RectTransform               
local OverseaManageCafeRecommendRoot = Class('OverseaManageCafeRecommendRoot')
local PackageNodePath = "Game.MaidCoffee.MaidCoffeeUIDialog.Cell.OverseaManageCafePackageNodeCell"
local MenuCount = 3

function OverseaManageCafeRecommendRoot:__init()
    self.menuEnv = nil
end

function OverseaManageCafeRecommendRoot:__delete()
    self.menuEnv = nil
end

function OverseaManageCafeRecommendRoot:Awake()
    self.menuEnv = {}
    if self.PackageNode then
        local env = CfUtils.GetLuaScr(self.PackageNode,PackageNodePath)
        if env then
            self.menuEnv[1] = env
        end
    end
    for i = 1, MenuCount do
        local node =self["PackageNode"..i]
        if node then
            local env = CfUtils.GetLuaScr(node,PackageNodePath)
            if env then
                self.menuEnv[i] = env
            end
        end
    end
end

function OverseaManageCafeRecommendRoot:RefreshShow()
    ---@type MaidCoffeeComponent
    local component = GameUtils.GetOrAddEntityCompByType(ECSComponentType.Constants.MaidCoffeeComponent)
    for i = 1, MenuCount do
        local _, isLock = component:GetIsLockedAndVolume(i)
        if not isLock then
            KTool.SetActive(self.menuEnv[i].controller,true)
            self.menuEnv[i]:RefreshUI(i)
        else
            KTool.SetActive(self.menuEnv[i].controller,false)
        end
    end
end

function OverseaManageCafeRecommendRoot:RefreshHighLine()
    local high = 0
    for i = 1, MenuCount do
        if self.menuEnv[i].controller.gameObject.activeSelf then
            high = high + self.menuEnv[i].controller.gameObject.transform.sizeDelta.y
        end
    end
    KTool.SetActive(self.ImgLine,high > 440)
end

function OverseaManageCafeRecommendRoot:Start()
end

function OverseaManageCafeRecommendRoot:OnDestroy()
    self.controller = nil--luabehaviour 中CSharp对象
    self:Delete()
end

return OverseaManageCafeRecommendRoot