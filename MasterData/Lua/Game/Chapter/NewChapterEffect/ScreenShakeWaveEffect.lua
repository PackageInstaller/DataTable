---
--- Generated by zhaoyuhao
--- Created by zhaoyuhao.
--- DateTime: 2022/11/28 15:33
--- Project: cat-proj
---

local BaseEffect = import('Game.Chapter.NewChapterEffect.BaseEffect')
local base = BaseEffect
local DOTween = CS.DG.Tweening.DOTween

---@class ScreenShakeWaveEffect : BaseEffect
local ScreenShakeWaveEffect = Class("ScreenShakeWaveEffect", BaseEffect)

function ScreenShakeWaveEffect:__init()
    self.offset = nil
    self.vibrato = nil
    self.duration = nil
    self.randomness = nil
    self.tween = nil
    self.waitTime = nil
    self._isFinish = nil
end

function ScreenShakeWaveEffect:__delete()
    self.offset = nil
    self.vibrato = nil
    self.duration = nil
    self.randomness = nil
    self.tween = nil
    self.waitTime = nil
    self._isFinish = nil
end

function ScreenShakeWaveEffect:Initialize(parameters)
    base.StartEffect(self, parameters)
    self.offset = Vector3.New(0.2,0.2,0)
    self.vibrato = 30
    self.duration = 0.9
    self.randomness = 40
    self.waitTime = 1.5
end

function ScreenShakeWaveEffect:StartEffect(object)
    base.StartEffect(self, object)
    local pos = object.transform.localPosition
    self.tween = DOTween.Sequence()
    self.tween:Append(object.transform:DOShakePosition(self.duration, self.offset, self.vibrato, self.randomness))
    self.tween:AppendInterval(self.waitTime)
    self.tween:SetLoops(99999)
    self.tween:OnComplete(function()
        object.transform.localPosition = pos
    end)
end

function ScreenShakeWaveEffect:IsFinish()
    base.IsFinish(self)
    return true
end

function ScreenShakeWaveEffect:EndEffect(object)
    base.EndEffect(self, object)
end

function ScreenShakeWaveEffect:RemoveEffect(object)
    base.RemoveEffect(self, object)
    if not self.tween:IsComplete() then
        self.tween:Complete()
        self.tween = nil
    end
end

return ScreenShakeWaveEffect