---
--- Generated by zhaoyuhao
--- Created by zhaoyuhao.
--- DateTime: 2021/7/27 17:30
--- Project: cat-proj
---

local BaseEffect = import('Game.Chapter.NewChapterEffect.BaseEffect')
local base = BaseEffect

---@class ScreenShakeEffect : BaseEffect
local ScreenShakeEffect = Class("ScreenShakeEffect", BaseEffect)

function ScreenShakeEffect:__init()
    self.offset = nil
    self.vibrato = nil
    self.duration = nil
    self.randomness = nil
    self.tween = nil
    self._isFinish = nil
end

function ScreenShakeEffect:__delete()
    self.offset = nil
    self.vibrato = nil
    self.duration = nil
    self.randomness = nil
    self.tween = nil
    self._isFinish = nil
end

---@param parameters NewChapter2DVo
function ScreenShakeEffect:Initialize(parameters)
    base.Initialize(self, parameters)
    self.offset = Vector3.New(0.2,0.2,0)
    self.vibrato = 30
    self.duration = 0.9
    self.randomness = 40
end


function ScreenShakeEffect:StartEffect(object)
    base.StartEffect(self, object)
    self._isFinish = false
    local pos = object.transform.localPosition
    self.tween = object.transform:DOShakePosition(self.duration, self.offset, self.vibrato, self.randomness):OnComplete(function()
        self._isFinish = true
        object.transform.localPosition = pos
    end)
end

function ScreenShakeEffect:IsFinish()
    base.IsFinish(self)
    return self._isFinish
end

function ScreenShakeEffect:EndEffect(object)
    base.EndEffect(self, object)
    if not isNull(self.tween) and not self.tween:IsComplete() then
        self.tween:Complete()
        self.tween = nil
    end
    self._isFinish = true
end

function ScreenShakeEffect:RemoveEffect(object)
    base.RemoveEffect(self, object)
end

return ScreenShakeEffect