---
--- Generated by Eric
--- Created by Eric.
--- DateTime: 2021/10/12 11:18
---

------------ import ------------
local UIEventProxy = CS.Engine.UI.UIEventProxy
local KTool = CS.Engine.Lib.KTool
local MazeMgr = import('Game.Maze.MazeMgr'):GetInstance()

------------ import ------------

------------ define ------------

------------ define ------------


---@class PatrolChoosePanelEventCellNext
---@field Txt                           	UnityEngine.UI.Text
---@field ImgNoChoose                   	UnityEngine.RectTransform
---@field ImgIcon                       	Engine.UI.RawImageAlterable
local PatrolChoosePanelEventCellNext = Class("PatrolChoosePanelEventCellNext")
function PatrolChoosePanelEventCellNext:__init()
    self.btnAction = nil
    self.data = {}
end

function PatrolChoosePanelEventCellNext:__delete()
    self.btnAction = nil
    self.data = nil

    self.controller = nil
end

---SetCallBack
---设置单击回调函数
function PatrolChoosePanelEventCellNext:SetBtnCallBack(cb)
    self.btnAction = cb
end

function PatrolChoosePanelEventCellNext:Awake()
    self.EventEnterChoose = Events.AddListener(Constants.EventNames.PatrolEventChoose, Bind(self, self.PlayChooseAnim))

    --SetButtonAction(self.controller, function ()
    --    if type(self.btnAction) == 'function' then
    --        self.btnAction()
    --    end
    --end)
end

function PatrolChoosePanelEventCellNext:FreshBaseUI(data)
    self.ImgIcon:LoadSprite(data.iconPath)
    self.Txt.text = data.text
    KTool.SetActive(self.ImgNoChoose, not data.isNextIcon)

end

function PatrolChoosePanelEventCellNext:PlayChooseAnim(index)
    if isNull(index) then
        KTool.SetActive(self.ImgNoChoose, false)
    end
end

function PatrolChoosePanelEventCellNext:OnDestroy()
    Events.RemoveListener(Constants.EventNames.PatrolEventChoose, self.EventEnterChoose)
    self.controller = nil --删除lua层引用到的c#对象 否则不会产生c# gc回收到对象
    self:Delete()
    self = nil --lua层的gc需要
end

return PatrolChoosePanelEventCellNext