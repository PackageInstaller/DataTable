
---
--- Generated by vscode.
--- Created by pikaqiu.
--- DateTime: 2024-03-29
---

------------ import ------------
local util = require 'XLua.util'
local KTool = CS.Engine.Lib.KTool
local CoWait = CS.Engine.Lib.CoWait
local UIModule = CS.Engine.UI.UIModule
local Yielders = CS.Engine.Lib.Yielders
local UIEventProxy = CS.Engine.UI.UIEventProxy
local cs_coroutine = require("XLua.cs_coroutine")
------------ import ------------

------------ define ------------
local NodeLuaPath = "Game.UI.Homeland.CheckIn.HomelandCheckInAttributeNode"
local CellName = 'AttributeNode'
------------ define ------------

--- from: Assets/BundleResources/Prefabs/Homeland/HomelandInforIntelligenceDialog.prefab > name: StatsNode
---@class HomelandCheckInAttributeGroup
---@field Env                           	HomelandCheckInAttributeGroup           
---@field controller                    	Engine.Modules.LuaBehaviour             
---@field AttributeNode                 	UnityEngine.RectTransform               
local HomelandCheckInAttributeGroup = Class("HomelandCheckInAttributeGroup")

------------ lifeCycle ------------

function HomelandCheckInAttributeGroup:__init()
    self:CleanProperties()
end

function HomelandCheckInAttributeGroup:__delete()
    self:CleanProperties()
end

function HomelandCheckInAttributeGroup:Awake()
    ---@type HomelandCheckInComponent
    self.comp = GameUtils.GetOrAddEntityCompByType(ECSComponentType.Constants.HomelandCheckInComponent)
end

function HomelandCheckInAttributeGroup:Start()
end

function HomelandCheckInAttributeGroup:OnDestroy()
    self.controller = nil--luabehaviour 中CSharp对象
    self:Delete()
    self = nil
end
------------ lifeCycle ------------


function HomelandCheckInAttributeGroup:CleanProperties()
    self.groupId = nil
    self.typeId = nil
    self.comp = nil
end

function HomelandCheckInAttributeGroup:Refresh(groupId, typeId)
    self.groupId = groupId
    self.typeId = typeId

    -- 计算可以达到的最高等级
    local activeInfo = self.comp:GetCheckInActiveInfo(groupId)
    local canActiveIndex = typeId == 1 and activeInfo.index1 or activeInfo.index2

    -- print("[录入]", table.dump(hasList), canActiveIndex, activeIndex, table.dump(activeInfo))

    local idList = self.comp:GetBonusGroupIdList(groupId, typeId)
    local data = {}
    for i, id in ipairs(idList) do
        table.insert(data, {typeId = typeId, voId = checkNumber(id), canActiveIndex = canActiveIndex})
    end

    GameUtils.HideAllChild(self.controller, CellName)

    GameUtils.RefreshAllChild(data, self.controller, self.AttributeNode, CellName, NodeLuaPath)
end

return HomelandCheckInAttributeGroup