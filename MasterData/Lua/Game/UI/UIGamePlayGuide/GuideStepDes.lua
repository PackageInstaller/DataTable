---
--- Generated by fantang
--- Created by fantang.
--- DateTime: 2020/10/19 14:03
---

------------ import ------------
local RectTransform = CS.UnityEngine.RectTransform
local KTool = CS.Engine.Lib.KTool
------------ import ------------

------------ define ------------
local V2_POS_UP = Vector2.New(0, 146)
local V2_POS_DOWN = Vector2.New(0, -208)
--- 位置
local ROOT_POS = {
    Up = 1,
    Down = 2,
    Middle = 3,
}
------------ define ------------

---@class GuideStepDes
---@field Title                            UnityEngine.RectTransform
---@field TxtDescription                    UnityEngine.UI.Text
---@field Fitter_TxtTitle                UnityEngine.UI.ContentSizeFitter
---@field TxtTitle                        UnityEngine.UI.Text
local GuideStepDes = Class('GuideStepDes')

------------ LifeCycle
function GuideStepDes:__init()
    self.goRect = nil
    self.go = nil
end

function GuideStepDes:__delete()
    self.goRect = nil
    self.go = nil
end
function GuideStepDes:Awake()
    self.go = self.controller.gameObject
    self.goRect = self.controller.gameObject:GetComponent(typeof(RectTransform))
end
function GuideStepDes:Start()
end
function GuideStepDes:OnDestroy()
    self.controller = nil--luabehaviour 中CSharp对象
    self:Delete()
    self = nil
end
------------ LifeCycle

------------ Function

---ShowDesc
---@param pos Vector2 说明节点 位置
---@param title string 标题
---@param desc string 描述
function GuideStepDes:ShowDesc(pos, title, desc)
    self.goRect.anchoredPosition = self:GetRootAnchorPos(pos) or Vector2.zero
    self.TxtTitle.text = title
    self.TxtDescription.text = desc
    CS.UnityEngine.UI.LayoutRebuilder.ForceRebuildLayoutImmediate(self.Title)
end

---SetRootShowState
---设置 节点显示状态
---@param boolean boolean 显示状态
function GuideStepDes:SetRootShowState(boolean)
    KTool.SetActive(self.go, boolean)
end

function GuideStepDes:GetRootAnchorPos(pos)
    local aimPos
    if pos == ROOT_POS.Up then
        aimPos = V2_POS_UP
    elseif pos == ROOT_POS.Down then
        aimPos = V2_POS_DOWN
    elseif pos == ROOT_POS.Middle then
        aimPos = Vector2.zero
    end
    return aimPos
end

------------ Function

return GuideStepDes
