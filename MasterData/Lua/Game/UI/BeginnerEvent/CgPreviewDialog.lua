---
--- Generated by boss
--- Created by boss.
--- DateTime: 2024/9/9 14:13
---

local util = require 'XLua.util'
local CoWait = CS.Engine.Lib.CoWait
local Animation = CS.UnityEngine.Animation
local RawImageAlterable = CS.Engine.UI.RawImageAlterable
------------ import ------------
------------ import ------------

------------ define ------------
------------ define ------------

--- from: Assets/BundleResources/Prefabs/BeginnerEvent/BeginnerEventLoadingPanel.prefab > name: BeginnerEventLoadingPanel
---@class CgPreviewDialog
---@field Env                           	CgPreviewDialog                         
---@field controller                    	Engine.UI.UILuaDialog                   
---@field Tips                          	UnityEngine.UI.Text                     	@ 0    
---@field ImgBg                         	UnityEngine.RectTransform               	@ 1    
local CgPreviewDialog = Class("CgPreviewDialog")

function CgPreviewDialog:__init()
    self.startTime = 0
    self.cb = nil
end

function CgPreviewDialog:__delete()
    self.controller = nil
    self.startTime = 0
    self.cb = nil
end

function CgPreviewDialog:Awake()
    self.startTime = os.time()
    SetButtonAction(self.ImgBg, function()
        if os.time() - self.startTime > 0.2 then
            CfUtils.DialogBack()
        end
    end)
end

function CgPreviewDialog:OnInitialize()
    ---@type Engine.Lib.CoWait
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        self.cb = self.controller.Argument.parameters.cb
        local cg = tostring(self.controller.Argument.parameters.cg)
        if not string.isEmpty(cg) then
            ---@type Engine.UI.ImageAlterable | Engine.UI.RawImageAlterable
            local goImage = self.ImgBg.transform:GetComponent(typeof(RawImageAlterable))
            if goImage then
                goImage.IsAutoAlpha = true
                goImage:LoadSprite(cg)
            end
        end
    end))
    return coWait
end

function CgPreviewDialog:OnFinalize()
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        if self.cb then
            self.cb()
        end
        self.controller = nil --删除lua层引用到的c#对象 否则不会产生c# gc回收到对象
        self:Delete()
        self = nil --lua层的gc需要
    end))
    return coWait
end

return CgPreviewDialog