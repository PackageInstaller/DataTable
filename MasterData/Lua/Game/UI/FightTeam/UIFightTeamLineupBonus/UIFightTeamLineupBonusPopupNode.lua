
---
--- Generated by vscode.
--- Created by pikaqiu.
--- DateTime: 2024-03-12
---

------------ import ------------
local util = require 'XLua.util'
local KTool = CS.Engine.Lib.KTool
local CoWait = CS.Engine.Lib.CoWait
local UIModule = CS.Engine.UI.UIModule
local Yielders = CS.Engine.Lib.Yielders
local UIEventProxy = CS.Engine.UI.UIEventProxy
local cs_coroutine = require("XLua.cs_coroutine")
------------ import ------------

------------ define ------------

------------ define ------------

--- from: Assets/BundleResources/Prefabs/FightTeam/FightTeamBonusPopup.prefab > name: Cell
---@class UIFightTeamLineupBonusPopupNode
---@field Env                           	UIFightTeamLineupBonusPopupNode         
---@field controller                    	Engine.Modules.LuaBehaviour             
---@field TxtDesc2                      	CustomText                              
---@field NormalIcon                    	UnityEngine.RectTransform               
---@field ActiveRoot                    	UnityEngine.RectTransform               
---@field ActiveIcon                    	UnityEngine.RectTransform               
---@field TxtDesc1                      	UnityEngine.RectTransform               
local UIFightTeamLineupBonusPopupNode = Class("UIFightTeamLineupBonusPopupNode")

------------ lifeCycle ------------

function UIFightTeamLineupBonusPopupNode:__init()
    self:CleanProperties()
end

function UIFightTeamLineupBonusPopupNode:__delete()
    self:CleanProperties()
end

function UIFightTeamLineupBonusPopupNode:Awake()
end

function UIFightTeamLineupBonusPopupNode:Start()
end

function UIFightTeamLineupBonusPopupNode:OnDestroy()
    self.controller = nil--luabehaviour 中CSharp对象
    self:Delete()
    self = nil
end
------------ lifeCycle ------------


function UIFightTeamLineupBonusPopupNode:CleanProperties()
    self.dialogData = nil
    self.nodeData = nil
end

function UIFightTeamLineupBonusPopupNode:SetData(dialogData, index)
    self.dialogData = dialogData
    self.nodeData = dialogData.nodeDataList[index]
end

function UIFightTeamLineupBonusPopupNode:Refresh()
    if isNull(self.nodeData) then
        return
    end
    
    local isActive = self.nodeData.active
    
    KTool.SetActive(self.ActiveRoot, isActive)
    KTool.SetActive(self.NormalIcon, not isActive)
    KTool.SetActive(self.ActiveIcon, isActive)
    
    CfUtils.SetUISwitchImage(self.NormalIcon, self.nodeData.id)
    CfUtils.SetUISwitchImage(self.ActiveIcon, self.nodeData.id)
    
    CfUtils.FillText(self.TxtDesc1, tostring(self.nodeData.detail1))
    CfUtils.FillText(self.TxtDesc2, tostring(self.nodeData.detail2))
    CfUtils.SetUISwitchText(self.TxtDesc1, isActive and 1 or 2)
    CfUtils.SetUISwitchText(self.TxtDesc2, isActive and 1 or 2)
end

return UIFightTeamLineupBonusPopupNode