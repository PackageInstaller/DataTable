---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2021-08-26 16:21:36
---
local CoWait = CS.Engine.Lib.CoWait
local util = require 'XLua.util'
local KTool = CS.Engine.Lib.KTool
local UIModule = CS.Engine.UI.UIModule
local comp = GameUtils.GetOrAddEntityCompByType(ECSComponentType.Constants.FishingComponent)

---@class UIFishPoolNoticePopupDialog
local UIFishPoolNoticePopupDialog = Class("UIFishPoolNoticePopupDialog")

function UIFishPoolNoticePopupDialog:__init() 
end

function UIFishPoolNoticePopupDialog:__delete()
end

function UIFishPoolNoticePopupDialog:Awake()
    SetButtonAction(self.controller.gameObject.transform:Find("ImgBlack"), Bind(self, self.OnBtnClose))
    SetButtonAction(self.BtnClose, Bind(self, self.OnBtnClose))
    self:FillSelf()
end

function UIFishPoolNoticePopupDialog:FillSelf()
    CfUtils.FillText(self.TextTitle, "")

    local isEmpty = string.isEmpty(comp:GetCurNotice())
    KTool.SetActive(self.TextContent, not isEmpty)
    KTool.SetActive(self.EmptyNode, isEmpty)

    CfUtils.FillText(self.TextContent, comp:GetCurNotice())
end

function UIFishPoolNoticePopupDialog:OnBtnClose()
    UIModule.CloseDialog(self.controller)
end

function UIFishPoolNoticePopupDialog:OnInitialize()
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
    end))
    return coWait
end

function UIFishPoolNoticePopupDialog:OnFinalize()
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        self.controller = nil --删除lua层引用到的c#对象 否则不会产生c# gc回收到对象
        self:Delete()
        self = nil --lua层的gc需要
    end))
    return coWait
end

return UIFishPoolNoticePopupDialog
