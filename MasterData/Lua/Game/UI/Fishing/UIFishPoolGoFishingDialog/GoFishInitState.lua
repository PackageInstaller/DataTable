---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by shentayibu.
--- DateTime: 2023/9/7 13:05
---

local KTool = CS.Engine.Lib.KTool
local GoFishBaseState = import("Game.UI.Fishing.UIFishPoolGoFishingDialog.GoFishBaseState")

local FishingMgr = import('Game.UI.Fishing.FishingMgr')
local FishingUtils = import('Game.UI.Fishing.FishingUtils')
local redPointCom = GameUtils.GetEntityCompByType(ECSComponentType.Constants.RedPointMgrComponent)
local UIModule = CS.Engine.UI.UIModule
local cs_coroutine = import('XLua.cs_coroutine')
local WaitForSeconds = CS.UnityEngine.WaitForSeconds
local sceneManagement = SceneManagement:GetInstance()

-----------------------------------------------

---@class GoFishInitState
local GoFishInitState = Class("GoFishInitState", GoFishBaseState)

function GoFishInitState.new(luaDialog)
    local obj = GoFishInitState.New()
    obj.luaDialog = luaDialog
    obj.TransferTo = luaDialog.TransferTo
    for k, v in pairs(luaDialog) do
        obj[k] = v
    end

    return obj
end

function GoFishInitState:CanTransfer(target)
    return true
end

function GoFishInitState:OnEnter()


    --if not sceneManagement:IsSceneLoaded(FishingMgr:GetInstance():GetCurFishSceneId()) then
    --    FishingMgr:EnranceFishPlace(self.placeId, self.goodsVo)
    --end
    self.placeNameServer:UpdateInfo()
    self.roleAnimServer:UpdateInfo()
    self.btnFishAtlasServer:UpdateInfo()
    self.btnLeaveServer:UpdateInfo()
    self.autoFishPanelServer:Reset()

    self.TransferTo(EGoFishState.Ready)
end

function GoFishInitState:Update()
end

function GoFishInitState:OnExit()
end

return GoFishInitState
