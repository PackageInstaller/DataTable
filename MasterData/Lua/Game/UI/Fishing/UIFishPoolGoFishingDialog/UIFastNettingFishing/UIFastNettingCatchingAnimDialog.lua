---
--- Generated by 
--- Created by shentayibu.
--- DateTime: 2023/11/8 17:54
---

--region import
local cs_coroutine = import('XLua.cs_coroutine')
local CriWareUtils = import('Game.Entry.CriWareUtilsFix')
local util = require 'XLua.util'
local FishingUtils = import('Game.UI.Fishing.FishingUtils')
--endregion import

--region define
local WaitForSeconds = CS.UnityEngine.WaitForSeconds
local UIEventProxy = CS.Engine.UI.UIEventProxy
local Debug = CS.UnityEngine.Debug
local BehaviourAction = CS.Engine.Lib.BehaviourAction
local KTool = CS.Engine.Lib.KTool
local UIModule = CS.Engine.UI.UIModule
local CoWait = CS.Engine.Lib.CoWait
local UIRaycastBlocker = CS.Engine.UI.UIRaycastBlocker.Instance
local Yielders = CS.Engine.Lib.Yielders
local CountDownTime = 2592000 --30 * 86400 30天的秒数
local CDTimerModule = CS.Engine.Modules.CDTimerModule
--endregion define

--- from: Assets/BundleResources/Prefabs/FishPool/FishPoolGoFishingAnimationPopup.prefab > name: FishPoolGoFishingAnimationPopup
---@class UIFastNettingCatchingAnimDialog
---@field Env                           	UIFastNettingCatchingAnimDialog         
---@field controller                    	Engine.UI.UILuaDialog                   
---@field Root                          	UnityEngine.RectTransform               
---@field ImgBackground                 	UnityEngine.RectTransform               
---@field Spine                         	UnityEngine.RectTransform               
local UIFastNettingCatchingAnimDialog = Class("UIFastNettingCatchingAnimDialog")

--region lifecycle

function UIFastNettingCatchingAnimDialog:__init()
    self.parameters = nil
end

function UIFastNettingCatchingAnimDialog:__delete()
    self.parameters = nil
    self.controller = nil
end

function UIFastNettingCatchingAnimDialog:Awake()
    self.parameters = self.controller.Argument.parameters
    self.rewards = checkTable(self.parameters.rewards)
    UIRaycastBlocker:AddCondition("BLOCKER_FAST_NETTING_ANIM_PLAYING")
    self._co = cs_coroutine.start(function()
        coroutine.yield(WaitForSeconds(2))
        UIRaycastBlocker:RemoveCondition("BLOCKER_FAST_NETTING_ANIM_PLAYING")
        --UIModule.OpenDialog({ id = Constants.UITypeIds.UICommonRewardPop, parameters = { rewards = self.rewards } })
        UIModule.OpenDialog({
            id = Constants.UITypeIds.UIFastNettingFishRewardDialog,
            parameters = {
                rewards = self.rewards,
                placeId = self.parameters.placeId,
                cb = self.parameters.cb
            }
        },{
            { id = Constants.UITypeIds.UIFastNettingFishCatchingAnimationDialog }
        })
    end)
end

function UIFastNettingCatchingAnimDialog:OnInitialize()
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        --self:InitHandler()
        --self.needDisableDialog = UIModule.CurrentScreen:GetDialog(Constants.UITypeIds.UIFishPoolGoFishingDialog)
        --CfUtils.SetActive(self.needDisableDialog, false)
        --KTool.SetRenderFeatureActiveByName(Constants.RenderFeatureNames.BLUR, true)
        --self._co = cs_coroutine.start(function()
        --    CS.Engine.UI.UIRaycastBlocker.Instance:AddCondition("UIFishHarvestDisPlayPopupDialogCatchingAnim")
        --    coroutine.yield(WaitForSeconds(3))
        --    CS.Engine.UI.UIRaycastBlocker.Instance:RemoveCondition("UIFishHarvestDisPlayPopupDialogCatchingAnim")
        --end)
    end))
    return coWait
end

function UIFastNettingCatchingAnimDialog:OnFocus(focus)
    if focus then
    else
    end
end

function UIFastNettingCatchingAnimDialog:OnShow()
end

function UIFastNettingCatchingAnimDialog:OnHide()
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
    end))
    return coWait
end

function UIFastNettingCatchingAnimDialog:OnUpdate()
end

function UIFastNettingCatchingAnimDialog:OnFinalize()
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        if self._co then
            cs_coroutine.stop(self._co)
            self._co = nil
        end

        self.controller = nil --删除lua层引⽤到的c#对象否则不会产⽣c# gc回收到对象
        self:Delete()
        self = nil --lua层的gc需要
    end))
    return coWait
end

--endregion lifecycle

--region init
function UIFastNettingCatchingAnimDialog:InitHandler()
    --SetButtonAction(self.ImgBackground, Bind(self, self.OnCloseHandler))
end
--endregion init

--region handler
function UIFastNettingCatchingAnimDialog:OnCloseHandler()
    --KTool.SetRenderFeatureActiveByName(Constants.RenderFeatureNames.BLUR, false)
    --UIModule.RemoveDialogRecord(Constants.UITypeIds.UIFastNettingFishDialog)
    --UIModule.BackDialog()
end
--endregion handler

--region private
--endregion private

--region public
--endregion public

--region get / set
--endregion get / set

return UIFastNettingCatchingAnimDialog