---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2020-11-07 16:18:46
---
local CoWait = CS.Engine.Lib.CoWait
local util = require 'XLua.util'

local Text = CS.UnityEngine.UI.Text
local Image = CS.UnityEngine.UI.Image
local ImageAlterable = CS.Engine.UI.ImageAlterable
local Button = CS.UnityEngine.UI.Button
local RectTransform = CS.UnityEngine.RectTransform
local cs_coroutine = import('XLua.cs_coroutine')
local WaitForSeconds = CS.UnityEngine.WaitForSeconds
local Animator = CS.UnityEngine.Animator

local KTool = CS.Engine.Lib.KTool
local UIModule = CS.Engine.UI.UIModule
local phoneComp = GameUtils.GetOrAddEntityCompByType(ECSComponentType.Constants.PhoneComponent)
local UIFeelingCommunicationRightNode = "Game.UI.Phone.News.UIFeelingCommunication.UIFeelingCommunicationRightNode"

---@class UIFeelingNaughtyFrameMessageDialog
---@field Root                          	UnityEngine.RectTransform               
---@field RightPanel                    	UnityEngine.RectTransform               
local UIFeelingNaughtyFrameMessageDialog = Class("UIFeelingNaughtyFrameMessageDialog")
function UIFeelingNaughtyFrameMessageDialog:__init() 
    self._curCatId = nil              --角色id
    self._curTopicId = nil             --当前的话题id
end

function UIFeelingNaughtyFrameMessageDialog:__delete()
    self._curCatId = nil              --角色id
    self._curTopicId = nil             --当前的话题id
end

function UIFeelingNaughtyFrameMessageDialog:Awake()
    local argument = self.controller.Argument.parameters
    if not argument then
        return
    end
    self._curTopicId = checkNumber(argument.topicId)  
    self._curCatId = checkNumber(argument.catId)  

    local scr = CfUtils.GetLuaScr(self.RightPanel, UIFeelingCommunicationRightNode)
    if scr then
        scr:Refresh(self._curCatId, Constants.CHATSOURCE.YAOYIYAO, self._curTopicId)
    end
end

function UIFeelingNaughtyFrameMessageDialog:OnInitialize()
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        CfUtils.SetTopCommonActive(false, false)
        SetButtonAction(self.Bg, function()
            UIModule.BackDialog()
        end)
    end))
    return coWait
end

function UIFeelingNaughtyFrameMessageDialog:OnFinalize()
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        CfUtils.SetTopCommonActive(true, false)
        self.controller = nil --删除lua层引用到的c#对象 否则不会产生c# gc回收到对象
        self:Delete()
        self = nil --lua层的gc需要
    end))
    return coWait
end

return UIFeelingNaughtyFrameMessageDialog
