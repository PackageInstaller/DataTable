---
--- Generated by fantang
--- Created by fantang.
--- DateTime: 2020/7/23 18:12
---

------------ import ------------
local util = require 'XLua.util'
local CoWait = CS.Engine.Lib.CoWait
local KTool = CS.Engine.Lib.KTool
local UIEventProxy = CS.Engine.UI.UIEventProxy
local UIModule = CS.Engine.UI.UIModule
local WaitForSeconds = CS.UnityEngine.WaitForSeconds
------------ import ------------

------------ define ------------
------------ define ------------

---@class UIBattlingPassRoundDialog
---@field AnimRoot                      	UnityEngine.Animator
---@field Btn_Pass                      	UnityEngine.RectTransform
---@field Btn_Back                      	UnityEngine.RectTransform
local UIBattlingPassRoundDialog = Class("UIBattlingPassRoundDialog")

------------ lifeCycle ------------

function UIBattlingPassRoundDialog:__init()
    self.funOnClickSure = nil
    self.funOnClickCancel = nil
    self.nodePos = nil
end

function UIBattlingPassRoundDialog:__delete()
    self.funOnClickSure = nil
    self.funOnClickCancel = nil
    self.nodePos = nil
    self.controller = nil
end

function UIBattlingPassRoundDialog:Awake()
    local argument = self.controller.Argument
    if argument and argument.parameters then
        self.funOnClickSure = argument.parameters.sureFun
        self.funOnClickCancel = argument.parameters.cancelFun
        self.nodePos = argument.parameters.nodePosition
    end
end

function UIBattlingPassRoundDialog:OnShow()
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        self.AnimRoot.transform.parent.position = self.nodePos + Vector3.New(0,1.5,0)
        KTool.SetActive(self.AnimRoot, true)
        self.AnimRoot:Play("UIBattlingPassRoundBtn")
        coroutine.yield(WaitForSeconds(0.33))
    end))
    return coWait
end

function UIBattlingPassRoundDialog:OnInitialize()
    xTry(function()
        KTool.GetOrAddComponent(self.Btn_Pass.gameObject, typeof(UIEventProxy)).onPointerClick = function()
            if self.funOnClickSure then
                self.funOnClickSure(self)
            end
            UIModule.CloseDialog(self.controller)
        end
        KTool.GetOrAddComponent(self.Btn_Back.gameObject, typeof(UIEventProxy)).onPointerClick = function()
            if self.funOnClickCancel then
                self.funOnClickCancel(self)
            end
            UIModule.CloseDialog(self.controller)
        end
    end)
end

function UIBattlingPassRoundDialog:OnFinalize()
    xTry(function()
        self.controller = nil --删除lua层引用到的c#对象 否则不会产生c# gc回收到对象
        self:Delete()
        self = nil --lua层的gc需要
    end)
end

------------ lifeCycle ------------

return UIBattlingPassRoundDialog
