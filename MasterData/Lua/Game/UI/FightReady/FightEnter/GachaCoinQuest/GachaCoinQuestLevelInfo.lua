---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by zf
--- DateTime: 2020/07/07/22 21:32
---

------------ import ------------
------------ import ------------

------------ define ------------
---@type GachaCoinQuestConstants
local GCQConstants = import('Game.UI.FightReady.FightEnter.GachaCoinQuest.GachaCoinQuestConstants')
---@type GachaCoinQuestUtils
local GCQUtils = import(GCQConstants.Path.Utils)

local UIConfig = {
    [0] = {
        BgPath = 'UIAtlas/BattleSelection/GachaCoinQuest/illusion_combat_level_bg_1.png'
    },
    [1] = {
        BgPath = 'UIAtlas/BattleSelection/GachaCoinQuest/illusion_combat_level_bg_2.png'
    },
}
local CurrentLevelBgPath = 'UIAtlas/BattleSelection/GachaCoinQuest/illusion_combat_level_bg_3.png'
------------ define ------------


---@class GachaCoinQuestLevelInfo 抽卡道具副本等级奖励信息格子
---@field UpperLabel                    	UnityEngine.UI.Text                     上限文字
---@field LevelLabel                    	UnityEngine.UI.Text                     等级文字
---@field LevelInfoBg                   	Engine.UI.ImageAlterable                背景图
local GachaCoinQuestLevelInfo = Class('GachaCoinQuestLevelInfo')


---__init
function GachaCoinQuestLevelInfo:__init()
end

---__delete
function GachaCoinQuestLevelInfo:__delete()
end

---Awake
function GachaCoinQuestLevelInfo:Awake()
    self:Init()
end


---------------------------------------------------
-- init begin --
---------------------------------------------------

---Init 初始化
function GachaCoinQuestLevelInfo:Init()
    ---@type integer @ 当前格子的index
    self.cellIdx = nil

    self.UIConfigAmount = table.count(UIConfig)
end


---------------------------------------------------
-- view begin --
---------------------------------------------------

---RefreshView 刷新界面
---@param index integer @ 位置序号
---@param level integer @ 等级
---@param rewardsUpper integer @ 奖励上限
function GachaCoinQuestLevelInfo:RefreshView(index, level, rewardsUpper)
    self.cellIdx = index
    -- 刷新背景图
    self:RefreshBg(index)
    -- 刷新奖励文字
    self:RefreshText(level, rewardsUpper)
end

---RefreshText 刷新文字
---@param level integer @ 等级
---@param rewardsUpper integer @ 奖励上限
function GachaCoinQuestLevelInfo:RefreshText(level, rewardsUpper)
    self.LevelLabel.text = string.format('%02d', checkInt(level))
    self.UpperLabel.text = rewardsUpper
end

---RefreshBg 根据序号刷新背景
---@param index integer @ 位置序号
function GachaCoinQuestLevelInfo:RefreshBg(index, bgPath)
    if nil == bgPath then
        bgPath = UIConfig[index % self.UIConfigAmount].BgPath
    end
    self.LevelInfoBg:LoadSprite(bgPath)
end

---SetCurrent 将格子设置为当前等级信息
---@param isCurrent boolean @ 是否是当前格子
function GachaCoinQuestLevelInfo:SetCurrent(isCurrent)
    local bgPath = nil
    if isCurrent then
        bgPath = CurrentLevelBgPath
    end
    self:RefreshBg(self.cellIdx, bgPath)
end


return GachaCoinQuestLevelInfo