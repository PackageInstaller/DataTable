---
--- Generated by panmeng
--- Created by panmeng.
--- DateTime: 2022/4/18 3:35 PM 
---
local util = require 'XLua.util'
local CoWait = CS.Engine.Lib.CoWait
local KTool = CS.Engine.Lib.KTool
local KCookie = CS.Engine.Lib.KCookie

---@type EquipQuestComponent
local equipQuestComp = GameUtils.GetOrAddEntityCompByType(ECSComponentType.Constants.EquipQuestComponent)
local cs_coroutine = require 'XLua.cs_coroutine'

---@class UIDisasterResponseDialog
---@field chapterRoot                   	UnityEngine.RectTransform
---@field map                           	UnityEngine.RectTransform
---@field chapter                       	UnityEngine.RectTransform
local UIDisasterResponseDialog = Class("UIDisasterResponseDialog")

function UIDisasterResponseDialog:__init()
end

function UIDisasterResponseDialog:__delete()
end

function UIDisasterResponseDialog:Awake()
        
end

function UIDisasterResponseDialog:OnShow()
    ---@type Engine.Lib.CoWait
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        self.isInitView_ = true
        if self.isInitData_ and not self.isInitViewData_ then
            self:InitViewData()
        end
    end))
    return coWait
end

function UIDisasterResponseDialog:OnFocus(focus)
    if focus then
        GameUtils.UpdateNavBarShowGoods({Constants.Currency.EquipCopyCurrency})
    end
end

function UIDisasterResponseDialog:OnInitialize()
    ---@type Engine.Lib.CoWait
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        equipQuestComp:SeniorEquipQuestHomeReq(function()
            self.isInitData_ = true
            if self.isInitView_ then
                self:InitViewData()
            end
        end)
    end))
    return coWait
end

function UIDisasterResponseDialog:InitViewData()
    self.isInitViewData_ = true
    self:UpdateNodeShow()
end

function UIDisasterResponseDialog:UpdateNodeShow()
    --local isHasQuest = checkInt(equipQuestComp:GetCurSeniorQuestId()) > 0
    --KTool.SetActive(self.map, isHasQuest)
    KTool.SetActive(self.chapter, true)

    --if not isHasQuest and not KTool.IsActive(self.chapterRoot) then
        --KTool.SetActive(self.chapterRoot, not isHasQuest)
    --end

    local env = nil
    --if isHasQuest then
    --    ---@type UIDisasterResponseMapPanel
    --    env = CfUtils.GetLuaScr(self.map, "Game.UI.FightReady.UIFastRelief.UIDisasterResponse.UIDisasterResponseMapPanel")
    --else
        ---@type UIDisasterResponsePanel
        env = CfUtils.GetLuaScr(self.chapter, "Game.UI.FightReady.UIFastRelief.UIDisasterResponse.UIDisasterResponsePanel")
    --end

    if env then
        env:SetChangeCB(Bind(self, self.UpdateNodeShow))
        env:Refresh()
    end

end

function UIDisasterResponseDialog:OnDestroy()
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        self.controller = nil --删除lua层引⽤用到的c#对象,否则不不会产⽣生c# gc回收到对象
        self:Delete()
        self = nil --lua层的gc需要
    end))
    return coWait
end


return UIDisasterResponseDialog