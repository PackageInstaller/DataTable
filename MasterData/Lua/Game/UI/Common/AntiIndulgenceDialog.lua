---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yuguoqing
--- DateTime: 2020/2/12
---

---@class AniIndulgenceDialog
---@field CommonButton_Main                UnityEngine.UI.Button
---@field tip                            UnityEngine.UI.Text
local AniIndulgenceDialog = Class("AniIndulgenceDialog")

------------ import ------------
local Button = CS.UnityEngine.UI.Button
local _UIModule = CS.Engine.UI.UIModule
local UIEventProxy = CS.Engine.UI.UIEventProxy
local CoWait = CS.Engine.Lib.CoWait
local util = require 'XLua.util'
------------ import ------------

function AniIndulgenceDialog:Awake()
    SetButtonAction(self.CommonButton_Main:GetComponent(typeof(Button)), Bind(self, self.OnBackButtonAction));
    UIEventProxy.Create(self.controller.gameObject).onPointerClick = Bind(self, self.OnBackButtonAction) -- 整个
end

function AniIndulgenceDialog:OnInitialize()
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        self:showTip(self.controller.Argument.parameters.tip)
    end))
    return coWait
end

---@param Tip  string
function AniIndulgenceDialog:showTip(Tip)
    self.tip.text = tostring(Tip)
end

function AniIndulgenceDialog:OnBackButtonAction()
    _UIModule.CloseDialog(self.controller)
    CS.UnityEngine.Application.Quit()
    if self.controller.Argument.parameters.callback then
        self.controller.Argument.parameters.callback()
    end
end

return AniIndulgenceDialog