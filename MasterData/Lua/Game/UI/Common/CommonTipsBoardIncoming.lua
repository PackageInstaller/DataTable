---
--- Generated by xiangyuan
--- Created by xiangyuan.
--- DateTime: 2023-02-10 10:05
---
--[[
通用提示板 显示道具的信息 带一个数量
unity prefab中的相关属性
--]]

------------ import ------------
local ContentSizeFitter = CS.UnityEngine.UI.ContentSizeFitter

local KTool = CS.Engine.Lib.KTool
------------ import ------------

------------ define ------------
local _ContentSizeFitter = typeof(ContentSizeFitter)
local TitleNodePath = "Game.UI.Common.CommonTitleNode"
local INCOMING_PATH = "Game.UI.Common.CommonIncomingNode"
local GoodsConfMgr = GoodsConfMgr

local Configs = {
    [Constants.CoffeeShowItem.GoldId] = {
        {name = localize('套餐售卖基础收益'), isConvertPercent = 0, valueFunc = function()
            local component = GameUtils.GetOrAddEntityCompByType(ECSComponentType.Constants.MaidCoffeeComponent)
            return component:GetBaseCoinIncome()
        end },
--[[        {name = localize('店长与店员加成'), isConvertPercent = 1,valueFunc = function()
            local component = GameUtils.GetOrAddEntityCompByType(ECSComponentType.Constants.MaidCoffeeComponent)
            return component:GetPersonAddBaseCoinIncome()
        end },
        {name = localize('餐厅等级收益'), isConvertPercent = 0,valueFunc = function()
            local component = GameUtils.GetOrAddEntityCompByType(ECSComponentType.Constants.MaidCoffeeComponent)
            return component:GetMaidCafeMinEarnings() --获取餐厅等级收益
        end },]]
    },
    [Constants.CoffeeShowItem.PraisePointId] = {
--[[        {name = localize('每份套餐获得概率'), isConvertPercent = 1,valueFunc = function()
            local component = GameUtils.GetOrAddEntityCompByType(ECSComponentType.Constants.MaidCoffeeComponent)
            local _,probability = component:GetPersonBasePraisePointProbabilityAndNum()
            return probability
        end },]]
--[[        {name = localize('每份套餐基础收益'), isConvertPercent = 0,valueFunc = function()
            local component = GameUtils.GetOrAddEntityCompByType(ECSComponentType.Constants.MaidCoffeeComponent)
            local base = component:GetPersonBasePraisePointProbabilityAndNum()
            return base
        end },]]
--[[        {name = localize('店长与店员加成'), isConvertPercent = 1,valueFunc = function()
            local component = GameUtils.GetOrAddEntityCompByType(ECSComponentType.Constants.MaidCoffeeComponent)
            return component:GetPersonAddPrisePointPercent()
        end },]]
        {name = localize('套餐售卖基础收益'), isConvertPercent = 0,valueFunc = function()
            local component = GameUtils.GetOrAddEntityCompByType(ECSComponentType.Constants.MaidCoffeeComponent)
            return component:GetCattySlotAdd()
        end },
        {name = localize('推荐料理额外收益'), isConvertPercent = 0,valueFunc = function()
            local component = GameUtils.GetOrAddEntityCompByType(ECSComponentType.Constants.MaidCoffeeComponent)
            local base = component:GetPraisePointRecommendPrisePoint()
            return base
        end },

    },
    [Constants.CoffeeShowItem.TipId] = {
--[[        {name = localize('每分钟获得小费概率'), isConvertPercent = 1,valueFunc = function()
            local component = GameUtils.GetOrAddEntityCompByType(ECSComponentType.Constants.MaidCoffeeComponent)
            local probability , _ = component:GetMenuAddTipPercent()
            return probability
        end },]]
        {name = localize('每份套餐概率获得'), isConvertPercent = 0,valueFunc = function()
            local component = GameUtils.GetOrAddEntityCompByType(ECSComponentType.Constants.MaidCoffeeComponent)
            local _,add = component:GetMenuAddTipPercent()
            return add
        end },
    },
}
------------ define ------------

--- @type CommonTipsBoard
local CommonTipsBoard = import('Game.UI.Common.CommonTipsBoard')

---@class CommonTipsBoardIncoming
---@field OpenAwayNode                UnityEngine.RectTransform
---@field PrefabGo                UnityEngine.RectTransform
---@field Arrow                            UnityEngine.RectTransform
---@field ContentTxt                        UnityEngine.UI.Text
---@field ContentTxt2                        UnityEngine.UI.Text
---@field TitleNode                        UnityEngine.RectTransform
---@field Bg                                UnityEngine.RectTransform
---@field Content                                UnityEngine.RectTransform
local CommonTipsBoardIncoming = Class('CommonTipsBoardIncoming', CommonTipsBoard)

function CommonTipsBoardIncoming:__init()
    CommonTipsBoard.__init(self)
end

function CommonTipsBoardIncoming:__delete()
    CommonTipsBoard.__delete(self)
end

function CommonTipsBoardIncoming:Awake()
    CommonTipsBoard.Awake(self)
    self:Init()
end

function CommonTipsBoardIncoming:Start()
    CommonTipsBoard.Start(self)
end

function CommonTipsBoardIncoming:OnDestroy()
    CommonTipsBoard.OnDestroy(self)
    self.controller = nil    --luabehaviour 中CSharp对象 self:Delete()
    self = nil
end

---------------------------------------------------
-- init begin --
---------------------------------------------------

---InitValue 初始化数值
function CommonTipsBoardIncoming:InitValue()
    CommonTipsBoard.InitValue(self)
end

---InitView 初始化界面
function CommonTipsBoardIncoming:InitView()
    CommonTipsBoard.InitView(self)
    -- 初始化界面
    self.ContentTxt.text = ''
end

---------------------------------------------------
-- init end --
---------------------------------------------------


---------------------------------------------------
-- content fix begin --
---------------------------------------------------

---RefreshBoardByGoodsId 根据道具id刷新提示板
---@param goodsId integer @ 道具id
function CommonTipsBoardIncoming:RefreshBoardByGoodsId(goodsId)
    local type = GoodsUtils.GetIdType(goodsId)
    ---@type GoodsVo
    local goodsVo
    if type == Constants.IDType.GoodsBattleFood then
        local CookingUtils = import('Game.UI.Phone.Cooking.CookingUtils')
        goodsVo = CookingUtils.GetBattleCookingFoodVo(goodsId)
    else
        goodsVo = GoodsConfMgr:GetInstance():GetGoodsVoById(goodsId)
    end

    if isNull(goodsVo) then
        -- 未找到道具配置
        self:RefreshBoardByContent(goodsVo,
                checkString(goodsId),
                string.format('%s%s', localize('未找到道具配置'), 0)
        )
    else
        self:FreshIncomingDetail(goodsId)
        -- 刷新
        self:RefreshBoardByContent(goodsVo, goodsVo.name, goodsVo.desc, goodsVo.description)
    end

    --local CountRt = CfUtils.GetNodeRect(self.Content)
    --if CountRt.height < 404 then
    --    self.ScrollView.sizeDelta = Vector2.New(self.ScrollView.sizeDelta.x, CountRt.height)
    --else
    --    self.ScrollView.sizeDelta = Vector2.New(self.ScrollView.sizeDelta.x, 404)
    --end
end

---FreshIncomingDetail
---刷新收益详情的逻辑
---@param goodsId number 道具id
function CommonTipsBoardIncoming:FreshIncomingDetail(goodsId)
    local t = Configs[checkNumber(goodsId)]
    if t and #t > 0 then
        ---存在获取途径
        local parentGo = self.Content.gameObject
        KTool.SetActive(self.OpenAwayNode.gameObject, false)
        for i, v in pairs(t) do
            local go = KTool.Instantiate(parentGo, self.OpenAwayNode.gameObject)
            KTool.SetActive(go, true)
            local node = go.transform:Find("Item2")
            local value = v.valueFunc()
            self:RefreshIncomingItemNode(node, v.name, value , v.isConvertPercent, i)
        end
    end
end

function CommonTipsBoardIncoming:RefreshIncomingItemNode(node, name, value, isConvertPercent, position)
    ---@type CommonIncomingNode
    local nodeEnv = CfUtils.GetLuaScr(node.gameObject, INCOMING_PATH)
    if nodeEnv == nil then return end
    nodeEnv:RefreshUI(name, value, isConvertPercent, position)
end

---RefreshBoardByContent 根据内容刷新提示板
---@param goodsVo GoodsVo @ 道具id
---@param titleText string @ 标题文字
---@param contentText string @ 内容文字
---@param descText string @ 一段描述
function CommonTipsBoardIncoming:RefreshBoardByContent(goodsVo, titleText, contentText, descText)
    -- 刷新文字
    local mb = GetLuaBehaviour(self.TitleNode.gameObject, TitleNodePath)
    if not isNull(mb) then
        if not KTool.IsNull(mb.Env) then
            ---@type CommonTitleNode
            local env = mb.Env
            local rare
            if checkInt(goodsVo.quality) > 0 then
                rare = checkInt(goodsVo.quality)
            end
            env:RefreshUI(titleText, rare)
        end
    end
    CfUtils.FillText(self.ContentTxt2, contentText)
    CfUtils.FillText(self.ContentTxt, descText)


    -- 刷新layout
    self.ContentTxt:GetComponent(_ContentSizeFitter):SetLayoutVertical()
    self.ContentTxt2:GetComponent(_ContentSizeFitter):SetLayoutVertical()
    --self.Bg:GetComponent(_ContentSizeFitter):SetLayoutVertical()
end

---------------------------------------------------
-- content fix end --
---------------------------------------------------

return CommonTipsBoardIncoming