---
--- Generated by shentayibu
--- Created by shentayibu.
--- DateTime: 2023/12/8 17:32
---

--region import
local util = require 'XLua.util'
local cs_coroutine = require 'XLua.cs_coroutine'
local CriWareUtils = import('Game.Entry.CriWareUtilsFix')
--endregion import

--region define
local KTool = CS.Engine.Lib.KTool
local CoWait = CS.Engine.Lib.CoWait
local Animator = CS.UnityEngine.Animator
local UIModule = CS.Engine.UI.UIModule
local Yielders = CS.Engine.Lib.Yielders
local WaitForSeconds = CS.UnityEngine.WaitForSeconds
local Debug = CS.UnityEngine.Debug
local UIRaycastBlocker = CS.Engine.UI.UIRaycastBlocker.Instance
local CDTimerModule = CS.Engine.Modules.CDTimerModule
--endregion define


--- Behavior生命周期：__init -> Awake -> Start -> OnDestroy -> __delete


--- from: Assets/BundleResources/Prefabs/PlayerNew/Player2DReportPopup.prefab > name: cell
---@class AccusationChooseTypeCell
---@field Env                           	AccusationChooseTypeCell                
---@field controller                    	Engine.Modules.LuaBehaviour             
---@field Selected                      	UnityEngine.RectTransform               
---@field Text                          	UnityEngine.RectTransform               
---@field BtnReportContent              	UnityEngine.RectTransform               
local AccusationChooseTypeCell = Class('AccusationChooseTypeCell')

--region lifecycle

function AccusationChooseTypeCell:__init()
    self.events = {}
    self._isCheck = false
    self._text = ""
end

function AccusationChooseTypeCell:__delete()
    self.events = nil
end

function AccusationChooseTypeCell:Awake()
    SetButtonAction(self.BtnReportContent, Bind(self, self.OnCheckHandler))
end

function AccusationChooseTypeCell:Start()

end

function AccusationChooseTypeCell:OnDestroy()
    for k, v in pairs(self.events) do
        Events.RemoveListener(k, v)
    end

    self.controller = nil --删除lua层引⽤到的c#对象否则不会产⽣c# gc回收到对象
    self:Delete()
    self = nil --lua层的gc需要
end
--endregion lifecycle

--region init
--endregion init

--region handler
function AccusationChooseTypeCell:OnCheckHandler()
    self.vo:SwitchCheckState()
    CfUtils.SetActive(self.Selected, self.vo.isChecked)
end
--endregion handler

--region public
---@param vo AccusationVo
function AccusationChooseTypeCell:Refresh(vo)
    self.vo = vo
    CfUtils.FillText(self.Text, self.vo.type)
    CfUtils.SetActive(self.controller.gameObject, true)
end
--endregion public

--region private
--endregion private

--region get / set
--endregion get / set

return AccusationChooseTypeCell
