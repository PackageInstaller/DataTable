---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yuhao.zhao
--- DateTime: 2020/6/28 11:36
--- Project: cat-proj
---
---@class ActivitySurveyDojo
local ActivitySurveyDojo = Class('StoreRecommandDojo')

function ActivitySurveyDojo:__init()
    self.url = 0               --问卷地址
    self.content = 0           --正文内容
    self.button = 0            --按钮内容
    self.submitted = 0         --是否提交
    self.rewards = 0           --奖励列表
    self.canDraw = 0           --是否可以直接领取
end

function ActivitySurveyDojo:__delete()
    self.url = nil               --问卷地址
    self.content = nil           --正文内容
    self.button = nil            --按钮内容
    self.submitted = nil         --是否提交
    self.rewards = nil           --奖励列表
    self.canDraw = nil           --是否可以直接领取
end

function ActivitySurveyDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData, key) and table.haskey(self, key) then
            self[key] = value
        end
    end
end

return ActivitySurveyDojo