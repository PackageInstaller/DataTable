---
--- Generated by vscode.
--- Created by pikaqiu.
--- DateTime: 2024-11-11
---

local ProductDojo = import("Game.Dojo.ProductDojo")

---@class ActivityFashionPassHomeDojo
local ActivityFashionPassHomeDojo = Class("ActivityFashionPassHomeDojo")

function ActivityFashionPassHomeDojo:__init(jsonData)
    self.activityId = checkNumber(jsonData.activityId)
    self.startTime = jsonData.startTime
    self.endTime = jsonData.endTime
    self.closeTime = jsonData.closeTime
    self.exp = checkNumber(jsonData.exp)
    self.bought = jsonData.bought   -- 是否购买了进阶 0:未购买 1:已购买
    ---@type ProductDojo[]
    self.products = {}
    for i, v in ipairs(checkTable(jsonData.products)) do
        table.insert(self.products, ProductDojo.New(v))
    end
    ---@type number[]
    self.drawnLevelRewards = checkTable(jsonData.drawnLevelRewards)
    self.dailyExpStatus = checkNumber(jsonData.dailyExpStatus)  -- 1:可领取 2:不可领取
end

function ActivityFashionPassHomeDojo:__delete()
end

---@return ProductDojo
function ActivityFashionPassHomeDojo:GetPassCardProduct()
    -- ["products"] = 
    -- {
    -- 	[1] = 
    -- 	{
    -- 		["price"] = function: 00007FFFEEC194A0,
    -- 		["windowsPriceId"] = function: 00007FFFEEC194A0,
    -- 		["productId"] = 54001,
    -- 		["channelProductId"] = 54001,
    -- 		["prices"] = 
    -- 		{
    -- 			["3000001"] = 
    -- 			{
    -- 				["original"] = 0,
    -- 				["current"] = 300,
    -- 			},
    -- 		},
    -- 		["channelProducts"] = function: 00007FFFEEC194A0,
    -- 	},
    -- },
    return self.products[1]
end

return ActivityFashionPassHomeDojo