---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yajieli.
--- DateTime: 2019-08-15 10:41
---  迷宫的角色

---@class MazeRoleDojo
local MazeRoleDojo = Class("MazeRoleDojo")

---__int
function MazeRoleDojo:__init()
    self.isAssistant = 1    --是1否0助手 |   默认是支援角色

    self.id = 1           --卡牌id或者助阵角色id
    self.cardId = 1       --
    self.hpPercent = 1  --血量百分比，0=死亡 |
    self.energy = 0    --能量点 |
    self.awakeningLevel = 0    --觉醒等级 |



    self.cardLevel = 1    -- 等级 |
    self.star = 1      --星级 |
end

--服务器数据填充
function MazeRoleDojo:Fill(role)
    for key, value in pairs(role) do
        --只更新自己关心的数据
        if isSet(role, key) and table.haskey(self, key) then
            self[key] = value
        end
    end
    return self
end

function MazeRoleDojo:__delete()
    self.isAssistant = 1    --是1否0助手 |   默认是支援角色

    self.id = 1           --卡牌id或者助阵角色id
    self.cardId = 1       --
    self.hpPercent = 1  --血量百分比，0=死亡 |
    self.energy = 1    --能量点 |

    self.level = 1    -- 等级 |
    self.star = 1      --星级 |
    self.awakeningLevel = 0    --觉醒等级 |

end

return MazeRoleDojo