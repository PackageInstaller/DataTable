---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by liuzhipeng.
--- DateTime: 2022/6/23 14:44
---

---@class StoreLevelGiftDojo
local StoreLevelGiftDojo = Class("StoreLevelGiftDojo")

function StoreLevelGiftDojo:__init()
    self.id         = 0
    self.type       = 1 -- 1:免费 2:付费
    self.level      = 0 -- 等级要求
    self.lastGiftId = 0 --上一个必买的礼包id
    self.rewards    = {} -- 奖励列表
    self.consume    = {} -- 消耗列表
    self.iconId     = 0 -- 图标id
    self.iconText   = '' -- 图标文本
    self.price      = 0 -- 原价
    self.showId     = 0 -- 展示道具id
end

function StoreLevelGiftDojo:__delete()
    self.id         = nil
    self.type       = nil
    self.level      = nil
    self.lastGiftId = nil
    self.rewards    = nil
    self.consume    = nil
    self.iconId     = nil
    self.iconText   = nil
    self.price      = nil
    self.showId     = nil
end

function StoreLevelGiftDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        if isSet(jsonData, key) and table.haskey(self, key) then
            if key == "rewards" then
                for ti = 1, #value do
                    table.insert(self.rewards, { goodsId = value[ti].goodsId, num = value[ti].num})
                end
            else
                self[key] = value
            end
        end
    end
end

return StoreLevelGiftDojo