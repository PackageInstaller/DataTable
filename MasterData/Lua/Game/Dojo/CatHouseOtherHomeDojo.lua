---
--- Generated by 
--- Created by zxr.
--- DateTime: 2021/12/9 5:45 PM
---

---@class CatHouseOtherHomeDojo
local CatHouseOtherHomeDojo = Class("CatHouseOtherHomeDojo")

function CatHouseOtherHomeDojo:__init()
    self.catList = {}          --- 猫列表
    self.masterPlayerId = 0
    self.catHousePopularity = 0
    self.catHousePraised = 0
    self.avatarId = 0
    self.level = 0
    self.avatarFrame = 0
    self.name = ""
end

function CatHouseOtherHomeDojo:__delete()
    self.catList = nil
    self.masterPlayerId = nil
    self.catHousePopularity =  nil
    self.catHousePraised =  nil
    self.avatarId =  nil
    self.level =  nil
    self.avatarFrame =  nil
    self.name =  nil
end

---Fill
---填充数据
---@param jsonData table
function CatHouseOtherHomeDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData, key) and table.haskey(self, key) then
            self[key] = value
        end
    end

    self.localCatShowNum = 0
    for i, v in ipairs(self.catList) do
        local isShow = checkNumber(v.isShow)
        self.localCatShowNum = self.localCatShowNum + isShow
    end
end

---FillFieldData
---填充某个字段数据
---@param key number
function CatHouseOtherHomeDojo:FillFieldData(key, value)
    if not table.haskey(self, key) then
        return
    end
    self[key] = value
end

return CatHouseOtherHomeDojo