---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yuhao.zhao
--- DateTime: 2021/1/18 17:15
--- Project: cat-proj
---

---@class StoreMonthCardDojo
local StoreMonthCardDojo = Class("StoreMonthCardDojo")

function StoreMonthCardDojo:__init()
    --
    self.productId = 0
    self.channelProductId = 0
    self.monthCardId = 0
    self.price = 0
    self.currency = 0
    self.duration = 0
    self.isDrawRewards = -1
    self.loginRewards = {}
    self.endTime = 0
    self.leftSeconds = 0
    self.roundLeftDay = 0
    self.windowsPriceId = 0
    self.accumulativeLoginNum = 0
    self.leftDrawnNum = 0
    self.initPrice = 0 -- 现金原价
    self.initPrices = {} -- 货币原价
    self.isRenew = 0
    self.renewLeftSeconds = 0
    self.prices = {}  --currency  current original
end

function StoreMonthCardDojo:__delete()
    --
    self.productId = nil
    self.channelProductId = nil
    self.monthCardId = nil
    self.price = nil
    self.currency = nil
    self.duration = nil
    self.loginRewards = nil
    self.endTime = nil
    self.leftSeconds = nil
    self.roundLeftDay = nil
    self.windowsPriceId = nil
    self.accumulativeLoginNum = nil
    self.leftDrawnNum = nil
    self.isDrawRewards = nil
    self.initPrice = nil -- 现金原价
    self.initPrices = nil -- 货币原价
    self.isRenew = nil
    self.renewLeftSeconds = nil
end

function StoreMonthCardDojo:SetDrawRewards(isDrawRewards)
    self.isDrawRewards = isDrawRewards
end

function StoreMonthCardDojo:GetDrawRewards()
    return checkInt(self.isDrawRewards)
end

function StoreMonthCardDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData, key) and table.haskey(self, key) then
            if key == 'loginRewards' then
                for i = 1, #value do
                    table.insert(self[key], value[i])
                end
            else
                self[key] = value
            end
        end
    end
end

return StoreMonthCardDojo