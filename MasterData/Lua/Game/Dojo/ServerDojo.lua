---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yajieli.
--- DateTime: 2019-08-16 10:44
---

---@class ServerDojo
local ServerDojo = Class("ServerDojo")

function ServerDojo:__init()
    self.id = 0
    self.type = 0
    self.name = ''
    self.isRecommend = 0
    self.openTime = 0
    self.playerId = 0
    self.playerName = ''
    self.serverId = 0
    self.payCumulative = 0
    self.serverName = ''
    self.status = 0
    self.isNew = 0
    self.areaId = 0
    self.chapterId = 0
    self.isPreOpened = 0
    self.playerLevel = 0
    self.sortIndex = 0
    self.host = ''
end

function ServerDojo:__delete()
    self.id = 0
    self.type = 0
    self.name = ''
    self.isRecommend = 0
    self.openTime = 0
    self.playerId = 0
    self.playerName = ''
    self.serverId = 0
    self.status = 0
    self.isNew = 0
    self.serverName = ''
    self.isPreOpened = 0
    self.playerLevel = 0
    self.payCumulative = 0
    self.areaId = 0
    self.sortIndex = 0
    self.chapterId = 0
    self.host = nil
end

---Update
---更新色彩数据
---@param jsonData table
function ServerDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData,key) and table.haskey(self,key) then
            self[key] = checkString(value)
            --TODO why?
            --if 'playerId' == key then
            --    -- 预下载的情况下此处需要初始化playerId
            --    local httpModule = CS.Engine.Http.HttpModule
            --    httpModule.RequestBodyMeta.PlayerId = checkString(value)
            --    xRecord.setUserPlayer({ playerId = tostring(value) })
            --end
        end
    end
    if checkInt(self.isRecommend) == 1 and checkInt(self.isNew) == 1 then
        self.sortIndex = 3 
    elseif  checkInt(self.isRecommend) == 1 then
        self.sortIndex = 2
    elseif  checkInt(self.isNew) == 1 then
        self.sortIndex = 1
    end
    
end

return ServerDojo