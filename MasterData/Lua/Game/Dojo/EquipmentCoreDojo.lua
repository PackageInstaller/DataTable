---
--- Generated by 
--- Created by zxr.
--- DateTime: 2020/10/15 11:08 上午
---

---@class EquipmentCoreDojo
local EquipmentCoreDojo = Class("EquipmentCoreDojo")

function EquipmentCoreDojo:__init()
    self.id              = 0
    self.playerCardId    = 0
    self.equipmentCoreId = 0
    self.locked          = 0 --- 是1 已锁定  否0
end

function EquipmentCoreDojo:__delete()
    self.id              = nil
    self.playerCardId    = nil
    self.equipmentCoreId = nil
    self.locked          = nil
end

---Fill
---填充数据
---@param jsonData table
function EquipmentCoreDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData, key) and table.haskey(self, key) then
            self[key] = checkString(value)
        end
    end
    return self
end

return EquipmentCoreDojo