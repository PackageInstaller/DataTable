---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yuhao.zhao
--- DateTime: 2020/7/23 18:41
--- Project: cat-proj
---

---@class InvestigateDojo
local InvestigateDojo = Class("InvestigateDojo")

local InvestigateAreaDojo = import("Game.Dojo.InvestigateAreaDojo")

function InvestigateDojo:__init()
    ---@type InvestigateAreaDojo[]
    self.areas = {}
    self.cityRankExp = 0
    self.investigateLeftTimes = 0
    self.leftFreeRefreshTimes = 0
    self.nextFreeRefreshLeftSeconds = 0
end

function InvestigateDojo:__delete()
    self.areas = nil
    self.cityRankExp = nil
    self.investigateLeftTimes = nil
    self.leftFreeRefreshTimes = nil
    self.nextFreeRefreshLeftSeconds = nil
end

function InvestigateDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData, key) and table.haskey(self, key) then
            if key == "areas" then
                for i = 1, #value do
                    ---@type InvestigateAreaDojo
                    local areaDojo = InvestigateAreaDojo.New()
                    areaDojo:Reset()
                    areaDojo:Fill(value[i])
                    self.areas[areaDojo.areaId] = areaDojo
                end
            else
                self[key] = value
            end
        end
    end
end


return InvestigateDojo