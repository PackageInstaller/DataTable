---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by fengtu.
--- DateTime: 2023/3/30 11:29
---
---@class LadderRecordDojo
local LadderRecordDojo = Class('LadderRecordDojo')

function LadderRecordDojo:__init()
    self.mainId = 0         
    self.subId = ''
    self.mainTeam = {}        -- mainTeam         
    self.subTeam = {}         -- subTeam     
    self.campDesc = {}        --队伍头像等信息
    self.isMainWin = 0        --main是否获胜
    self.mainScore = 0          
    self.mainSegment = 0
    self.mainSubSegment = 0
    self.mainDiffScore = 0
    self.subScore = 0
    self.subSegment = 0
    self.subSubSegment = 0
    self.subDiffScore = 0
    self.robotId = 0
    self.time = ''

    self.mainData = {}
    self.subData = {}
end

function LadderRecordDojo:__delete()
    self.mainId = 0
    self.subId = ''
    self.mainTeam = {}        -- mainTeam         
    self.subTeam = {}         -- subTeam     
    self.campDesc = {}        --队伍头像等信息
    self.isMainWin = 0        --main是否获胜

    self.mainScore = 0
    self.mainSegment = 0
    self.mainSubSegment = 0
    self.mainDiffScore = 0

    self.subScore = 0
    self.subSegment = 0
    self.subSubSegment = 0
    self.subDiffScore = 0

    self.robotId = 0
    self.time = ''

    self.mainData = {}
    self.subData = {}
end

function LadderRecordDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData, key) and table.haskey(self, key) then
            self[key] = value
        end
    end

    local campDesc = {}
    for k, v in pairs(self.campDesc) do
        campDesc[checkNumber(k)] = v
    end

    self.mainData = {
        id = checkNumber(self.mainId),
        team = self.mainTeam,
        win = self.isMainWin,
        score = self.mainScore,
        segment = self.mainSegment,
        subSegment = self.mainSubSegment,
        diffScore = self.mainDiffScore,
        desc = campDesc[checkNumber(self.mainId)],
    }
    self.subData = {
        id = checkNumber(self.subId),
        team = self.subTeam,
        win = not self.isMainWin,
        score = self.subScore,
        segment = self.subSegment,
        subSegment = self.subSubSegment,
        diffScore = self.subDiffScore,
        desc = campDesc[checkNumber(self.subId)],
    }
end

function LadderRecordDojo:GetMyData()
    local myPlayerId = GameUtils.GetPlayerId()
    if checkNumber(myPlayerId) == checkNumber(self.mainId) then
        return self.mainData
    else
        return self.subData
    end
end

function LadderRecordDojo:GetOtherData()
    local myPlayerId = GameUtils.GetPlayerId()
    if checkNumber(myPlayerId) == checkNumber(self.subId) then
        return self.mainData
    else
        return self.subData
    end
end

return LadderRecordDojo