---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yuhao.zhao
--- DateTime: 2020/6/28 11:36
--- Project: cat-proj
---
---@class FishingPlaceDojo
local FishingPlaceDojo = Class('StoreRecommandDojo')

function FishingPlaceDojo:__init()
    self.id = 0   --钓场id
    self.partner = 0               --陪钓卡牌唯一id
    self.collectNum = 0             --收集数量
    self.isUnlock = 0 ---是否已解锁
    self.activity = {}             --活动信息
    self.offline = 0               --是否挂机，1是0否
end

function FishingPlaceDojo:__delete()
    self.id = nil   --钓场id
    self.partner = nil               --陪钓卡牌唯一id
    self.collectNum = nil             --收集数量
    self.isUnlock = 0 ---是否已解锁
    self.activity = nil             --活动信息    
    self.offline = nil               --是否挂机，1是0否 
end

function FishingPlaceDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData, key) and table.haskey(self, key) then
            self[key] = value
        end
    end
end

function FishingPlaceDojo:AddCollectNum(num)
    self.collectNum = self.collectNum + num
end

return FishingPlaceDojo