---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yuhao.zhao
--- DateTime: 2020/11/24 15:10
--- Project: cat-proj
---

---@class PetSuitDojo
local PetSuitDojo = Class("PetSuitDojo")

function PetSuitDojo:__init()
    --
    self.uniqueCustomSuitId = 0
    self.name = ''
    self.parts = {}
end

function PetSuitDojo:__delete()
    --
    self.uniqueCustomSuitId = nil
    self.name = nil
    self.parts = nil
end

function PetSuitDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData, key) and table.haskey(self, key) then
            if key == 'parts' then
                for i = 1, #value do
                    table.insert(self[key], value[i])
                end
            else
                self[key] = value
            end
        end
    end
end

return PetSuitDojo