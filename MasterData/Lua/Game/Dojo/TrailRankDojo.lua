---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yajieli.
--- DateTime: 2019-08-16 10:44
---

---@class TrailRankDojo
local TrailRankDojo = Class("TrailRankDojo")


function TrailRankDojo:__init()
    self.room = nil
    self.top = nil
    self.playerRank = 0
end

function TrailRankDojo:__delete()
    self.room = nil
    self.top = nil
    self.playerRank = 0
end

---Update
---更新色彩数据
---@param jsonData table
function TrailRankDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData, key) and table.haskey(self, key) then
            self[key] = checkString(value)
        end
    end

    ---设置rooms相关的数据
    if isSet(jsonData, 'room') then
        local rooms = checkTable(jsonData.room)
        local playerDojo = nil
        self.room = {}
        for i, v in pairs(rooms) do
            ---@type TrailPlayerDojo
            playerDojo = {}
            playerDojo.playerId = checkNumber(v.playerId)
            playerDojo.name = v.name
            playerDojo.level = checkNumber(v.level)
            playerDojo.score = checkNumber(v.score)
            playerDojo.team = checkTable(v.team)
            table.insert(self.room, playerDojo)
        end
    end

    if isSet(jsonData, 'top') then
        local rooms = checkTable(jsonData.room)
        local playerDojo = nil
        self.top = {}
        for i, v in pairs(rooms) do
            ---@type TrailPlayerDojo
            playerDojo = {}
            playerDojo.playerId = checkNumber(v.playerId)
            playerDojo.name = v.name
            playerDojo.level = checkNumber(v.level)
            playerDojo.score = checkNumber(v.score)
            playerDojo.team = checkTable(v.team)
            table.insert(self.top, playerDojo)
        end
    end
end

return TrailRankDojo