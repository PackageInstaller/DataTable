---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yuhao.zhao
--- DateTime: 2020/6/28 11:36
--- Project: cat-proj
---
---@class FishingFishHangUpQueueDojo
local FishingFishHangUpQueueDojo = Class('StoreRecommandDojo')

function FishingFishHangUpQueueDojo:__init()
    self.teamId = 0                   --队伍id
    self.placeId = 0                  --钓场id
    self.cards = 0                    --卡牌唯一id
    self.totalTimes = 0               --总次数
    self.remainTimes = 0              --剩余次数
    self.remainTime = 0               --剩余时间，0已完成
    self.rewards = 0                  --预计奖励
    self.fish = 0                     --鱼
end

function FishingFishHangUpQueueDojo:__delete()
    self.teamId = nil                   --队伍id
    self.placeId = nil                  --钓场id
    self.cards = nil                    --卡牌唯一id
    self.totalTimes = nil               --总次数
    self.remainTimes = nil              --剩余次数
    self.remainTime = nil               --剩余时间，0已完成
    self.rewards = nil                  --预计奖励
    self.fish = nil                     --鱼     
end

function FishingFishHangUpQueueDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData, key) and table.haskey(self, key) then
            self[key] = value
        end
    end
end

return FishingFishHangUpQueueDojo