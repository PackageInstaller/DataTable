---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by xingweihao.
--- DateTime: 2023/4/15 13:12
---

---@class DailyOfferDojo
local DailyOfferDojo = Class("DailyOfferDojo")
function DailyOfferDojo:__init()
    self.productId = 0
    self.dailyOfferId = 0
    self.currentStock = 0
    self.isDraw = 0
    self.stock = 0
    self.price = 0
    self.prices = {}
    self.leftSeconds = 0
    self.windowsPriceId = 0
    self.channelProductId = 0
    self.win = 0
    self.currentTime = 0
end
function DailyOfferDojo:__delete()
    self.productId = nil
    self.dailyOfferId = nil
    self.currentStock = nil
    self.isDraw = nil
    self.stock = nil
    self.price = nil
    self.prices = nil
    self.isDraw = nil
    self.leftSeconds = nil
    self.windowsPriceId = nil
    self.channelProductId = nil
    self.currentTime = nil
end
function DailyOfferDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    self.prices =  jsonData.prices 
    jsonData.prices  = nil
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData,key) and table.haskey(self,key) then
            self[key] = checkString(value)
        end
    end
    self["currentTime"] = os.time()
    return self
end
function DailyOfferDojo:GetLeftSecond()
    return  (self.currentTime + self.leftSeconds)  - os.time()
end

---IsGoodExchange
---是否是道具兑换的逻辑
function DailyOfferDojo:IsGoodExchange()
    if isNotNull(self.prices) then        
        return table.count(self.prices) > 0
    end
end

return DailyOfferDojo