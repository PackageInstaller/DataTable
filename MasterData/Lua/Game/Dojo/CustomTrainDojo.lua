---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yuhao.zhao
--- DateTime: 2020/11/24 15:08
--- Project: cat-proj
---

---@class CustomTrainDojo
local CustomTrainDojo = Class("CustomTrainDojo")

local PetSuitDojo = import('Game.Dojo.PetSuitDojo')

function CustomTrainDojo:__init()
    self.schedule = {
        id = 0 , 
        startDay = 0 ,
        endDay = 0 ,
        groupId = 0,
        upGroup = 0,
        startDayTimestamp = 0,
        endDayTimestamp = 0,
    }
    self.rewardResetLeftSeconds = 0
    self.hasDrawnDamageRewards = 0
    self.isOpened = 0
    self.statusLeftSeconds = 0
    self.damages = {}
    --
end

function CustomTrainDojo:__delete()
    self.schedule = nil
    self.rewardResetLeftSeconds = 0
    self.hasDrawnDamageRewards = 0
    self.isOpened = 0
    self.statusLeftSeconds = 0
    self.damages = nil
end

function CustomTrainDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData, key) and table.haskey(self, key) then
            self[key] = value
        end
    end
end

function CustomTrainDojo:GetStatusLeftSeconds()
    return self.statusLeftSeconds 
end
function CustomTrainDojo:GetIsOpened()
    return self.isOpened 
end
function CustomTrainDojo:GetStartTime()
    return self.schedule.startDayTimestamp
end
function CustomTrainDojo:SethasDrawnDamageRewards(value)
    self.hasDrawnDamageRewards = value
end
function CustomTrainDojo:GetEndTime()
    return self.schedule.endDayTimestamp
end

return CustomTrainDojo