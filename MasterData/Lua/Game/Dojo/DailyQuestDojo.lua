---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yajieli.
--- DateTime: 2019-08-16 09:32
---

---@class DailyQuestDojo
local DailyQuestDojo = Class("DailyQuestDojo")

function DailyQuestDojo:__init()
    self.type = 0 ---副本类型
    self.newestPassedQuest = 0 ---最新完成的关卡
    self.open = 0 ---是否已开启
    self.unlock = 0 ---是否已解锁
    self.energyPoint = 0             --能量点
    self.energyBoxId = 0             --能量宝箱id
    self.energyBoxRemainTime = 0     --能量宝箱剩余时间
    self.freeTimesRemain = 0
end

function DailyQuestDojo:__delete()
    self.type = 0 ---副本类型
    self.newestPassedQuest = 0 ---最新完成的关卡
    self.open = 0 ---是否已开启
    self.unlock = 0 ---是否已解锁
    self.energyPoint = 0
    self.energyBoxId = 0
    self.energyBoxRemainTime = 0
    self.freeTimesRemain = 0

end

function DailyQuestDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData,key) and table.haskey(self,key) then
            self[key] = checkString(value)
        end
    end
    return self
end

return DailyQuestDojo