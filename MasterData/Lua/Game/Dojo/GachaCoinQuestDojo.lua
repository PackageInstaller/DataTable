---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by zf
--- DateTime: 2020/07/07/23 15:15
---

------------ import ------------
------------ import ------------

------------ define ------------
------------ define ------------


---@class GachaCoinQuestDojo
local GachaCoinQuestDojo = Class('GachaCoinQuestDojo')


---__init
function GachaCoinQuestDojo:__init()
    ---@type integer @ 当前挑战的Id
    self.currentId          = 0
    ---@type integer @ 副本等级
    self.copyLevel          = 0
    ---@type integer @ 通过的最新关卡id
    self.newPassedCopyId    = 0
    ---@type integer @ 本周获得的道具数量
    self.goodsSum           = 0
    ---@type table @ 卡牌信息
    self.cardsInfo          = {}
    ---@type number @ boss血量信息
    self.bossHpPercent      = 0
    ---@type table<string, table> 挑战副本类型信息
    self.copyChallengeInfo  = {}
    ---@type integer @ cd刷新剩余时间
    self.timeLeft           = 0
end

---__delete
function GachaCoinQuestDojo:__delete()
    self.currentId          = nil
    self.copyLevel          = nil
    self.newPassedCopyId    = nil
    self.goodsSum           = nil
    self.cardsInfo          = nil
    self.bossHpPercent      = nil
    self.copyChallengeInfo  = nil
    self.timeLeft           = nil
end


---------------------------------------------------
-- logic begin --
---------------------------------------------------

function GachaCoinQuestDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData,key) and table.haskey(self,key) then
            self[key] = value
            if 'newPassedCopyId' == key then
                if 0 == checkInt(value) then
                    -- TODO --
                    self[key] = 90000
                    -- TODO --
                end
            end
        end
    end

    -- debug --
    -- self.newPassedCopyId = 90020
    -- self.currentId = 90020
    -- debug --
end


return GachaCoinQuestDojo