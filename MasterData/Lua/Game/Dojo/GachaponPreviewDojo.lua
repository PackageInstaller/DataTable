---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yuhao.zhao
--- DateTime: 2020/5/14 14:44
--- Project: cat-proj
---

local BaseDojo = import('Game.Dojo.BaseDojo')

---@class GachaponPreviewDojo : BaseDojo
---@field New fun():GachaponPreviewDojo
local GachaponPreviewDojo = Class('GachaponPreviewDojo')


function GachaponPreviewDojo:__init()
    ---@type integer
    self.itemId = 0

    ---@type integer 
    self.itemNum = 0

    ---@type integer @ 是否up（1是0否）
    self.isUp = 0

    ---@type integer @ 出现概率
    self.displayNum = 0

    ---@type integer @ 展示的品质分组
    self.rareGroup = 0
end


function GachaponPreviewDojo:__delete()
    self.itemId     = nil
    self.itemNum    = nil
    self.isUp       = nil
    self.displayNum = nil
    self.rareGroup  = nil
end


---@param jsonData table
---@return GachaponPreviewDojo
function GachaponPreviewDojo:Fill(jsonData)
    for key, value in pairs(jsonData or {}) do
        --只更新自己关心的数据
        if isNotNull(value) and table.haskey(self,key) then
            self[key] = checkInt(value)
        end
    end

    if jsonData then
        self.displayNum = checkNumber(jsonData.displayNum) * 100
    end
    return self
end


return GachaponPreviewDojo
