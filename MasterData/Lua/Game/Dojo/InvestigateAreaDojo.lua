---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yuhao.zhao
--- DateTime: 2020/7/20 14:19
--- Project: cat-proj
---

---@class InvestigateAreaDojo
local InvestigateAreaDojo = Class("InvestigateAreaDojo")
local CardDojo = import('Game.Dojo.CardDojo')

function InvestigateAreaDojo:__init()
    --
    self.areaId = 0
    self.taskId = 0
    self.star = 0
    self.baseDifficulty = {}
    self.extraDifficulty = {}
    self.status = 1
    self.leftSeconds = 0
    self.team = {}
    self.extraDifficultyMeet = {}
    self.cardInfo = {}
end

function InvestigateAreaDojo:__delete()
    --
    self.areaId = nil
    self.taskId = nil
    self.star = nil
    self.baseDifficulty = nil
    self.extraDifficulty = nil
    self.status = nil
    self.leftSeconds = nil
    self.team = nil
    self.extraDifficultyMeet = nil
    self.cardInfo = nil
end

function InvestigateAreaDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData, key) and table.haskey(self, key) then

            if key == "baseDifficulty" or key == "extraDifficulty" or key == "team" or key == "extraDifficultyMeet" then
                self[key] = {}
                for _, num in ipairs(checkTable(value)) do
                    table.insert(self[key], checkNumber(num))
                end
            elseif key == "cardInfo" then
                self[key] = {}
                for _, data in ipairs(checkTable(value)) do
                    local dojo = CardDojo.New()
                    dojo:Fill(data)
                    table.insert(self[key], dojo)
                end
            else
                self[key] = value
            end

        end
    end
end

function InvestigateAreaDojo:Reset()
    self.taskId = 0
    self.star = 0
    self.baseDifficulty = {}
    self.extraDifficulty = {}
    self.status = 1
    self.leftSeconds = 0
    self.team = {}
    self.extraDifficultyMeet = {}
    self.cardInfo = {}
end

return InvestigateAreaDojo