---
--- Generated by 
--- Created by zxr.
--- DateTime: 2021/3/9 4:08 下午
---

---@class PVPArenaEntryDojo
local PVPArenaEntryDojo = Class("PVPArenaEntryDojo")

function PVPArenaEntryDojo:__init()
    self.seasonId                   = 0   --- 赛季id
    self.seasonStatus               = 0   --- 赛季状态 1:等待下一期开启 2:竞猜期 3:公示期 4:比赛期
    self.nextStageLeftSeconds       = 0   --- 当前阶段结束剩余秒数

    self.battleStatus               = 0   --- 比赛期状态 1:等待开始 2:战前准备 3:战斗中 4:结算 (只有比赛期返回数据)
    self.battleNextStageLeftSeconds = 0   --- 当前比赛期阶段结束剩余秒数 (只有比赛期返回数据)

    self.identity                   = 0   --- 身份 1:参选选手 2:观众

    --- warn: Arena/entry (竞技场入口) 独有数据  Arena/home (竞技场首页) 没有这个字段
    self.isEnd                      = -1  --- 参选选手是否比赛失败
end

function PVPArenaEntryDojo:__delete()
    self.seasonStatus               = nil
    self.nextStageLeftSeconds       = nil
    self.battleStatus               = nil
    self.battleNextStageLeftSeconds = nil
    self.identity                   = nil
    self.isEnd                      = nil
end

---Fill
---填充数据
---@param jsonData table
function PVPArenaEntryDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData, key) and table.haskey(self, key) then
            self[key] = value
        end
    end
end

return PVPArenaEntryDojo