---
--- Generated by 
--- Created by zxr.
--- DateTime: 2021/12/9 5:45 PM
---

---@class CatHouseHomeDojo
local CatHouseHomeDojo = Class("CatHouseHomeDojo")

function CatHouseHomeDojo:__init()
    self.catList = {}          --- 猫列表
    self.deliveryList = {}     --- 培育列表
    self.rallySignupList = {}  --- 集会登记列表
    self.exploreList = {}       ---探险列表
    self.exploreBoxId = 0 --- 探索宝箱Id
    self.exploreRefreshTimes = 0 --- 探索刷新次数
    self.exploreNextRefreshTimesSeconds = 0 --- 探索下一次刷新剩余秒数
    self.exploreBoxProgress = 0 --- 探索宝箱进度
    self.toys = {} ---玩具背包
    self.maxCatsNum = 0 ---猫咪列表最大数量
    self.maxSignUpNum = 0 ---登记列表最大数量
    self.maxDeliveryNum = 0 ---培育列表最大数量
    self.partyCatId = 0 --- 伙伴猫id
    self.privilegeCards = {} --- 特权卡数据,KEY:特权卡ID,VALUE:剩余秒数
    self.popularityNum = 0    --- 人气值
    self.visitList = {}      --- 访客列面
    -------------------
    ---客户端自定义字段
    self.localCatShowNum = 0 --- 显示在主界面数量

end

function CatHouseHomeDojo:__delete()
    self.catList = nil
    self.deliveryList = nil
    self.rallySignupList = nil
    self.exploreList = nil
    self.exploreBoxId = nil
    self.exploreBoxProgress = nil
    self.exploreRefreshTimes = nil
    self.exploreNextRefreshTimesSeconds = nil
    self.toys = nil
    self.maxCatsNum = nil
    self.maxSignUpNum = nil
    self.maxDeliveryNum = nil
    self.partyCatId = nil
    self.privilegeCards = nil
    self.popularityNum = nil
    self.visitList = nil
    self.localCatShowNum = nil
end

---Fill
---填充数据
---@param jsonData table
function CatHouseHomeDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData, key) and table.haskey(self, key) then
            self[key] = value
        end
    end

    self.localCatShowNum = 0
    for i, v in ipairs(self.catList) do
        local isShow = checkNumber(v.isShow)
        self.localCatShowNum = self.localCatShowNum + isShow
    end
end

---FillFieldData
---填充某个字段数据
---@param key number
function CatHouseHomeDojo:FillFieldData(key, value)
    if not table.haskey(self, key) then
        return
    end
    self[key] = value
end

return CatHouseHomeDojo