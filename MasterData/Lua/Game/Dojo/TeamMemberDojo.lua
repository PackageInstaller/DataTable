---
--- Generated by EmmyLua(https://github.com/EmmyLua)
---
---@class TeamMemberDojo
local TeamMemberDojo = Class('TeamMemberDojo')

function TeamMemberDojo:__init()
    self.type = 1          --1自己的卡/2助战
    self.refId = 0         --服务器唯一id
    self.cardId = 0        --卡牌id
    self.fettersId = 0         --羁绊id 服务器唯一id
    self.fettersCardId = 0         --羁绊卡牌id
end

function TeamMemberDojo:__delete()
    self.type = nil          --0自己的卡/1助战
    self.refId = nil         --服务器唯一id
    self.cardId = 0        --卡牌id
    self.fettersId = 0         --羁绊id
    self.fettersCardId = 0         --羁绊id 服务器唯一id
end

function TeamMemberDojo:Fill(data)
    data = checkTable(data)
    for key, value in pairs(data) do
        if isSet(data, key) and table.haskey(self, key) then
            self[key] = checkNumber(value)
        end
    end
end

return TeamMemberDojo