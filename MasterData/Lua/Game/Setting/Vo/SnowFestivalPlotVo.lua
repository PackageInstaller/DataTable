---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2022-11-24 17:12:39
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class SnowFestivalPlotVo
local SnowFestivalPlotVo = Class("SnowFestivalPlotVo", SettingRefBase)

---__int
function SnowFestivalPlotVo:__init()
    self.id = nil              --ID
    self.activetyId = nil              --活动ID
    self.chapter = nil              --对应章节ID（0为序章）
    self.name = nil              --副本名
    self.type = nil              --剧情类型（1纯剧情2有战斗）
    self.unlockPoint = nil              --解锁点数（0为不消耗）
    self.unlockPlot = nil              --前置剧情ID
    self.regionId = nil              --剧情关卡ID（对应剧情关卡表）
    self.plotDesc = nil              --剧情入口简介
    self.plotImage = nil              --关卡入口图片
    self.fristRewards = nil              --首次通关掉落
    self.fristRewardsNum = nil              --掉落数量
    self.showItem = nil              --编队UI展示奖励道具
    self.showItemTag = nil              --编队UI展示奖励道具标签，1必定掉落、2概率掉落、3首次通关掉落、4三星奖励
    self.showItemNum = nil              --编队UI展示奖励道具数量
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function SnowFestivalPlotVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.activetyId = parse_number(row, "activetyId")
    self.chapter = parse_number(row, "chapter")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.type = parse_number(row, "type")
    self.unlockPoint = parse_number(row, "unlockPoint")
    self.unlockPlot = parse_string(row, "unlockPlot")
    self.regionId = parse_number(row, "regionId")
    self.plotDesc = ParseUtils.ParseLocalizeText(row, "plotDesc")
    self.plotImage = ParseUtils.ParsePathName(row, "plotImage")
    self.fristRewards = parse_string(row, "fristRewards")
    self.fristRewardsNum = parse_string(row, "fristRewardsNum")
    self.showItem = parse_string(row, "showItem")
    self.showItemTag = parse_string(row, "showItemTag")
    self.showItemNum = parse_string(row, "showItemNum")
    self.status = parse_number(row, "status")
end

function SnowFestivalPlotVo:__delete()
    self.id = nil
    self.activetyId = nil
    self.chapter = nil
    self.name = nil
    self.type = nil
    self.unlockPoint = nil
    self.unlockPlot = nil
    self.regionId = nil
    self.plotDesc = nil
    self.plotImage = nil
    self.fristRewards = nil
    self.fristRewardsNum = nil
    self.showItem = nil
    self.showItemTag = nil
    self.showItemNum = nil
    self.status = nil
end

return SnowFestivalPlotVo
