---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-01-18 10:16:02
---好感度经验奖励表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils
local parse_number = parse_number
local parse_string = parse_string

---@class FavorableExpRewardVo
local FavorableExpRewardVo = Class("FavorableExpRewardVo", SettingRefBase)

---__int
function FavorableExpRewardVo:__init()
    self.id = nil              --索引ID（卡牌id+等级00-05）
    self.cardId = nil              --卡牌id
    self.level = nil              --好感度等级
    self.consNum = nil              --升级需要好感度
    self.rewardType = nil              --主奖励类型：1、道具 2、语音 3、钻石货币（猫眼石）4、动作 5、属性
    self.rewardItem = nil              --奖励道具id
    self.rewardNum = nil              --奖励道具数量
    self.voice = nil              --语音id
    self.action = nil              --动作id
    self.file = nil              --档案id
    self.property = nil              --奖励属性值
    self.mood = nil                  --心情值上限
end

---FillVo
---@param row TableML.TableFileRow
function FavorableExpRewardVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_string(row, "id")
    self.cardId = parse_number(row, "cardId")
    self.level = parse_number(row, "level")
    self.consNum = parse_string(row, "consNum")
    self.rewardType = parse_number(row, "rewardType")
    self.rewardIcon = parse_pathName(row, "rewardIcon")
    self.rewards = parse_rewards(parse_string(row, "rewardItem"), parse_string(row, "rewardNum"))
    self.voice = parse_string(row, "voice")
    self.action = parse_string(row, "action")
    self.action_txt = parse_localizeText(row, "action_txt")
    self.file = parse_number(row, "file")
    self.property = parse_string(row, "property")
    self.mood = parse_number(row,"mood")
end

function FavorableExpRewardVo:GetAttributes()
    if self.attributes == nil then
        self.attributes = parseTwoDelimiterStr2Dict(self.property)
    end
    return self.attributes
end

function FavorableExpRewardVo:__delete()
    self.id = nil
    self.cardId = nil
    self.level = nil
    self.consNum = nil
    self.rewardType = nil
    self.rewardItem = nil
    self.rewardNum = nil
    self.voice = nil
    self.action = nil
    self.file = nil
    self.property = nil
    self.mood = nil
end

return FavorableExpRewardVo
