---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2024-05-25 16:35:17
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class CrisisEventLevelVo
local CrisisEventLevelVo = Class("CrisisEventLevelVo", SettingRefBase)

---__int
function CrisisEventLevelVo:__init()
    self.id = nil              --关卡id
    self.activityId = nil              --活动id
    self.type = nil              --关卡类型（1常驻 2轮替）
    self.unlockTime = nil              --解锁时间(单位秒，以活动开启为起始）
    self.name = nil              --名称
    self.desc = nil              --描述
    self.battleRules = nil              --战斗规则字段
    self.proposalLevel = nil              --推荐战斗力
    self.sceneId = nil              --场景ID
    self.rounds = nil              --回合限制(超出后直接失败)
    self.petLineup = nil              --机器人阵容ID（填0为不需要机器人阵容）
    self.roleNun = nil              --最多上阵人数（1为下限 4为上限）
    self.roleRestrictions = nil              --角色限制ID
    self.repeatButton = nil              --重复挑战关卡按钮显示（0不1是）
    self.powerRatioTeamID = nil              --战力碾压组别
    self.normalFit = nil              --小怪数值自适应ID组
    self.bossFit = nil              --boss数值自适应ID组
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function CrisisEventLevelVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.activityId = parse_number(row, "activityId")
    self.type = parse_number(row, "type")
    self.unlockTime = parse_string(row, "unlockTime")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
    self.battleRules = parse_string(row, "battleRules")
    self.proposalLevel = parse_number(row, "proposalLevel")
    self.sceneId = parse_number(row, "sceneId")
    self.rounds = parse_number(row, "rounds")
    self.petLineup = parse_number(row, "petLineup")
    self.roleNun = parse_string(row, "roleNun")
    self.roleRestrictions = parse_number(row, "roleRestrictions")
    self.repeatButton = parse_number(row, "repeatButton")
    self.powerRatioTeamID = parse_number(row, "powerRatioTeamID")
    self.normalFit = parse_string(row, "normalFit")
    self.bossFit = parse_string(row, "bossFit")
    self.status = parse_number(row, "status")
end

function CrisisEventLevelVo:__delete()
    self.id = nil
    self.activityId = nil
    self.type = nil
    self.unlockTime = nil
    self.name = nil
    self.desc = nil
    self.battleRules = nil
    self.proposalLevel = nil
    self.sceneId = nil
    self.rounds = nil
    self.petLineup = nil
    self.roleNun = nil
    self.roleRestrictions = nil
    self.repeatButton = nil
    self.powerRatioTeamID = nil
    self.normalFit = nil
    self.bossFit = nil
    self.status = nil
end

return CrisisEventLevelVo
