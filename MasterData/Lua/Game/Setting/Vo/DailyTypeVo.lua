---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Administrator.
--- DateTime: 2020/3/26 15:29
---




local SettingRefBase = require('Game.Setting.SettingRefBase')

---@class DailyTypeVo
local DailyTypeVo = Class("DailyTypeVo", SettingRefBase)

local ParseUtils = CS.ParseUtils
local base = SettingRefBase

function DailyTypeVo:Init()
    self.id = nil
    self.name = nil  -- 名字
    self.autoName = nil  -- 委托作战类型名
    self.autoDesc = nil  -- 委托作战描述
    self.autoIcon = nil         -- 委托作战图标
    self.sequence = nil  -- 位置
    self.battleMap = nil  --  背景地图
    self.OpenDescr = nil   -- 开放描述
    self.openWeekday = nil  -- 开放时间
    self.status = nil
    self.battleSelectionItem = nil
    self.energyPoint = nil
    self.weekday = nil
    self.unlockType = nil
    self.unlockNum = nil
    self.energyPointItem = nil
    self.freeTimes = nil --免费次数

end

function DailyTypeVo:__init()
    self:Init()
end

function DailyTypeVo:__delete()
    self:Init()
end


---FillVo
---@param row TableML.TableFileRow
function DailyTypeVo:FillVo(row)
    base.FillVo(self, row)
    self.id = parse_number(row, "id", 0)
    self.name = parse_string(row, "name", '')
    self.autoName = ParseUtils.ParseLocalizeText(row, "autoName")
    self.autoDesc = ParseUtils.ParseLocalizeText(row, "autoDesc")
    self.autoIcon = ParseUtils.ParsePathName(row, "autoIcon")
    self.sequence = parse_number(row, "sequence", 0)
    self.costItem = parse_number(row, "costItem", 0)
    self.OpenDescr = parse_string(row, "OpenDescr", '')
    self.openWeekday = parse_string(row, "openWeekday", '')
    self.battleMap = parse_string(row, "battleMap", '')
    self.battleSelectionItem = ParseUtils.ParsePathName(row, "battleSelectionItem")
    self.energyPoint = ParseUtils.ParsePathName(row, "energyPoint")
    self.unlockType = parse_number(row, "unlockType", 0)
    self.unlockNum = parse_number(row, "unlockNum", 0)
    self.freeTimes = parse_number(row, "freeTimes", 0)
    local weekday = string.split(self.openWeekday, ";")
    self.weekday = weekday
    self.energyPointItem = parse_string(row, "energyPointItem", '')
end
return DailyTypeVo