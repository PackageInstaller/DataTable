---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2020-09-24 15:31:19
--- 高级装备副本类型表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class QuestSeniorEquipTypeVo
local QuestSeniorEquipTypeVo = Class("QuestSeniorEquipTypeVo", SettingRefBase)

---__int
function QuestSeniorEquipTypeVo:__init()
    self.id = nil              --索引（对应副本类型）
    self.name = nil              --副本类型名称
    self.suitId = nil              --套装id
    self.sequence = nil              --位置编号
    self.enterImage = nil              --入口资源
    self.unlockNum = nil              --解锁条件值（对应初级装备本关卡ID，填充为0）
    self.unlockDesc = nil              --解锁条件描述
    self.showItem = nil             --对应线索卡道具
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function QuestSeniorEquipTypeVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = parse_localizeText(row, "name")
    self.sequence = parse_number(row, "sequence")
    self.enterImage = ParseUtils.ParsePathName(row, "enterImage")
    self.unlockNum = parse_string(row, "unlockNum")
    self.showItem = parse_number(row, "showItem")
    self.unlockDesc = parse_localizeText(row, "unlockDesc")
    self.status = parse_number(row, "status")

    self.suitId = {}
    for index, id in pairs(ParseUtils.ParseListStrByHeaderName(row, "suitId")) do
        self.suitId[index + 1] = tonumber(id)
    end
end

function QuestSeniorEquipTypeVo:__delete()
    self.id = nil
    self.name = nil
    self.sequence = nil
    self.enterImage = nil
    self.unlockNum = nil
    self.unlockDesc = nil
    self.unlockNum = nil    
    self.status = nil
end

return QuestSeniorEquipTypeVo
