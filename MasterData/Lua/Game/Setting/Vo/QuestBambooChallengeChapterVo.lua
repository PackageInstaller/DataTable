---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2021-03-02 11:52:51
--- PT挑战副本类型表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class QuestBambooChallengeChapterVo
local QuestBambooChallengeChapterVo = Class("QuestBambooChallengeChapterVo", SettingRefBase)

---__int
function QuestBambooChallengeChapterVo:__init()
    self.id = nil              --索引（对应副本类型）
    self.activetyId = nil              --活动ID
    self.name = nil              --副本类型名称
    self.sequence = nil              --位置编号
    self.unlockChapter = nil              --前置章节（填充为0）
    self.unlockDesc = nil              --解锁条件文本
    self.regionIcon = nil              --关卡图标
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function QuestBambooChallengeChapterVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.activetyId = parse_number(row, "activetyId")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.sequence = parse_number(row, "sequence")
    self.unlockChapter = parse_string(row, "unlockChapter")
    self.unlockDesc = ParseUtils.ParseLocalizeText(row, "unlockDesc")
    self.regionIcon = ParseUtils.ParsePathName(row, "regionIcon")
    self.status = parse_number(row, "status")
end

function QuestBambooChallengeChapterVo:__delete()
    self.id = nil
    self.activetyId = nil
    self.name = nil
    self.sequence = nil
    self.unlockChapter = nil
    self.unlockDesc = nil
    self.regionIcon = nil
    self.status = nil
end

return QuestBambooChallengeChapterVo
