---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2020-07-21 14:20:25
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class TrailSegmentRewardVo
local  TrailSegmentRewardVo = Class("TrailSegmentRewardVo", SettingRefBase)

---__int
function TrailSegmentRewardVo:__init()
    self.id = nil              --段位id
    self.name = nil
    self.upGrade = nil
    self.keepGrade = nil
    self.downGrade = nil
    self.upRewards = nil
    self.keepRewards = nil
    self.downRewards = nil
    self.limitNum = nil
    self.upGradeText = nil
    self.keepGradeText = nil
    self.downGradeText = nil
    self.icon = nil
    self.roundNum = nil
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function TrailSegmentRewardVo:FillVo(row)
    base.FillVo(self, row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.upGradeText = ParseUtils.ParseLocalizeText(row, "upGradeText")
    self.keepGradeText = ParseUtils.ParseLocalizeText(row, "keepGradeText")
    self.downGradeText = ParseUtils.ParseLocalizeText(row, "downGradeText")
    self.icon = parse_number(row, "icon", 1)
    self.roundNum = parse_number(row, "roundNum", 1)
    local rankRewardId = parse_string(row, "upPrizeId")
    local rankRewardNum = parse_string(row, "upPrizeNum")
    local upGradeStr = parse_string(row, "upGrade")
    local keepGradeStr = parse_string(row, "keepGrade", '0')
    local downGradeStr = parse_string(row, "downGrade", '0')
    if upGradeStr == '0' then
        self.upGrade = 0
    else
        self.upGrade = string.split(upGradeStr, ';')
    end
    if keepGradeStr == '0' then
        self.keepGrade = 0
    else
        self.keepGrade = string.split(keepGradeStr, ';')
    end
    if downGradeStr == '0' then
        self.downGrade = 0
    else
        self.downGrade = string.split(downGradeStr, ';')
    end

    self.upRewards = parse_rewards(rankRewardId, rankRewardNum)
    rankRewardId = parse_string(row, "keepPrizeId", '')
    rankRewardNum = parse_string(row, "keeepPrizeNum", '')
    self.keepRewards = parse_rewards(rankRewardId, rankRewardNum)
    rankRewardId = parse_string(row, "downPrizeId", '')
    rankRewardNum = parse_string(row, "downPrizeNum", '')
    self.downRewards = parse_rewards(rankRewardId, rankRewardNum)
    self.limitNum = parse_number(row, "limitNum")
    self.status = parse_number(row, "status")
end

function TrailSegmentRewardVo:__delete()
    self.id = nil
    self.name = nil
    self.upRewards = nil
    self.upGrade = nil
    self.keepRewards = nil
    self.downRewards = nil
    self.limitNum = nil
    self.upGradeText = nil
    self.keepGradeText = nil
    self.downGradeText = nil
    self.icon = nil
    self.roundNum = nil
    self.status = nil
end

return TrailSegmentRewardVo
