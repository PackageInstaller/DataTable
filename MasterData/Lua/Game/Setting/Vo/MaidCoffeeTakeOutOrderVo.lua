---
--- Generated by suixin
--- Created by suixin.
--- DateTime: 2023/2/23 14:37
---
local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils
---@class MaidCoffeeTakeOutOrderVo : SettingRefBase
local MaidCoffeeTakeOutOrderVo = Class("MaidCoffeeTakeOutOrderVo", SettingRefBase)

function MaidCoffeeTakeOutOrderVo:__init()
    self.area = nil                 ---对应区域
    self.areaLevel = nil     ---外卖解锁等级
    --self.restaurantLevel = nil     ---餐厅等级
    self.group = nil     ---所属位置
    --self.name = nil                 ---订单名称
    self.decr = nil                 ---描述
    self.foodNum = nil              ---任务目标值（num）
    self.rewardId = nil             ---需求食物种类
    self.rewardNum = nil            ---奖励道具id
    self.goods = nil
    self.mood = nil                 ---奖励道具数量
    self.time = nil                 ---消耗心情值
    --self.icon = nil                 ---头像
    --self.rewardUpId = nil       ---倾向道具
end

---FillVo
---@param row TableML.TableFileRow
function MaidCoffeeTakeOutOrderVo:FillVo(row)
    base.FillVo(self, row)
    self.area = parse_number(row, "area")
    self.areaLevel = parse_number(row, "areaLv")
    --self.restaurantLevel = parse_number(row, "restaurantLv")
    self.group = parse_number(row, "group")
    --self.name = parse_string(row, "name")
    self.decr = parse_localizeText(row, "decr", "")
    self.foodNum = parse_number(row, "foodNum")
    self.rewardId = parse_string(row, "rewardId")
    self.rewardNum = parse_string(row, "rewardNum")
    self.goods = parse_rewards(self.rewardId,self.rewardNum,";")
    self.mood = parse_number(row, "mood")
    self.time = parse_number(row, "time")
    --self.icon = ParseUtils.ParsePathName(row, "icon")
    --self.rewardUpId = parse_number(row, "rewardUpId")
end

function MaidCoffeeTakeOutOrderVo:__delete()
    self.area = nil
    --self.name = nil
    self.decr = nil
    self.foodNum = nil
    self.rewardId = nil
    self.rewardNum = nil
    self.mood = nil
    self.time = nil
    --self.icon = nil
    self.goods = nil
    --self.rewardUpId = nil
end

return MaidCoffeeTakeOutOrderVo