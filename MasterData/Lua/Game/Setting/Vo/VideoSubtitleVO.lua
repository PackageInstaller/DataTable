---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-08-08 16:15:26
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class VideoSubtitleVO
local VideoSubtitleVO = Class("VideoSubtitleVO", SettingRefBase)

---__int
function VideoSubtitleVO:__init()
    self.id = nil              --序号
    self.animeId = nil              --视频id    对应剧情表中的 sceneId
    self.subtitle = nil              --字幕内容
    self.timeFrom = nil              --字幕进入时间
    self.timeTo = nil              --字幕消失时间
    self.fadeIn = nil              --淡入时长
    self.fadeOut = nil              --淡出时长
    self.status = nil              --状态开关1开0关
end

---FillVo
---@param row TableML.TableFileRow
function VideoSubtitleVO:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.animeId = parse_string(row, "animeId")
    self.subtitle = ParseUtils.ParseLocalizeText(row, "subtitle")

    -- 时间换算成秒
    self.timeFrom = parse_number(row, "inTime") / 1000.0
    self.timeTo = parse_number(row, "outTime") / 1000.0
    self.fadeIn = parse_number(row, "fadeIn") / 1000.0
    self.fadeOut = parse_number(row, "fadeOut") / 1000.0 

    self.status = parse_number(row, "status")
end

function VideoSubtitleVO:__delete()
    self.id = nil
    self.animeId = nil
    self.subtitle = nil
    self.timeFrom = nil
    self.timeTo = nil
    self.fadeIn = nil
    self.fadeOut = nil
    self.status = nil
end

return VideoSubtitleVO
