---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2020-10-26 11:33:20
--- 剧情副本关卡表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class QuestRoleStoryVo
local QuestRoleStoryVo = Class("QuestRoleStoryVo", SettingRefBase)

---__int
function QuestRoleStoryVo:__init()
    self.id = nil              --关卡ID（3开头；角色ID（两位）；关卡数（1~99））
    self.name = nil              --名称
    self.stageID = nil              --对应章节ID（剧情副本章节奖励表）
    self.monsteType = nil              --怪物类型（1普通 2BOSS  0无战斗填充）
    self.type = nil              --剧情类型（2剧情+战斗 1有剧情  0仅战斗）
    self.unlockNum1 = nil              --解锁条件值（填充为0，固定为前置关卡解锁）
    self.plotId = nil              --剧情ID
    self.opportunity = nil              --出现时机（1战前 2-5波次 6战后）
    self.SceneId = nil              --场景ID
    self.rounds = nil              --回合限制(超出后直接失败)
    self.petLineup = nil              --机器人阵容ID（填0为不需要机器人阵容）
    self.roleNun = nil              --最多上阵人数（1为下限 4为上限）
    self.extraSkill = nil              --最多上阵人数（1为下限 4为上限）
    self.battleRules = nil              --最多上阵人数（1为下限 4为上限）
    self.posX = nil              --最多上阵人数（1为下限 4为上限）
    self.posY = nil              --最多上阵人数（1为下限 4为上限）
    self.finale = nil              --最多上阵人数（1为下限 4为上限）
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function QuestRoleStoryVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.stageID = parse_number(row, "stageID")
    self.monsteType = parse_number(row, "monsteType")
    self.type = parse_number(row, "type")
    self.unlockNum1 = parse_string(row, "unlockNum1")
    self.unlockType = parse_string(row, "unlockType")
    self.unlockNum2 = parse_string(row, "unlockNum2")
    self.unlockNum3 = parse_string(row, "unlockNum3")
    self.plotId = parse_string(row, "plotId")
    self.opportunity = parse_string(row, "opportunity")
    self.SceneId = parse_number(row, "SceneId")
    self.rounds = parse_number(row, "rounds")
    self.petLineup = parse_number(row, "petLineup")
    self.roleNun = parse_string(row, "roleNun")
    self.extraSkill = parse_string(row, "extraSkill")
    self.battleRules = parse_string(row, "battleRules")
    self.posX = parse_number(row, "posX", 0)
    self.posY = parse_number(row, "posY", 0)
    self.finale = parse_number(row, "finale", 0)
    self.status = parse_number(row, "status")
end

function QuestRoleStoryVo:__delete()
    self.id = nil
    self.name = nil
    self.stageID = nil
    self.monsteType = nil
    self.type = nil
    self.unlockNum = nil
    self.plotId = nil
    self.opportunity = nil
    self.SceneId = nil
    self.rounds = nil
    self.petLineup = nil
    self.roleNun = nil
    self.extraSkill = nil              --最多上阵人数（1为下限 4为上限）
    self.battleRules = nil              --最多上阵人数（1为下限 4为上限）
    self.posX = nil              --最多上阵人数（1为下限 4为上限）
    self.posY = nil              --最多上阵人数（1为下限 4为上限）
    self.status = nil
end

return QuestRoleStoryVo
