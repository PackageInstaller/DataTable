---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yajieli.
--- DateTime: 2020/4/15 1:42 PM
--- 材料合成的数据对像

local SettingRefBase = require('Game.Setting.SettingRefBase')

---@class MatCompositeVo
local MatCompositeVo = Class("MatCompositeVo", SettingRefBase)
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class RewardVo
local RewardVo = {}
function RewardVo.New(goodsId, goodsNum)
    ---@type RewardVo
    local this = {}
    setmetatable(this, { __index = RewardVo })
    this.goodsId = goodsId
    this.goodsNum = goodsNum
    return this
end

---__int
function MatCompositeVo:__init()
    self.id = nil
    self.type = nil
    self.gainVo = nil
end

---FillVo
---@param row TableML.TableFileRow
function MatCompositeVo:FillVo(row)
    base.FillVo(self, row)
    self.id = row:GetPrimaryKey()
    self.type = parse_number(row, "type", 0)
    self.type2 = parse_number(row, "type2", 0)
    local goodsId = parse_string(row, "gainId", '')
    local goodsNum = parse_string(row, "gainNum", '')
    self.gainVo = RewardVo.New(goodsId, goodsNum)
    self.costItem = parse_number(row, "costIteam", 0)
    self.costItemNum = parse_number(row, "costIteamNum", 0)
    self.gainId = parse_number(row, "gainId", 0)
end

function MatCompositeVo:__delete()
    self.id = nil
    self.gainVo = nil
end

return MatCompositeVo