---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2024-03-22 18:21:49
--- 战略强化切页表
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class HomelandTalentLabelVo
local HomelandTalentLabelVo = Class("HomelandTalentLabelVo", SettingRefBase)

---__int
function HomelandTalentLabelVo:__init()
    self.id = nil              --第几条线
    self.name = nil              --天赋名称
    self.unlockType = nil              --开启条件（对应解锁条件表里的数值）
    self.unlockId = nil              --开启条件ID
    self.unlockNum = nil              --开启条件数值
    self.icon = nil              --图标
    self.resetItem = nil
    self.resetNum = nil
    self.resetConsumes = nil
end

---FillVo
---@param row TableML.TableFileRow
function HomelandTalentLabelVo:FillVo(row)
    base.FillVo(self,row)
    self.id         = parse_number(row, "id")
    self.name       = parse_localizeText(row, "name")
    self.unlockType = parse_string(row, "unlockType")
    self.unlockId   = parse_number(row, "unlockId")
    self.unlockNum  = parse_string(row, "unlockNum")
    local icon      = parse_string(row, "icon")
    if not string.isEmpty(icon) then
        self.icon                 = parse_pathNameByData(icon, ".png")
    end
    self.resetItem      = parse_string(row, "resetItem")
    self.resetNum       = parse_string(row, "resetNum")
    self.resetConsumes  = parse_rewards(self.resetItem, self.resetNum)
end

function HomelandTalentLabelVo:__delete()
    self.id = nil
    self.name = nil
    self.unlockType = nil
    self.unlockId = nil
    self.unlockNum = nil
    self.icon = nil
    self.resetItem = nil
    self.resetNum = nil
    self.resetConsumes = nil
end

return HomelandTalentLabelVo
