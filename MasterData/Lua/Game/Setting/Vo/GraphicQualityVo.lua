---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2020-11-10 13:46:46
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

local LocalizeNameMap = {
    [0] = function() return localize("流畅") end,
    [1] = function() return localize("快速") end,
    [2] = function() return localize("标准") end,
    [3] = function() return localize("高级") end,
    [4] = function() return localize("超清") end,
}

---@class GraphicQualityVo
local GraphicQualityVo = Class("GraphicQualityVo", SettingRefBase)

---__int
function GraphicQualityVo:__init()
    self.level = nil              --配置等级
    self.name = nil              --显示名
    self.graphicLevel = nil              --画质等级
    self.shadowLevel = nil              --阴影等级
    self.resolutionLevel = nil              --分辨率等级
    self.bloomLevel = nil              --bloom等级
    self.fxaaLevel = nil              --抗锯齿等级
    self.fpsLevel = nil              --fps等级
    self.lodLevel = nil              --材质等级
    --self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function GraphicQualityVo:FillVo(row)
    --base.FillVo(self,row)
    self.level = parse_number(row, "level")
    self.name = LocalizeNameMap[self.level]()
    self.graphicLevel = parse_number(row, "graphicLevel")
    self.shadowLevel = parse_number(row, "shadowLevel")
    self.resolutionLevel = parse_number(row, "resolutionLevel")
    self.bloomLevel = parse_number(row, "bloomLevel")
    self.fxaaLevel = parse_number(row, "fxaaLevel")
    self.fpsLevel = parse_number(row, "fpsLevel")
    self.lodLevel = parse_number(row, "lodLevel")
    --self.status = parse_number(row, "status")
end

function GraphicQualityVo:__delete()
    self.level = nil
    self.name = nil
    self.graphicLevel = nil
    self.shadowLevel = nil
    self.resolutionLevel = nil
    self.bloomLevel = nil
    self.fxaaLevel = nil
    self.fpsLevel = nil
    self.lodLevel = nil
    --self.status = nil
end

return GraphicQualityVo
