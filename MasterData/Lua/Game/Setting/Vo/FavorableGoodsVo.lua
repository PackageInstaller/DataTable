---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-01-18 10:16:14
---好感度礼物道具表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class FavorableGoodsVo
local FavorableGoodsVo = Class("FavorableGoodsVo", SettingRefBase)

---__int
function FavorableGoodsVo:__init()
    self.id = nil              --道具编号
    self.name = nil              --名称（读字库）
    self.desc = nil              --描述
    self.description = nil              --功能说明
    self.picture = nil              --图片
    self.type = nil              --品质色
    self.sort = nil              --排序
    self.openType = nil              --跳转目标（填功能切割表里的ID）
    self.openParameter = nil              --跳转参数（填写关卡ID）
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function FavorableGoodsVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
    self.description = ParseUtils.ParseLocalizeText(row, "description")
    self.picture = parse_string(row, "picture")
    self.type = parse_string(row, "type")
    self.sort = parse_number(row, "sort")
    self.openType = parse_string(row, "openType")
    self.openParameter = parse_string(row, "openParameter")
    self.status = parse_number(row, "status")
end

function FavorableGoodsVo:__delete()
    self.id = nil
    self.name = nil
    self.desc = nil
    self.description = nil
    self.picture = nil
    self.type = nil
    self.sort = nil
    self.openType = nil
    self.openParameter = nil
    self.status = nil
end

return FavorableGoodsVo
