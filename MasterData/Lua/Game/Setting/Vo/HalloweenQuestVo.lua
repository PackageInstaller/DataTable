---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2022-10-11 16:50:24
--- 奖池副本关卡表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class HalloweenQuestVo
local HalloweenQuestVo = Class("HalloweenQuestVo", SettingRefBase)

---__int
function HalloweenQuestVo:__init()
    self.id = nil              --ID
    self.type = nil              --副本类型（对应中型PT活动表ID）
    self.label = nil              --标题
    self.name = nil              --副本名
    self.desc = nil              --关卡描述
    self.regionLevel = nil              --难度等级
    self.starCondition = nil              --3星条件
    self.starConditionNum = nil              --3星条件数值
    self.starConditionRound = nil              --条件回合数限制
    self.unlockNum = nil              --解锁条件值（填充为0）
    self.proposalLevel = nil              --推荐战斗力
    self.frequency = nil              --消耗的副本进入次数
    self.costItem = nil              --副本消耗道具
    self.costNum = nil              --消耗道具数量
    self.costNumLose = nil              --战斗失败时消耗道具数量
    self.SceneId = nil              --场景ID
    self.rounds = nil              --回合限制(超出后直接失败)
    self.teamLimit = nil              --队伍武装值要求
    self.powerMin = nil              --通关队伍角色最高战力要求
    self.powerTeamMin = nil              --通关编队战力要求
    self.repeatButton = nil              --重复挑战关卡按钮显示（0不1是）
    self.showContinueBtn = nil              --是否显示连续战斗按钮
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function HalloweenQuestVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.type = parse_number(row, "type")
    self.label = ParseUtils.ParseLocalizeText(row, "label")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
    self.regionLevel = parse_number(row, "regionLevel")
    self.starCondition = parse_string(row, "starCondition")
    self.starConditionNum = parse_string(row, "starConditionNum")
    self.starConditionRound = parse_string(row, "starConditionRound")
    self.unlockNum = parse_number(row, "unlockNum")
    self.proposalLevel = parse_number(row, "proposalLevel")
    self.frequency = parse_number(row, "frequency")
    self.costItem = parse_number(row, "costItem")
    self.costNum = parse_number(row, "costNum")
    self.costNumLose = parse_number(row, "costNumLose")
    self.SceneId = parse_number(row, "SceneId")
    self.rounds = parse_number(row, "rounds")
    self.teamLimit = parse_number(row, "teamLimit")
    self.powerMin = parse_number(row, "powerMin")
    self.powerTeamMin = parse_number(row, "powerTeamMin")
    self.repeatButton = parse_number(row, "repeatButton")
    self.showContinueBtn = parse_number(row, "showContinueBtn")
    self.status = parse_number(row, "status")
end

function HalloweenQuestVo:__delete()
    self.id = nil
    self.type = nil
    self.label = nil
    self.name = nil
    self.desc = nil
    self.regionLevel = nil
    self.starCondition = nil
    self.starConditionNum = nil
    self.starConditionRound = nil
    self.unlockNum = nil
    self.proposalLevel = nil
    self.frequency = nil
    self.costItem = nil
    self.costNum = nil
    self.costNumLose = nil
    self.SceneId = nil
    self.rounds = nil
    self.teamLimit = nil
    self.powerMin = nil
    self.powerTeamMin = nil
    self.repeatButton = nil
    self.status = nil
end

return HalloweenQuestVo
