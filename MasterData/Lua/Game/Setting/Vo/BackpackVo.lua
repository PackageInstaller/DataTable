---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- DateTime: 2023-02-17 13:19:32

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class BackpackVo : SettingRefBase
local BackpackVo = Class("BackpackVo", SettingRefBase)

---__int
function BackpackVo:__init()
    self.id = nil              --页签id
    self.name = nil              --背包页签名
    self.text = nil              --背包页签名
    ---@type string[]
    self.itemType = nil              --包含的道具类型id
    self.initialNum = nil              --格子初始上限
    self.isSaled = nil              --是否可以出售格子
    self.costItemId = nil              --买格子消耗道具id
    self.cellNum = nil              --每次买格子数量
    self.costNum = nil              --每次买格子消耗道具数量（配几个就是可以买几次）
    self.selectImgPath = nil
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function BackpackVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.text = ParseUtils.ParseLocalizeText(row, "name")
    self.selectImgPath = ParseUtils.ParsePathName(row, 'imgTag')
    self.itemType = parse_strings(parse_string(row, "itemType"))
    self.initialNum = parse_number(row, "initialNum")
    self.isSaled = parse_number(row, "isSaled")
    self.costItemId = parse_number(row, "costItemId")
    self.cellNum = parse_number(row, "cellNum")
    self.costNum = parse_strings(parse_string(row, "costNum"))
    self.status = parse_number(row, "status")
end

function BackpackVo:__delete()
    self.id = nil
    self.name = nil
    self.itemType = nil
    self.initialNum = nil
    self.isSaled = nil
    self.costItemId = nil
    self.cellNum = nil
    self.costNum = nil
    self.selectImgPath = nil
    self.status = nil
end

return BackpackVo
