---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2020-09-10 11:03:12
--- 关卡条件表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class QuestConditionVo
local QuestConditionVo = Class("QuestConditionVo", SettingRefBase)

---__int
function QuestConditionVo:__init()
    self.id = nil              --条件编号
    self.condition = nil              --条件内容（显示用）
    self.type = nil              --类型（对应关卡条件类型表）
    self.comparisonOperators = nil              --比较运算符（0或空无比较，1大于等于，2小于等于）
    self.otherParam = nil              --其他相关参数
    self.status = nil              --状态
    self.refreshType = nil
    self.roundLimit = nil
    self.showType = nil
end

---FillVo
---@param row TableML.TableFileRow
function QuestConditionVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id", 0)
    self.condition = ParseUtils.ParseLocalizeText(row, "condition")
    self.type = parse_number(row, "type", 0)
    self.comparisonOperators = parse_number(row, "comparisonOperators", 0)
    self.otherParam = parse_number(row, "otherParam", 0)
    self.refreshType = parse_number(row, "refresh", 0)
    self.roundLimit = parse_number(row, "limit", 0)
    self.status = parse_number(row, "status", 0)
    self.showType = parse_number(row, "showType", 0)
end

function QuestConditionVo:__delete()
    self.id = nil
    self.condition = nil
    self.type = nil
    self.comparisonOperators = nil
    self.otherParam = nil
    self.status = nil
    self.refreshType = nil
    self.roundLimit = nil
    self.showType = nil
end

return QuestConditionVo
