---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-02-20 20:25:24
--- 日常SP材料点位表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class RegionDailyTypeVo
local RegionDailyTypeVo = Class("RegionDailyTypeVo", SettingRefBase)

---__int
function RegionDailyTypeVo:__init()
    self.id = nil              --分类ID
    self.label = nil              --类型标签
    self.name = nil              --名称
    self.title = nil              --主标题
    self.subTitle = nil              --附标题
    self.titleBanner = nil              --分类banner资源
    self.bonus = nil              --额外奖励道具ID
    self.bonusNum = nil              --额外奖励数量
    self.tab = nil              --页签位置
    self.content = nil              --内部日常关卡
    self.unlockType = nil              --区域解锁条件类型，对应解锁条件表
    self.unlockParam = nil              --解锁条件参数
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function RegionDailyTypeVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.label = ParseUtils.ParseLocalizeText(row, "label")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.title = ParseUtils.ParseLocalizeText(row, "title")
    self.subTitle = ParseUtils.ParseLocalizeText(row, "subTitle")
    self.titleBanner = ParseUtils.ParsePathName(row, "titleBanner")
    self.subtitleBanner = ParseUtils.ParsePathName(row, "subtitleBanner")
    self.vertexTitleBanner = ParseUtils.ParsePathName(row, "vertexTitleBanner")

    self.bonus = parse_string(row, "bonus")
    self.bonusNum = parse_string(row, "bonusNum")
    self.tab = parse_number(row, "tab")
    self.content = parse_string(row, "content")
    self.unlockType = parse_string(row, "unlockType")
    self.unlockId = parse_string(row, "unlockId")
    self.unlockNum = parse_string(row, "unlockNum")

    self.status = parse_number(row, "status")
end

function RegionDailyTypeVo:__delete()
    self.id = nil
    self.label = nil
    self.name = nil
    self.title = nil
    self.subTitle = nil
    self.titleBanner = nil
    self.bonus = nil
    self.bonusNum = nil
    self.tab = nil
    self.content = nil
    self.unlockType = nil
    self.unlockParam = nil
    self.status = nil
end

return RegionDailyTypeVo
