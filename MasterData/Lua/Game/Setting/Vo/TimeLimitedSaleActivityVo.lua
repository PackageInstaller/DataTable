---
--- Generated by shentayibu
--- Created by shentayibu.
--- DateTime: 2023/10/8 20:14
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class TimeLimitedSaleActivityVo
local TimeLimitedSaleActivityVo = Class("TimeLimitedSaleActivityVo", SettingRefBase)

function TimeLimitedSaleActivityVo:__init()
    self.id             = nil   --活动id
    self.name           = nil   --活动名称
    self.icon           = nil   --页签图标
    self.rewardImg      = nil   --看板娘
    self.ruleID         = nil   --功能规则ID
    self.trigger        = nil   --触发方式
    self.time           = nil   --持续时间

    self.roleIdMove =     nil
    self.roleIdRotation = nil
    self.lensRotation =   nil
    self.showRoleId =     nil
    self.text =           nil

    self.status         = nil   --状态
end

---@param row TableML.TableFileRow
function TimeLimitedSaleActivityVo:FillVo(row)
    base.FillVo(self, row)
    self.id = parse_number(row, "id")
    self.name = parse_string(row, "name")
    self.icon = ParseUtils.ParsePathName(row, "icon")
    self.rewardImg = ParseUtils.ParsePathName(row, "rewardImg")
    self.ruleID = parse_number(row, "ruleID")
    self.trigger = parse_number(row, "trigger")
    self.time = parse_number(row, "time")

    self.roleIdMove = string.split(parse_string(row,"roleIdMove"),";")
    self.roleIdRotation = string.split(parse_string(row,"roleIdRotation"),";")
    self.lensRotation = string.split(parse_string(row,"lensRotation"),";")
    self.showRoleId = parse_string(row,"showRoleId")
    self.text = ParseUtils.ParseLocalizeText(row,"text")

    self.status = parse_number(row, "status")
end

function TimeLimitedSaleActivityVo:__delete()
    self.id = nil
    self.name = nil
    self.icon = nil
    self.rewardImg = nil
    self.ruleID = nil
    self.trigger = nil
    self.time = nil

    self.roleIdMove =     nil
    self.roleIdRotation = nil
    self.lensRotation =   nil
    self.showRoleId =     nil
    self.text =           nil

    self.status = nil
end

return TimeLimitedSaleActivityVo
