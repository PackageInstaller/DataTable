---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2020-09-11 11:08:20
--- 章节节点表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class QuestMainChapterNodeVo
local QuestMainChapterNodeVo = Class("QuestMainChapterNodeVo", SettingRefBase)

---__int
function QuestMainChapterNodeVo:__init()
    self.id = nil              --节点ID
    self.mapId = nil              --所属章节
    self.label = nil              --标题
    self.name = nil              --名称
    self.sceneId = nil              --场景ID
    self.pointImage = nil              --事件点使用图片
    self.timeType = nil              --时间类型，0白天，1黑夜
    self.timeTxt = nil              --时间显示
    self.colorWaveform = nil              --情绪粒子波形图（根据类型调用）
    self.colorNum = nil              --情绪粒子浓度显示
    self.rateOfProgress = nil              --剧情进度
    self.loadingType = nil              --载入界面类型
    self.unlockNum = nil              --解锁条件值（填充为0，类型固定为前置阶段解锁）
    self.regionStageMain = nil              --阶段所属主线关卡（仅1个）
    self.regionStageSide = nil              --支线所属关卡（最多3个）
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function QuestMainChapterNodeVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id", 0)
    self.mapId = parse_number(row, "mapId", 0)
    self.label = ParseUtils.ParseLocalizeText(row, "label")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.sceneId = ParseUtils.ParsePathName(row, "sceneId")
    self.pointImage = ParseUtils.ParsePathName(row, "pointImage")
    self.timeType = parse_number(row, "timeType", 0)
    self.timeTxt = ParseUtils.ParseLocalizeText(row, "timeTxt")
    self.colorWaveform = parse_number(row, "colorWaveform", 0)
    self.colorNum = parse_string(row, "colorNum", '')
    self.rateOfProgress = parse_number(row, "rateOfProgress", 0)
    self.loadingType = parse_number(row, "loadingType", 0)
    self.unlockNum = parse_number(row, "unlockNum", 0)
    self.regionStageMain = parse_string(row, "regionStageMain", '')
    self.regionStageSide = parse_string(row, "regionStageSide", '')
    self.status = parse_number(row, "status", 0)
end

function QuestMainChapterNodeVo:__delete()
    self.id = nil
    self.mapId = nil
    self.label = nil
    self.name = nil
    self.sceneId = nil
    self.pointImage = nil
    self.timeType = nil
    self.timeTxt = nil
    self.colorWaveform = nil
    self.colorNum = nil
    self.rateOfProgress = nil
    self.loadingType = nil
    self.unlockNum = nil
    self.regionStageMain = nil
    self.regionStageSide = nil
    self.status = nil
end

return QuestMainChapterNodeVo
