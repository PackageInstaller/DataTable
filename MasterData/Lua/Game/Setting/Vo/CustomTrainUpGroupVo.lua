---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2026-03-11 11:38:53
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class CustomTrainUpGroupVo
local CustomTrainUpGroupVo = Class("CustomTrainUpGroupVo", SettingRefBase)

---__int
function CustomTrainUpGroupVo:__init()
    self.id = nil              --编号
    self.group = nil              --分组编号（2001-2999）
    self.upType = nil              --增益类型（1卡牌，2职业，3属性，4猫种，5品质）
    self.upParam = nil              --参数（根据类型决定）
    self.name = nil              --备注
    self.upBuff = nil              --关卡增益效果表ID
    self.upDesc = nil              --增益描述备注
    self.status = nil              --状态
    self.additions = {}
end

---FillVo
---@param row TableML.TableFileRow
function CustomTrainUpGroupVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.group = parse_number(row, "group")
    self.upType = parse_number(row, "upType")
    self.upParam = parse_number(row, "upParam")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    
    local upBuff = parse_numbers(row, "upBuff",";")
    local upStar = parse_numbers(row, "upStar",";")
    for i, v in ipairs(upBuff) do
        self.additions[i] = {
            target = i - 1 , 
            commonAddId = upBuff[i]
        }
    end
    self.upDesc = ParseUtils.ParseLocalizeText(row, "upDesc")
    self.status = parse_number(row, "status")
end

function CustomTrainUpGroupVo:__delete()
    self.id = nil
    self.group = nil
    self.upType = nil
    self.upParam = nil
    self.name = nil
    self.upDesc = nil
    self.status = nil
end

return CustomTrainUpGroupVo
