---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2024-08-07 17:27:07
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class GachaponTaskVo
local GachaponTaskVo = Class("GachaponTaskVo", SettingRefBase)

---__int
function GachaponTaskVo:__init()
    self.id = nil              --任务的ID（1~1000）
    self.desc = nil              --任务描述
    self.taskContentType = nil              --任务目标类型
    self.taskContentId = nil              --任务目标（id）
    self.taskContenNum = nil              --任务目标值（num）
    self.rewards = nil               -- 奖励列表
    self.openType = nil              --获取途径（枚举）
    self.openParameter = nil              --跳转参数（填写关卡ID）
    self.groupId = nil              --组别编号
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function GachaponTaskVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
    self.taskContentType = parse_number(row, "taskContentType")
    self.taskContentId = parse_number(row, "taskContentId")
    self.taskContenNum = parse_number(row, "taskContenNum")
    local rewardId = parse_string(row, "rewardId")
    local rewardNum = parse_string(row, "rewardNum")
    self.rewards = parse_rewards(rewardId, rewardNum)
    self.openType = parse_string(row, "openType")
    self.openParameter = parse_string(row, "openParameter")
    self.groupId = parse_number(row, "groupId")
    self.status = parse_number(row, "status")
end

function GachaponTaskVo:__delete()
    self.id = nil
    self.desc = nil
    self.taskContentType = nil
    self.taskContentId = nil
    self.taskContenNum = nil
    self.rewards = nil
    self.openType = nil
    self.openParameter = nil
    self.groupId = nil
    self.status = nil
end

return GachaponTaskVo
