---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2022-10-11 16:51:12
--- 奖池副本奖池表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class HalloweenRewardPoolVo
local HalloweenRewardPoolVo = Class("HalloweenRewardPoolVo", SettingRefBase)

---__int
function HalloweenRewardPoolVo:__init()
    self.id = nil              --奖池ID
    self.type = nil              --副本类型（对应中型PT活动表ID）
    self.consumeId = nil              --消耗道具Id
    self.consumeNumSingle = nil              --单抽消耗道具数量
    self.consumeNum = nil              --十连消耗道具数量
    self.number = nil              --保底次数（未达到该次数前无法抽出最后的限定奖励
    self.total = nil              --总次数(-1为无限
    self.displayProRare = nil              --显示限定概率 (万分数,以正数显示
    self.displayProNormal = nil              --显示普通概率 (万分数,以正数显示
    self.plotId = nil              --对应剧情
    self.plotId = nil              --解锁剧情
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function HalloweenRewardPoolVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.type = parse_number(row, "type")
    self.consumeId = parse_string(row, "consumeId")
    self.consumeNumSingle = parse_number(row, "consumeNumSingle")
    self.consumeNum = self.consumeNumSingle * 10
    self.number = parse_number(row, "number")
    self.total = parse_number(row, "total")
    self.displayProRare = ParseUtils.ParseLocalizeText(row, "displayProRare")
    self.displayProNormal = ParseUtils.ParseLocalizeText(row, "displayProNormal")
    self.plotId = parse_number(row, "plotId")
    self.Last = parse_number(row, "Last")
    self.status = parse_number(row, "status")
end

function HalloweenRewardPoolVo:__delete()
    self.id = nil
    self.type = nil
    self.consumeId = nil
    self.consumeNumSingle = nil
    self.consumeNum = nil
    self.number = nil
    self.total = nil
    self.displayProRare = nil
    self.displayProNormal = nil
    self.plotId = nil
    self.plotId = nil
    self.status = nil
end

return HalloweenRewardPoolVo
