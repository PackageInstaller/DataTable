---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2021-03-02 11:53:10
--- PT活动副本表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class BambooActivityCopyVo
local BambooActivityCopyVo = Class("BambooActivityCopyVo", SettingRefBase)

---__int
function BambooActivityCopyVo:__init()
    self.id = nil              --ID
    self.activetyId = nil              --活动ID
    self.name = nil              --副本名
    self.regionLevel = nil              --难度等级
    self.unlockDay = nil              --解锁天数（活动开始为1）
    self.unlockChapter = nil              --前置章节（填充为0）
    self.specialUnlockType = nil              --特殊解锁条件（0无，1剧情选项，2完成指定节点）
    self.specialUnlockParam = nil              --特殊解锁条件参数（0无，1剧情选项编号，2指定节点编号）
    self.SceneId = nil              --场景ID
    self.reward = nil              --通关奖励道具Id
    self.rewardNum = nil              --通关奖励道具数量
    self.showCat = nil              --玩家使用的猫形象
    self.mapLength = nil              --地图长（格子数）
    self.mapWide = nil              --地图宽（格子数）
    self.petLineup = nil           --机器人阵容ID（填0为不需要机器人阵容）
    self.bgImage = nil           --
    self.tips = nil           --
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function BambooActivityCopyVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.activetyId = parse_number(row, "activetyId")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.tips = parse_localizeText(row, "tips", "")
    self.regionLevel = parse_number(row, "regionLevel")
    self.unlockDay = parse_number(row, "unlockDay")
    self.unlockChapter = parse_string(row, "unlockChapter")
    self.specialUnlockType = parse_string(row, "specialUnlockType")
    self.specialUnlockParam = parse_string(row, "specialUnlockParam")
    self.SceneId = parse_number(row, "SceneId")
    self.reward = parse_string(row, "reward")
    self.rewardNum = parse_string(row, "rewardNum")
    self.showCat = parse_number(row, "showCat")
    self.mapLength = parse_number(row, "mapLength")
    self.mapWide = parse_number(row, "mapWide")
    self.bgImage = ParseUtils.ParsePathName(row, "bgImage")
    self.petLineup = parse_number(row, "petLineup")           --机器人阵容ID（填0为不需要机器人阵容）
    self.status = parse_number(row, "status")
end

function BambooActivityCopyVo:__delete()
    self.id = nil
    self.activetyId = nil
    self.name = nil
    self.regionLevel = nil
    self.unlockDay = nil
    self.unlockChapter = nil
    self.specialUnlockType = nil
    self.specialUnlockParam = nil
    self.SceneId = nil
    self.reward = nil
    self.rewardNum = nil
    self.showCat = nil
    self.mapLength = nil
    self.mapWide = nil
    self.bgImage = nil           --
    self.tips = nil           --
    self.petLineup = nil           --机器人阵容ID（填0为不需要机器人阵容）
    self.status = nil
end

return BambooActivityCopyVo
