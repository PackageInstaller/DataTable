---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2024-03-01 16:02:43
--- 玩法更新提示表
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local parse_number       = parse_number
local parse_string       = parse_string
local parse_pathName     = parse_pathName
local parse_localizeText = parse_localizeText

---@class ActionRecommendVo
local ActionRecommendVo = Class("ActionRecommendVo", SettingRefBase)

---__int
function ActionRecommendVo:__init()
    self.Id = nil              --功能编号
    self.title = nil              --标题文字
    self.image = nil              --标题图片
    self.desc = nil              --提示文本
    self.closeTime = nil              --关闭限制时间/s（填0为可立即关闭）
    self._upRole = nil              --推荐角色列表（角色卡牌ID，多个角色用分号;隔开，上限5个）
end

---FillVo
---@param row TableML.TableFileRow
function ActionRecommendVo:FillVo(row)
    base.FillVo(self,row)
    self.Id = parse_number(row, "Id")
    self.title = parse_localizeText(row, "title", true)
    self.image = parse_pathName(row, "image")
    self.head1 = parse_localizeText(row, "head1", true)
    self.desc1 = parse_localizeText(row, "desc1", true)
    self.head2 = parse_localizeText(row, "head2", true)
    self.desc2 = parse_localizeText(row, "desc2", true)
    self.desc3 = parse_localizeText(row, "desc3", true)
    self.closeTime = parse_number(row, "closeTime")
    self._upRole = parse_string(row, "upRole")
end

function ActionRecommendVo:__delete()
    self.Id = nil
    self.title = nil
    self.image = nil
    self.desc = nil
    self.closeTime = nil
    self._upRole = nil
end

function ActionRecommendVo:GetRoles()
    if self._roles == nil then
        self._roles = parse_strings(self._upRole)
    end
    return self._roles
end

return ActionRecommendVo
