---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2022-05-22 16:56:45
--- 剧情副本结局cg表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class QuestRoleStoryCgVo
local QuestRoleStoryCgVo = Class("QuestRoleStoryCgVo", SettingRefBase)

---__int
function QuestRoleStoryCgVo:__init()
    self.id = nil              --结局id（章节idxxxx+编号xx）
    self.chapterId = nil              --结局关卡id
    self.cgIcon = nil              --cg缩略图路径
    self.cg = nil              --cg大图路径
    self.posX = nil              --最多上阵人数（1为下限 4为上限）
    self.posY = nil              --最多上阵人数（1为下限 4为上限）
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function QuestRoleStoryCgVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.chapterId = parse_string(row, "chapterId")
    self.cgIcon = ParseUtils.ParsePathName(row, "cgIcon")
    self.cg = ParseUtils.ParsePathName(row, "cg")
    self.posX = parse_number(row, "posX", 0)
    self.posY = parse_number(row, "posY", 0)
    self.text = parse_localizeText(row, "text")
    self.status = parse_number(row, "status")
end

function QuestRoleStoryCgVo:__delete()
    self.id = nil
    self.chapterId = nil
    self.cgIcon = nil
    self.cg = nil
    self.posX = nil              --最多上阵人数（1为下限 4为上限）
    self.posY = nil              --最多上阵人数（1为下限 4为上限）
    self.status = nil
end

return QuestRoleStoryCgVo
