---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2022-02-21 11:11:42
--- 47定制装备表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class GoodsOptionalGiftVo
local GoodsOptionalGiftVo = Class("GoodsOptionalGiftVo", SettingRefBase)

---__int
function GoodsOptionalGiftVo:__init()
    self.id = nil              --道具编号
    self.type = nil              --道具类型:
    self.name = nil              --名字
    self.desc = nil              -- 用途
    self.description = nil        -- 文案包装
    self.quality = nil            --显示品质
    self.photoPath = nil            -- 图片路径
    self.goodList = nil
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function GoodsOptionalGiftVo:FillVo(row)
    base.FillVo(self,row)
    local parse_number = parse_number
    self.id = parse_number(row, "id")
    self.type = parse_number(row, "type")
    self.type = parse_number(row, "type")
    self.quality = parse_number(row, "quality")
    self.description = ParseUtils.ParseLocalizeText(row, "description")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
    self.redPoint = parse_number(row, "redPoint")
    self.bagUsed = parse_number(row, "bagUsed", 0)
    --图片路径,同意用 photoPath
    self.photoPath = string.format("%s",ParseUtils.ParsePathName(row,"photoId"))
    --print("#self.goodList " ,parse_string(row,"effectId"))
    --print("#self.goodList " ,parse_string(row,"effectNum"))
    local effectIds = parse_string(row,"effectId","")
    self.showType = parse_number(row, "showType", 0)
    -- 可以兑换道具的列表
    if string.len(effectIds) > 0  then
        local effectNums = parse_string(row,"effectNum", "")
        self.goodList = parse_rewards(effectIds, effectNums,";")
    else
        self.goodList = {}
    end

    self.status = parse_number(row, "status")
end

function GoodsOptionalGiftVo:__delete()
    self.id = nil
    self.type = nil
    self.name = nil
    self.effectType = nil
    self.desc = nil
    self.description = nil
    self.photoPath = nil
    self.goodList = nil
    self.status = nil
end

return GoodsOptionalGiftVo
