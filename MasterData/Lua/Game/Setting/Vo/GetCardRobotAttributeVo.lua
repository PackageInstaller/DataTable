---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-10-20 17:32:38
--- 定制角色卡牌属性表
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class GetCardRobotAttributeVo
local GetCardRobotAttributeVo = Class("GetCardRobotAttributeVo", SettingRefBase)

---__int
function GetCardRobotAttributeVo:__init()
    self.id = nil              --id
    self.cardLevel = nil              --等级
    self.star = nil              --星级
    self.advanceLevel = nil              --初始升阶等级（对应升阶等级上限表的索引id）
    self.cardId = nil              --对应角色ID
    self.career = nil         -- 职业（1、治愈；2、增益；3、减益；4、单攻；5、群攻；6、防护）
    self.attributeType = nil   -- 角色颜色属性（1红，2绿，3蓝，4金（光），5紫（暗））
end

---FillVo
---@param row TableML.TableFileRow
function GetCardRobotAttributeVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.cardLevel = parse_number(row, "Level")
    self.star = parse_number(row, "star")
    self.advanceLevel = parse_number(row, "startRareLevel")
    self.career = parse_number(row, "location")
    self.attributeType = parse_number(row, "attribute")
    self.cardId = parse_number(row, "roleId")
end

function GetCardRobotAttributeVo:__delete()
    self.id = nil
    self.cardLevel = nil
    self.star = nil
    self.advanceLevel = nil
    self.cardId = nil
    self.career = nil
    self.attributeType = nil
end

return GetCardRobotAttributeVo
