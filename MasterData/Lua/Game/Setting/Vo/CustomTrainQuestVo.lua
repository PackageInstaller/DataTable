---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2025-07-17 20:01:26
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class CustomTrainQuestVo
local CustomTrainQuestVo = Class("CustomTrainQuestVo", SettingRefBase)

---__int
function CustomTrainQuestVo:__init()
    self.id = nil              --关卡ID
    self.groupId = nil              --关卡组ID
    self.areaId = nil              --区域ID
    self.name = nil              --关卡名字
    self.rewardGroup = nil              --奖励组ID
    self.characteristic = nil              --关卡特点
    self.SceneId = nil              --场景ID
    self.rounds = nil              --回合限制(超出后直接失败)
    self.roleNun = nil              --最多上阵人数（1为下限 4为上限）
    self.proposalLevel = nil              --推荐战斗力区间
    self.powerRatioTeamID = nil              --战力碾压组别
    self.damageTotal = nil              --伤害总显示
    self.proposalLevelInterval = nil              --状态
    self.status = nil              --状态
    
end

---FillVo
---@param row TableML.TableFileRow
function CustomTrainQuestVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.groupId = parse_number(row, "groupId")
    self.areaId = parse_number(row, "areaId")
    self.name = parse_localizeText(row, "name")
    self.characteristic = ParseUtils.ParseLocalizeText(row, "characteristic")
    self.SceneId = parse_number(row, "SceneId")
    self.rounds = parse_number(row, "rounds")
    self.rewardGroup = parse_number(row, "rewardGroup")
    self.roleNun = parse_string(row, "roleNun")
    self.proposalLevel = parse_string(row, "proposalLevel")
    self.proposalLevelInterval = parse_string(row, "proposalLevelInterval")
    self.powerRatioTeamID = parse_number(row, "powerRatioTeamID")
    self.damageTotal = parse_number(row, "damageTotal")
    self.status = parse_number(row, "status")
end

function CustomTrainQuestVo:__delete()
    self.id = nil
    self.groupId = nil
    self.areaId = nil
    self.rewardGroup = nil
    self.characteristic = nil
    self.SceneId = nil
    self.rounds = nil
    self.roleNun = nil
    self.name = nil
    self.proposalLevel = nil
    self.powerRatioTeamID = nil
    self.damageTotal = nil
    self.proposalLevelInterval = nil
    self.status = nil
end

return CustomTrainQuestVo
