---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-01-17 18:29:50
---阵营表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils
local parse_number = parse_number
local ParseLocalizeText = ParseUtils.ParseLocalizeText
local ParsePathName = ParseUtils.ParsePathName

---@class RoleRaceVo
local RoleRaceVo = Class("RoleRaceVo", SettingRefBase)

---__int
function RoleRaceVo:__init()
    self.raceId = nil              --阵营ID
    self.name = nil              --名称（角色名ID）
    self.icon = nil              --图片名
    self.largeIcon = nil              --图片名
    self.desc = nil              --描述
    -- self.signboard = nil ----是否可以看板
end

---FillVo
---@param row TableML.TableFileRow
function RoleRaceVo:FillVo(row)
    base.FillVo(self,row)
    self.raceId = parse_number(row, "raceId")
    self.name = ParseLocalizeText(row, "name")
    self.icon = ParsePathName(row, "icon")
    self.largeIcon = ParsePathName(row, "largeIcon")
    self.desc = ParseLocalizeText(row, "desc")
    self.introduction = ParseLocalizeText(row, "introduction")
    -- self.signboard = parse_number(row, "playboard", 0)
end

function RoleRaceVo:__delete()
    self.raceId = nil
    self.name = nil
    self.icon = nil
    self.largeIcon = nil
    self.desc = nil
    -- self.signboard = nil ----是否可以看板
end

return RoleRaceVo
