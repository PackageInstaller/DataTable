---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2024-11-13 17:03:40
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class MournPlotVo
local MournPlotVo = Class("MournPlotVo", SettingRefBase)

---__int
function MournPlotVo:__init()
    self.id = nil              --ID
    self.activetyId = nil              --活动ID
    self.chapter = nil              --对应章节ID（0为序章）
    self.desc = nil              --小节描述
    self.type = nil              --剧情类型（1纯剧情2有战斗）
    self.unlockItem = nil              --解锁道具（0为不消耗）
    self.unlockNum = nil              --解锁道具需求数量
    self.unlockPlot = nil              --前置剧情ID
    self.endFlag = nil              --结束标志（0非1是）
    self.regionId = nil              --剧情关卡ID（对应剧情关卡表）
    self.plotId = nil              --剧情ID
    self.buffUnlockTips = nil
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function MournPlotVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.activetyId = parse_number(row, "activetyId")
    self.chapter = parse_number(row, "chapter")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
    self.type = parse_number(row, "type")
    self.unlockItem = parse_number(row, "unlockItem")
    self.unlockNum = parse_number(row, "unlockNum")
    self.unlockPlot = parse_string(row, "unlockPlot")
    self.endFlag = parse_number(row, "endFlag")
    self.regionId = parse_number(row, "regionId")
    self.plotId = parse_string(row, "plotId")
    self.buffUnlockTips = ParseUtils.ParseLocalizeText(row,"buffUnlockTips")
    self.status = parse_number(row, "status")
end

function MournPlotVo:__delete()
    self.id = nil
    self.activetyId = nil
    self.chapter = nil
    self.desc = nil
    self.type = nil
    self.unlockItem = nil
    self.unlockNum = nil
    self.unlockPlot = nil
    self.endFlag = nil
    self.regionId = nil
    self.plotId = nil
    self.buffUnlockTips = nil
    self.status = nil
end

return MournPlotVo
