---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-12-07 15:48:43
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class ChaoticRecallTagVo
local ChaoticRecallTagVo = Class("ChaoticRecallTagVo", SettingRefBase)

---__int
function ChaoticRecallTagVo:__init()
    self.groupId = nil              --组id(做个保护，如果未配置，默认读取id=101的数据）
    self.name = nil              --名称
    self.title = nil
    self.iconLight = nil              --选中
    self.iconDark = nil              --非选中
    self.skipLevel = nil            -- 是否是跳关的关卡 打完这关 前面的全都3星通关
    self.background = nil              --背景图
    self.groupType = nil            --页签类型 0是默认模式  1是挑战模式
    self.condition = nil
    self.rewards = nil
    self.rankRewards = nil
    self.ruleId = nil
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function ChaoticRecallTagVo:FillVo(row)
    base.FillVo(self,row)
    self.groupId = parse_number(row, "groupId")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.title = ParseUtils.ParseLocalizeText(row, "title")
    self.iconLight = ParseUtils.ParsePathName(row, "iconLight")
    self.iconDark = ParseUtils.ParsePathName(row, "iconDark")
    self.skipLevel = parse_number(row, "skipLevel")        -- 是否是跳关的关卡 打完这关 前面的全都3星通关
    self.background = ParseUtils.ParsePathName(row, "background")
    self.background = ParseUtils.ParsePathName(row, "background")
    self.groupType =  parse_number(row, "groupType")
    local groupRewards = parse_string(row, "groupRewards","")
    local groupRewardsNum = parse_string(row, "groupRewardsNum","")
    self.rewards = parse_rewards(groupRewards, groupRewardsNum)
    self.condition =  parse_number(row, "condition")
    
    local rankRewards = parse_string(row, "boadReward","")
    local rankRewardsNum = parse_string(row, "boadRewardNum","")
    self.rankRewards = parse_rewards(rankRewards, rankRewardsNum)
    
    self.ruleId = parse_number(row,"ruleId")
    self.tipBoard =  parse_number(row, "tipBoad")
    self.status = parse_number(row, "status")
end

function ChaoticRecallTagVo:__delete()
    self.groupId = nil
    self.name = nil
    self.title = nil
    self.iconLight = nil
    self.iconDark = nil
    self.skipLevel = nil            -- 是否是跳关的关卡 打完这关 前面的全都3星通关
    self.background = nil
    self.groupType = nil
    self.condition = nil
    self.rewards = nil
    self.rankRewards = nil
    self.ruleId = nil
    self.status = nil
end

return ChaoticRecallTagVo
