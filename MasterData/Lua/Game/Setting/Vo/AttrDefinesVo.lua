---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-01-29 15:05:17
---属性定义表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils
local parse_number = parse_number
local parse_localizeText = parse_localizeText
---@class AttrDefinesVo
local AttrDefinesVo = Class("AttrDefinesVo", SettingRefBase)

---__int
function AttrDefinesVo:__init()
    self.id = nil              --属性ID
    self.name = nil              --属性名称
    self.attributeDesc = nil              --属性描述
    -- self.columnName = nil              --属性列
    self.type = nil              --类型（特殊显示，否为0
    self.isConversion = nil              --是否显示成百分比（0不需要 1百分比显示）
    self.combatEff = nil              --战斗力计算系数
    self.order = nil              -- 显示顺序（配了数字的显示逻辑，配0就不显示）
    self.icon = nil              --图标
    self.isPercent = nil -- 是否显示成百分比
end

---FillVo
---@param row TableML.TableFileRow
function AttrDefinesVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "attributeId", 0)
    self.name = parse_localizeText(row, "attributeName")
    self.attributeDesc = parse_localizeText(row, "attributeDesc")
    -- self.columnName = parse_string(row, "attributeColumn")
    self.type = parse_number(row, "type", 0)
    self.isConversion = parse_number(row, "isConversion", 0)
    self.combatEff = parse_number(row, "combatEff", 0)
    self.order = parse_number(row, "order", 0)
    self.icon = ParseUtils.ParsePathName(row, "icon")

    self.isPercent = self.isConversion > 0
end

function AttrDefinesVo:__delete()
    self.id = nil
    self.attributeName = nil
    self.attributeDesc = nil
    self.attributeColumn = nil
    self.type = nil
    self.isConversion = nil
    self.combatEff = nil
    self.order = nil
    self.icon = nil
end

return AttrDefinesVo
