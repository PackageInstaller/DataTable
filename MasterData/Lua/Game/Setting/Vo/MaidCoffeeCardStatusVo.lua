---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-03-21 10:37:59
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils
local parse_number = parse_number
local parse_strings = parse_strings
local parse_string = parse_string

---@class MaidCoffeeCardStatusVo
local MaidCoffeeCardStatusVo = Class("MaidCoffeeCardStatusVo", SettingRefBase)

---__int
function MaidCoffeeCardStatusVo:__init()
    self.actionId = nil              --动作id
    self.name = nil              --动作名称
    self.strictId = nil              --限制状态id：1管理中，2服务中，3外卖中，4休息中
    self.exclusion = nil              --动作互斥：1当作店长、2当作店员、3派送外卖、4休息室休息
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function MaidCoffeeCardStatusVo:FillVo(row)
    base.FillVo(self,row)
    self.actionId  = parse_number(row, "actionId")
    self.name      = ParseUtils.ParseLocalizeText(row, "name")
    local strictId = parse_string(row, "astrictId")
    ---配置为空时 string.spit 会解析一个{[1] = ""} checkNum后就会变成0 会把状态为0的卡给屏蔽了
    if strictId ~= "" then
        self.strictId  = parse_strings(strictId)
    else
        self.strictId  = {}
    end

    self.exclusion = parse_strings(parse_string(row, "exclusion"))
end

function MaidCoffeeCardStatusVo:__delete()
    self.actionId = nil
    self.name = nil
    self.strictId = nil
    self.exclusion = nil
    self.status = nil
end

return MaidCoffeeCardStatusVo
