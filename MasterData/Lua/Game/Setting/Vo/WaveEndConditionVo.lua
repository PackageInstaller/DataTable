---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yajieli.
--- DateTime: 2019-08-15 10:41
---
local Condition = import('Game.Battle.Core.Condition')

local SettingRefBase = require('Game.Setting.SettingRefBase')

---@class WaveEndConditionVo
local WaveEndConditionVo = Class("WaveEndConditionVo", SettingRefBase)
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---__int
function WaveEndConditionVo:__init()
    self.conditions = nil
    self.show = nil
end


---FillVo
---@param row TableML.TableFileRow
function WaveEndConditionVo:FillVo(row)
    base.FillVo(self,row)
    local len = 3
    self.conditions = {}
    for i = 1, len do
        local type = parse_number(row, "type"..i, 0)
        local group = parse_number(row, "group"..i, 0)
        if not Tools.isNullKey(group) and not Tools.isNullKey(type) then
            if self.conditions[group] == nil then
                self.conditions[group] = {}
            end
            local gs = self.conditions[group]
            ---@type Condition
            local con = Condition.New(type)
            con.operator = parse_number(row, "comparisonOperators"..i, 0)
            con.conditionParam = parse_number(row, "conditionParam"..i, 0)
            con.targetNum = parse_number(row, "otherParam"..i, 0)

            table.insert(gs, con)
        end
    end
    self.show = parse_number(row, "show", 0)
end

function WaveEndConditionVo:__delete()
    self.conditions = nil
    self.show = nil
end

return WaveEndConditionVo