---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-04-26 10:35:55
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class PVPBuffVo
local PVPBuffVo = Class("PVPBuffVo", SettingRefBase)

---__int
function PVPBuffVo:__init()
    self.id = nil              --ID（PVP功能id：4.实时；5.歼灭战；6.异步）
    self.buffId = nil              --战中推进BUFF
    self.attributeId = nil              --属性ID
    self.addTypeNum = nil              --加成值
    self.globalDamage = nil              --全局伤害
    self.globalTreatment = nil              --全局治疗
    self.globalBlood = nil              --全局血量变更
    self.globalShield = nil              --全局护盾
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function PVPBuffVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.buffId = parse_number(row, "buffId")
    self.attributeId = parse_number(row, "attributeId")
    self.addTypeNum = parse_number(row, "addTypeNum")
    self.globalDamage = parse_number(row, "globalDamage")
    self.globalTreatment = parse_number(row, "globalTreatment")
    self.globalBlood = parse_number(row, "globalBlood")
    self.globalShield = parse_number(row, "globalShield")
    self.status = parse_number(row, "status")
end

function PVPBuffVo:__delete()
    self.id = nil
    self.buffId = nil
    self.attributeId = nil
    self.addTypeNum = nil
    self.globalDamage = nil
    self.globalTreatment = nil
    self.globalBlood = nil
    self.globalShield = nil
    self.status = nil
end

return PVPBuffVo
