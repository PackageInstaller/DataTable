---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by zou hanjie.
--- DateTime: 2023-07-24 19:10:59
--- 成长任务主题表


local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class UITaskGrowUpThemeVO
local UITaskGrowUpThemeVO = Class("UITaskGrowUpThemeVO", SettingRefBase)

---__int
function UITaskGrowUpThemeVO:__init()
    self.Id = nil                   --主题ID
    self.desc = nil                 --组tips描述
    self.bigReward = nil            --大奖卡牌显示
    self.bigRewardResource = nil    --大奖卡牌资源
    self.card = nil                 --包含卡牌
    self.taskGroup = nil            --对应任务组
    self.targetGroup = nil          --对应宝箱组
    self.unlock = nil               --解锁条件
    self.unlockParameter = nil     --解锁参数
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function UITaskGrowUpThemeVO:FillVo(row)
    base.FillVo(self,row)

    self.Id                 = parse_number(row, "Id")
    self.desc               = ParseUtils.ParseLocalizeText(row, "desc")
    self.bigReward          = parse_number(row, "bigReward")
    self.bigRewardResource  = ParseUtils.ParsePathName(row, "bigRewardResource", ".png") -- 能得到类似: "Arts/Icons/Task/task_img_101013.png" 的 path
    self.card               = Tools.splitToNum(parse_string(row, "card", 0), ';')   -- {int,int,...}
    self.taskGroup          = Tools.splitToNum(parse_string(row, "taskGroup", 0), ';') -- {int,int,...}
    self.targetGroup        = parse_number(row, "targetGroup")
    self.unlock             = parse_number(row, "unlock")
    self.unlockParameter    = parse_number(row, "unlockParameter")
end


function UITaskGrowUpThemeVO:__delete()
    self.Id = nil                   --主题ID
    self.desc = nil                 --组tips描述
    self.bigReward = nil            --大奖卡牌显示
    self.bigRewardResource = nil    --大奖卡牌资源
    self.card = nil                 --包含卡牌
    self.taskGroup = nil            --对应任务组
    self.targetGroup = nil          --对应宝箱组
    self.unlock = nil               --解锁条件
    self.unlockParameter = nil     --解锁参数
    self.status = nil              --状态
end

return UITaskGrowUpThemeVO
