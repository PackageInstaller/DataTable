---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2020-09-24 15:31:24
--- 高级装备副本表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class QuestSeniorEquipChapterVo
local QuestSeniorEquipChapterVo = Class("QuestSeniorEquipChapterVo", SettingRefBase)

---__int
function QuestSeniorEquipChapterVo:__init()
    self.id = nil              --ID
    self.type = nil              --副本类型（对应高级副本类型表）
    self.label = nil              --标题
    self.name = nil              --副本名
    self.desc = nil              --关卡描述
    self.regionLevel = nil              --难度等级
    self.unlockNum = nil              --解锁条件值（填充为0）
    self.frequency = nil              --消耗的副本进入次数
    self.costItem = nil              --副本消耗道具
    self.costNum = nil              --消耗道具数量
    self.SceneId = nil              --场景ID
    self.pointID = nil              --各关卡ID
    self.pointPos = nil              --各关卡坐标
    self.pointType = nil              --各关卡类型
    self.initialCost = nil              --初始费用
    self.extraCost = nil              --关卡可获得费用
    self.showReward = nil              --展示奖励
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function QuestSeniorEquipChapterVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id", 0)
    self.type = parse_number(row, "type", 0)
    self.label = ParseUtils.ParseLocalizeText(row, "label")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
    self.regionLevel = parse_number(row, "regionLevel", 0)
    self.unlockNum = parse_number(row, "unlockNum", 0)
    self.frequency = parse_number(row, "frequency", 0)
    self.costItem = parse_number(row, "costItem", 0)
    self.costNum = parse_number(row, "costNum", 0)
    self.SceneId = parse_number(row, "SceneId", 0)
    self.pointID = parse_string(row, "pointID", '')
    self.pointPos = parse_string(row, "pointPos", '')
    self.pointType = parse_string(row, "pointType", '')
    self.initialCost = parse_number(row, "initialCost", 0)
    self.extraCost = parse_number(row, "extraCost", 0)
    self.showReward = parse_string(row, "showReward", '')
    self.status = parse_number(row, "status", 0)
end

function QuestSeniorEquipChapterVo:__delete()
    self.id = nil
    self.type = nil
    self.label = nil
    self.name = nil
    self.desc = nil
    self.regionLevel = nil
    self.unlockNum = nil
    self.frequency = nil
    self.costItem = nil
    self.costNum = nil
    self.SceneId = nil
    self.pointID = nil
    self.pointPos = nil
    self.pointType = nil
    self.initialCost = nil
    self.extraCost = nil
    self.showReward = nil
    self.status = nil
end

return QuestSeniorEquipChapterVo
