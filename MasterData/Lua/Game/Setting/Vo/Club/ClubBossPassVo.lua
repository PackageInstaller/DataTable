---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-11-11 16:55:55
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class ClubBossPassVo
local ClubBossPassVo = Class("ClubBossPassVo", SettingRefBase)

---__int
function ClubBossPassVo:__init()
    self.id = nil              --关卡ID
    self.copyId = nil            --对应的关卡组Id
    self.name = nil              --关卡特点
    self.bossTypes = nil   --Boss类型
    self.SceneId = nil              --场景ID
    self.rounds = nil              --回合限制(超出后直接失败)
    self.roleNun = nil              --最多上阵人数（1为下限 4为上限）
    self.proposalLevel = nil              --推荐战斗力
    self.powerRatioTeamID = nil              --战力碾压组别
    self.illustrate = nil              --特殊机制说明
    self.damageTotal = nil          -- 控制战中是否显示总伤害UI
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function ClubBossPassVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.copyId = parse_number(row, "copyId")            --对应的关卡组Id
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.types = parse_number(row, "types")
    self.SceneId = parse_number(row, "SceneId")
    self.rounds = parse_number(row, "rounds")
    self.bossTypes = parse_number(row, "bossTypes")
    self.roleNun = parse_string(row, "roleNun")
    self.proposalLevel = parse_number(row, "proposalLevel")
    self.powerRatioTeamID = parse_number(row, "powerRatioTeamID")
    self.illustrate = ParseUtils.ParseLocalizeText(row, "illustrate")
    self.damageTotal = parse_number(row, "damageTotal", 0)
    self.status = parse_number(row, "status")
end

function ClubBossPassVo:__delete()
    self.id = nil
    self.copyId = nil
    self.name = nil
    self.types = nil
    self.SceneId = nil
    self.bossTypes = nil
    self.rounds = nil
    self.roleNun = nil
    self.proposalLevel = nil
    self.powerRatioTeamID = nil
    self.illustrate = nil
    self.damageTotal = nil
    self.status = nil
end

return ClubBossPassVo
