---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2024-08-31 15:01:53
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class ThrowDiceBuffEffectVo
local ThrowDiceBuffEffectVo = Class("ThrowDiceBuffEffectVo", SettingRefBase)

---__int
function ThrowDiceBuffEffectVo:__init()
    self.Id = nil              --编号
    self.name = nil              --BUFF名称
    self.quality = nil              --buff品质颜色（1=紫色，2=橙色）
    self.cehua = nil              --策划备注
    self.Icon = nil              --BUFF图片
    self.desc = nil              --描述
    self.times = nil              --后续几次有效（0=立即生效
    self.points = nil              --生效条件：点数为多少时生效（0=无条件,任意点数）
    self.type = nil              --生效效果类型（1=点数增加n，2=点数*n,3=提升点数n的概率变成m%, 4=点数必为n，5=免费获得n个m道具，6=点数必定大于N
    self.parameter = nil              --生效效果参数
    self.limit = nil              --生效几次后buff消失,0或者不配置则取times字段
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function ThrowDiceBuffEffectVo:FillVo(row)
    base.FillVo(self,row)
    self.Id = parse_number(row, "Id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.quality = parse_number(row, "quality")
    self.cehua = parse_string(row, "cehua")
    self.Icon = parse_string(row, "Icon")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
    self.times = parse_number(row, "times")
    self.points = parse_string(row, "points")
    self.type = parse_string(row, "type")
    self.parameter = string.split(parse_string(row, "parameter") , ";") 
    self.limit = parse_number(row, "limit")
    self.status = parse_number(row, "status")
end

function ThrowDiceBuffEffectVo:__delete()
    self.Id = nil
    self.name = nil
    self.quality = nil
    self.cehua = nil
    self.Icon = nil
    self.desc = nil
    self.times = nil
    self.points = nil
    self.type = nil
    self.parameter = nil
    self.limit = nil
    self.status = nil
end

return ThrowDiceBuffEffectVo
