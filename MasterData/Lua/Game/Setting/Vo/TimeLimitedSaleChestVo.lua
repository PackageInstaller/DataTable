---
--- Generated by shentayibu
--- Created by shentayibu.
--- DateTime: 2023/10/8 16:07
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class TimeLimitedSaleChestVo
local TimeLimitedSaleChestVo = Class("TimeLimitedSaleChestVo", SettingRefBase)

function TimeLimitedSaleChestVo:__init()
    self.id                 = nil   --编号（礼包id）
    self.group              = nil   --活动id
    self.pos                = nil   --显示顺序
    self.goodsId            = nil   --道具id
    self.goodsNum           = nil   --获得数量
    self.goodsConsum        = nil   --消耗道具（0表示现金）
    self.goodsConsumNum     = nil   --消耗道具数量
    self.text               = nil   --描述文本
    self.value              = nil   --显示价值道具数量
    self.status             = nil   --状态
end

---@param row TableML.TableFileRow
function TimeLimitedSaleChestVo:FillVo(row)
    base.FillVo(self, row)
    self.id = parse_number(row, "id")
    self.group = parse_number(row, "group")
    self.pos = parse_number(row, "pos")
    self.goodsId = string.split(parse_string(row,"goodsId"),';')
    self.goodsNum = string.split(parse_string(row,"goodsNum"),';')
    self.goodsConsum = parse_string(row, "goodsConsum")
    self.goodsConsumNum = parse_string(row, "goodsConsumNum")
    self.text = ParseUtils.ParseLocalizeText(row, "text")
    self.value = parse_number(row, "value")
    self.status = parse_number(row, "status")
end

function TimeLimitedSaleChestVo:__delete()
    self.id                 = nil   --编号（礼包id）
    self.group              = nil   --活动id
    self.pos                = nil   --显示顺序
    self.goodsId            = nil   --道具id
    self.goodsNum           = nil   --获得数量
    self.goodsConsum        = nil   --消耗道具（0表示现金）
    self.goodsConsumNum     = nil   --消耗道具数量
    self.text               = nil   --描述文本
    self.value              = nil   --显示价值道具数量
    self.status             = nil   --状态
end

return TimeLimitedSaleChestVo
