---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by zou hanjie.
--- DateTime: 2023-07-24 19:10:59
--- 成长任务宝箱表


local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class UITaskGrowUpChestVO
local UITaskGrowUpChestVO = Class("UITaskGrowUpChestVO", SettingRefBase)

---__int
function UITaskGrowUpChestVO:__init()
    self.taskAchId = nil    --ID
    self.id = nil           --主题ID
    self.taskGroup = nil    --宝箱组
    self.after = nil        --后续宝箱
    self.desc = nil         --描述
    self.taskIds = nil       --任务id（填成长任务表id

    self.rewardIds = nil     --任务奖励
    self.rewardNums = nil    --奖励数量
    self.rewardDatas = nil   --奖励数据汇总

    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function UITaskGrowUpChestVO:FillVo(row)
    base.FillVo(self,row)

    self.taskAchId      = parse_number(row, "taskAchId")
    self.id             = parse_number(row, "id")
    self.taskGroup      = parse_number(row, "taskGroup")
    self.after          = parse_number(row, "after")
    self.desc           = ParseUtils.ParseLocalizeText(row, "desc")
    self.taskIds         = Tools.splitToNum(parse_string(row, "taskId", 0), ';')   -- {int,int,...}

    self.rewardIds       = Tools.splitToNum(parse_string(row, "rewardId", 0), ';')   -- {int,int,...}
    self.rewardNums      = Tools.splitToNum(parse_string(row, "rewardNum", 0), ';')   -- {int,int,...}

    --self.rewardDatas      = parse_rewards(row:Get("rewardId"), row:Get("rewardNum"))
    self.rewardDatas      = parse_rewards(parse_string(row, "rewardId", 0), parse_string(row, "rewardNum", 0))

end

function UITaskGrowUpChestVO:__delete()
    self.taskAchId = nil    --ID
    self.id = nil           --主题ID
    self.taskGroup = nil    --宝箱组
    self.after = nil        --后续宝箱
    self.desc = nil         --描述
    self.taskIds = nil       --任务id（填成长任务表id

    self.rewardIds = nil     --任务奖励
    self.rewardNums = nil    --奖励数量
    self.rewardDatas = nil   --奖励数据汇总

    self.status = nil              --状态
end


function UITaskGrowUpChestVO:DebugLog()
    local CheckNilString = function(s)
        return isNull(s) and "nil" or tostring(s)
    end
    local log = ""
    log = log .. "\n taskAchId: " .. CheckNilString( self.taskAchId )
    log = log .. "\n id: " .. CheckNilString( self.id )
    log = log .. "\n taskGroup: " .. CheckNilString( self.taskGroup )
    log = log .. "\n after: " .. CheckNilString( self.after )
    log = log .. "\n desc: " .. CheckNilString( self.desc )
    log = log .. "\n ... "
    return log
end



return UITaskGrowUpChestVO
