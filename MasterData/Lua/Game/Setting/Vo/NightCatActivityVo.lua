---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2022-08-25 15:53:43
--- 暗夜猫活动表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class NightCatActivityVo
local NightCatActivityVo = Class("NightCatActivityVo", SettingRefBase)

---__int
function NightCatActivityVo:__init()
    self.id = nil              --活动ID
    self.name = nil              --副本名
    self.ruleId = nil              --功能规则ID
    self.rewardItem = nil              --剧情通关奖励道具ID
    self.rewardNum = nil              --剧情通关奖励数量
    self.layers = nil              --爬塔层数
    self.showCard = nil              --奖励卡牌展示
    self.sceneId = nil              --背景
    self.entranceImage = nil              --入口图片
    self.unlockType = nil              --开启条件（对应解锁条件表里的数值）
    self.unlockId = nil              --开启条件ID
    self.unlockNum = nil              --开启条件数值
    self.resetTime = nil              --重置时间
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function NightCatActivityVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.ruleId = parse_number(row, "ruleId")
    self.rewardItem = parse_string(row, "rewardItem")
    self.rewardNum = parse_string(row, "rewardNum")
    self.layers = parse_number(row, "layers")
    self.showCard = parse_number(row, "showCard")
    self.sceneId = parse_string(row, "sceneId")
    self.entranceImage = parse_string(row, "entranceImage")
    self.unlockType = parse_string(row, "unlockType")
    self.unlockId = parse_number(row, "unlockId")
    self.unlockNum = parse_string(row, "unlockNum")
    self.resetTime = parse_string(row, "resetTime")

    self.token = parse_string(row, "token")

    self.status = parse_number(row, "status")
end

function NightCatActivityVo:__delete()
    self.id = nil
    self.name = nil
    self.ruleId = nil
    self.rewardItem = nil
    self.rewardNum = nil
    self.layers = nil
    self.showCard = nil
    self.sceneId = nil
    self.entranceImage = nil
    self.unlockType = nil
    self.unlockId = nil
    self.unlockNum = nil
    self.resetTime = nil
    self.status = nil
end

return NightCatActivityVo
