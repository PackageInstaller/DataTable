---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-07-25 16:43:47
--- 猫球卡牌表
---

local parse_number = parse_number
local parse_string = parse_string
local parse_strings = parse_strings
local checkNumber = checkNumber

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class CatBallVo
local CatBallVo = Class("CatBallVo", SettingRefBase)

---__int
function CatBallVo:__init()
    self.roleId = nil              --猫球ID
    self.name = nil              --猫球名称
    self.types = nil              --猫球类型（1.主动，2.被动，3.经营）
    self.subtype = nil              --猫球偏向（1.攻击，2.防御，3.辅助，4.经营预留位1，5.经营预留位2，6.经营预留位3）
    self.rare = nil              --稀有度（1.R，2.SR，3.SSR）
    self.starMin = nil              --初始星级
    self.starMax = nil              --星级满级
    self.catId = nil              --星级满级
    self.kind = nil              --对应猫咪种类
    self.inHandBook = nil              --是否进入图鉴
    self.picture = nil              --图片
    self.skillPicture = nil              --图片
end

---FillVo
---@param row TableML.TableFileRow
function CatBallVo:FillVo(row)
    base.FillVo(self,row)
    self.roleId = parse_number(row, "roleId")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.types = parse_number(row, "types")
    self.subtype = parse_number(row, "subtype")
    self.rare = parse_number(row, "rare")
    self.starMin = parse_number(row, "starMin")
    self.starMax = parse_number(row, "starMax")
    self.catId = parse_number(row, "catId")
    local kind = parse_strings(parse_string(row, "kind"))
    local t = {}
    for i, v in pairs(kind) do
        if checkNumber(v) > 0 then
            table.insert(t, checkNumber(v))
        end
    end
    self.kind = t
    self.inHandBook = parse_number(row, "inHandBook") == 1
    self.picture = parse_pathName(row, "picture")
    self.skillPicture = parse_pathName(row, "skillPicture")
end

function CatBallVo:__delete()
    self.roleId = nil
    self.name = nil
    self.types = nil
    self.subtype = nil
    self.rare = nil
    self.starMin = nil
    self.starMax = nil
    self.kind = nil
    self.inHandBook = nil
    self.picture = nil
    self.skillPicture = nil
end

return CatBallVo
