---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-07-12 15:08:04
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class MaidCoffeeRenovateSuitVo
local MaidCoffeeRenovateSuitVo = Class("MaidCoffeeRenovateSuitVo", SettingRefBase)

---__int
function MaidCoffeeRenovateSuitVo:__init()
    self.id = nil              --套装id
    self.name = nil              --名称
    self.description = nil              --文案（暂时不填）
    self.photoPath = nil              --套装图标
    self.quality = nil              --套装品质
    self.furnitureId = nil              --包含家具
    self.furnitureNum = nil              --数量（都是1，可以省略）
    self.type = nil              --0，整包无家具单价；1，由单价计算
    self.goodsId = nil              --购买所需货币类型
    self.isDirect = nil
    self.originalNum = nil              --原价
    self.goodsNum = nil              --折扣价
    self.hot = nil              --是否推荐（1，推荐）
    self.status = nil              --1，正常；0，禁用
end

---FillVo
---@param row TableML.TableFileRow
function MaidCoffeeRenovateSuitVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.description = ParseUtils.ParseLocalizeText(row, "description")
    self.photoPath = ParseUtils.ParsePathName(row,"photoId")              --图片
    self.quality = parse_number(row, "quality")
    self.furnitureIds = parse_strings(parse_string(row, "furnitureId"),";")
    self.furnitureNums = parse_strings(parse_string(row, "furnitureNum"),";")
    self.type = parse_number(row, "type")  ---为0是整包 不单卖
    self.isDirect = parse_number(row,"isDirect")
    self.goodsId = parse_number(row, "goodsId")
    self.originalNum = parse_number(row, "originalNum")
    self.goodsNum = parse_number(row, "goodsNum")
    self.hot = parse_number(row, "hot")
    self.status = parse_number(row, "status")
end

function MaidCoffeeRenovateSuitVo:__delete()
    self.id = nil
    self.name = nil
    self.description = nil
    self.photoPath = nil
    self.quality = nil
    self.furnitureId = nil
    self.furnitureNum = nil
    self.type = nil
    self.isDirect = nil
    self.goodsId = nil
    self.originalNum = nil
    self.goodsNum = nil
    self.hot = nil
    self.status = nil
end

return MaidCoffeeRenovateSuitVo
