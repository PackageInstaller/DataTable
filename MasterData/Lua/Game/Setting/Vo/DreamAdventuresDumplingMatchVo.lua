---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2024-04-18 14:01:13
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class DreamAdventuresDumplingMatchVo
local DreamAdventuresDumplingMatchVo = Class("DreamAdventuresDumplingMatchVo", SettingRefBase)

---__int
function DreamAdventuresDumplingMatchVo:__init()
    self.id = nil              --编号
    self.activityId = nil              --活动id
    self.materialId = nil              --对应所需材料的id（不分前后顺序）
    self.dumplingId = nil              --对应团子id：有id表示组合可搭配出团子，配置为0则代表该组合不可被搭配
    self.groupTitleNew = nil              --新组合提示语
    self.groupTitleOld = nil              --重复组合提示语
    self.groupTitleFail = nil              --失败组合提示语
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function DreamAdventuresDumplingMatchVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.activityId = parse_number(row, "activityId")
    self.materialId = parse_strings(parse_string(row, "materialId"), ";")
    self.dumplingId = parse_number(row, "dumplingId")
    self.groupTitleNew = ParseUtils.ParseLocalizeText(row, "groupTitleNew")
    self.groupTitleOld = ParseUtils.ParseLocalizeText(row, "groupTitleOld")
    self.groupTitleFail = ParseUtils.ParseLocalizeText(row, "groupTitleFail")
    self.status = parse_number(row, "status")
end

function DreamAdventuresDumplingMatchVo:__delete()
    self.id = nil
    self.activityId = nil
    self.materialId = nil
    self.dumplingId = nil
    self.groupTitleNew = nil
    self.groupTitleOld = nil
    self.groupTitleFail = nil
    self.status = nil
end

return DreamAdventuresDumplingMatchVo
