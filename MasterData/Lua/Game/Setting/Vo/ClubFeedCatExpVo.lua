---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2021-10-13 11:38:23
--- 好感度料理表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class ClubFeedCatExpVo
local ClubFeedCatExpVo = Class("ClubFeedCatExpVo", SettingRefBase)

function ClubFeedCatExpVo:__init()
    self.id = nil                        --神兽等级
    self.phase = nil                     --神兽阶段
    self.consNum = nil                   --升级需要经验值
    self.catSatiety = nil                --饱食度上限
    self.buff = nil                      --GVG全局效果
    self.txtLevel = nil                  --升级对话
    self.txtBuff = nil                   --GVG全局效果升级描述
end

---FillVo
---@param row TableML.TableFileRow
function ClubFeedCatExpVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "level")
    self.phase = parse_number(row, "phase")
    self.consNum = parse_number(row, "consNum")
    self.catSatiety = parse_number(row, "catSatiety")
    self.buff = parse_string(row, "buff")
    self.txtLevel = ParseUtils.ParseLocalizeText(row, "txtLevel")
    self.txtBuff = ParseUtils.ParseLocalizeText(row, "txtBuff")
end

function ClubFeedCatExpVo:__delete()
    self.id = nil
    self.phase = nil
    self.consNum = nil
    self.catSatiety = nil
    self.buff = nil
    self.txtLevel = nil
    self.txtBuff = nil
end

return ClubFeedCatExpVo
