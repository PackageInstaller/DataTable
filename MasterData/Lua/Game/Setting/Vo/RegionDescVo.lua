---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-02-25 13:29:04
--- 跑马灯提示文本表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class RegionDescVo
local RegionDescVo = Class("RegionDescVo", SettingRefBase)

---__int
function RegionDescVo:__init()
    self.id = nil              --ID
    self.lable = nil              --标签
    self.describe = nil              --区域坐标点（根据此坐标生成点击区域）
    self.showPriority = nil              --展示优先级: 1为1级区域地图下展示的提示，2为2级地图普通状态下，3为2级地图歼灭战状态下
    self.unlockType = nil              --区域解锁条件类型，对应解锁条件表
    self.unlockId = nil              --解锁的目标ID
    self.unlockNum = nil              --解锁条件参数
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function RegionDescVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.lable = ParseUtils.ParseLocalizeText(row, "lable")
    self.describe = ParseUtils.ParseLocalizeText(row, "describe")
    self.showPriority = parse_number(row, "showPriority")
    self.unlockType = parse_number(row, "unlockType")
    self.unlockId = parse_number(row, "unlockId")
    self.unlockNum = parse_number(row, "unlockNum")
    self.status = parse_number(row, "status")
end

function RegionDescVo:__delete()
    self.id = nil
    self.lable = nil
    self.describe = nil
    self.showPriority = nil
    self.unlockType = nil
    self.unlockId = nil
    self.unlockNum = nil
    self.status = nil
end

return RegionDescVo
