---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2021-12-07 16:58:23
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class CatHouseTaskVo
local CatHouseTaskVo = Class("CatHouseTaskVo", SettingRefBase)

---__int
function CatHouseTaskVo:__init()
    self:CleanField()
end

function CatHouseTaskVo:__delete()
    self:CleanField()
end

---FillVo
---@param row TableML.TableFileRow
function CatHouseTaskVo:FillVo(row)
    base.FillVo(self,row)
    self.taskId = parse_number(row, "taskId")
    self.site = parse_number(row, "site")
    self.attributeId = parse_string(row, "attributeId")
    self.taskQuality = parse_string(row, "taskQuality")
    self.taskStar = parse_number(row, "taskStar")
    self.taskTime = parse_string(row, "taskTime")

    --- 属性要求 与 属性奖励
    self.attributeNumOne = parse_number(row, "attributeNumOne", 0)
    self.awardOneId =  parse_number(row, "awardOneId", 0)
    self.awardOneNum = parse_number(row, "awardOneNum", 0)
    
    self.attributeNumTwo = parse_number(row, "attributeNumTwo", 0)
    self.awardTwoId = parse_number(row, "awardTwoId", 0)
    self.awardTwoNum = parse_number(row, "awardTwoNum", 0)
    
    self.attributeNumThree = parse_number(row, "attributeNumThree", 0)
    self.awardThreeId = parse_number(row, "awardThreeId", 0)
    self.awardThreeNum = parse_number(row, "awardThreeNum", 0)
    
    self.attributeNumFour = parse_number(row, "attributeNumFour", 0)
    self.awardFourId = parse_number(row, "awardFourId", 0)
    self.awardFourNum = parse_number(row, "awardFourNum", 0)
    
    self.attributeNumFive = parse_number(row, "attributeNumFive", 0)
    self.awardFiveId = parse_number(row, "awardFiveId", 0)
    self.awardFiveNum = parse_number(row, "awardFiveNum", 0)

    --- 特殊要求 与 特殊要求奖励
    self.requireOne = parse_number(row, "requireOne", 0)
    self.rewardOneId = parse_number(row, "rewardOneId", 0)
    self.rewardOneNum = parse_number(row, "rewardOneNum", 0)
    
    self.requireTwo = parse_number(row, "requireTwo", 0)
    self.rewardTwoId = parse_number(row, "rewardTwoId", 0)
    self.rewardTwoNum = parse_number(row, "rewardTwoNum", 0)
    
    self.requireThree = parse_number(row, "requireThree", 0)
    self.rewardThreeId = parse_number(row, "rewardThreeId", 0)
    self.rewardThreeNum = parse_number(row, "rewardThreeNum", 0)
    
    self.taskName = ParseUtils.ParseLocalizeText(row, "taskName")
    self.taskDescribe = ParseUtils.ParseLocalizeText(row, "taskDescribe")
    self.strengthNum = parse_number(row, "strengthNum", 0)
    self.playerStrengthId = parse_number(row, "playerStrengthId", 0)
    self.playerStrengthNum = parse_number(row, "playerStrengthNum", 0)
    self.exp = parse_number(row, "exp", 0)
    
    self.attributeDesc = parse_string(row, "attributeDesc", "")
    self.attributeNumDesc = parse_string(row, "attributeNumDesc", "")
    
    self.picture = ParseUtils.ParsePathName(row, "picture")
    self.boxPicture = ParseUtils.ParsePathName(row, "boxPicture")
end

function CatHouseTaskVo:CleanField()
    self.taskId = nil   --- 任务id
    self.site = nil
    self.attributeId = nil   --- 属性id：1敏捷、2力量、3聪慧、4魅力
    self.taskQuality = nil   --- 任务品质：1R、2SR、3SSR
    self.taskTime = nil   --- 任务时长（以秒为单位）

    self.attributeNumOne = nil
    self.awardOneId = nil
    self.awardOneNum = nil
    self.attributeNumTwo = nil
    self.awardTwoId = nil
    self.awardTwoNum = nil
    self.attributeNumThree = nil
    self.awardThreeId = nil
    self.awardThreeNum = nil
    self.attributeNumFour = nil
    self.awardFourId = nil
    self.awardFourNum = nil
    self.attributeNumFive = nil
    self.awardFiveId = nil
    self.awardFiveNum = nil

    --- 特殊要求 与 特殊要求奖励
    self.requireOne = nil
    self.rewardOneId = nil
    self.rewardOneNum = nil
    self.requireTwo = nil
    self.rewardTwoId = nil
    self.rewardTwoNum = nil
    self.requireThree = nil
    self.rewardThreeId = nil
    self.rewardThreeNum = nil
    
    self.taskName = nil   ---任务名称
    self.taskDescribe = nil   ---任务描述
    self.strengthNum = nil ---消耗猫咪体力
    self.PlayerStrengthNum = nil ---消耗玩家体力
    self.exp = nil ---主角经验
    self.attributeDesc = nil ---探索任务属性要求描述字段
    self.picture = nil
    self.boxPicture = nil ---奖励宝箱图片
end

return CatHouseTaskVo
