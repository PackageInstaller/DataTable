---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-11-22 17:55:45
--- 定制装备属性表
---

local SettingRefBase = require('Game.Setting.SettingRefBase')

local parse_number  = parse_number
local parse_string  = parse_string
local parse_strings = parse_strings

---@class EquipCustomAttrVo
local EquipCustomAttrVo = Class("EquipCustomAttrVo", SettingRefBase)

---__int
function EquipCustomAttrVo:__init()
    self.id = nil              --定制类型id
    self.star = nil              --星级
    self.level = nil              --等级
    self.mainAttribute = nil              --主属性id（0-不指定
    self.mainAttrInit = nil              --主属性数值
    self.secAttribute = nil              --副属性id（0-不指定
    self.secValue = nil              --副属性值
end

---FillVo
---@param row TableML.TableFileRow
function EquipCustomAttrVo:FillVo(row)
    SettingRefBase.FillVo(self,row)
    self.id                 = parse_number(row, "id")
    self.star               = parse_number(row, "star")
    self.level              = parse_number(row, "level")
    self.mainAttribute      = parse_number(row, "mainAttribute")
    self.mainAttrInit       = parse_number(row, "initialMin")
    self.secAttribute       = parse_string(row, "secAttribute")
    self.secValue           = parse_string(row, "secValue")
end

function EquipCustomAttrVo:__delete()
    self.id = nil
    self.star = nil
    self.level = nil
    self.mainAttribute = nil
    self.mainAttrInit = nil
    self.secAttribute = nil
    self.secValue = nil
end

function EquipCustomAttrVo:GetSubAttributes()
    local attrIds     = parse_strings(self.secAttribute)
    local attrVs      = parse_strings(self.secValue)
    local attrIdCount = #attrIds
    local attrVCount  = #attrVs
    local times       = math.min(attrVCount, attrIdCount)
    local t           = {}
    for i = 1, times do
        table.insert(t, {attrIds[i], attrVs[i]})
    end
    return t
end

return EquipCustomAttrVo
