---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2022-11-07 17:28:09
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class MapDecryptionDecryptedMapsVo
local MapDecryptionDecryptedMapsVo = Class("MapDecryptionDecryptedMapsVo", SettingRefBase)

---__int
function MapDecryptionDecryptedMapsVo:__init()
    self.id = nil              --地图编号
    self.activityId = nil              --活动id
    self.name = nil              --地图名称
    self.desc = nil              --地图描述
    self.explain = nil              --提示文本
    self.unlockNum = nil              --解锁条件值（填充为0，固定为前置关卡解锁）
    self.time = nil              --解锁日期
    self.rewards = nil              --全解密奖励道具
    self.sence = nil              --地图内场景
    self.place = nil              --地点图
    self.antiquePicture = nil              --古物图
    self.noAntique = nil              --古物未解锁图
    self.antiqueId = nil              --对应古物id
    self.groupOne = nil              --未获得古物时地图组
    self.groupTwo = nil              --获得古物但未修复时地图组
    self.groupThree = nil              --修复古物后地图组
end

---FillVo
---@param row TableML.TableFileRow
function MapDecryptionDecryptedMapsVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.activityId = parse_number(row, "activityId")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
    self.explain = string.gsub( ParseUtils.ParseLocalizeText(row, "explain"), "\\n", "\n")
    self.unlockNum = parse_number(row, "unlockNum")
    self.time = parse_string(row, "time")
    self.rewards = base.ParseRewards(self, row, "reward", "rewardNum")
    self.sence = parse_string(row, "sence")
    self.place = ParseUtils.ParsePathName(row, "place")
    self.antiquePicture = ParseUtils.ParsePathName(row, "antiquePicture")
    self.noAntique = ParseUtils.ParsePathName(row, "noAntique")
    self.antiqueId = parse_number(row, "antiqueId")
    self.groupOne = parse_number(row, "groupOne")
    self.groupTwo = parse_number(row, "groupTwo")
    self.groupThree = parse_number(row, "groupThree")
end

function MapDecryptionDecryptedMapsVo:__delete()
    self.id = nil
    self.activityId = nil
    self.name = nil
    self.desc = nil
    self.explain = nil
    self.unlockNum = nil
    self.time = nil
    self.rewards = nil
    self.sence = nil
    self.place = nil
    self.antiquePicture = nil
    self.noAntique = nil
    self.antiqueId = nil
    self.groupOne = nil
    self.groupTwo = nil
    self.groupThree = nil
end

return MapDecryptionDecryptedMapsVo
