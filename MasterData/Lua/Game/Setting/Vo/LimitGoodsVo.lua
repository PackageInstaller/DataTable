---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2025-05-06 17:05:18
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class LimitGoodsVo
local LimitGoodsVo = Class("LimitGoodsVo", SettingRefBase)

---__int
function LimitGoodsVo:__init()
    self.id = nil              --礼包的ID
    self.type = nil              --道具类型
    self.showType = nil              --显示类型
    self.name = nil              --名称（礼包道具属获取时直接打开的）
    self.desc = nil              --用途（加粗文本
    self.description = nil              --文案包装（普通文本
    self.quality = nil              --界面显示品质
    self.stack = nil              --背包堆叠:最大堆叠数量
    self.sellitem = nil              --贩卖货币id
    self.sellMoney = nil              --贩卖价格(转化成多少新海币）
    self.openType = nil              --跳转目标（填功能切割表里的ID）
    self.openParameter = nil              --跳转参数（填写关卡ID）
    self.photoId = nil              --图片id（待出资源，先用其他资源显示)
    self.bagUsed = nil              --背包使用按钮是否显示
    self.redPoint = nil              --是否获得后显示红点
    self.effectType = nil              --效果类型 (读效果类型枚举表
    self.effectId = nil              --对应ID
    self.effectNum = nil              --效果数量
    self.duration = nil              --时限（单位/小时）
    self.status = nil              --状态
    self.jumpAway = nil
end

---FillVo
---@param row TableML.TableFileRow
function LimitGoodsVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.type = parse_number(row, "type")
    self.showType = parse_number(row, "showType")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
    self.description = ParseUtils.ParseLocalizeText(row, "description")
    self.quality = parse_number(row, "quality")
    self.stack = parse_number(row, "stack")
    self.sellitem = parse_number(row, "sellitem")
    self.sellMoney = parse_string(row, "sellMoney")
     ---跳转的逻辑
     local openType = parse_string(row, "openType", "")
     if not string.isEmpty(openType) then
         local openTypes = string.split(openType, ";")
         local t = {}
         for i, v in pairs(openTypes) do
             if checkInt(v) > 0 then
                 table.insert(t, checkInt(v))
             end
         end
         self.jumpAway = t
         ---只有跳转不为空时参数才有存在的意义
         local openParameter = parse_string(row, "openParameter", "")
         if not string.isEmpty(openParameter) then
             local params = string.split(openParameter, ";")
             local t = {}
             for i, v in pairs(params) do
                 --if checkInt(v) > 0 then
                     table.insert(t, checkInt(v))
                 --end
             end
             self.jumpParameter = t
         end
     end
    self.photoPath = ParseUtils.ParsePathName(row, "photoId")
    self.bagUsed = parse_string(row, "bagUsed")
    self.redPoint = parse_number(row, "redPoint")
    self.effectType = parse_number(row, "effectType")
    self.effectId = parse_string(row, "effectId")
    self.effectNum = parse_string(row, "effectNum")
    self.duration = parse_number(row, "duration")
    self.status = parse_number(row, "status")
end

function LimitGoodsVo:__delete()
    self.id = nil
    self.type = nil
    self.showType = nil
    self.name = nil
    self.desc = nil
    self.description = nil
    self.quality = nil
    self.stack = nil
    self.sellitem = nil
    self.sellMoney = nil
    self.openType = nil
    self.openParameter = nil
    self.photoId = nil
    self.bagUsed = nil
    self.redPoint = nil
    self.effectType = nil
    self.effectId = nil
    self.effectNum = nil
    self.duration = nil
    self.status = nil
    self.jumpAway = nil
end

return LimitGoodsVo
