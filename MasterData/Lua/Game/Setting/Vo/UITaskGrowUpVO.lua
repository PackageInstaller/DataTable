---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by zou hanjie.
--- DateTime: 2023-07-24 19:10:59
--- 成长任务表


local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class UITaskGrowUpVO
local UITaskGrowUpVO = Class("UITaskGrowUpVO", SettingRefBase)

---__int
function UITaskGrowUpVO:__init()
    self.taskAchId = nil        --任务的ID
    self.id = nil              --主题id
    self.taskGroup = nil        -- 任务组
    self.desc = nil             -- 任务描述
    self.after = nil            -- 后续任务
    self.taskContentType = nil  -- 任务目标类型
    self.taskContentId = nil    -- 任务目标 -- 当 角色卡牌 id 来用
    self.taskContenNum = nil    -- 任务值

    self.rewardIds = nil         -- 任务奖励
    self.rewardNums = nil        -- 奖励数量
    --self.rewards = nil

    self.openType = nil         -- 跳转目标
    self.openParameter = nil    -- 跳转参数
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function UITaskGrowUpVO:FillVo(row)
    base.FillVo(self,row)

    self.taskAchId          = parse_number(row, "taskAchId")
    self.id                 = parse_number(row, "id")
    self.taskGroup          = parse_number(row, "taskGroup")
    self.desc               = ParseUtils.ParseLocalizeText(row, "desc")
    self.after              = parse_string(row, "after") -- 配表里类型标注为 string
    self.taskContentType    = parse_number(row, "taskContentType")
    self.taskContentId      = parse_number(row, "taskContentId")
    self.taskContenNum      = parse_number(row, "taskContenNum")

    self.rewardIds           = Tools.splitToNum(parse_string(row, "rewardId", 0), ';') -- {int,int,...}
    self.rewardNums          = Tools.splitToNum(parse_string(row, "rewardNum", 0), ';') -- {int,int,...}
    --self.rewards             = parse_rewardData(row  , "rewardId", "rewardNum", ";") -- table[] @ { goodsId : integer, goodsNum : integer }


    self.openType           = parse_number(row, "openType")
    self.openParameter      = parse_number(row, "openParameter")
end

function UITaskGrowUpVO:__delete()
    self.taskAchId = nil        --任务的ID
    self.id = nil              --主题id
    self.taskGroup = nil        -- 任务组
    self.desc = nil             -- 任务描述
    self.after = nil            -- 后续任务
    self.taskContentType = nil  -- 任务目标类型
    self.taskContentId = nil    -- 任务目标
    self.taskContenNum = nil    -- 任务值

    self.rewardIds = nil         -- 任务奖励
    self.rewardNums = nil        -- 奖励数量
    --self.rewards = nil

    self.openType = nil         -- 跳转目标
    self.openParameter = nil    -- 跳转参数
    self.status = nil              --状态
end

return UITaskGrowUpVO
