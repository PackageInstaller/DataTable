---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yajieli.
--- DateTime: 2019-08-15 10:41
---

local SettingRefBase = require('Game.Setting.SettingRefBase')

---@class RolesVo : SettingRefBase @卡牌配表的数据
local RolesVo = Class("RolesVo", SettingRefBase)

local ParseUtils = CS.ParseUtils

local base = SettingRefBase

---__int
function RolesVo:__init()
    self.roleId         = nil  -- 角色id
    self.name           = nil  -- 名称
    self.englishName    = nil  -- 英文名
    -- self.cv             = nil  -- 日配CV
    self.allName        = nil  -- 全名
    self.birthday       = nil  -- 生日
    self.personality    = nil  -- 性格
    self.symbolize      = nil  -- 象征
    self.faith          = nil  -- 信条
    self.info           = nil  -- 简介
    self.catId          = nil  -- 猫形态对应id
    self.catName        = nil  -- 猫名
    self.subtitle       = nil  -- 角色副标题
    self.image          = nil  -- 角色图
    self.character      = nil  --小头像图片
    self.catHead        = nil  -- 角色图
    self.timeLineName   = nil  -- 升阶TL名称
end

---FillVo
---@param row TableML.TableFileRow
function RolesVo:FillVo(row)
    base.FillVo(self, row)

    self.roleId = parse_number(row, "roleId", 0)
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.catName = ParseUtils.ParseLocalizeText(row, "catName")
    self.subtitle = ParseUtils.ParseLocalizeText(row, "subtitle")
    -- self.cv = ParseUtils.ParseLocalizeText(row, "CV")
    self.allName = ParseUtils.ParseLocalizeText(row, "allName")
    self.birthday = ParseUtils.ParseLocalizeText(row, "birthday")
    self.personality = ParseUtils.ParseLocalizeText(row, "personality")
    self.symbolize = ParseUtils.ParseLocalizeText(row, "symbolize")
    self.faith = parse_localizeText(row, "faith", true)
    self.info = parse_localizeText(row, "info", true)
    self.image = ParseUtils.ParsePathName(row, "image")
    self.catHead = ParseUtils.ParsePathName(row, "catHead")
    self.englishName = ParseUtils.ParseLocalizeText(row, "englishName")
    self.character = ""
    self.man = ParseUtils.ParseLocalizeText(row, "man")
    self.woman = ParseUtils.ParseLocalizeText(row, "woman")
    self.timeLineName = parse_string(row, "TimeLineName")
    self.cardHeadList = parse_string(row, "cardHeadList")
    self.catId = parse_number(row, "catId", 0)
end

function RolesVo:__delete()
    self.roleId = nil
    self.name = nil
    self.catName = nil
    self.subtitle = nil
    self.image = nil
    self.character = nil
    self.catHead = nil
    self.englishName = nil
    self.catId = nil
end

return RolesVo