---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-02-07 16:56:12
--- 一级大地图表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class RegionPrimaryMapVo
local RegionPrimaryMapVo = Class("RegionPrimaryMapVo", SettingRefBase)

---__int
function RegionPrimaryMapVo:__init()
    self.id = nil              --区域编号
    self.name = nil              --名称
    self.coordinate = nil              --区域位置坐标
    self.infoCoordinate = nil              --区域信息锚点位置坐标
    self.regionSpec = nil              --区域特殊玩法标记
    self.regionMap = nil              --区域地图资源
    self.unlockType = nil              --解锁条件类型
    self.unlockParam = nil              --解锁条件参数
    self.takeawayIcon = nil
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function RegionPrimaryMapVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.lable = ParseUtils.ParseLocalizeText(row, "lable")
    self.taskName = ParseUtils.ParseLocalizeText(row, "taskName")
    self.taskImage = ParseUtils.ParsePathName(row, "taskImage")

    self.coordinate = parse_string(row, "coordinate")
    self.infoCoordinate = parse_string(row, "infoCoordinate")
    self.regionSpec = ParseUtils.ParsePathName(row, "regionSpec")
    self.rsCoordinate = parse_string(row, "rsCoordinate")
    self.OverviewBanner = ParseUtils.ParsePathName(row, "OverviewBanner")
    self.regionSpecShow = parse_number(row, "regionSpecShow")
    self.mapIcon = ParseUtils.ParsePathName(row, "mapIcon")

    self.regionMap = ParseUtils.ParsePathName(row, "regionMap")
    self.unlockType = parse_string(row, "unlockType")
    self.unlockId = parse_string(row, "unlockId")
    self.unlockNum = parse_string(row, "unlockNum")

    --self.enterCameraPos = parse_string(row, "enterCameraPos")
    self.cameraPosX = parse_string(row, "cameraPosX")
    self.cameraPosY = parse_string(row, "cameraPosY")
    self.takeawayIcon = ParseUtils.ParsePathName(row, "takeawayIcon")
    self.status = parse_number(row, "status")
end

function RegionPrimaryMapVo:__delete()
    self.id = nil
    self.name = nil
    self.coordinate = nil
    self.infoCoordinate = nil
    self.takeawayIcon = nil
    self.regionSpec = nil
    self.regionMap = nil
    self.unlockType = nil
    self.unlockParam = nil
    self.status = nil
end

return RegionPrimaryMapVo
