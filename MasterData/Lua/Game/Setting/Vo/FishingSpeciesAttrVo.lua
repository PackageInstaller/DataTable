---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2021-08-23 14:26:18
--- 鱼种属性表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class FishingSpeciesAttrVo
local FishingSpeciesAttrVo = Class("FishingSpeciesAttrVo", SettingRefBase)

---__int
function FishingSpeciesAttrVo:__init()
    self.id = nil              --序号
    self.name = nil              --名称
    self.life = nil              --血量上下限
    self.speed = nil              --速度
    self.escapeCoefficient = nil              --逃逸系数
    self.initialDistance = nil              --初始距离
    self.type = nil              --类型（1-按时间 2-按血量百分比
    self.escapeBlock = nil              --逃逸区间
    self.ordinaryBlock = nil              --普通区间
    self.advanceDistance = nil              --前进距离
    self.ordinarySpeed = nil              --无序运动下普通速度
    self.escapeSpeed = nil              --无序运动下逃逸速度
    self.forwardRunTime = nil              --往前游的时间
    self.forwardRunSpeed = nil              --往前游的速度+
    self.distanceLimits = nil              --运动范围
    self.buoySpeed = nil              --数量
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function FishingSpeciesAttrVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.life = parse_string(row, "life")
    self.speed = parse_number(row, "speed")
    self.escapeCoefficient = parse_number(row, "escapeCoefficient")
    self.initialDistance = parse_number(row, "initialDistance")
    self.type = parse_number(row, "type")
    self.escapeBlock = parse_string(row, "escapeBlock")
    self.ordinaryBlock = parse_string(row, "ordinaryBlock")
    --self.advanceDistance = parse_number(row, "advanceDistance")
    self.ordinarySpeed = parse_number(row, "ordinarySpeed")
    self.escapeSpeed = parse_number(row, "escapeSpeed")
    self.status = parse_number(row, "status")
    self.forwardRunTime = parse_number(row, "forwardRunTime", 2)
    self.forwardRunSpeed = parse_number(row, "forwardRunSpeed", 1)
    self.buoySpeed = parse_string(row, "buoySpeed")              --数量
    self.distanceLimits = parse_string(row, "distanceLimits", "")
end

function FishingSpeciesAttrVo:__delete()
    self.id = nil
    self.name = nil
    self.life = nil
    self.speed = nil
    self.type = nil              --类型（1-按时间 2-按血量百分比
    self.escapeBlock = nil              --逃逸区间
    self.ordinaryBlock = nil              --普通区间
    self.escapeCoefficient = nil
    self.initialDistance = nil
    self.advanceDistance = nil              --前进距离
    self.ordinarySpeed = nil              --无序运动下普通速度
    self.escapeSpeed = nil              --无序运动下逃逸速度
    self.forwardRunTime = nil              --往前游的时间
    self.forwardRunSpeed = nil              --往前游的速度
    self.distanceLimits = nil              --运动范围
    self.buoySpeed = nil              --数量
    self.status = nil
end

return FishingSpeciesAttrVo
