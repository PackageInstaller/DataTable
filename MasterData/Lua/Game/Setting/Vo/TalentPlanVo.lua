---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2021-08-17 17:28:09
--- 天赋材料副本排期表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class TalentPlanVo
local TalentPlanVo = Class("TalentPlanVo", SettingRefBase)

---__int
function TalentPlanVo:__init()
    self.id = nil              --编号
    self.startDay = nil              --出现时间
    self.endDay = nil              --结束时间
    self.NextID = nil              --下一期的ID
    self.upType = nil              --增益类型（1卡牌，2职业，3属性，4猫种，5品质）
    self.upParam = nil              --参数（根据类型决定）
    self.upBuff = nil              --增益buffID，对应基础表/关卡增益效果表ID
    self.type = nil              --副本类型（对应副本表）
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function TalentPlanVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.startDay = parse_string(row, "startDay")
    self.endDay = parse_string(row, "endDay")
    self.NextID = parse_number(row, "NextID")
    self.upType = parse_number(row, "upType")
    self.upParam = parse_number(row, "upParam")
    self.upBuff = parse_number(row, "upBuff")
    self.type = parse_number(row, "type")
    self.status = parse_number(row, "status")
end

function TalentPlanVo:__delete()
    self.id = nil
    self.startDay = nil
    self.endDay = nil
    self.NextID = nil
    self.upType = nil
    self.upParam = nil
    self.upBuff = nil
    self.type = nil
    self.status = nil
end

return TalentPlanVo
