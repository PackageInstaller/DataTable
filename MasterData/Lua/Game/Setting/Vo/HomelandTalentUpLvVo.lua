---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2024-03-22 11:35:47
--- 战略强化升级表
---

local parse_number = parse_number
local parse_string = parse_string
local parse_localizeText = parse_localizeText

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class HomelandTalentUpLvVo
local HomelandTalentUpLvVo = Class("HomelandTalentUpLvVo", SettingRefBase)

---__int
function HomelandTalentUpLvVo:__init()
    self.id = nil              --序号
    self.talentId = nil              --天赋id
    self.level = nil              --等级
    self.attAddTypeId = nil              --效果枚举ID
    self.param1 = nil              --参数 用于条件判断
    self.attAddNum = nil              --数值
    self.buffId = nil              --对应的buffid
    self.costItem = nil              --升级消耗道具
    self.costNum = nil              --消耗数量
    self.combat = nil              --虚战力（攻防血暴击一般不配，配了就叠加）
end

---FillVo
---@param row TableML.TableFileRow
function HomelandTalentUpLvVo:FillVo(row)
    base.FillVo(self,row)
    self.id           = parse_number(row, "id")
    self.talentId     = parse_number(row, "talentId")
    self.desc         = parse_localizeText(row, "descrp")
    self.level        = parse_number(row, "level")
    self.attAddTypeId = parse_number(row, "attAddTypeId")
    self.param1       = parse_number(row, "param1")
    self.param2       = parse_number(row, "param2")
    self.param3       = parse_number(row, "param3")
    self.attAddNum    = parse_number(row, "attAddNum")
    self.buffId       = parse_string(row, "buffId")
    self.costItem     = parse_number(row, "costItem")
    self.costNum      = parse_number(row, "costNum")
    self.combat       = parse_number(row, "combat")
end

function HomelandTalentUpLvVo:__delete()
    self.id = nil
    self.talentId = nil
    self.level = nil
    self.attAddTypeId = nil
    self.param1 = nil
    self.attAddNum = nil
    self.buffId = nil
    self.costItem = nil
    self.costNum = nil
    self.combat = nil
end

function HomelandTalentUpLvVo:GetConsumes()
    if self._consumes == nil then
        self._consumes = {{goodsId = self.costItem, num = self.costNum}}
    end
    return self._consumes
end

return HomelandTalentUpLvVo
