---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-07-11 16:26:49
--- 剧情组别表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class ActivityPtPlotVo
local ActivityPtPlotVo = Class("ActivityPtPlotVo", SettingRefBase)

---__int
function ActivityPtPlotVo:__init()
    self.id = nil              --编号
    self.storyGroup = nil              --剧情组别ID
    self.storyId = nil              --剧情id
    self.unlockProp = nil              --解锁所需道具
    self.unlockPropNum = nil              --解锁所需道具累计数量
    self.rewardId = nil              --阅读奖励
    self.rewardNum = nil              --阅读奖励数量
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function ActivityPtPlotVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = parse_localizeText(row, "name")
    self.desc = parse_localizeText(row, "desc")
    self.chapter = parse_localizeText(row, "chapter")

    self.group = parse_number(row, "group")
    self.storyId = parse_number(row, "storyId")
    self.unlockProp = parse_number(row, "unlockProp")
    self.unlockPropNum = parse_number(row, "unlockPropNum")
    self.unlockStory = parse_number(row, "unlockStory")

    self.rewardId = parse_number(row, "rewardId")
    self.rewardNum = parse_number(row, "rewardNum")
    self.firstStory = parse_number(row, "firstStory")

    self.status = parse_number(row, "status")
end

function ActivityPtPlotVo:__delete()
    self.id = nil
    self.storyGroup = nil
    self.storyId = nil
    self.unlockProp = nil
    self.unlockPropNum = nil
    self.rewardId = nil
    self.rewardNum = nil
    self.status = nil
end

return ActivityPtPlotVo
