---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-12-05 16:35:31
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class ChaoticRecallLevelVo
local ChaoticRecallLevelVo = Class("ChaoticRecallLevelVo", SettingRefBase)

---__int
function ChaoticRecallLevelVo:__init()
    self.id = nil              --关卡id
    self.name = nil
    self.proposalLevel = nil              --推荐战斗力
    self.tag = nil              --推荐标签
    self.roleRestrictions = nil              --角色限制ID
    self.sceneId = nil              --场景ID
    self.buffDec = nil              --关卡buff描述
    self.battleRules = nil              --战斗规则字段
    self.roleNun = nil              --最多上阵人数（1为下限 4为上限）
    self.powerRatioTeamID = nil              --战力碾压组别
    self.rounds = nil              --回合限制(超出后直接失败)
    self.exp = nil              --主角经验
    self.cardExp = nil              --卡牌经验
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function ChaoticRecallLevelVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = parse_localizeText(row, "name")
    self.proposalLevel = parse_number(row, "proposalLevel")
    self.tag = parse_strings(parse_string(row, "tag"),";")
    self.roleRestrictions = parse_number(row, "roleRestrictions")
    self.sceneId = parse_number(row, "sceneId")
    self.buffDec = ParseUtils.ParseLocalizeText(row, "buffDec")
    self.battleRules = parse_string(row, "battleRules")
    self.roleNun = parse_string(row, "roleNun")
    self.powerRatioTeamID = parse_number(row, "powerRatioTeamID")
    self.rounds = parse_number(row, "rounds")
    self.exp = parse_number(row, "exp")
    self.cardExp = parse_number(row, "cardExp")
    self.restartBattleBtn = parse_number(row, "restartBattleBtn")
    self.damageTotal = parse_number(row, "damageTotal", 0)  -- 控制战中界面是否显示全场总伤害UI
    self.status = parse_number(row, "status")

    -- self里面有部分字段是从ChaoticRecallNodeVo里面写入的
end

function ChaoticRecallLevelVo:__delete()
    self.id = nil
    self.name = nil
    self.proposalLevel = nil
    self.tag = nil
    self.roleRestrictions = nil
    self.sceneId = nil
    self.buffDec = nil
    self.battleRules = nil
    self.roleNun = nil
    self.powerRatioTeamID = nil
    self.rounds = nil
    self.exp = nil
    self.cardExp = nil
    self.status = nil
end

return ChaoticRecallLevelVo
