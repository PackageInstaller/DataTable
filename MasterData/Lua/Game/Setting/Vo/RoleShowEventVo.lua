---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2021-07-23 11:26:45
--- 看板娘事件表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class RoleShowEventVo
local RoleShowEventVo = Class("RoleShowEventVo", SettingRefBase)

---__int
function RoleShowEventVo:__init()
    self.id = nil              --事件ID
    self.roleId = nil              --对应角色编号
    self.name = nil              --对应角色名称
    self.type = nil              --事件类型（见枚举）
    self.shape = nil              --角色外形（0人形，1猫形）
    self.unlockType = nil              --开启条件（对应解锁条件表里的数值）
    self.unlockId = nil              --开启条件ID
    self.unlockNum = nil              --开启条件数值
    self.position = nil              --事件发生的位置
    self.cardId = nil              --卡牌ID（不填即为通用）
    self.targetNum = nil              --好感阶段（1.初级 2.中级 3.高级 不填为没有好感度限制）
    self.given = nil              --特定日期（配置节日的日期，不配为不走节日）1.七夕 2.春节 3.端午 4.中秋 5.玩家生日
    self.weight = nil              --事件权重
    self.standby = nil              --待机表现
    self.plotId = nil              --调用的剧情表现ID
    self.endCall = nil              --结束时调用事件ID
    self.scRoleId = nil              --配角ID
    self.scStandby = nil              --配角待机表现
    self.scEndCall = nil              --结束时调用事件ID
    self.endTime = nil              --事件倒计时（仅类型8有效）
    self.targetPosition = nil              --目标地点（type9有效）
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function RoleShowEventVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.roleId = parse_number(row, "roleId")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.type = parse_number(row, "type")
    self.shape = parse_number(row, "shape")
    self.unlockType = parse_string(row, "unlockType")
    self.unlockId = parse_number(row, "unlockId")
    self.unlockNum = parse_string(row, "unlockNum")
    self.position = parse_number(row, "position")
    self.cardId = parse_number(row, "cardId")
    self.targetNum = parse_string(row, "targetNum")
    self.given = parse_string(row, "given")
    self.weight = parse_number(row, "weight")
    self.standby = parse_string(row, "standby")
    self.plotId = parse_string(row, "plotId")
    self.endCall = parse_string(row, "endCall")
    self.scRoleId = parse_number(row, "scRoleId")
    self.scStandby = parse_string(row, "scStandby")
    self.scEndCall = parse_string(row, "scEndCall")
    self.endTime = parse_number(row, "endTime")
    self.targetPosition = parse_number(row, "targetPosition")
    self.status = parse_number(row, "status")
end

function RoleShowEventVo:__delete()
    self.id = nil
    self.roleId = nil
    self.name = nil
    self.type = nil
    self.shape = nil
    self.unlockType = nil
    self.unlockId = nil
    self.unlockNum = nil
    self.position = nil
    self.cardId = nil
    self.targetNum = nil
    self.given = nil
    self.weight = nil
    self.standby = nil
    self.plotId = nil
    self.endCall = nil
    self.scRoleId = nil
    self.scStandby = nil
    self.scEndCall = nil
    self.endTime = nil
    self.targetPosition = nil
    self.status = nil
end

return RoleShowEventVo
