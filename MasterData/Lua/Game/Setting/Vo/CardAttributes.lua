---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yajieli.
--- DateTime: 2019-08-15 10:41
---

local checkNumber = checkNumber

---@class CardAttributes
local CardAttributes = Class("CardAttributes")

---__int
function CardAttributes:__init()
    self.attrs = nil
end

function CardAttributes:__delete()
    self.attrs = nil
end
---FillVo
---@param row TableML.TableFileRow
---@param property string
---@return table
function CardAttributes:FillVo(row, property)
    local propertyStr = parse_string(row, property or "property")
    self:FillVoByPropertyStr(propertyStr)
    return self
end

function CardAttributes:FillVoByPropertyStr(propertyStr)
    self.attrs = {}
    ---分隔出加成属性的
    local attr_dict = parse_strings(propertyStr, ";")
    if not isNull(attr_dict) then
        for _, v in pairs(attr_dict) do
            local attrPair = string.split(v, ':')
            if table.count(attrPair) == 2 then
                ---排除不为2长度的
                local attrAddId, attrV = attrPair[1], attrPair[2]
                self.attrs[checkNumber(attrAddId)] = checkNumber(attrV)
            end
        end
    end
end

return CardAttributes