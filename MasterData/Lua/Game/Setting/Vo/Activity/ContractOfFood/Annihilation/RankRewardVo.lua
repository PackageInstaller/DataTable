---
--- Generated by vscode.
--- Created by pikaqiu.
--- DateTime: 2024-01-12
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class RankRewardVo
local RankRewardVo = Class("RankRewardVo", SettingRefBase)

function RankRewardVo:__init()
    self:CleanFields()
end

function RankRewardVo:__delete()
    self:CleanFields()
end

function RankRewardVo:CleanFields()
    self.id = nil           -- 编号
    self.activityId = nil   -- 活动id
    self.upperLimit = nil   -- 排名上限
    self.lowerLimit = nil   -- 排名下限
    self.name = nil         -- 档位文本显示
    self.type = nil         -- 排名类型（1名次，2百分比）
    self.reward = nil       -- 奖励道具
    self.rewardNum = nil    -- 奖励数量
    self.mailID = nil       -- 邮件ID
    self.status = nil       -- 状态
end

---FillVo
---@param row TableML.TableFileRow
function RankRewardVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id", 1)
    self.activityId = parse_number(row, "activityId", 1)
    self.upperLimit = parse_number(row, "upperLimit", 1)
    self.lowerLimit = parse_number(row, "lowerLimit", 100)
    self.name = parse_localizeText(row, "name", "")
    self.type = parse_number(row, "type", 1)
    self.reward = parse_numbers(row, "reward")
    self.rewardNum = parse_numbers(row, "rewardNum")
    self.mailID = parse_number(row, "mailID", 0)
    self.status = parse_number(row, "status", 0)

    
    self.rewardsList = parse_rewardData(row, "reward", "rewardNum", ";")
end

return RankRewardVo