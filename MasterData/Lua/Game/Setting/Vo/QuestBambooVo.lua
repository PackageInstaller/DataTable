---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2021-03-02 11:53:08
--- PT活动副本剧情关卡表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class QuestBambooVo
local QuestBambooVo = Class("QuestBambooVo", SettingRefBase)

---__int
function QuestBambooVo:__init()
    self.id = nil              --关卡ID
    self.label = nil              --标题
    self.name = nil              --名称
    self.monsteType = nil              --怪物类型（1普通 2BOSS  0无战斗填充）
    self.type = nil              --剧情类型（2剧情+战斗 1有剧情  0仅战斗）
    self.unlockNum = nil              --解锁条件值（填充为0，固定为前置关卡解锁）
    self.level = nil              --进入等级
    self.exp = nil              --主角经验
    self.cardExp = nil              --卡牌经验
    self.plotId = nil              --剧情ID
    self.opportunity = nil              --出现时机（1战前 2-5波次 6战后）
    self.proposalLevel = nil              --推荐战斗力
    self.SceneId = nil              --场景ID
    self.rounds = nil              --回合限制(超出后直接失败)
    self.roleNun = nil              --最多上阵人数（1为下限 4为上限）
    self.status = nil              --状态
    self.petLineup = nil
end

---FillVo
---@param row TableML.TableFileRow
function QuestBambooVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.label = ParseUtils.ParseLocalizeText(row, "label")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.monsteType = parse_number(row, "monsteType")
    self.type = parse_number(row, "type")
    self.unlockNum = parse_string(row, "unlockNum")
    self.level = parse_number(row, "level")
    self.exp = parse_number(row, "exp")
    self.cardExp = parse_number(row, "cardExp")
    self.plotId = parse_string(row, "plotId")
    self.opportunity = parse_string(row, "opportunity")
    self.proposalLevel = parse_number(row, "proposalLevel")
    self.SceneId = parse_number(row, "SceneId")
    self.rounds = parse_number(row, "rounds")
    self.roleNun = parse_string(row, "roleNun")
    self.status = parse_number(row, "status")
    self.petLineup = parse_number(row, "petLineup")
end

function QuestBambooVo:__delete()
    self.id = nil
    self.label = nil
    self.name = nil
    self.monsteType = nil
    self.type = nil
    self.unlockNum = nil
    self.level = nil
    self.exp = nil
    self.cardExp = nil
    self.plotId = nil
    self.opportunity = nil
    self.proposalLevel = nil
    self.SceneId = nil
    self.rounds = nil
    self.roleNun = nil
    self.status = nil
    self.petLineup = nil
end

return QuestBambooVo
