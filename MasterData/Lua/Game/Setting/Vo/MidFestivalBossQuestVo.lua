---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-11-13 14:09:38
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class MidFestivalBossQuestVo
local MidFestivalBossQuestVo = Class("MidFestivalBossQuestVo", SettingRefBase)

---__int
function MidFestivalBossQuestVo:__init()
    self.id = nil              --关卡ID
    self.activityId = nil              --活动id
    self.characteristic = nil              --关卡特点
    self.sceneId = nil              --背景ID
    self.rounds = nil              --回合限制(超出后直接失败)
    self.roleNun = nil              --最多上阵人数（1为下限 4为上限）
    self.proposalLevel = nil              --推荐战斗力
    self.powerRatioTeamID = nil              --战力碾压组别
    self.costItem = nil
    self.costNum = nil
    self.costNumLose = nil
    self.desc = nil
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function MidFestivalBossQuestVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.activityId = parse_number(row, "activityId")
    self.characteristic = ParseUtils.ParseLocalizeText(row, "characteristic")
    self.sceneId = parse_number(row, "sceneId")
    self.rounds = parse_number(row, "rounds")
    self.roleNun = parse_string(row, "roleNun")
    self.proposalLevel = parse_number(row, "proposalLevel")
    self.powerRatioTeamID = parse_number(row, "powerRatioTeamID")
    self.damageTotal = parse_number(row, "damageTotal", 0)  -- 控制战中界面是否显示全场总伤害UI
    self.costItem = parse_number(row, "costItem",0)
    self.costNum = parse_number(row, "costNum",0)
    self.costNumLose = parse_number(row, "costNumLose",0)
    self.desc = ParseUtils.ParseLocalizeText(row,"desc")
    self.status = parse_number(row, "status")
end

function MidFestivalBossQuestVo:__delete()
    self.id = nil
    self.activityId = nil
    self.characteristic = nil
    self.sceneId = nil
    self.rounds = nil
    self.roleNun = nil
    self.proposalLevel = nil
    self.powerRatioTeamID = nil
    self.costItem = nil
    self.costNum = nil
    self.costNumLose = nil
    self.desc = nil
    self.status = nil
end

return MidFestivalBossQuestVo
