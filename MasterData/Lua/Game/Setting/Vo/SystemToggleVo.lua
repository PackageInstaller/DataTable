---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2020-08-12 14:44:55
--- SystemToggleVo

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class SystemToggleVo
local SystemToggleVo = Class("SystemToggleVo", SettingRefBase)

---__int
function SystemToggleVo:__init()
    self.id = nil              --功能ID
    self.name = nil             -- 名称
    self.unlockType = nil              --开启条件（对应解锁条件表里的数值）
    self.unlockId = nil              --开启条件ID
    self.unlockNum = nil              --开启条件数值
    self.lockShowType = nil              --未解锁表现，0隐藏入口，1锁定入口按钮
    self.unlockShowType = nil              --解锁表现方式，0无表现，1解锁弹窗
    self.unlockIcon = nil              --解锁图标
    self.switch = nil              --功能开关（0关闭，1开启）
    self.status = nil              --状态
    self.realName = nil -- 包装名称

end

---FillVo
---@param row TableML.TableFileRow
function SystemToggleVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id", 0)
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.unlockType = parse_string(row, "unlockType", '')
    self.unlockId = parse_string(row, "unlockId", '')
    self.unlockNum = parse_string(row, "unlockNum", '')
    self.lockShowType = parse_number(row, "lockShowType", 0)
    self.unlockShowType = parse_number(row, "unlockShowType", 0)
    self.unlockIcon = ParseUtils.ParsePathName(row, "unlockIcon")
    self.switch = parse_number(row, "switch", 0)
    self.status = parse_number(row, "status", 0)
    self.realName = ParseUtils.ParseLocalizeText(row, "realRame")
    self.unlockDes = parse_localizeText(row, "unlockDes", true, "")
end

function SystemToggleVo:__delete()
    self.id = nil
    self.name = nil
    self.unlockType = nil
    self.unlockId = nil
    self.unlockNum = nil
    self.lockShowType = nil
    self.unlockShowType = nil
    self.unlockIcon = nil
    self.switch = nil
    self.status = nil
    self.realName = nil
end

return SystemToggleVo
