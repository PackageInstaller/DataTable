---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2022-10-08 14:41:04
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class AdvancedTrainingQuestVo
local AdvancedTrainingQuestVo = Class("AdvancedTrainingQuestVo", SettingRefBase)

---__int
function AdvancedTrainingQuestVo:__init()
    self.id = nil              --ID
    self.type = nil              --副本类型
    --self.diff = nil              --副本小类(关卡后3位)，后端继承数据需要
    self.label = nil              --标题
    self.name = nil              --副本名
    self.desc = nil              --关卡描述
    self.regionLevel = nil              --难度等级
    self.Exp = nil              --主角经验
    self.cardExp = nil              --卡牌经验
    self.unlockNum = nil              --解锁条件值（填充为0）前一关的关卡id
    self.proposalLevel = nil              --推荐战斗力
    self.conditionId = nil              --卡牌上阵条件（对应关卡条件表id）
    self.conditionValue = nil              --卡牌上阵条件参数
    self.powerMin = nil              --通关队伍角色最高战力要求
    self.powerTeamMin = nil              --编队最高战力要求
    --self.frequency = nil              --消耗的副本进入次数
    --self.costItem = nil              --副本消耗道具
    --self.costNum = nil              --消耗道具数量
    --self.costNumLose = nil              --战斗失败时消耗道具数量
    self.SceneId = nil              --场景ID
    self.monsterShow = nil              --怪物预览
    self.repeatButton = nil              --重复挑战
    --self.location = nil              --所属位置坐标
    self.rounds = nil              --回合限制(超出后直接失败)
    --self.battleRules = nil              --战斗规则字段
    self.isEnd = nil              --是否是当前类型的最后一关（1是0不是）
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function AdvancedTrainingQuestVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.type = parse_number(row, "type")
    --self.diff = parse_number(row, "diff")
    self.label = ParseUtils.ParseLocalizeText(row, "label")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
    self.regionLevel = parse_number(row, "regionLevel")
    self.Exp = parse_number(row, "Exp")
    self.cardExp = parse_number(row, "cardExp")
    self.unlockNum = parse_string(row, "unlockNum")
    self.proposalLevel = parse_number(row, "proposalLevel")
    self.conditionId = parse_number(row, "condition")
    self.conditionValue = parse_number(row, "num")
    self.powerMin = parse_number(row, "powerMin")
    self.powerTeamMin = parse_number(row, "powerTeamMin")
    --self.frequency = parse_number(row, "frequency")
    --self.costItem = parse_number(row, "costItem")
    --self.costNum = parse_number(row, "costNum")
    --self.costNumLose = parse_number(row, "costNumLose")
    self.SceneId = parse_number(row, "SceneId")
    self.monsterShow = parse_number(row, "monsterShow")
    self.repeatButton = parse_number(row, "repeatButton")
    --self.location = parse_string(row, "location")
    self.rounds = parse_number(row, "rounds")
    self.isEnd = parse_number(row, "isEnd")
    --self.battleRules = parse_string(row, "battleRules")
    self.status = parse_number(row, "status")
end

function AdvancedTrainingQuestVo:__delete()
    self.id = nil
    self.type = nil
    --self.diff = nil
    self.label = nil
    self.name = nil
    self.desc = nil
    self.regionLevel = nil
    self.Exp = nil
    self.cardExp = nil
    self.unlockNum = nil
    self.proposalLevel = nil
    self.conditionId = nil
    self.conditionValue = nil
    self.powerMin = nil
    self.powerTeamMin = nil
    --self.frequency = nil
    --self.costItem = nil
    --self.costNum = nil
    --self.costNumLose = nil
    self.SceneId = nil
    self.monsterShow = nil
    self.repeatButton = nil
    --self.location = nil
    self.rounds = nil
    --self.battleRules = nil
    self.isEnd = nil
    self.status = nil
end

return AdvancedTrainingQuestVo
