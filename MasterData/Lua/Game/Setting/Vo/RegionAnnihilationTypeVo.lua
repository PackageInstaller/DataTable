---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-02-18 14:26:07
--- 歼灭战类型表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class RegionAnnihilationTypeVo
local RegionAnnihilationTypeVo = Class("RegionAnnihilationTypeVo", SettingRefBase)

---__int
function RegionAnnihilationTypeVo:__init()
    self.id = nil              --编号
    self.name = nil              --歼灭战名称
    self.resource = nil              --歼灭战Boss模型资源
    self.describe = nil              --歼灭战描述
    self.unlockType = nil              --区域解锁条件类型，1为主线任务进度，2为其他
    self.unlockParm = nil              --解锁条件参数
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function RegionAnnihilationTypeVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.lable = ParseUtils.ParseLocalizeText(row, "lable")
    self.describe = ParseUtils.ParseLocalizeText(row, "describe")

    self.resource = ParseUtils.ParsePathName(row, "resource", ".prefab")
    self.contentLevel = parse_string(row, "contentLevel")
    self.bossPortrait = ParseUtils.ParsePathName(row, "bossPortrait")
    self.overviewBanner = ParseUtils.ParsePathName(row, "overviewBanner")
    self.bossPortrait2 = ParseUtils.ParsePathName(row, "bossPortrait2")
    self.bgPath = ParseUtils.ParsePathName(row, "bgPath")
    self.stance = parse_string(row, "stance")

    self.direction = parse_string(row, "direction")
    self.scale = parse_string(row, "scale")

    self.unlockType = parse_string(row, "unlockType")
    self.unlockId = parse_string(row, "unlockId")
    self.unlockNum = parse_string(row, "unlockNum")

    self.teamLocation1 = parse_string(row, "teamLocation1")
    self.teamLocation2 = parse_string(row, "teamLocation2")

    self.cameraPosition = parse_string(row, "cameraPosition")
    self.cameraRotation = parse_string(row, "cameraRotation")

    self.status = parse_number(row, "status")
end

function RegionAnnihilationTypeVo:__delete()
    self.id = nil
    self.name = nil
    self.resource = nil
    self.describe = nil
    self.unlockType = nil
    self.unlockParm = nil
    self.status = nil
end

return RegionAnnihilationTypeVo
