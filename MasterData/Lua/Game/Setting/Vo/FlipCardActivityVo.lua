---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2022-11-16 10:49:08
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class FlipCardActivityVo
local FlipCardActivityVo = Class("FlipCardActivityVo", SettingRefBase)

---__int
function FlipCardActivityVo:__init()
    self.id = nil              --活动ID
    self.name = nil              --活动名
    self.changeId = nil              --活动页签跳转
    self.sceneId = nil              --背景
    self.card = nil              --牌背
    self.rewardImg = nil              --看板娘ID
    self.rewardUI = nil              --看板娘跳转界面
    self.unlockType = nil              --开启条件（对应解锁条件表里的数值）
    self.unlockId = nil              --开启条件ID
    self.unlockNum = nil              --开启条件数值
    self.consume = nil              --消耗货币
    self.cost = nil              --单次消耗
    self.itemId = nil              --显示货币ID
    self.ruleID = nil              --功能规则ID
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function FlipCardActivityVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.changeId = parse_number(row, "changeId")
    self.sceneId = parse_string(row, "sceneId")
    self.card = parse_string(row, "card")
    self.rewardImg = parse_number(row, "rewardImg")
    self.rewardUI = parse_number(row, "rewardUI")
    self.unlockType = parse_string(row, "unlockType")
    self.unlockId = parse_number(row, "unlockId")
    self.unlockNum = parse_string(row, "unlockNum")
    self.consume = parse_number(row, "consume")
    self.cost = parse_number(row, "cost")
    self.itemId = parse_number(row, "itemId")
    self.ruleID = parse_number(row, "ruleID")
    self.status = parse_number(row, "status")
end

function FlipCardActivityVo:__delete()
    self.id = nil
    self.name = nil
    self.changeId = nil
    self.sceneId = nil
    self.card = nil
    self.rewardImg = nil
    self.rewardUI = nil
    self.unlockType = nil
    self.unlockId = nil
    self.unlockNum = nil
    self.consume = nil
    self.cost = nil
    self.itemId = nil
    self.ruleID = nil
    self.status = nil
end

return FlipCardActivityVo
