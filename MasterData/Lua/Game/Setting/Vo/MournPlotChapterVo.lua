---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2024-11-08 15:45:40
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class MournPlotChapterVo
local MournPlotChapterVo = Class("MournPlotChapterVo", SettingRefBase)

---__int
function MournPlotChapterVo:__init()
    self.id = nil              --ID
    self.activetyId = nil              --活动ID
    self.name = nil              --副本名
    self.unlockChapter = nil              --前置章节（填充为0）
    self.tips = nil              --解锁提示
    self.picture1 = nil              --剧情入口图
    self.picture2 = nil              --剧情内图片
    self.icon = nil
    self.date = nil              --开放时间（按照活动开启当天计算，单位为秒，0为默认解锁）
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function MournPlotChapterVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.activetyId = parse_number(row, "activetyId")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.unlockChapter = parse_string(row, "unlockChapter")
    self.tips = ParseUtils.ParseLocalizeText(row, "tips")
    self.picture1 = parse_pathName(row, "picture1")
    self.picture2 = parse_pathName(row, "picture2")
    self.icon = parse_pathName(row,"icon")
    self.date = parse_number(row, "date")
    self.status = parse_number(row, "status")
end

function MournPlotChapterVo:__delete()
    self.id = nil
    self.activetyId = nil
    self.name = nil
    self.unlockChapter = nil
    self.tips = nil
    self.picture1 = nil
    self.picture2 = nil
    self.icon = nil
    self.date = nil
    self.status = nil
end

return MournPlotChapterVo
