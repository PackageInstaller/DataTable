---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2021-07-23 11:26:26
--- 看板娘角色表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class RoleShowVo
local RoleShowVo = Class("RoleShowVo", SettingRefBase)

---__int
function RoleShowVo:__init()
    self.roleId = nil              --角色ID101（角色编号）
    self.name = nil              --名称（角色名ID）
    self.catName = nil              --猫名
    self.unlockNum = nil              --开启条件数值（主线关卡ID）
    self.permanent = nil              --常驻标志（0不常驻，1常驻）
    self.position = nil              --位置
    self.dress = nil              --默认服装ID
    self.modelPath = nil              --默认服装ID
    self.layoutBtn = nil              --交互功能入口（1好感2换装3剧情4料理5点单6邀请）
    self.enterAin = nil              --入场动作
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function RoleShowVo:FillVo(row)
    base.FillVo(self,row)
    self.roleId = parse_number(row, "roleId")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.catName = ParseUtils.ParseLocalizeText(row, "catName")
    self.unlockNum = parse_number(row, "unlockNum")
    self.unlockType = parse_number(row, "unlockType")

    self.permanent = parse_number(row, "permanent")
    self.position = parse_string(row, "position")
    self.dress = parse_number(row, "dress")
    self.modelPath = ParseUtils.ParsePathName(row, "modelPath")
    self.layoutBtn = parse_string(row, "layoutBtn")
    self.enterAin = parse_string(row, "enterAin", "")
    self.roleCamera = parse_string(row, "roleCamera", "")
    self.status = parse_number(row, "status")
end

function RoleShowVo:__delete()
    self.roleId = nil
    self.name = nil
    self.catName = nil
    self.unlockNum = nil
    self.permanent = nil
    self.position = nil
    self.dress = nil
    self.modelPath = nil  
    self.layoutBtn = nil                       
    self.status = nil
    self.enterAin = nil              --入场动作
end

return RoleShowVo
