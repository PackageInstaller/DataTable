---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2024-04-12 11:17:15
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class EntryWelfareArgVo
local EntryWelfareArgVo = Class("EntryWelfareArgVo", SettingRefBase)

---__int
function EntryWelfareArgVo:__init()
    self.id = nil              --状态ID
    self.name = nil              --参数名称
    self.num = nil              --数量
    self.rewardData = nil
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function EntryWelfareArgVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.num = parse_string(row, "num")
    local rewardId = parse_string(row, "rewardId")
    if string.len(rewardId) > 0  then
        local rewardNum = parse_string(row, "rewardNum")
        self.rewardData = parse_rewards2(rewardId,rewardNum , ";" )    
    else 
        self.rewardData = {}
    end 
    self.status = parse_number(row, "status")
end

function EntryWelfareArgVo:__delete()
    self.id = nil
    self.name = nil
    self.num = nil
    self.rewardData = nil
    self.status = nil
end

return EntryWelfareArgVo
