---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by zou hanjie
--- DateTime: 2024-01-19 16:26:40
--- 外观推广员活动排期表


local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class ActivityPromoterSchedulesVO
local ActivityPromoterSchedulesVO = Class("ActivityPromoterSchedulesVO", SettingRefBase)

---__int
function ActivityPromoterSchedulesVO:__init()
    self.id = nil              --编号
    self.changeId = nil             --换皮资源
    self.sceneId = nil             --背景图
    self.appearanceId = nil             --对应外观id
    self.costItem = nil             --对应活动道具id
    self.costNum = nil             --兑换外观所需道具数量
    self.rewardId = nil             --每日1次分享奖励id
    self.rewardNum = nil             --每日1次分享奖励数量
    self.times = nil             --每日可被助力的次数上限
    self.itemNum = nil           -- 每次被助力成功可获得的道具数量
    self.ruleID = nil               -- 规则说明
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function ActivityPromoterSchedulesVO:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")

    self.sceneId = ParseUtils.ParsePathName(row, "sceneId")
    self.appearanceId = parse_number(row, "appearanceId")
    self.costItem = parse_number(row, "costItem")
    self.costNum = parse_number(row, "costNum")
    self.rewardId = parse_number(row, "rewardId")
    self.rewardNum = parse_number(row, "rewardNum")
    self.times = parse_number(row, "times")
    self.itemNum = parse_number(row, "itemNum")
    self.ruleID = parse_number(row, "ruleID")
    self.status = parse_number(row, "status")
end

function ActivityPromoterSchedulesVO:__delete()
    self.id = nil              --编号
    self.changeId = nil             --换皮资源
    self.sceneId = nil             --背景图
    self.appearanceId = nil             --对应外观id
    self.costItem = nil             --对应活动道具id
    self.costNum = nil             --兑换外观所需道具数量
    self.rewardId = nil             --每日1次分享奖励id
    self.rewardNum = nil             --每日1次分享奖励数量
    self.times = nil             --每日可被助力的次数上限
    self.itemNum = nil           -- 每次被助力成功可获得的道具数量
    self.ruleID = nil               -- 规则说明
    self.status = nil              --状态
end

return ActivityPromoterSchedulesVO
