---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2020-08-07 10:35:20
--- 迷宫道具表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class MazeGoodVo
local MazeGoodVo = Class("MazeGoodVo", SettingRefBase)

---__int
function MazeGoodVo:__init()
    self.id = nil              --ID
    self.name = nil              --名称
    self.desc = nil              --描述
    self.effectType = nil              --效果类型 (1.恢复 2.复活 3.消除)
    self.effectNum = nil              --类型参数
    self.targetType = nil              --目标类型(1.指定 2.全体 3.随机)
    self.targetNum = nil              --目标参数（目标数量1一个，2个）
    self.quality = nil              --道具品质,1,白;2,绿;3,蓝;4,紫;5,橙;6,彩色
    self.stackMax = nil              --背包堆叠:最大堆叠数量
    self.photoPath = nil              --图片id
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function MazeGoodVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id", 0)
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.desc = ParseUtils.ParseLocalizeText(row, "description")--修改为和通用道具表统一
    self.effectType = parse_number(row, "effectType", 0)
    self.effectNum = parse_string(row, "effectNum", '')
    self.targetType = parse_number(row, "targetType", 0)
    self.targetNum = parse_string(row, "targetNum", '')
    self.quality = parse_number(row, "quality", 0)
    self.stackMax = parse_number(row, "stack", 0)
    self.photoPath = ParseUtils.ParsePathName(row, "photoId")
    self.status = parse_number(row, "status", 0)
end

function MazeGoodVo:__delete()
    self.id = nil
    self.name = nil
    self.desc = nil
    self.effectType = nil
    self.effectNum = nil
    self.targetType = nil
    self.targetNum = nil
    self.quality = nil
    self.stackMax = nil
    self.photoPath = nil
    self.status = nil
end

return MazeGoodVo
