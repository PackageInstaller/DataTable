---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2021-08-17 10:42:04
--- 天赋材料副本关卡表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class TalentQuestVo
local TalentQuestVo = Class("TalentQuestVo", SettingRefBase)

---__int
function TalentQuestVo:__init()
    self.id = nil              --ID
    self.group = nil              --关卡分组（对应天赋材料副本表）
    self.pointType = nil              --各关卡类型（2BOSS、3精英、1小怪）
    self.label = nil              --标题
    self.name = nil              --副本名
    self.pos = nil              --难度等级
    self.Exp = nil              --主角经验
    self.cardExp = nil              --卡牌经验
    self.unlockNum = nil              --解锁条件值（填充为0）
    self.proposalLevel = nil              --推荐战斗力
    self.frequency = nil              --消耗的副本进入次数
    self.costItem = nil              --副本消耗道具
    self.costNum = nil              --消耗道具数量
    self.costNumLose = nil              --战斗失败时消耗道具数量
    self.SceneId = nil              --场景ID
    self.rounds = nil              --回合限制(超出后直接失败)
    self.status = nil              --状态
    self.desc = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function TalentQuestVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.group = parse_number(row, "group")
    self.pointType = parse_string(row, "pointType")
    self.label = ParseUtils.ParseLocalizeText(row, "label")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.pos = parse_number(row, "pos")
    self.Exp = parse_number(row, "Exp")
    self.cardExp = parse_number(row, "cardExp")
    self.unlockNum = parse_number(row, "unlockNum")
    self.proposalLevel = parse_number(row, "proposalLevel")
    self.frequency = parse_number(row, "frequency")
    self.costItem = parse_number(row, "costItem")
    self.costNum = parse_number(row, "costNum")
    self.costNumLose = parse_number(row, "costNumLose")
    self.SceneId = parse_number(row, "SceneId")
    self.rounds = parse_number(row, "rounds")
    self.showMonster = parse_number(row, "showMonster")
    self.status = parse_number(row, "status")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
end

function TalentQuestVo:__delete()
    self.id = nil              --ID
    self.group = nil              --关卡分组（对应天赋材料副本表）
    self.pointType = nil              --各关卡类型（2BOSS、3精英、1小怪）
    self.label = nil              --标题
    self.name = nil              --副本名
    self.pos = nil              --难度等级
    self.Exp = nil              --主角经验
    self.cardExp = nil              --卡牌经验
    self.unlockNum = nil              --解锁条件值（填充为0）
    self.proposalLevel = nil              --推荐战斗力
    self.frequency = nil              --消耗的副本进入次数
    self.costItem = nil              --副本消耗道具
    self.costNum = nil              --消耗道具数量
    self.costNumLose = nil              --战斗失败时消耗道具数量
    self.SceneId = nil              --场景ID
    self.rounds = nil              --回合限制(超出后直接失败)
    self.status = nil              --状态
    self.desc = nil              --状态
end

return TalentQuestVo
