---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yunchuan.
--- DateTime: 2024/3/5 14:09
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class ClubCatCarSpeedGradeVo
local ClubCatCarSpeedGradeVo = Class("ClubCatCarSpeedGradeVo", SettingRefBase)
--id,grade,mileage,Jackpot,questId,showProgressRewards,progressRewards,progressNumber,leftBackground,rightBackground,Background,connectBackground,connectGround,status
--段位ID,对应段位（1，D；2，C；3，B；4，A；5，S）,达到该段位的点数,对应奖池ID,对应关卡id,展示段位奖励道具,段位进度奖励,段位进度奖励数量,背景左半段,背景右半段,地面,衔接背景,衔接地面,状态
--int,int,int,int,int,int,string,string,string,string,string,string,string,int

function ClubCatCarSpeedGradeVo:__init()
    self.id = nil                       --段位ID
    self.grade = nil                    --对应段位（1，D；2，C；3，B；4，A；5，S）
    self.mileage = nil                  --达到该段位的点数
    self.Jackpot = nil                  --对应奖池ID
    self.questId = nil                  --对应关卡id
    self.showProgressRewards = nil      --展示段位奖励道具
    self.progressRewards =  nil         --段位进度奖励
    self.progressNumber = nil           --段位进度奖励数量
     self.leftBackground = nil          --背景左半段
    self.rightBackground = nil          --背景右半段
    self.Background = nil               --地面
    self.connectBackground = nil        --衔接背景
    self.connectGround = nil            --衔接地面
end

function ClubCatCarSpeedGradeVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.grade = parse_number(row, "grade")
    self.mileage = parse_number(row, "mileage")
    self.Jackpot = parse_number(row, "Jackpot")
    self.questId = parse_number(row, "questId")
    self.showProgressRewards = parse_number(row, "showProgressRewards")
    self.progressRewards = parse_numbers(row, "progressRewards")
    self.progressNumber = parse_numbers(row, "progressNumber")
    self.leftBackground = ParseUtils.ParsePathName(row, "leftBackground")
    self.rightBackground = ParseUtils.ParsePathName(row, "rightBackground")
    self.Background = ParseUtils.ParsePathName(row, "Background")
    self.connectBackground = ParseUtils.ParsePathName(row, "connectBackground")
    self.connectGround = ParseUtils.ParsePathName(row, "connectGround")
end

function ClubCatCarSpeedGradeVo:__delete()
    self.id = nil
    self.grade = nil
    self.mileage = nil
    self.Jackpot = nil
    self.questId = nil
    self.showProgressRewards = nil
    self.progressRewards = nil
    self.progressNumber = nil
    self.leftBackground = nil
    self.rightBackground = nil
    self.Background = nil
    self.connectBackground = nil
    self.connectGround = nil
end

return ClubCatCarSpeedGradeVo