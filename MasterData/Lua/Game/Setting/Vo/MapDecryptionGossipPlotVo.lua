---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2022-12-02 10:55:28
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils
local parse_number = parse_number
local parse_string = parse_string

---@class MapDecryptionGossipPlotVo
local MapDecryptionGossipPlotVo = Class("MapDecryptionGossipPlotVo", SettingRefBase)

---__int
function MapDecryptionGossipPlotVo:__init()
    self.id = nil              --编号
    self.name = nil              --角色名 
    self.storyId = nil              --对应剧情编号
    self.groupId = nil              --组别id
    self.people = nil              --说话对象（台词框）：1角色，2玩家
    self.desc = nil              --剧情文案
    self.jumpId = nil              --通用跳转id
    -- self.choiceOne = nil              --玩家选项1
    -- self.choiceOneId = nil              --选项1跳转ID
    -- self.choiceTwo = nil              --玩家选项2
    -- self.choiceTwoId = nil              --选项2跳转id
    -- self.choiceThree = nil              --玩家选项3
    -- self.choiceThreeId = nil              --选项3跳转id
    self.choices = nil
    self.faceId = nil              --表情id
    self.action = nil              --动作id
    self.time = nil              --循环动作持续时间
    self.music = nil              --音乐id
    self.sound = nil              --音效id
    self.voice = nil              --语音ID：男
    self.voiceLady = nil              --语音ID：女
    self.endFlag = nil              --结尾标注：1
    
end

---FillVo
---@param row TableML.TableFileRow
function MapDecryptionGossipPlotVo:FillVo(row)
    base.FillVo(self,row)
    self.id      = parse_number(row, "id")
    self.name    = ParseUtils.ParseLocalizeText(row, "name")
    self.storyId = parse_number(row, "storyId")
    self.groupId = parse_number(row, "groupId")
    self.people = parse_number(row, "people")
    self.desc = parse_localizeText(row, "desc", true)

    self.jumpId = parse_number(row, "jumpId")
    local choiceOne = ParseUtils.ParseLocalizeText(row, "choiceOne")
    local choiceOneId = parse_number(row, "choiceOneId")
    local choiceTwo = ParseUtils.ParseLocalizeText(row, "choiceTwo")
    local choiceTwoId = parse_number(row, "choiceTwoId")
    local choiceThree = ParseUtils.ParseLocalizeText(row, "choiceThree")
    local choiceThreeId = parse_number(row, "choiceThreeId")

    local choices = {}
    local t = {choiceOne, choiceTwo, choiceThree}
    local t1 = {choiceOneId, choiceTwoId, choiceThreeId}
    for i = 1, 3, 1 do
        local choiceName, choiceId = t[i], t1[i]
        if not string.isEmpty(choiceName) and checkNumber(choiceId) > 0 then
            table.insert(choices, {choiceName, choiceId})
        end    
    end
    
    self.choices = choices

    self.faceId = parse_number(row, "faceId")
    self.action = parse_string(row, "action")
    self.time = parse_string(row, "time")
    self.music = parse_string(row, "music")
    self.sound = parse_string(row, "sound")
    self.voice = parse_string(row, "voice")
    self.voiceLady = parse_string(row, "voiceLady")
    self.endFlag = parse_number(row, "end")
    self.status = parse_number(row, "status")
end

function MapDecryptionGossipPlotVo:__delete()
    self.id = nil
    self.name = nil
    self.storyId = nil
    self.groupId = nil
    self.people = nil
    self.desc = nil
    self.jumpId = nil
    -- self.choiceOne = nil
    -- self.choiceOneId = nil
    -- self.choiceTwo = nil
    -- self.choiceTwoId = nil
    -- self.choiceThree = nil
    -- self.choiceThreeId = nil
    self.choices = nil
    self.faceId = nil
    self.action = nil
    self.time = nil
    self.music = nil
    self.sound = nil
    self.voice = nil
    self.voiceLady = nil
    self.endFlag = nil
end

return MapDecryptionGossipPlotVo
