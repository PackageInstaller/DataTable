---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2022-09-15 10:34:58
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class CardExperienceVo
local CardExperienceVo = Class("CardExperienceVo", SettingRefBase)

---__int
function CardExperienceVo:__init()
    self.id = nil              --ID
    self.type = nil              --副本类型（对应角色活动合集表内的ID）
    self.label = nil              --标题
    self.name = nil              --副本名
    self.desc = nil              --关卡描述
    self.unlockNum = nil              --解锁条件值（填充为0）
    self.SceneId = nil              --场景ID
    self.rounds = nil              --回合限制(超出后直接失败)
    self.fristRewards = nil              --首次通关掉落
    self.fristRewardsNum = nil              --掉落数量
    self.showItem = nil              --编队UI展示奖励道具
    self.showItemTag = nil              --编队UI展示奖励道具标签，1必定掉落、2概率掉落、3首次通关掉落、4三星奖励
    self.showItemNum = nil              --编队UI展示奖励道具数量
    self.petLineup = nil              --机器人阵容ID（填0为不需要机器人阵容）
    self.roleNun = nil              --最多上阵人数（1为下限 4为上限）
    self.extraSkill = nil              --剧情用合卡技
    self.battleRules = nil              --战斗规则表ID
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function CardExperienceVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.type = parse_number(row, "type")
    self.label = ParseUtils.ParseLocalizeText(row, "label")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
    self.unlockNum = parse_number(row, "unlockNum")
    self.SceneId = parse_number(row, "SceneId")
    self.rounds = parse_number(row, "rounds")
    self.fristRewards = parse_string(row, "fristRewards")
    self.fristRewardsNum = parse_string(row, "fristRewardsNum")
    self.showItem = parse_string(row, "showItem")
    self.showItemTag = parse_string(row, "showItemTag")
    self.showItemNum = parse_string(row, "showItemNum")
    self.petLineup = parse_number(row, "petLineup")
    self.roleNun = parse_number(row, "roleNun")
    self.extraSkill = parse_number(row, "extraSkill")
    self.battleRules = parse_number(row, "battleRules")
    self.status = parse_number(row, "status")
end

function CardExperienceVo:__delete()
    self.id = nil
    self.type = nil
    self.label = nil
    self.name = nil
    self.desc = nil
    self.unlockNum = nil
    self.SceneId = nil
    self.rounds = nil
    self.fristRewards = nil
    self.fristRewardsNum = nil
    self.showItem = nil
    self.showItemTag = nil
    self.showItemNum = nil
    self.petLineup = nil
    self.roleNun = nil
    self.extraSkill = nil
    self.battleRules = nil
    self.status = nil
end

return CardExperienceVo
