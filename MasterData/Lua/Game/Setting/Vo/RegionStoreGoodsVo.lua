---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-02-15 10:19:46
--- 区域商品表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class RegionStoreGoodsVo
local RegionStoreGoodsVo = Class("RegionStoreGoodsVo", SettingRefBase)

---__int
function RegionStoreGoodsVo:__init()
    self.id = nil              --编号
    self.name = nil              --商品姓名
    self.resource = nil              --商品icon资源
    self.type = nil              --商品类型，对应商品类型表
    self.sourceID = nil              --商品道具ID
    self.sellerID = nil              --贩售商人ID
    self.quality = nil              --商品品质
    self.currency = nil              --货币类型
    self.price = nil              --单价
    self.inventory = nil              --最大库存数目
    self.unlock = nil              --解锁好感度等级
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function RegionStoreGoodsVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.resource = parse_number(row, "iconId")
    self.type = parse_number(row, "type")
    self.sourceID = parse_number(row, "sourceID", 0)
    self.sellerID = parse_number(row, "sellerID")
    self.quality = parse_number(row, "quality", 0)
    self.currency = parse_number(row, "currency", 0)
    self.price = parse_number(row, "price", 0)
    self.inventory = parse_number(row, "inventory", 0)
    self.unlockType = parse_number(row, "unlockType", 0)
    self.unlock = parse_number(row, "unlockType", 0)
    self.unlockId = parse_number(row, "unlockId", 0)
    self.unlockNum = parse_number(row, "unlockNum", 0)
    self.status = parse_number(row, "status")
end

function RegionStoreGoodsVo:__delete()
    self.id = nil
    self.name = nil
    self.resource = nil
    self.type = nil
    self.sourceID = nil
    self.sellerID = nil
    self.quality = nil
    self.currency = nil
    self.price = nil
    self.inventory = nil
    self.unlock = nil
    self.status = nil
end

return RegionStoreGoodsVo
