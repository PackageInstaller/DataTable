---
--- Generated by vscode.
--- Created by pikaqiu.
--- DateTime: 2024-03-28
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class CheckInGroupVo
local CheckInGroupVo = Class("CheckInGroupVo", SettingRefBase)

function CheckInGroupVo:__init()
    self:CleanFields()
end

function CheckInGroupVo:__delete()
    self:CleanFields()
end

function CheckInGroupVo:CleanFields()
end

---FillVo
---@param row TableML.TableFileRow
function CheckInGroupVo:FillVo(row)
    base.FillVo(self,row)
    self.groupId = parse_number(row, 'groupId', 0)
    self.name = parse_localizeText(row, 'name')
    self.member1 = parse_number(row, 'member1', 0)
    self.member2 = parse_number(row, 'member2', 0)
    self.member3 = parse_number(row, 'member3', 0)
    self.member4 = parse_number(row, 'member4', 0)
    self.member5 = parse_number(row, 'member5', 0)
    self.pic = parse_pathName(row, 'pic')

    self.memberMax = 5
    self.members = {}
    for i = 1, self.memberMax, 1 do
        local memberId = checkNumber(self['member'..i])
        if memberId > 0 then
            table.insert(self.members, memberId)
        end
    end
end

return CheckInGroupVo