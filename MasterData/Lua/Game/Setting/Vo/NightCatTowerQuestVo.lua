---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2022-08-25 15:54:33
--- 暗夜猫爬塔关卡表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class NightCatTowerQuestVo
local NightCatTowerQuestVo = Class("NightCatTowerQuestVo", SettingRefBase)

---__int
function NightCatTowerQuestVo:__init()
    self.id = nil              --关卡ID
    self.activeId = nil              --活动ID
    self.storey = nil              --层数
    self.label = nil              --标题
    self.name = nil              --副本名
    self.monsteType = nil              --怪物类型（1普通 2BOSS 3精英  0无战斗填充）
    self.nextLevel = nil              --下一层关卡ID
    self.unlockNum = nil              --解锁条件值（填充为0，固定为前置关卡解锁）
    self.exp = nil              --主角经验
    self.cardExp = nil              --卡牌经验
    self.costItem = nil              --副本消耗道具
    self.costNum = nil              --消耗道具数量
    self.costNumLose = nil              --战斗失败时消耗道具数量
    self.proposalLevel = nil              --推荐战斗力
    self.SceneId = nil              --场景ID
    self.rounds = nil              --回合限制(超出后直接失败)
    self.roleNun = nil              --最多上阵人数（1为下限 4为上限）
    self.battleRules = nil              --战斗规则表ID
    self.powerMin = nil              --单角色需求战力
    self.powerTeamMin = nil              --编队需求战力
    self.showMonster = nil              --展示怪物
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function NightCatTowerQuestVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.activeId = parse_number(row, "activeId")
    self.storey = parse_number(row, "storey")
    self.label = ParseUtils.ParseLocalizeText(row, "label")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.monsteType = parse_number(row, "monsteType")
    self.nextLevel = parse_string(row, "nextLevel")
    self.unlockNum = parse_string(row, "unlockNum")
    self.exp = parse_number(row, "exp")
    self.cardExp = parse_number(row, "cardExp")
    self.costItem = parse_number(row, "costItem")
    self.costNum = parse_number(row, "costNum")
    self.costNumLose = parse_number(row, "costNumLose")
    self.proposalLevel = parse_number(row, "proposalLevel")
    self.SceneId = parse_number(row, "SceneId")
    self.rounds = parse_number(row, "rounds")
    self.roleNun = parse_number(row, "roleNun")
    self.battleRules = parse_number(row, "battleRules")
    self.powerMin = parse_number(row, "powerMin")
    self.powerTeamMin = parse_number(row, "powerTeamMin")
    self.showMonster = parse_number(row, "showMonster")
    self.status = parse_number(row, "status")
end

function NightCatTowerQuestVo:__delete()
    self.id = nil
    self.activeId = nil
    self.storey = nil
    self.label = nil
    self.name = nil
    self.monsteType = nil
    self.nextLevel = nil
    self.unlockNum = nil
    self.exp = nil
    self.cardExp = nil
    self.costItem = nil
    self.costNum = nil
    self.costNumLose = nil
    self.proposalLevel = nil
    self.SceneId = nil
    self.rounds = nil
    self.roleNun = nil
    self.battleRules = nil
    self.powerMin = nil
    self.powerTeamMin = nil
    self.showMonster = nil
    self.status = nil
end

return NightCatTowerQuestVo
