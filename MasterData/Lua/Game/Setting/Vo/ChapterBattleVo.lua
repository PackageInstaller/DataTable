---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2024-06-20 16:03:40
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class ChapterBattleVo
local ChapterBattleVo = Class("ChapterBattleVo", SettingRefBase)

---__int
function ChapterBattleVo:__init()
    self.bulletId = nil              --弹幕ID
    self.battleGroupId = nil              --弹幕组ID
    self.bulletType = nil              --弹幕类型
    self.bulletText = nil              --弹幕文本
    self.IntervalTime = nil              --间隔时间(s)
    self.startPosition = nil              --字幕位置（x;y)
    self.endPosition = nil              --字幕位置（x;y)
    self.isRandom = nil -- 是否需要随机位置
    self.runningTime = nil              --持续时间(s）
    self.status = nil              --状态开关
end

---FillVo
---@param row TableML.TableFileRow
function ChapterBattleVo:FillVo(row)
    base.FillVo(self,row)
    self.bulletId = parse_number(row, "bulletId")
    self.battleGroupId = parse_string(row, "battleGroupId")
    self.bulletType = parse_number(row, "bulletType")
    self.bulletText = ParseUtils.ParseLocalizeText(row, "bulletText")
    self.IntervalTime = parse_number(row, "IntervalTime")
    self.startPosition = string.split(parse_string(row, "startPosition"),";","")
    self.endPosition = string.split(parse_string(row, "endPosition"),";","")
    self.isRandom = parse_number(row, "isRandom") == 1
    self.runningTime = parse_number(row, "runningTime")
    self.status = parse_number(row, "status")

end

function ChapterBattleVo:__delete()
    self.bulletId = nil
    self.battleGroupId = nil
    self.bulletType = nil
    self.bulletText = nil
    self.IntervalTime = nil
    self.startPosition = nil              --字幕位置（x;y)
    self.endPosition = nil              --字幕位置（x;y)
    self.isRandom = nil -- 是否需要随机位置
    self.runningTime = nil
    self.status = nil
end

return ChapterBattleVo
