---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yajieli.
--- DateTime: 2019-08-15 10:41
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local Vector3 = import("Frame.UnityEngine.Vector3")
---@class ModelVo
local ModelVo = Class("ModelVo", SettingRefBase)
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---__int
function ModelVo:__init()
    self:CleanProperties()
end

function ModelVo:__delete()
    self:CleanProperties()
end

function ModelVo:CleanProperties()
    self.id = nil
    self.bossCutInFrame = nil
    self.modelRes = nil
    self.hitPos = nil
    self.bulletHitPos = nil
    self.cameraOffset = nil
    self.cameraRotation = nil
    self.detailPosition = nil
    self.detailDirection = nil
    self.animatorController = nil
    self.color=nil
    self.canFloat = nil
    self.timeLineScale = nil
    self.whetherLookAt = nil
    self.bonesLook = nil
    self.size = nil
    self.floatWord = nil
    self.width = nil
    self.depth = nil
    self.bossTime = nil
    self.camPosition = nil
    self.camRotation = nil
    self.battleScale = nil
    self.effectScale = nil
end

---FillVo
---@param row TableML.TableFileRow
function ModelVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row,"id", 0)
    self.bossCutInFrame = parse_number(row,"bossDebut", 0)
    self.modelRes = parse_string(row, "modeString", '')
    --@RefType [Frame.UnityEngine.Vector3#Vector3]
    self.hitPos = Vector3.New(SafeUnpack(Tools.splitToNum(parse_string(row,"hitPos", ''), ';')))
    --@RefType [Frame.UnityEngine.Vector3#Vector3]
    self.bulletHitPos = Vector3.New(SafeUnpack(Tools.splitToNum(parse_string(row,"bulletHit", ''), ';')))
    --@RefType [Frame.UnityEngine.Vector3#Vector3]
    self.cameraOffset = Vector3.New(SafeUnpack(Tools.splitToNum(parse_string(row,"skillCamera", ''), ';')))
    --@RefType [Frame.UnityEngine.Vector3#Vector3]
    self.cameraRotation = Vector3.New(SafeUnpack(Tools.splitToNum(parse_string(row,"skillTowards", ''), ';')))
    --@RefType [Frame.UnityEngine.Vector3#Vector3]
    self.detailPosition = Vector3.New(SafeUnpack(Tools.splitToNum(parse_string(row,"position", ''), ';')))
    --@RefType [Frame.UnityEngine.Vector3#Vector3]
    self.detailDirection = Vector3.New(SafeUnpack(Tools.splitToNum(parse_string(row,"direction", ''), ';')))
    self.animatorController = parse_string(row, "animatorControl", '')
    self.color = parse_string(row, "colour")
    self.canFloat = parse_number(row,"float") ==  1
    self.timeLineScale = parse_number(row, "timeLineScale", 0)
    self.whetherLookAt = parse_number(row, "WhetherLook", 1)
    self.bonesLook = parse_string(row, "bonesLook", '')
    self.size = parse_number(row, "habitus", 1)
    self.transHabitus = parse_number(row, "transHabitus", 0)
    self.floatWord = Vector3.New(SafeUnpack(Tools.splitToNum(parse_string(row,"floatWord", ''), ';')))
    self.width = 0
    self.depth = 0
    self.bossTime = parse_string(row, "bossTime")

    local camPosition = parse_string(row,"camPosition", '')
    if string.isEmpty(camPosition) then
        self.camPosition = nil
    else
        self.camPosition = Vector3.New(SafeUnpack(Tools.splitToNum(camPosition, ';')))
    end
    self.camRotation = Vector3.New(SafeUnpack(Tools.splitToNum(parse_string(row,"camRotation", ''), ';')))

    self.battleScale = parse_number(row,"battleScale", 1)
    self.effectScale = parse_number(row,"effectScale", 1)
end

return ModelVo