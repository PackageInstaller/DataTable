---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2021-08-23 14:24:03
--- 钓场表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class FishingPlaceVo
local FishingPlaceVo = Class("FishingPlaceVo", SettingRefBase)

---__int
function FishingPlaceVo:__init()
    self.id = nil              --序号
    self.name = nil              --名称
    self.level = nil              --解锁需要等级
    self.rare = nil              --稀有度
    self.isSpecialPond = nil     --是否为活动钓场（0-不是 1-是
    self.probability = nil              --概率（走瀑布概率
    self.currency = nil              --挂机单次消耗货币
    self.num = nil              --挂机单次消耗数量
    self.sceneId = nil              --场景id
    self.rolePath = nil              --人物路径
    self.background = nil              --UI背景图
    self.desc = nil              --UI背景图
    self.startPoint = nil              --鱼的运动起点
    self.commonPoint = nil              --一般抛竿时水花位置 
    self.goodPoint = nil              --良好抛竿时水花位置 
    self.perfectPoint = nil              --完美抛竿时水花位置 
    self.moveToward = nil              --鱼得运动方向 
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function FishingPlaceVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.level = parse_number(row, "level", 0)
    self.isSpecialPond = parse_number(row, "isSpecialPond")
    self.rare = parse_number(row, "rare")
    self.currency = parse_number(row, "properties")
    self.num = parse_number(row, "num")
    self.probability = parse_string(row, "probability")
    self.sceneId = parse_number(row, "sceneId")
    self.rolePath = parse_string(row, "rolePath")
    self.background = ParseUtils.ParsePathName(row, "background")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
    self.cost = parse_string(row, "cost")
    self.num = parse_string(row, "num")
    self.costNum = parse_string(row, "costNum")
    self.properties = parse_number(row, "properties")
    self.unlockType = parse_string(row, "unlockType")
    self.unlockId = parse_string(row, "unlockId")
    self.unlockNum = parse_string(row, "unlockNum")

    local t = CfUtils.Split(parse_string(row, "startPoint"))
    self.startPoint = Vector3(t[1], t[2], t[3])

    t = CfUtils.Split(parse_string(row, "commonPoint"))
    self.commonPoint = Vector3(t[1], t[2], t[3])

    t = CfUtils.Split(parse_string(row, "goodPoint"))
    self.goodPoint = Vector3(t[1], t[2], t[3])

    t = CfUtils.Split(parse_string(row, "perfectPoint"))
    self.perfectPoint = Vector3(t[1], t[2], t[3])

    self.moveToward = CfUtils.Split(parse_string(row, "moveToward"))
    --self.moveToward = Vector3(t[1], t[2], t[3])

    self.status = parse_number(row, "status")
end

function FishingPlaceVo:__delete()
    self.id = nil
    self.name = nil
    self.level = nil
    self.rare = nil
    self.currency = nil             
    self.num = nil         
    self.isSpecialPond = nil        
    self.probability = nil
    self.sceneId = nil              --场景id
    self.rolePath = nil              --人物路径
    self.background = nil              --UI背景图
    self.desc = nil              --UI背景图
    self.startPoint = nil              --鱼的运动起点
    self.commonPoint = nil              --一般抛竿时水花位置 
    self.goodPoint = nil              --良好抛竿时水花位置 
    self.perfectPoint = nil              --完美抛竿时水花位置 
    self.moveToward = nil              --鱼得运动方向 
    self.status = nil
end

return FishingPlaceVo
