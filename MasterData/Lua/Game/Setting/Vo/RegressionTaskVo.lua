---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2024-08-29 11:32:32
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class RegressionTaskVo
local RegressionTaskVo = Class("RegressionTaskVo", SettingRefBase)

---__int
function RegressionTaskVo:__init()
    self.id = nil              --任务ID
    self.group = nil              --组别
    self.name = nil              --任务名称
    self.descr = nil              --任务描述
    self.openDay = nil              --任务开放时间（建号天数）
    self.Numtype = nil              --计数类型
    self.startType = nil              --开始计数类型（1-默认计数  2-完成前置任务后开始计数
    self.taskContentType = nil              --任务目标类型
    self.taskContentId = nil              --任务目标
    self.taskContenNum = nil              --任务值
    self.point = nil              --活跃度
    self.exp = nil              --主角经验
    self.rewards = nil          --任务奖励
    self.openType = nil              --跳转目标
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function RegressionTaskVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.group = parse_number(row, "group")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.descr = ParseUtils.ParseLocalizeText(row, "descr")
    self.openDay = parse_number(row, "openDay")
    self.Numtype = parse_number(row, "Numtype")
    self.startType = parse_number(row, "startType")
    self.taskContentType = parse_number(row, "taskContentType")
    self.taskContentId = parse_number(row, "taskContentId")
    self.taskContenNum = parse_number(row, "taskContenNum")
    self.point = parse_number(row, "point")
    self.exp = parse_number(row, "exp")
    local rewardId = parse_string(row, "rewardId")
    local rewardNum = parse_string(row, "rewardNum")
    self.rewards = parse_rewards(rewardId, rewardNum)
    self.openType = parse_number(row, "openType")
    self.status = parse_number(row, "status")
end

function RegressionTaskVo:__delete()
    self.id = nil
    self.group = nil
    self.name = nil
    self.descr = nil
    self.openDay = nil
    self.Numtype = nil
    self.startType = nil
    self.taskContentType = nil
    self.taskContentId = nil
    self.taskContenNum = nil
    self.point = nil
    self.exp = nil
    self.rewards = nil
    self.openType = nil
    self.status = nil
end

return RegressionTaskVo
