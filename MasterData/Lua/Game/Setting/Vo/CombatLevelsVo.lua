---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2020-11-06 14:45:24
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class CombatLevelsVo
local CombatLevelsVo = Class("CombatLevelsVo", SettingRefBase)

---__int
function CombatLevelsVo:__init()
    self.id = nil              --关卡ID
    self.taskIcon = nil              --关卡图标
    self.sortId = nil              --关卡类
    self.monsteType = nil              --怪物类型（1普通 2BOSS  0无战斗填充）
    self.taskContentType = nil              --关卡完成条件
    self.taskContentNum = nil              --关卡完成条件值
    self.conditionValue = nil              --关卡完成条件
    self.conditionId = nil              --关卡完成条件值
    self.rewardId = nil              --任务奖励
    self.rewardNum = nil              --任务数量
    self.name = nil              --任务标题
    self.name2 = nil              --任务名称
    self.descr = nil              --任务描述
    self.unlockMission = nil      --主线关卡解锁
    self.unlockNum = nil              --解锁条件值（填充为0，固定为前置关卡解锁）
    self.SceneId = nil              --场景ID
    self.rounds = nil              --回合限制(超出后直接失败)
    self.petLineup = nil              --机器人阵容ID（填0为不需要机器人阵容）
    self.roleNun = nil              --最多上阵人数（1为下限 4为上限）
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function CombatLevelsVo:FillVo(row)
    base.FillVo(self, row)
    self.id = parse_number(row, "id")
    self.taskIcon = ParseUtils.ParsePathName(row, "taskIcon")
    self.sortId = parse_number(row, "sortId")
    self.monsteType = parse_number(row, "monsteType")

    ------------ Battling ------------
    self.taskContentNum = parse_string(row, "taskContentNum")
    self.taskContentType = parse_string(row, "taskContentType")
    ------------ Battling ------------

    ------------ BattleEntry ------------
    self.conditionValue = parse_string(row, "taskContentNum")
    self.conditionId = parse_string(row, "taskContentType")
    ------------ BattleEntry ------------

    self.rewardId = parse_string(row, "rewardId")
    self.rewardNum = parse_string(row, "rewardNum")
    self.name = parse_string(row, "name")
    self.name2 = parse_string(row, "name2")
    self.descr = parse_string(row, "descr")
    self.unlockMission = parse_number(row, "unlockMission")
    self.unlockNum = parse_string(row, "unlockNum")
    self.SceneId = parse_number(row, "SceneId")
    self.rounds = parse_number(row, "rounds")
    self.petLineup = parse_number(row, "petLineup")
    self.roleNun = parse_string(row, "roleNun")
    self.status = parse_number(row, "status")
end

function CombatLevelsVo:__delete()
    self.id = nil
    self.taskIcon = nil
    self.sortId = nil
    self.monsteType = nil
    self.taskContentType = nil
    self.taskContentNum = nil
    self.conditionValue = nil
    self.conditionId = nil
    self.rewardId = nil
    self.rewardNum = nil
    self.name = nil
    self.name2 = nil
    self.descr = nil
    self.unlockMission = nil
    self.unlockNum = nil
    self.SceneId = nil
    self.rounds = nil
    self.petLineup = nil
    self.roleNun = nil
    self.status = nil
end

return CombatLevelsVo
