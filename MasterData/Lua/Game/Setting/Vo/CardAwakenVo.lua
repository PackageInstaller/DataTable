---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yajieli.
--- DateTime: 2019-08-15 10:41
---角色突破表

local parse_number   = parse_number
local SettingRefBase = require('Game.Setting.SettingRefBase')
---@class CardAwakenVo
local CardAwakenVo = Class("CardAwakenVo", SettingRefBase)
local base = SettingRefBase
-- local ParseUtils = CS.ParseUtils

---__int
function CardAwakenVo:__init()
    self.cardId = nil              --角色ID
    self.times = nil              --突破次数
    self.openLevel = nil              --突破开放大招等级
    -- self.itemCost = nil              --突破消耗道具id
    -- self.costNum = nil              --突破消耗道具数量
    self.openTalent = nil              --开放天赋
    self.isHiding = nil              --隐藏天赋的显示
    self.capabilityValue = nil      --- 战力值
    -- self.consume = nil          -- 突破开放大招消耗
end


---FillVo
---@param row TableML.TableFileRow
function CardAwakenVo:FillVo(row)
    base.FillVo(self,row)
    self.cardId          = parse_number(row, "roleId", 0)
    self.times           = parse_number(row, "times", 0)
    self.openLevel       = parse_number(row, "awakenLevel", 0)
    self.openTalent      = parse_number(row, "openAbility", 0)
    self.isHiding        = parse_number(row, "isHiding", 0)
    self.capabilityValue = parse_number(row, "capabilityValue", 0)
    -- self.consume    = base.ParseRewards(self, row, "itemCost", "costNum")
end

function CardAwakenVo:__delete()
    self.cardId = nil
    self.times = nil
    -- self.name = nil
    -- self.desc = nil
    self.openLevel = nil
    self.openTalent = nil
    self.attributes = nil
    self.isHiding = nil ---隐藏天赋的显示
    -- self.consume = nil
end

return CardAwakenVo