---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2020-09-24 15:30:56
--- 初级装备副本关卡表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class QuestPrimaryEquipVo
local QuestPrimaryEquipVo = Class("QuestPrimaryEquipVo", SettingRefBase)

---__int
function QuestPrimaryEquipVo:__init()
    self.id = nil              --ID
    self.type = nil              --副本类型（对应初级装备副本类型）
    self.label = nil              --标题
    self.name = nil              --副本名
    self.desc = nil              --关卡描述
    self.regionLevel = nil              --难度等级
    self.Exp = nil              --主角经验
    self.cardExp = nil              --卡牌经验
    self.unlockNum = nil              --解锁条件值（填充为0）
    self.proposalLevel = nil              --推荐战斗力
    self.frequency = nil              --消耗的副本进入次数
    self.costItem = nil              --副本消耗道具
    self.costNum = nil              --消耗道具数量
    self.costNumLose = nil              --战斗失败时消耗道具数量
    self.conditionId = nil              --3星条件
    self.conditionValue = nil              --3星条件数值
    self.SceneId = nil              --场景ID
    self.monsterShow = nil              --怪物预览
    self.location = nil              --所属位置坐标
    self.rounds = nil              --回合限制(超出后直接失败)
    self.appointment = nil              --委任战斗功能开放条件，0不开放，1通关，2三星通关
    self.teamLimit = nil              --队伍武装值要求
    self.time = nil              --委任战斗时间
    self.status = nil              --状态
    self.repeatButton = nil    --重复挑战显示
end

---FillVo
---@param row TableML.TableFileRow
function QuestPrimaryEquipVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id", 0)
    self.type = parse_number(row, "type", 0)
    self.label = ParseUtils.ParseLocalizeText(row, "label")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
    self.regionLevel = parse_number(row, "regionLevel", 0)
    self.Exp = parse_number(row, "Exp", 0)
    self.cardExp = parse_number(row, "cardExp", 0)
    self.unlockNum = parse_number(row, "unlockNum", 0)
    self.proposalLevel = parse_number(row, "proposalLevel", 0)
    self.frequency = parse_number(row, "frequency", 0)
    self.costItem = parse_number(row, "costItem", 0)
    self.costNum = parse_number(row, "costNum", 0)
    self.costNumLose = parse_number(row, "costNumLose", 0)
    self.conditionId = parse_string(row, "starCondition", '')
    self.conditionValue = parse_string(row, "starConditionNum", '')
    self.SceneId = parse_number(row, "SceneId", 0)
    self.monsterShow = parse_string(row, "monsterShow", '')
    self.location = parse_string(row, "location", '')
    self.rounds = parse_number(row, "rounds", 0)
    self.appointment = parse_number(row, "appointment", 0)
    self.teamLimit = parse_number(row, "teamLimit", 0)
    self.time = parse_number(row, "time", 0)
    self.status = parse_number(row, "status", 0)
    self.repeatButton = parse_number(row, "repeatButton")
end

function QuestPrimaryEquipVo:__delete()
    self.id = nil
    self.type = nil
    self.label = nil
    self.name = nil
    self.desc = nil
    self.regionLevel = nil
    self.Exp = nil
    self.cardExp = nil
    self.unlockNum = nil
    self.proposalLevel = nil
    self.frequency = nil
    self.costItem = nil
    self.costNum = nil
    self.costNumLose = nil
    self.conditionId = nil
    self.conditionValue = nil
    self.SceneId = nil
    self.monsterShow = nil
    self.location = nil
    self.rounds = nil
    self.appointment = nil
    self.teamLimit = nil
    self.time = nil
    self.status = nil
    self.repeatButton = nil
end

return QuestPrimaryEquipVo
