---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-02-20 20:25:38
--- 区域好感度升级表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class RegionLikabilityVo
local RegionLikabilityVo = Class("RegionLikabilityVo", SettingRefBase)

---__int
function RegionLikabilityVo:__init()
    self.id = nil              --编号
    self.lable = nil              --标题
    self.name = nil              --名称
    self.regionId = nil              --区域
    self.level = nil              --好感度等级
    self.levelUpExp = nil              --升级至下一等级所需经验，0表示当前等级为max等级
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function RegionLikabilityVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.lable = ParseUtils.ParseLocalizeText(row, "lable")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.regionId = parse_number(row, "regionId")
    self.level = parse_number(row, "level")
    self.isMax = parse_number(row, "isMax", 0)
    self.levelUpCost = parse_string(row, "levelUpCost")
    self.levelUpNum = parse_string(row, "levelUpNum")
    self.rewardGoods = parse_string(row, "rewardGoods")
    self.goodsNum = parse_string(row, "goodsNum")

    self.levelUpExplain = parse_string(row, "levelUpExplain")

    self.status = parse_number(row, "status")
end

function RegionLikabilityVo:__delete()
    self.id = nil
    self.lable = nil
    self.name = nil
    self.regionId = nil
    self.level = nil
    self.levelUpExp = nil
    self.status = nil
end

return RegionLikabilityVo
