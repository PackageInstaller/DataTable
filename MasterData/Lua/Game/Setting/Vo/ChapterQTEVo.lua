---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yuhao.zhao
--- DateTime: 2020/7/15 15:54
--- Project: cat-proj
---

local SettingRefBase = import('Game.Setting.SettingRefBase')

---@class ChapterQTEVo : SettingRefBase
local ChapterQTEVo = Class("ChapterQTEVo", SettingRefBase)
local ParseUtils = CS.ParseUtils

local base = SettingRefBase

function ChapterQTEVo:__init()
    --
    self.id = nil
    self.next = nil
    self.groupId = nil
    self.type = nil
    self.area = nil
    self.delay = nil
    self.life = nil
    self.count = nil
end

function ChapterQTEVo:__delete()
    --
    self.id = nil
    self.next = nil
    self.groupId = nil
    self.type = nil
    self.area = nil
    self.delay = nil
    self.life = nil
    self.count = nil
end

function ChapterQTEVo:FillVo(row)
    base.FillVo(self, row)
    self.id = parse_number(row,"id", 0)
    self.groupId = parse_number(row,"groupId", 0)
    self.area = {}
    local tmp = ParseUtils.ParseListStrByHeaderName(row,"area")

    if tmp.Count == 2 then
        local vec1 = ParseUtils.ParseVector2(tmp[0],",")
        local vec2 = ParseUtils.ParseVector2(tmp[1],",")
        self.area.center = (vec1 + vec2) / 2
        self.area.width = math.abs(vec2.x - vec1.x)
        self.area.height = math.abs(vec1.y - vec2.y)
    end
    self.delay = parse_number(row, "delay", 0)
    self.life = parse_number(row, "life", 0)
    self.next = parse_number(row, "next", 0)
    self.count = parse_number(row, "count", 0)
end

return ChapterQTEVo