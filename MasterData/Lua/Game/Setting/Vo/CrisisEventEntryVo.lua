---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2024-05-25 16:33:52
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class CrisisEventEntryVo
local CrisisEventEntryVo = Class("CrisisEventEntryVo", SettingRefBase)

---__int
function CrisisEventEntryVo:__init()
    self.id = nil              --ID
    self.name = nil              --名称
    self.description = nil              --文案包装（普通文本
    self.activityId = nil              --出现的活动
    self.groupId = nil              --集合ID
    self.tagOr = nil              --or词条关联组ID（非or词条填0）
    self.tagClash = nil              --冲突词条组ID（无冲突填0）
    self.unlockTask = nil              --解锁任务ID（提示：完成<任务从属关卡名>中的怪谈档案<任务名>后解锁）
    self.unlockTag = nil              --前置节点(前置是or类型则填or组ID）
    self.tagScore = nil              --词条分数
    self.iconId = nil              --词条图标id
    self.status = nil              --状态
    self.nextTag = nil             --后续节点
end

---FillVo
---@param row TableML.TableFileRow
function CrisisEventEntryVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.description = ParseUtils.ParseLocalizeText(row, "description")
    self.activityId = parse_number(row, "activityId")
    self.groupId = parse_string(row, "groupId")
    self.tagOr = parse_string(row, "tagOr")
    self.tagClash = parse_string(row, "tagClash")
    self.unlockTask = parse_string(row, "unlockTask")
    self.unlockTag = parse_number(row, "unlockTag")
    self.tagScore = parse_number(row, "tagScore")
    self.iconId = parse_string(row, "iconId")
    self.status = parse_number(row, "status")
    local nextTag =  parse_string(row, "nextTag")
    self.nextTag = string.split(nextTag,";")
end

function CrisisEventEntryVo:__delete()
    self.id = nil
    self.name = nil
    self.description = nil
    self.activityId = nil
    self.groupId = nil
    self.tagOr = nil
    self.tagClash = nil
    self.unlockTask = nil
    self.unlockTag = nil
    self.tagScore = nil
    self.iconId = nil
    self.status = nil
    self.nextTag = nil             
end

return CrisisEventEntryVo
