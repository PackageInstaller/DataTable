---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2020-10-28 20:06:48
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local ParseUtils = CS.ParseUtils

---@class EquipFilterVo : SettingRefBase
local EquipFilterVo = Class("EquipFilterVo", SettingRefBase)

---__int
function EquipFilterVo:__init()
    ---@type integer
    self.id              = nil -- 套装类型
    ---@type string
    self.name            = nil -- 套装名称（包装后的名称）
    ---@type string
    self.desc            = nil -- 技能说明
    ---@type string
    self.desc2           = nil -- 道具技能说明
    ---@type string
    self.illustratedDesc = nil -- 技能说明
    ---@type string
    self.icon            = nil -- 套装ICON资源路径
    ---@type string
    self.pagingIcon            = nil -- 装备本tab图标
    ---@type integer
    self.suitNum         = nil -- 套装件数
    ---@type table<string, number>
    self.attributes      = nil -- 属性加成
end

---FillVo
---@param row TableML.TableFileRow
function EquipFilterVo:FillVo(row)
    SettingRefBase.FillVo(self,row)

    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
    self.desc2 = ParseUtils.ParseLocalizeText(row, "desc2")
    self.illustratedDesc = ParseUtils.ParseLocalizeText(row, "illustratedDesc")
    self.icon = ParseUtils.ParsePathName(row, "icon")
    self.pagingIcon = ParseUtils.ParsePathName(row, "pagingIcon")
    self.suitNum = parse_number(row, "suitNum")
    
    self.attAddType = parse_string(row, "attAddType")
    self.addNum = parse_string(row, "addNum")
    -- local attAddType = parse_strings(parse_string(row, "attAddType"))
    -- local addNum = parse_strings(parse_string(row, "addNum"))
   
    -- self.attributes = attributes
end

function EquipFilterVo:__delete()
    self.id              = nil
    self.name            = nil
    self.desc            = nil
    self.desc2           = nil
    self.illustratedDesc = nil
    self.icon            = nil
    self.suitNum         = nil
    self.attributes      = nil
end

function EquipFilterVo:GetAttributes()
    if self.attributes == nil then
        local attributes = {}
        local attAddType = parse_strings(self.attAddType)
        local addNum = parse_strings(self.addNum)
        for i, v in ipairs(attAddType) do
            attributes[checkNumber(v)] = checkNumber(addNum[i])
        end
        self.attributes = attributes
    end
    return self.attributes
end

return EquipFilterVo
