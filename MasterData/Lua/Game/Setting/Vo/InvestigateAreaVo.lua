---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2020-07-23 17:29:02
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class InvestigateAreaVo
local InvestigateAreaVo = Class("InvestigateAreaVo", SettingRefBase)

---__int
function InvestigateAreaVo:__init()
    self.areaId     = nil              --区域编号
    self.name       = nil              --区域名称
    self.coordinate = nil              --对应坐标
    self.taskPos    = nil              --任务坐标
    self.areaMap    = nil              --区域地图
    self.status     = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function InvestigateAreaVo:FillVo(row)
    base.FillVo(self,row)

    self.areaId     = parse_number(row, "areaId", 0)
    self.name       = ParseUtils.ParseLocalizeText(row, "name")
    self.coordinate = ParseUtils.ParseVector2ByHeaderName(row, "coordinate", ",")
    self.taskPos    = ParseUtils.ParseVector2ByHeaderName(row, "taskCoordinate", ",")
    self.areaMap    = ParseUtils.ParsePathName(row, "areaMap")
    self.status     = parse_number(row, "status", 0)
end

function InvestigateAreaVo:__delete()
    self.areaId     = nil              --区域编号
    self.name       = nil              --区域名称
    self.coordinate = nil              --对应坐标
    self.taskPos    = nil              --任务坐标
    self.areaMap    = nil              --区域地图
    self.status     = nil              --状态il
end

return InvestigateAreaVo
