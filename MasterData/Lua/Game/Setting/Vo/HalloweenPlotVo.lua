---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2022-10-11 16:51:43
--- 奖池副本剧情表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class HalloweenPlotVo
local HalloweenPlotVo = Class("HalloweenPlotVo", SettingRefBase)

---__int
function HalloweenPlotVo:__init()
    self.id = nil              --ID
    self.activetyId = nil              --活动ID
    self.name = nil              --副本名
    self.type = nil              --剧情类型（1纯剧情2有战斗）
    self.unlockItem = nil              --解锁道具(0为不消耗
    self.unlockNum = nil              --解锁道具需求数量
    self.unlockCost = nil              --解锁道具消耗(0为不消耗
    self.unlockPlot = nil              --前置奖池ID
    self.endFlag = nil              --结束标志（0非1是）
    self.regionId = nil              --剧情关卡ID（对应剧情关卡表）
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function HalloweenPlotVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.activetyId = parse_number(row, "activetyId")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.type = parse_number(row, "type")
    self.unlockItem = parse_string(row, "unlockItem")
    self.unlockNum = parse_string(row, "unlockNum")
    self.unlockCost = parse_string(row, "unlockCost")
    self.unlockPlot = parse_number(row, "unlockPlot")
    self.endFlag = parse_number(row, "endFlag")
    self.regionId = parse_number(row, "regionId")
    self.status = parse_number(row, "status")
end

function HalloweenPlotVo:__delete()
    self.id = nil
    self.activetyId = nil
    self.name = nil
    self.type = nil
    self.unlockItem = nil
    self.unlockNum = nil
    self.unlockCost = nil
    self.unlockPlot = nil
    self.endFlag = nil
    self.regionId = nil
    self.status = nil
end

return HalloweenPlotVo
