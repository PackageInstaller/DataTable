---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2021-10-13 15:05:25
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class PatrolPointTypeVo
local PatrolPointTypeVo = Class("PatrolPointTypeVo", SettingRefBase)

---__int
function PatrolPointTypeVo:__init()
    self.incidentId = nil              --ID
    self.name = nil              --名称
    self.desc = nil              --描述
    self.butDesc = nil              --按钮文字
    self.icon = nil              --关卡点图标
    self.previewIcon = nil              --关卡点图标
    self.floorUpperLimited = nil              --起始楼层(0为根据概率进行填充)
    self.floorLowerLimited = nil              --结束楼层
    self.generatePro = nil              --关卡生成概率（针对单个事件点的概率）
    self.incidentNum = nil              --事件数量(0,代表不限数量,初级警报和中级警报是写死的，只能读生成概率
    self.olnyFloor = nil              --是否单个点事件（1是，0否）
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function PatrolPointTypeVo:FillVo(row)
    base.FillVo(self,row)
    self.incidentId = parse_number(row, "incidentId")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
    self.butDesc = ParseUtils.ParseLocalizeText(row, "butDesc")
    self.icon = ParseUtils.ParsePathName(row, "icon")
    self.previewIcon = ParseUtils.ParsePathName(row, "previewIcon")
    self.floorUpperLimited = parse_string(row, "floorUpperLimited")
    self.floorLowerLimited = parse_string(row, "floorLowerLimited")
    self.generatePro = parse_string(row, "generatePro")
    self.incidentNum = parse_string(row, "incidentNum")
    self.olnyFloor = parse_number(row, "olnyFloor")
    self.status = parse_number(row, "status")
end

function PatrolPointTypeVo:__delete()
    self.incidentId = nil
    self.name = nil
    self.desc = nil
    self.butDesc = nil
    self.icon = nil
    self.previewIcon = nil
    self.floorUpperLimited = nil
    self.floorLowerLimited = nil
    self.generatePro = nil
    self.incidentNum = nil
    self.olnyFloor = nil
    self.status = nil
end

return PatrolPointTypeVo
