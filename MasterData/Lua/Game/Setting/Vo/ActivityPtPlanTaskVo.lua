---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-07-11 16:27:07
--- 一次性任务表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class ActivityPtPlanTaskVo
local ActivityPtPlanTaskVo = Class("ActivityPtPlanTaskVo", SettingRefBase)

---__int
function ActivityPtPlanTaskVo:__init()
    self.taskAchId = nil              --任务的ID
    self.desc = nil              --任务描述
    self.unlockId = nil              --解锁所需系统ID
    self.taskContentType = nil              --任务目标类型
    self.taskContentId = nil              --任务目标（id）
    self.taskContenNum = nil              --任务目标值（num）
    self.group2 = nil              --一次性任务组别id
    self.group = nil              --刷新规则：1每日刷新，2根据账号定制，3解锁后再刷新（随机1~2），优先级2>3>1
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function ActivityPtPlanTaskVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
    self.unlockId = parse_number(row, "unlockId")
    self.taskContentType = parse_number(row, "taskContentType")
    self.taskContentId = parse_number(row, "taskContentId")
    self.taskContenNum = parse_number(row, "taskContenNum")
    self.rewardId = parse_string(row, "rewardId")
    self.rewardNum = parse_string(row, "rewardNum")
    self.openType = parse_number(row, "openType")
    self.openParameter = parse_number(row, "openParameter")

    self.group = parse_number(row, "group")
    self.status = parse_number(row, "status")
end

function ActivityPtPlanTaskVo:__delete()
    self.taskAchId = nil
    self.desc = nil
    self.unlockId = nil
    self.taskContentType = nil
    self.taskContentId = nil
    self.taskContenNum = nil
    self.group2 = nil
    self.group = nil
    self.status = nil
end

return ActivityPtPlanTaskVo
