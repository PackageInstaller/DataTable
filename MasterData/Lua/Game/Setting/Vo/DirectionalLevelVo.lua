---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2021-06-09 16:30:04
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class DirectionalLevelVo
local DirectionalLevelVo = Class("DirectionalLevelVo", SettingRefBase)

---__int
function DirectionalLevelVo:__init()
    self.id = nil              --ID
    self.type = nil              --副本类型（对应定时副本类型表）
    self.label = nil              --标题
    self.name = nil              --副本名
    self.desc = nil              --关卡描述
    self.regionLevel = nil              --难度等级(E1,D2,C3,B4,A5,S6)
    self.unlockNum = nil              --解锁条件值（填充为0）
    self.proposalLevel = nil              --推荐战斗力
    self.SceneId = nil              --场景ID
    self.monsterShow = nil              --怪物预览
    self.conditionId = nil            --胜利条件
    self.conditionValue = nil            --胜利条件数值
    self.rounds = nil              --回合限制(超出后直接失败)
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function DirectionalLevelVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.type = parse_number(row, "type")
    self.label = ParseUtils.ParseLocalizeText(row, "label")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
    self.regionLevel = parse_string(row, "regionLevel")
    self.unlockNum = parse_number(row, "unlockNum")
    self.proposalLevel = parse_number(row, "proposalLevel")
    self.SceneId = parse_number(row, "SceneId")
    self.monsterShow = parse_number(row, "monsterShow")
    self.conditionId = parse_number(row, "condition")
    self.conditionValue = parse_number(row, "conditionValue")
    self.rounds = parse_number(row, "rounds")
    self.status = parse_number(row, "status")
end

function DirectionalLevelVo:__delete()
    self.id = nil
    self.type = nil
    self.label = nil
    self.name = nil
    self.desc = nil
    self.regionLevel = nil
    self.unlockNum = nil
    self.proposalLevel = nil
    self.SceneId = nil
    self.monsterShow = nil
    self.conditionId = nil
    self.conditionValue = nil
    self.rounds = nil
    self.status = nil
end

return DirectionalLevelVo
