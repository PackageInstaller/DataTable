---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2022-03-28 13:51:36
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class QuestDifficultMainVo
local QuestDifficultMainVo = Class("QuestDifficultMainVo", SettingRefBase)

---__int
function QuestDifficultMainVo:__init()
    self.id = nil              --关卡ID
    self.label = nil              --标题
    self.name = nil              --名称
    self.chapter = nil              --章节id
    self.effect = nil            -- 效果描述
--[[    self.starCondition = nil              --3星条件
    self.starConditionNum = nil              --3星条件数值
    self.starConditionRound = nil]]
    self.monsteType = nil              --怪物类型（1普通 2BOSS  0无战斗填充）
    self.unlockNum = nil              --解锁条件值（填充为0，固定为前置关卡解锁）
    self.level = nil              --进入等级
    --self.teamlevel = nil              --进入小队评级
    self.exp = nil              --主角经验
    self.cardExp = nil              --卡牌经验
    self.proposalLevel = nil              --推荐战斗力
    self.costItem = nil              --消耗道具ID
    self.costNum = nil              --成功时消耗的数量
    self.costNumLose = nil              --进入时扣除的数量
    self.SceneId = nil              --场景ID
    self.rounds = nil              --回合限制(超出后直接失败)
    --self.petLineup = nil              --机器人阵容ID（填0为不需要机器人阵容）
    self.roleNun = nil              --最多上阵人数（1为下限 4为上限）
    --self.monsterShow = nil              --最多上阵人数（1为下限 4为上限）
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function QuestDifficultMainVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.label = ParseUtils.ParseLocalizeText(row, "label")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.effect = ParseUtils.ParseLocalizeText(row, "effect")
    self.chapter = parse_number(row, "chapter")
--[[    self.starCondition = parse_string(row, "starCondition")
    self.starConditionRound = parse_string(row, "starConditionRound","")
    self.starConditionNum = parse_string(row, "starConditionNum")]]
    self.monsteType = parse_number(row, "monsteType")
    self.unlockNum = parse_string(row, "unlockNum")
    self.level = parse_number(row, "level")
    --self.teamlevel = parse_number(row, "teamlevel")
    self.exp = parse_number(row, "exp")
    self.cardExp = parse_number(row, "cardExp")
    self.proposalLevel = parse_number(row, "proposalLevel")
    self.costItem = parse_number(row, "costItem")
    self.costNum = parse_number(row, "costNum")
    self.costNumLose = parse_number(row, "costNumLose")
    self.SceneId = parse_number(row, "SceneId")
    self.rounds = parse_number(row, "rounds")
    --self.petLineup = parse_number(row, "petLineup")
    self.roleNun = parse_string(row, "roleNun")
    --self.monsterShow = parse_string(row, "monsterShow")

    self.status = parse_number(row, "status")
end

function QuestDifficultMainVo:__delete()
    self.id = nil
    self.label = nil
    self.name = nil
    self.chapter = nil
    self.effect = nil
--[[    self.starCondition = nil
    self.starConditionNum = nil
    self.starConditionRound = nil]]
    self.monsteType = nil
    self.unlockNum = nil
    self.level = nil
    --self.teamlevel = nil
    self.exp = nil
    self.cardExp = nil
    self.proposalLevel = nil
    self.costItem = nil
    self.costNum = nil
    self.costNumLose = nil
    self.SceneId = nil
    self.rounds = nil
    --self.petLineup = nil
    self.roleNun = nil
    --self.monsterShow = nil              --最多上阵人数（1为下限 4为上限）
    self.status = nil
end

return QuestDifficultMainVo
