---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2021-04-25 10:22:46
--- 行动模块功能表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class ActionTypeVo
local ActionTypeVo = Class("ActionTypeVo", SettingRefBase)

---__int
function ActionTypeVo:__init()
    self.id = nil              --id
    self.name = nil              --功能名
    self.realRame = nil              --主标题
    self.note = nil              --副标题
    self.actionIcon = nil              --功能背景图路径
    self.order = nil              --排列顺序
    self.time = nil              --时间显示（0不显示，1显示）
    self.duration = nil              --副本持续时间（秒）
    self.tab = nil              --普通和限时
    self.tipsTxt = nil              --页签提示文字
    self.timeTxt = nil              --页签提示文字
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function ActionTypeVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = parse_localizeText(row, "name")
    self.realRame = parse_localizeText(row, "realRame")
    self.note = parse_localizeText(row, "note")
    self.timeTxt = parse_localizeText(row, "timeTxt", "")
    self.actionIcon = ParseUtils.ParsePathName(row, "actionIcon")
    self.order = parse_number(row, "order")
    self.time = parse_number(row, "time")
    self.duration = parse_number(row, "duration")
    self.tab = parse_number(row, "tab", 0)
    self.tipsTxt = parse_localizeText(row, "tipsTxt")
    self.status = parse_number(row, "status")
end

function ActionTypeVo:__delete()
    self.id = nil
    self.name = nil
    self.realRame = nil
    self.note = nil
    self.actionIcon = nil
    self.order = nil
    self.time = nil
    self.duration = nil              --副本持续时间（秒）
    self.tipsTxt = nil              --页签提示文字
    self.timeTxt = nil              --页签提示文字
    self.tab = nil              --普通和限时
    self.status = nil
end

return ActionTypeVo
