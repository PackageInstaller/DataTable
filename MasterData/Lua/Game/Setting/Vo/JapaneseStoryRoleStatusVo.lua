---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2024-08-27 18:15:33
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class JapaneseStoryRoleStatusVo
local JapaneseStoryRoleStatusVo = Class("JapaneseStoryRoleStatusVo", SettingRefBase)

---__int
function JapaneseStoryRoleStatusVo:__init()
    self.id = nil              --编号
    self.activityId = nil              --活动id
    self.nodeId = nil              --对应spine节点编号，为空则仅显示背景图
    self.touchLineId = nil              --对应触碰台词(需跟前面spine节点一一对应，分号分隔，一个分号内逗号分隔的随机播放)
    self.greetingLineId = nil              --对应问候台词(在配置的id中随机播放，每次进入界面时仅播放其中一句)
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function JapaneseStoryRoleStatusVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.activityId = parse_number(row, "activityId")
    self.nodeIds = parse_strings(parse_string(row, "nodeId"))
    self.touchLineIds = {}
    local tmp = parse_strings(parse_string(row, "touchLineId"))
    for i, v in ipairs(tmp) do
        local list = parse_strings(v, ',')
        table.insert(self.touchLineIds, list)
    end
    self.greetingLineIds = parse_strings(parse_string(row, "greetingLineId"))
    self.status = parse_number(row, "status")
end

function JapaneseStoryRoleStatusVo:__delete()
    self.id = nil
    self.activityId = nil
    self.nodeId = nil
    self.touchLineId = nil
    self.greetingLineId = nil
    self.status = nil
end

return JapaneseStoryRoleStatusVo
