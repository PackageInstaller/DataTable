---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2020-08-17 14:20:41
--- 迷宫主题类型表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class MazeThemeVo
local MazeThemeVo = Class("MazeThemeVo", SettingRefBase)

---__int
function MazeThemeVo:__init()
    self.id = nil              --ID
    self.name = nil              --迷宫活动
    self.costId = nil              --进入消耗道具
    self.costNum = nil              --每次进入消耗道具数量
    self.unlockType = nil              --关卡解锁ID（与解锁条件的ID对应）
    self.unlockId = nil              --解锁条件的目标ID（此处只能填写为副本具体ID，道具具体ID，填充则为0）
    self.unlockNum = nil              --解锁条件值（填写具体的值，比如等级多少值，道具数量等等）
    self.starringId = nil              --主角id
    self.regularReward = nil
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function MazeThemeVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id", 0)
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.costId = parse_number(row, "costId", 0)
    self.costNum = parse_number(row, "costNum", 0)
    self.unlockType = parse_string(row, "unlockType", '')
    self.unlockId = parse_string(row, "unlockId", '')
    self.unlockNum = parse_string(row, "unlockNum", '')
    self.starringId = parse_string(row, "starringId", '')
    self.regularReward = parse_string(row, "regularReward", '')
    self.status = parse_number(row, "status", 0)
end

function MazeThemeVo:__delete()
    self.id = nil
    self.name = nil
    self.costId = nil
    self.costNum = nil
    self.unlockType = nil
    self.unlockId = nil
    self.unlockNum = nil
    self.rolePath = nil
    self.regularReward = nil
    self.status = nil
end

return MazeThemeVo
