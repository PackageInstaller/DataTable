---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-02-07 16:56:27
--- 二级区域地图表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class RegionSecondaryMapVo
local RegionSecondaryMapVo = Class("RegionSecondaryMapVo", SettingRefBase)

---__int
function RegionSecondaryMapVo:__init()
    self.id = nil              --点位编号
    self.name = nil              --点位名称
    self.LTableID = nil              --多语言表行数
    self.regionBelong = nil              --所属区域ID
    self.type = nil              --点位类型
    self.content = nil              --点位内容
    self.pointBuilding = nil              --点位贴图资源
    self.coordinate = nil              --点位位置坐标
    self.infoCoordinate = nil              --点位信息锚点位置坐标
    self.unlockType = nil              --解锁条件类型
    self.unlockParam = nil              --解锁条件参数
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function RegionSecondaryMapVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.label = ParseUtils.ParseLocalizeText(row, "label")

    self.regionBelong = parse_number(row, "regionBelong")
    self.type = parse_string(row, "type")
    self.content = parse_string(row, "content")
    self.reward = parse_string(row, "reward")
    --self.infoCoordinate = parse_string(row, "infoCoordinate")
    --self.coordinate = parse_string(row, "coordinate")
    self.rewardCoordinate = parse_string(row, "rewardCoordinate")
    self.camera = parse_string(row, "camera")

    self.unlockType = parse_string(row, "unlockType")
    self.unlockId = parse_string(row, "unlockId")
    self.unlockNum = parse_string(row, "unlockNum")

    self.mainEquipType = parse_number(row, "mainEquipType", 0)
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
    self.showIcon1 = ParseUtils.ParsePathName(row, "showIcon1")
    self.showIcon2 = ParseUtils.ParsePathName(row, "showIcon2")
    self.showBG = ParseUtils.ParsePathName(row, "showBG")

    self.status = parse_number(row, "status")
end

function RegionSecondaryMapVo:__delete()
    self.id = nil
    self.name = nil
    self.LTableID = nil
    self.regionBelong = nil
    self.type = nil
    self.content = nil
    self.pointBuilding = nil
    self.coordinate = nil
    self.infoCoordinate = nil
    self.unlockType = nil
    self.unlockParam = nil
    self.status = nil
end

return RegionSecondaryMapVo
