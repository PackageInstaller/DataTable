---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2021-03-02 11:53:11
--- PT活动表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class BambooActivityVo
local BambooActivityVo = Class("BambooActivityVo", SettingRefBase)

---__int
function BambooActivityVo:__init()
    self.id = nil              --活动ID
    self.name = nil              --副本名
    self.mainMap = nil              --主界面地图
    self.ptItem = nil              --pt点道具ID
    self.challengeRegionUI = nil              --挑战关卡UI界面
    self.rewardUI = nil              --累计奖励UI界面
    self.upGroup = nil              --增益角色组
    self.mailId = nil              --活动货币转换邮件ID
    self.storeSceneId = nil              --商店背景图
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function BambooActivityVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.mainMap = parse_string(row, "mainMap")
    self.ptItem = parse_number(row, "ptItem")
    self.challengeRegionUI = parse_string(row, "challengeRegionUI")
    self.rewardUI = parse_string(row, "rewardUI")
    self.upGroup = parse_string(row, "upGroup")
    self.mailId = parse_string(row, "mailId")
    self.sceneId = ParseUtils.ParsePathName(row, "sceneId")
    self.cardId = parse_number(row, "cardId")                                --展示武装ID
    self.rewardTitleIcon = ParseUtils.ParsePathName(row, "rewardTitleIcon")  --奖励栏图标
    self.ruleId = parse_number(row, "ruleId")
    self.regionDesc = parse_localizeText(row, "regionDesc")

    self.unlockType = parse_string(row, "unlockType")
    self.unlockId = parse_string(row, "unlockId")
    self.unlockNum = parse_string(row, "unlockNum")
    self.storeSceneId = ParseUtils.ParsePathName(row, "storeSceneId")
    self.status = parse_number(row, "status")
end

function BambooActivityVo:__delete()
    self.id = nil
    self.name = nil
    self.mainMap = nil
    self.ptItem = nil
    self.challengeRegionUI = nil
    self.rewardUI = nil
    self.upGroup = nil
    self.mailId = nil
    self.status = nil
end

return BambooActivityVo
