---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2020-09-24 15:31:02
--- 初级装备副本类型表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class QuestPrimaryEquipTypeVo
local QuestPrimaryEquipTypeVo = Class("QuestPrimaryEquipTypeVo", SettingRefBase)

---__int
function QuestPrimaryEquipTypeVo:__init()
    self.id = nil              --索引（对应副本类型）
    self.name = nil              --副本类型名称
    self.autoName = nil         -- 委托作战类型名
    self.autoDesc = nil         -- 委托作战描述
    self.autoIcon = nil         -- 委托作战图标
    self.sequence = nil              --位置编号
    self.enterImage = nil              --入口资源
    self.item = nil                --奖励id
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function QuestPrimaryEquipTypeVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id", 0)
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.autoName = ParseUtils.ParseLocalizeText(row, "autoName")
    self.autoDesc = ParseUtils.ParseLocalizeText(row, "autoDesc")
    self.autoIcon = ParseUtils.ParsePathName(row, "autoIcon")
    self.sequence = parse_number(row, "sequence", 0)
    self.enterImage = parse_string(row, "enterImage", '')
    self.item = parse_string(row, "item", '')
    self.status = parse_number(row, "status", 0)
end

function QuestPrimaryEquipTypeVo:__delete()
    self.id = nil
    self.name = nil
    self.autoName = nil
    self.autoDesc = nil
    self.autoIcon = nil
    self.sequence = nil
    self.item = nil 
    self.enterImage = nil
    self.status = nil
end

return QuestPrimaryEquipTypeVo
