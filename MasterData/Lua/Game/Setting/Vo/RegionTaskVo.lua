---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-06-05 14:01:49
--- 区域任务表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class RegionTaskVo
local RegionTaskVo = Class("RegionTaskVo", SettingRefBase)

---__int
function RegionTaskVo:__init()
    self.id = nil              --任务编号
    self.name = nil              --任务名称
    self.regionBelong = nil              --所属区域ID
    self.areaLv = nil              --对应区域等级
    self.taskGroup = nil              --任务组分类（每种成就内的一组任务填写相同的ID）
    self.successorTasks = nil              --后续任务ID（后续任务继承上个任务的数值。填写0时，则无后续任务填充作用。)
    self.startType = nil              --开始计数类型（1-默认计数  2-解锁后开始计数
    self.taskContentType = nil              --任务目标类型
    self.taskContentId = nil              --任务目标
    self.taskContenNum = nil              --任务值
    self.rewardId = nil              --任务奖励
    self.rewardNum = nil              --奖励数量
    self.openType = nil              --跳转目标
    self.openParameter = nil              --跳转参数（填写关卡ID）
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function RegionTaskVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.regionBelong = parse_number(row, "regionBelong")
    self.areaLv = parse_number(row, "areaLv")
    self.taskGroup = parse_number(row, "taskGroup")
    self.before = parse_number(row, "before")
    self.startType = parse_number(row, "startType")
    self.taskContentType = parse_number(row, "taskContentType")
    self.taskContentId = parse_number(row, "taskContentId")
    self.taskContenNum = parse_number(row, "taskContenNum")
    self.rewardId = parse_string(row, "rewardId")
    self.rewardNum = parse_string(row, "rewardNum")
    self.openType = parse_number(row, "openType")
    self.openParameter = parse_string(row, "openParameter")
    self.status = parse_number(row, "status")
end

function RegionTaskVo:__delete()
    self.id = nil
    self.name = nil
    self.regionBelong = nil
    self.areaLv = nil
    self.taskGroup = nil
    self.successorTasks = nil
    self.startType = nil
    self.taskContentType = nil
    self.taskContentId = nil
    self.taskContenNum = nil
    self.rewardId = nil
    self.rewardNum = nil
    self.openType = nil
    self.openParameter = nil
    self.status = nil
end

return RegionTaskVo
