---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2020-04-17 10:41
---   迷宫商店出售的商品
  
local SettingRefBase = require('Game.Setting.SettingRefBase')

---@class MazeGoodsVo
local MazeGoodsVo = Class("MazeGoodsVo", SettingRefBase)
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---__int
function MazeGoodsVo:__init()
    self.id = nil
    self.name = nil
    self.desc = nil
    self.itemId = nil
    self.oldPrice = nil           --原价
    self.newPrice = nil    --折扣价
    self.itemNum = nil    --数量
    self.probability = nil    --概率
    self.goodsConsum = nil    --使用货币
    self.status = nil    --状态
end


---FillVo
---@param row TableML.TableFileRow
function MazeGoodsVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id", 0)
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.desc = string.gsub(ParseUtils.ParseLocalizeText(row, "desc"), "\\n", "\n")
    
    self.itemId = parse_number(row, "itemid", 0)
    self.oldPrice = parse_number(row, "oldPrice", 0)
    self.newPrice = parse_number(row, "newPrice", 0)
    self.itemNum = parse_number(row, "itemNum", 0)
    self.probability = parse_number(row, "probability", 0)
    self.goodsConsum = parse_string(row, "goodsConsum", '')
    self.status = parse_number(row, "status", 0)
end

function MazeGoodsVo:__delete()
    self.id = nil
    self.name = nil
    self.desc = nil
    self.itemId = nil
    self.oldPrice = nil           --原价
    self.newPrice = nil    --折扣价
    self.itemNum = nil    --数量
    self.probability = nil    --概率
    self.goodsConsum = nil    --使用货币
    self.status = nil    --状态
end

return MazeGoodsVo