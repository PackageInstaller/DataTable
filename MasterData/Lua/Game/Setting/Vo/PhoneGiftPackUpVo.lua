---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2020-10-26 14:09:25
--- 礼物包装加成表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class PhoneGiftPackUpVo
local PhoneGiftPackUpVo = Class("PhoneGiftPackUpVo", SettingRefBase)

---__int
function PhoneGiftPackUpVo:__init()
    self.id = nil              --编号
    --self.grade = nil              --包装档次：1，高、2中、3免费
    self.coinId = nil              --对应货币id
    self.coinAmount = nil              --对应货币消耗
    self.addition = nil              --对应好感度加成（百分比）
    self.photoId = nil              --包装图标
    self.status = nil              --状态
    self.name = nil              --名称（读字库）
end

---FillVo
---@param row TableML.TableFileRow
function PhoneGiftPackUpVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    --self.grade = parse_number(row, "grade")
    self.coinId = parse_number(row, "coinId")
    self.coinAmount = parse_number(row, "coinAmount")
    self.addition = parse_number(row, "addition")
    self.photoId = ParseUtils.ParsePathName(row, "icon")
    self.status = parse_number(row, "status")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
end

function PhoneGiftPackUpVo:__delete()
    self.id = nil
    --self.grade = nil
    self.coinId = nil
    self.coinAmount = nil
    self.addition = nil
    self.photoId = nil
    self.status = nil
    self.name = nil
end

return PhoneGiftPackUpVo
