---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2022-10-11 16:50:56
--- 奖池副本活动表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class HalloweenPlanVo
local HalloweenPlanVo = Class("HalloweenPlanVo", SettingRefBase)

---__int
function HalloweenPlanVo:__init()
    self.id = nil              --活动ID
    self.name = nil              --副本名
    self.changeId = nil              --活动页签跳转
    self.ruleId = nil              --功能规则ID
    self.storyId = nil              --序章剧情ID
    self.pools = nil              --奖池数
    self.rewardSum = nil              --有限池奖励上限
    self.showCard = nil              --奖励卡牌展示
    self.unlockType = nil              --开启条件（对应解锁条件表里的数值）
    self.unlockId = nil              --开启条件ID
    self.unlockNum = nil              --开启条件数值
    self.openType = nil              --跳转目标
    self.openParameter = nil              --跳转参数
    self.token = nil              --货币栏显示
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function HalloweenPlanVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.changeId = parse_number(row, "changeId")
    self.ruleId = parse_number(row, "ruleId")
    self.storyId = parse_number(row, "storyId")
    self.pools = parse_number(row, "pools")
    self.showCard = parse_number(row, "showCard")
    self.unlockType = parse_string(row, "unlockType")
    self.unlockId = parse_number(row, "unlockId")
    self.drawConsume = parse_number(row, "drawConsume", 0)
    self.redPoint = parse_number(row, "redPoint", 0)
    self.unlockNum = parse_string(row, "unlockNum")
    self.openType = parse_string(row, "openType")
    self.openParameter = parse_string(row, "openParameter")
    self.token = parse_string(row, "token")
    self.status = parse_number(row, "status")
end

function HalloweenPlanVo:__delete()
    self.id = nil
    self.name = nil
    self.changeId = nil
    self.ruleId = nil
    self.storyId = nil
    self.pools = nil
    self.rewardSum = nil
    self.showCard = nil
    self.unlockType = nil
    self.unlockId = nil
    self.unlockNum = nil
    self.openType = nil
    self.openParameter = nil
    self.token = nil
    self.status = nil
end

return HalloweenPlanVo
