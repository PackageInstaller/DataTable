---
--- Generated by shentayibu
--- Created by shentayibu.
--- DateTime: 2023/10/10 14:37
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class MidFestivalMainEntryVo
local MidFestivalMainEntryVo = Class("MidFestivalMainEntryVo", SettingRefBase)

function MidFestivalMainEntryVo:__init()
    self.id             = nil   --- 入口编号
    self.activityId     = nil   --- 活动id
    self.type           = nil   --- 功能类型：1主线关卡、2复刷关卡、3活动boss、4活动商店、5活动任务
    self.name           = nil   --- 功能名称
    self.unlockLevel    = nil   --- 解锁条件值（默认解锁为0，固定为活动主线关卡解锁）
    self.lockPicture    = nil   --- 解锁前入口图
    self.unlockPicture  = nil   --- 解锁后入口图
    self.status         = nil   --- 状态
end

---@param row TableML.TableFileRow
function MidFestivalMainEntryVo:FillVo(row)
    base.FillVo(self, row)
    self.id = parse_number(row, "id")
    self.activityId = parse_number(row, "activityId")
    self.type = parse_number(row, "type")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.unlockLevel = parse_string(row, "unlockLevel")
    self.lockPicture = ParseUtils.ParsePathName(row, "lockPicture")
    self.unlockPicture = ParseUtils.ParsePathName(row, "unlockPicture")
    self.status = parse_number(row, "status")
end

function MidFestivalMainEntryVo:__delete()
    self.id = nil
    self.activityId = nil
    self.type = nil
    self.name = nil
    self.unlockLevel = nil
    self.lockPicture = nil
    self.unlockPicture = nil
    self.status = nil
end

return MidFestivalMainEntryVo
