---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by panmeng.
--- DateTime: 2022/7/28 10:04
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class MusicPlayerVo
local MusicPlayerVo = Class("MusicPlayerVo", SettingRefBase)

---__int
function MusicPlayerVo:__init()
    self.id         = nil  -- 专辑id
    self.name       = nil -- 音乐名
    self.lockDesc   = nil -- 上锁介绍
    self.unlockDesc = nil -- 解锁介绍
    self.musicId    = nil -- 音乐id
    self.coverImg   = nil -- 封面资源
    self.tagType    = nil -- 标签类型
end

---FillVo
---@param row TableML.TableFileRow
function MusicPlayerVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.lockDesc = ParseUtils.ParseLocalizeText(row, "lockDesc")
    self.unlockDesc = parse_localizeText(row, "unlockDesc", true)
    self.musicId = parse_string(row, "musicId")
    self.coverImg = ParseUtils.ParsePathName(row, "coverId")
    self.tagType = parse_number(row, "Tag")
end

function MusicPlayerVo:__delete()
    self.id         = nil  -- 专辑id
    self.name       = nil -- 音乐名
    self.lockDesc   = nil -- 上锁介绍
    self.unlockDesc = nil -- 解锁介绍
    self.musicId    = nil -- 音乐id
    self.coverImg   = nil -- 封面资源
    self.tagType    = nil -- 标签类型
end

return MusicPlayerVo