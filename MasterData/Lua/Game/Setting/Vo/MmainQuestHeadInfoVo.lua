---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2022-05-10 16:41:10
--- 主线关卡头像索引表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class MmainQuestHeadInfoVo
local MmainQuestHeadInfoVo = Class("MmainQuestHeadInfoVo", SettingRefBase)

---__int
function MmainQuestHeadInfoVo:__init()
    self.id = nil              --编号
    self.type = nil              --类型（1玩家头像，0角色头像）
    self.backColor = nil        --头像底色
    self.head = nil              --头像资源路径
    self.maleHead = nil              --头像资源路径
    self.femaleHead = nil              --头像资源路径

    --self.headUI = nil              --头像资源路径
    --self.maleHeadUI = nil              --头像资源路径
    --self.femaleHeadUI = nil              --头像资源路径

    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function MmainQuestHeadInfoVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.type = parse_number(row, "type")
    self.backColor = parse_string(row, "backColor")
    self.head = ParseUtils.ParsePathName(row, "head")
    self.maleHead = ParseUtils.ParsePathName(row, "maleHead")
    self.femaleHead = ParseUtils.ParsePathName(row, "femaleHead")

    --self.headUI = ParseUtils.ParsePathName(row, "headUI")
    --self.maleHeadUI = ParseUtils.ParsePathName(row, "maleHeadUI")
    --self.femaleHeadUI = ParseUtils.ParsePathName(row, "femaleHeadUI")

    self.status = parse_number(row, "status")
end

function MmainQuestHeadInfoVo:__delete()
    self.id = nil
    self.type = nil
    self.backColor = nil
    self.head = nil
    self.maleHead = nil
    self.femaleHead = nil
    --self.headUI = nil              --头像资源路径
    --self.maleHeadUI = nil              --头像资源路径
    --self.femaleHeadUI = nil              --头像资源路径
    self.status = nil
end

return MmainQuestHeadInfoVo
