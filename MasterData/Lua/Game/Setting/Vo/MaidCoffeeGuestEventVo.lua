---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-10-08 10:03:08
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class MaidCoffeeGuestEventVo
local MaidCoffeeGuestEventVo = Class("MaidCoffeeGuestEventVo", SettingRefBase)

---__int
function MaidCoffeeGuestEventVo:__init()
    self.eventId = nil              --事件id
    self.name = nil              --事件名字
    self.storyId = nil              --对应的storyId-客人对话剧情表
    self.npcId = nil              --专属客人id
    self.type = nil              --类型
    self.randomId = nil              --随机库id(专属不填)
    self.unlockItem = nil              --解锁的喜好
    self.eventWeight = nil              --初始权重
    self.eventUnlockWeight = nil              --专属事件解锁后权重（随机没有）
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function MaidCoffeeGuestEventVo:FillVo(row)
    base.FillVo(self,row)
    self.eventId = parse_number(row, "eventId")
    self.name = parse_localizeText(row, "name")
    self.storyId = parse_number(row, "storyId")
    self.npcId = parse_number(row, "npcId")
    self.type = parse_number(row, "type")
    self.randomId = parse_number(row, "randomId")
    self.unlockItem = parse_string(row, "unlockItem")
    self.eventWeight = parse_number(row, "eventWeight")
    self.eventUnlockWeight = parse_number(row, "eventUnlockWeight")
    self.status = parse_number(row, "status")
end

function MaidCoffeeGuestEventVo:__delete()
    self.eventId = nil
    self.name = nil
    self.storyId = nil
    self.npcId = nil
    self.type = nil
    self.randomId = nil
    self.unlockItem = nil
    self.eventWeight = nil
    self.eventUnlockWeight = nil
    self.status = nil
end

return MaidCoffeeGuestEventVo
