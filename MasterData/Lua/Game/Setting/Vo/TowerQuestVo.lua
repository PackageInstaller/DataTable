---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2021-01-06 14:49:48
--- 爬塔关卡表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class TowerQuestVo
local TowerQuestVo = Class("TowerQuestVo", SettingRefBase)

---__int
function TowerQuestVo:__init()
    self.id = nil              --关卡ID
    self.label = nil              --标题
    self.type = nil              --排期id
    self.name = nil              --标题
    self.effect = nil ---效果
    self.monsteType = nil              --怪物类型（1普通 2BOSS  0无战斗填充）
    self.unlockNum = nil              --解锁条件值（填充为0，固定为前置关卡解锁）
    self.level = nil              --解锁条件值（填充为0，固定为前置关卡解锁）
    self.exp = nil              --主角经验
    self.cardExp = nil              --卡牌经验
    self.proposalLevel = nil              --推荐战斗力
    self.costItem = nil              --消耗道具ID
    self.costNum = nil              --成功时消耗的数量
    self.costNumLose = nil              --进入时扣除的数量
    self.SceneId = nil              --场景ID
    self.rounds = nil              --回合限制(超出后直接失败)
    self.roleNun = nil              --最多上阵人数（1为下限 4为上限）
    self.monsterShow = nil              
    self.nextLevel = nil            --下一层
    self.rewardGroupId = nil --奖励组
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function TowerQuestVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    --self.label = ParseUtils.ParseLocalizeText(row, "label")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.effect = parse_localizeText(row, "effect", nil, '')
    self.monsteType = parse_number(row, "monsteType")
    self.storey = parse_number(row, "storey")

    self.type = parse_number(row, "type")
    self.unlockNum = parse_string(row, "unlockNum")
    self.exp = parse_number(row, "exp")
    self.cardExp = parse_number(row, "cardExp")
    self.proposalLevel = parse_number(row, "proposalLevel")
    self.costItem = parse_number(row, "costItem")
    self.costNum = parse_number(row, "costNum")
    self.costNumLose = parse_number(row, "costNumLose")
    self.SceneId = parse_number(row, "SceneId")
    self.rounds = parse_number(row, "rounds")
    self.roleNun = parse_string(row, "roleNun")
    self.status = parse_number(row, "status")
    self.nextLevel = parse_number(row, "nextLevel")        
    self.repeatButton = parse_number(row, "repeatButton")
    self.replayButton = parse_number(row, "replayButton")
    self.restartBattleBtn = parse_number(row, "restartBattleBtn")

    self.unlockItem = parse_number(row, "unlockItem")        
    self.unlockItemNum = parse_number(row, "unlockItemNum")

    self.battleRules = parse_number(row, "battleRules", 0)
    self.rewardGroupId = parse_number(row, "rewardGroupId", 0)
end

function TowerQuestVo:__delete()
    self.type = nil              --排期id
    self.id = nil
    self.label = nil
    self.name = nil             
    self.monsteType = nil
    self.unlockNum = nil
    self.exp = nil
    self.cardExp = nil
    self.proposalLevel = nil
    self.costItem = nil
    self.costNum = nil              --成功时消耗的数量
    self.costNumLose = nil              --进入时扣除的数量
    self.SceneId = nil
    self.rounds = nil
    self.roleNun = nil
    self.monsterShow = nil        
    self.nextLevel = nil            --下一层     
    self.level = nil            --下一层       
    self.status = nil
    self.rewardGroupId = nil
end

return TowerQuestVo
