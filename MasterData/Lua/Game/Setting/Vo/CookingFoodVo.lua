---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2021-10-13 11:40:37
--- 战斗料理表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class CookingFoodVo
local CookingFoodVo = Class("CookingFoodVo", SettingRefBase)

---__int
function CookingFoodVo:__init()
    self.id = nil              --道具编号
    self.name = nil              --名称（读字库）
    self.desc = nil              --描述
    self.description = nil              --功能说明
    self.effectId = nil              --属性效果id（对应料理属性效果表）
    self.effectTimes = nil              --料理有效次数
    self.menu = nil              --对应食谱
    self.goodsConsum = nil              --消耗货币（0表示现金）
    self.goodsConsumNum = nil              --消耗道具数量
    self.picture = nil              --图片
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function CookingFoodVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.description = ParseUtils.ParseLocalizeText(row, "desc")
    self.desc = ParseUtils.ParseLocalizeText(row, "description")
    self.effectId = parse_string(row, "effectId")
    self.effectTimes = parse_number(row, "effectTimes")
    self.menu = parse_string(row, "menu")
    self.goodsConsum = parse_string(row, "goodsConsum")
    self.goodsConsumNum = parse_string(row, "goodsConsumNum")
    self.picture = ParseUtils.ParsePathName(row, "picture")
    self.status = parse_number(row, "status")
end

function CookingFoodVo:__delete()
    self.id = nil
    self.name = nil
    self.desc = nil
    self.description = nil
    self.effectId = nil
    self.effectTimes = nil
    self.menu = nil
    self.goodsConsum = nil
    self.goodsConsumNum = nil
    self.picture = nil
    self.status = nil
end

return CookingFoodVo
