---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-02-20 16:36:15
--- 战斗总览入口表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class RegionEntryVo
local RegionEntryVo = Class("RegionEntrysVo", SettingRefBase)

---__int
function RegionEntryVo:__init()
    self.id = nil              --ID
    self.uid = nil              --程序功能ID
    self.lable = nil              --标签
    self.title = nil              --标题描述
    self.subtitle = nil              --副标题（填写为0时，等于没有）
    self.bgPath = nil              --底图路径
    self.imgPath = nil              --面图路径
    self.position = nil              --入口位置
    self.areaPointId = nil  ----跳转对应二级地图的位置信息的id
    self.unlockType = nil              --开启条件（对应解锁条件表里的数值）
    self.unlockId = nil              --开启条件ID
    self.unlockNum = nil              --开启条件数值
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function RegionEntryVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.uid = parse_number(row, "uid")
    self.lable = ParseUtils.ParseLocalizeText(row, "lable")
    self.title = ParseUtils.ParseLocalizeText(row, "title")
    self.subtitle = ParseUtils.ParseLocalizeText(row, "subtitle")
    self.bgPath = ParseUtils.ParsePathName(row, "bgPath")
    self.iconColorValue = parse_string(row, "iconColorValue")
    self.position = parse_number(row, "position")
    self.areaPointId = parse_number(row, "areaPointId")
    self.bgPathOpen = ParseUtils.ParsePathName(row, "bgPathOpen")

    self.unlockType = parse_string(row, "unlockType")
    self.unlockId = parse_string(row, "unlockId")
    self.unlockNum = parse_string(row, "unlockNum")
    self.status = parse_number(row, "status")
end

function RegionEntryVo:__delete()
    self.id = nil
    self.uid = nil
    self.lable = nil
    self.title = nil
    self.subtitle = nil
    self.bgPath = nil
    self.imgPath = nil
    self.position = nil
    self.areaPointId = nil
    self.unlockType = nil
    self.unlockId = nil
    self.unlockNum = nil
    self.status = nil
end

return RegionEntryVo
