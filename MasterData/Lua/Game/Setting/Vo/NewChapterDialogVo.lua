---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yuhao.zhao
--- DateTime: 2020/4/28 14:35
--- Project: cat-proj
---
local SettingRefBase = import('Game.Setting.SettingRefBase')

---@class NewChapterDialogVo : SettingRefBase
local NewChapterDialogVo = Class("NewChapterDialogVo", SettingRefBase)
local ParseUtils = CS.ParseUtils

local base = SettingRefBase

function NewChapterDialogVo:__init()
    --
    self.Id = nil
    self.next = nil
    self.owner = nil
    self.content = nil
    self.option = nil
    self.event = nil
    self.parameter = nil
    self.headicon = nil
    self.speedLevel = nil
end

function NewChapterDialogVo:__delete()
    --
    self.Id = nil
    self.next = nil
    self.owner = nil
    self.content = nil
    self.option = nil
    self.event = nil
    self.parameter = nil
    self.headicon = nil
    self.speedLevel = nil
end

function NewChapterDialogVo:FillVo(row)
    base.FillVo(self,row)
    self.Id = parse_number(row,"Id", 0)
    self.next = parse_number(row,"next", 0)
    self.owner = parse_string(row,"owner", '')
    self.content = parse_string(row,"content", '')
    self.option = parse_string(row,"option", '')
    local tmp = ParseUtils.ParseListStrByHeaderName(row, "event")
    self.event = {}
    for i = 0, tmp.Count - 1 do
        table.insert(self.event, tmp[i])
    end

    tmp = ParseUtils.ParseListStrByHeaderName(row, "parameter")
    self.parameter = {}
    for i = 0, tmp.Count - 1 do
        table.insert(self.parameter, tmp[i])
    end

    self.headicon = parse_string(row, "headicon", '')
    --self.appearId = parse_number(row,"appearId", 0)
    self.speedLevel = parse_number(row, "SpeedLevel", 0)
end

return NewChapterDialogVo