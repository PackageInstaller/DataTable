---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2024-01-24 14:45:12
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class DreamAdventuresMainStatusVo
local DreamAdventuresMainStatusVo = Class("DreamAdventuresMainStatusVo", SettingRefBase)

---__int
function DreamAdventuresMainStatusVo:__init()
    self.id = nil              --编号
    self.activityId = nil              --活动id
    self.nodeIds = nil              --对应spine节点编号，为空则仅显示背景图
    self.touchLineIds = nil              --对应触碰台词
    self.greetingLineIds = nil              --对应问候台词
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function DreamAdventuresMainStatusVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.activityId = parse_number(row, "activityId")
    self.nodeIds = parse_strings(parse_string(row, "nodeId"))
    self.touchLineIds = {}
    local tmp = parse_strings(parse_string(row, "touchLineId"))
    for i, v in ipairs(tmp) do
        local list = parse_strings(v, ',')
        table.insert(self.touchLineIds, list)
    end
    self.greetingLineIds = parse_strings(parse_string(row, "greetingLineId"))
    self.status = parse_number(row, "status")
end

function DreamAdventuresMainStatusVo:__delete()
    self.id = nil
    self.activityId = nil
    self.nodeIds = nil
    self.touchLineIds = nil
    self.greetingLineIds = nil
    self.status = nil
end

return DreamAdventuresMainStatusVo
