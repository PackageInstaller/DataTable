---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-03-29 13:44:17
--- 猫球配置表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class MaidCoffeeCatRollConfigVo
local MaidCoffeeCatRollConfigVo = Class("MaidCoffeeCatRollConfigVo", SettingRefBase)

---__int
function MaidCoffeeCatRollConfigVo:__init()
    self.id = nil              --猫球id
    self.name = nil              --猫球名称
    self.desc = nil              --猫球描述
    self.quality = nil              --对应猫球品质类型：1基础型猫球、2成长型猫球、3成熟型猫球、4主题型猫球
    self.catId = nil              --对应猫种ID
    self.kind1 = nil              --满星前包含基因ID
    self.kind2 = nil              --满星后包含基因ID
    self.picture1 = nil              --满星前图片
    self.picture2 = nil              --满星后图片
    self.energy = nil              --初始能量值
    self.kind = nil              --基因大类
    self.num = nil              --基因数量（实际数量为num+指定+肤色）
    self.appoint = nil              --指定基因
    self.skinKind = nil              --随机身体肤色范围
    self.skinColor = nil              --指定身体肤色基因：0为随机，id为指定
    self.coffee = nil              --咖啡馆技能
    self.coffeeNum = nil              --咖啡馆技能数值
    self.skillId = nil              --对应光环技能ID
    self.changeId = nil              --同卡转换道具ID
    self.changeNum = nil              --同卡转换道具数量
end

---FillVo
---@param row TableML.TableFileRow
function MaidCoffeeCatRollConfigVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")

    self.catId = parse_number(row, "catId")
    self.quality = parse_number(row, "quality")
    local kind1 = parse_strings(parse_string(row, "kind1"))
    local t = {}
    for i, v in pairs(kind1) do
        if checkNumber(v) > 0 then
            table.insert(t, checkNumber(v))
        end
    end
    self.kind1 = t
    local kind2 = parse_strings(parse_string(row, "kind2"))
    local t = {}
    for i, v in pairs(kind2) do
        if checkNumber(v) > 0 then
            table.insert(t, checkNumber(v))
        end
    end
    self.kind2 = t
    self.picture1 = ParseUtils.ParsePathName(row, "picture1")
    self.picture2 = ParseUtils.ParsePathName(row, "picture2")
    self.energy = parse_number(row, "energy")
    self.kind = parse_string(row, "kind")
    self.num = parse_string(row, "num")
    self.appoint = parse_string(row, "appoint")
    self.skinKind = parse_string(row, "skinKind")
    self.skinColor = parse_string(row, "skinColor")
    self.coffee = parse_number(row, "coffee")
    self.coffeeNum = parse_strings(parse_string(row, "coffeeNum"))
    self.skillId = parse_number(row, "skillId")
    self.changeId = parse_number(row, "changeId")
    self.changeNum = parse_number(row, "changeNum")
end

function MaidCoffeeCatRollConfigVo:__delete()
    self.id = nil
    self.name = nil
    self.quality = nil
    self.catId = nil
    self.kind1 = nil
    self.kind2 = nil
    self.picture1 = nil
    self.picture2 = nil
    self.energy = nil
    self.kind = nil
    self.num = nil
    self.appoint = nil
    self.skinKind = nil
    self.skinColor = nil
    self.coffee = nil
    self.coffeeNum = nil
    self.skillId = nil
    self.changeId = nil
    self.changeNum = nil
end

return MaidCoffeeCatRollConfigVo
