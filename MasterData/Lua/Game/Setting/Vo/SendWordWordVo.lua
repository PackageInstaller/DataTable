---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2022-11-10 13:50:02
--- 送信活动信件表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class SendWordWordVo
local SendWordWordVo = Class("SendWordWordVo", SettingRefBase)

---__int
function SendWordWordVo:__init()
    self.id = nil              --剧情ID
    self.group = nil              --组别(对应剧情活动表id
    self.letterTitle = nil              --信封标题
    self.taskTitle = nil              --内容标题
    self.desc = nil              --文本
    self.fedesc = nil              --文本
    self.characterId = nil              --对应角色id
    self.picture = nil              --角色图
    self.catHead = nil              --猫头图
    self.unlockItem = nil              --解锁所需道具
    self.unlockNum = nil              --解锁所需道具数
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function SendWordWordVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.group = parse_number(row, "group")
    self.letterTitle = ParseUtils.ParseLocalizeText(row, "letterTitle")
    self.taskTitle = ParseUtils.ParseLocalizeText(row, "taskTitle")
    self.desc = ParseUtils.ParseLocalizeText(row, "desc")
    self.fedesc = ParseUtils.ParseLocalizeText(row, "fedesc")
    self.characterId = parse_number(row, "characterId")
    self.picture = ParseUtils.ParsePathName(row, "picture")
    self.catHead = ParseUtils.ParsePathName(row, "catHead")
    self.unlockItem = parse_number(row, "unlockItem")
    self.unlockNum = parse_number(row, "unlockNum")
    self.status = parse_number(row, "status")
end

function SendWordWordVo:__delete()
    self.id = nil
    self.group = nil
    self.letterTitle = nil
    self.taskTitle = nil
    self.desc = nil
    self.fedesc = nil
    self.characterId = nil
    self.picture = nil
    self.catHead = nil
    self.unlockItem = nil
    self.unlockNum = nil
    self.status = nil
end

return SendWordWordVo
