---
--- Generated by shentayibu
--- Created by shentayibu.
--- DateTime: 2023/10/16 16:05
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class DirectDrawPlanVo
local DirectDrawPlanVo = Class("DirectDrawPlanVo", SettingRefBase)

function DirectDrawPlanVo:__init()
    self.id                 = nil   --- 活动排期id
    self.singleProp         = nil   --- 单抽道具
    self.singleConsume      = nil   --- 单抽消耗
    self.tenProp            = nil   --- 10连道具
    self.tenConsume         = nil   --- 10连消耗
    self.changeId           = nil   --- 换皮资源
    self.sceneId            = nil   --- 背景图
    self.cardId             = nil   --- 当期新增活动角色id
    --self.teamId             = nil   --- 包含卡组
    --self.teamWeight         = nil   --- 卡组权重
    --self.probability        = nil   --- 卡池概率(显示用)
    self.drawTimes          = nil   --- 每X抽获得保底值
    self.drawNum            = nil   --- 每X抽获得保底值X点
    self.upperLimit         = nil   --- 保底值上限
    self.ruleID             = nil   --- 功能规则id
    self.video              = nil   --- 卡池视频
    self.status             = nil   --- 状态
end

---@param row TableML.TableFileRow
function DirectDrawPlanVo:FillVo(row)
    base.FillVo(self, row)
    self.id = parse_number(row, "id")
    self.singleProp = parse_string(row, "singleProp")
    self.singleConsume = parse_number(row, "singleConsume")
    self.tenProp = parse_string(row, "tenProp")
    self.tenConsume = parse_number(row, "tenConsume")
    self.changeId = parse_number(row, "changeId")
    self.sceneId = ParseUtils.ParsePathName(row, "sceneId")
    self.cardId = parse_number(row, "cardId")
    --self.teamId = string.split(parse_string(row, "teamId"), ';')
    --self.teamWeight = string.split(parse_string(row, "teamWeight"), ';')
    --self.probability = string.split(parse_string(row, "probability"), ';')
    self.drawTimes = parse_number(row, "drawTimes")
    self.drawNum = parse_number(row, "drawNum")
    self.upperLimit = parse_number(row, "upperLimit")
    self.ruleID = parse_number(row, "ruleID")
    self.video = parse_string(row, "video")
    self.status = parse_number(row, "status")
end

function DirectDrawPlanVo:__delete()
    self.id = nil
    self.singleProp = nil
    self.singleConsume = nil
    self.tenProp = nil
    self.tenConsume = nil
    self.changeId = nil
    self.sceneId = nil
    self.cardId = nil
    --self.teamId = nil
    --self.teamWeight = nil
    --self.probability = nil
    self.drawTimes = nil
    self.drawNum = nil
    self.upperLimit = nil
    self.ruleID = nil
    self.status = nil
end

return DirectDrawPlanVo
