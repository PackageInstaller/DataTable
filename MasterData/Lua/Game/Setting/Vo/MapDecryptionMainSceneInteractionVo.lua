---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2022-11-07 17:28:36
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class MapDecryptionMainSceneInteractionVo
local MapDecryptionMainSceneInteractionVo = Class("MapDecryptionMainSceneInteractionVo", SettingRefBase)

---__int
function MapDecryptionMainSceneInteractionVo:__init()
    self.id = nil              --编号
    self.activityId = nil              --活动id（解密活动排期表ID）
    self.name = nil              --名称（角色名ID）
    self.title = nil              --身份名称
    self.ability = nil              --交互入口：1探索、2追踪、3古物修复、4新年转盘、5新年商店、6看烟花、7闲聊、8返回
    self.action = nil               -- 状态机
    self.action1 = nil              --有红点状态时动作
    self.action2 = nil              --有新闲聊话题时动作
    self.icon = nil              --头顶icon（正常）
    self.modelPath = nil              --角色模型
    self.sex = nil                 --性别1男2女
    self.disableTurn = nil         --禁用转身

end

---FillVo
---@param row TableML.TableFileRow
function MapDecryptionMainSceneInteractionVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.activityId = parse_number(row, "activityId")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.title = ParseUtils.ParseLocalizeText(row, "title")
    self.ability = parse_strings(parse_string(row, "ability"))
    self.action = ParseUtils.ParsePathName(row, "action")
    self.action1 = parse_string(row, "action1")
    self.action2 = parse_string(row, "action2")
    self.icon = parse_string(row, "icon")
    self.modelPath = ParseUtils.ParsePathName(row, "modelPath")
    self.sex = parse_number(row, "sex")
    self.disableTurn = parse_number(row, "disableTurn", 0)
end

function MapDecryptionMainSceneInteractionVo:__delete()
    self.id = nil              --编号
    self.activityId = nil              --活动id（解密活动排期表ID）
    self.name = nil              --名称（角色名ID）
    self.title = nil              --身份名称
    self.ability = nil              --交互入口：1探索、2追踪、3古物修复、4新年转盘、5新年商店、6看烟花、7闲聊、8返回
    self.action = nil               -- 状态机
    self.action1 = nil              --有红点状态时动作
    self.action2 = nil              --有新闲聊话题时动作
    self.icon = nil              --头顶icon
    self.modelPath = nil              --角色模型
    self.sex = nil                 --性别1男2女
    self.disableTurn = nil         --禁用转身
end

return MapDecryptionMainSceneInteractionVo
