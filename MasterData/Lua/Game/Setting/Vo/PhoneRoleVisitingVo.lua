---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2020-10-26 18:24:56
--- 角色名片表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class PhoneRoleVisitingVo
local PhoneRoleVisitingVo = Class("PhoneRoleVisitingVo", SettingRefBase)

---__int
function PhoneRoleVisitingVo:__init()
    self.id = nil              --对应角色
    self.unlockType = nil              --开启条件（对应解锁条件表里的数值）
    self.unlockId = nil              --开启条件ID
    self.unlockNum = nil              --开启条件数值
    self.text = nil              --个签内容
    self.cardSkinId = nil
    self.cup = nil                --杯子模型
    self.plate = nil              --盘子模型

    self.ear = nil              --盘子模型
    self.face = nil              --盘子模型
    self.armLeft = nil              --盘子模型
    self.armRight = nil              --盘子模型
    self.body = nil              --盘子模型

    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function PhoneRoleVisitingVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.unlockType = parse_string(row, "unlockType")
    self.unlockId = parse_number(row, "unlockId")
    self.unlockNum = parse_string(row, "unlockNum")
    self.text = ParseUtils.ParseLocalizeText(row, "text")
    self.cardSkinId = parse_number(row, "cardSkinId")
    self.cup = ParseUtils.ParsePathName(row, "cup")
    self.plate = ParseUtils.ParsePathName(row, "plate")
    self.status = parse_number(row, "status")

    self.animator = ParseUtils.ParsePathName(row, "animator")
    self.ear = parse_number(row, "ear")
    self.face = parse_number(row, "face")
    self.armLeft = parse_number(row, "armLeft")
    self.armRight = parse_number(row, "armRight")
    self.body = parse_number(row, "body")
end

function PhoneRoleVisitingVo:__delete()
    self.id = nil
    self.unlockType = nil
    self.unlockId = nil
    self.unlockNum = nil
    self.text = nil
    self.cardSkinId = nil
    self.cup = nil
    self.plate = nil
    self.status = nil
    self.ear = nil              --盘子模型
    self.face = nil              --盘子模型
    self.armLeft = nil              --盘子模型
    self.armRight = nil              --盘子模型
    self.body = nil              --盘子模型
end

return PhoneRoleVisitingVo
