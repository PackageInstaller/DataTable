---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2023-12-08 17:46:34
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class AccusationVo
local AccusationVo = Class("AccusationVo", SettingRefBase)

---__int
function AccusationVo:__init()
    self.id = nil                --编号
    self.type = nil              --分享类型
    self.isHideOutOfChat = nil   --是否隐藏(有一些从其他界面点击过来的不显示在举报界面)
    self.status = nil            --状态
    self.isChecked = false       -- 选中状态
end

---FillVo
---@param row TableML.TableFileRow
function AccusationVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.isHideOutOfChat = parse_number(row, "isHideOutOfChat") == 1
    self.type = ParseUtils.ParseLocalizeText(row, "type")
    self.status = parse_number(row, "status")
end

function AccusationVo:__delete()
    self.id = nil
    self.isHideOutOfChat = nil
    self.type = nil
    self.status = nil
    self.isChecked = nil
end

function AccusationVo:SwitchCheckState()
    self.isChecked = not self.isChecked
    Events.Broadcast("UPDATE_ACCUSATION_CELL_CHECK_STATUES", {id=self.id, text = self.type, isChecked=self.isChecked})
end

return AccusationVo
