---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2021-02-25 18:38:58
--- PT活动副本事件点表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class BambooEventVo
local BambooEventVo = Class("BambooEventVo", SettingRefBase)

---__int
function BambooEventVo:__init()
    self.id = nil              --索引（副本ID*100+事件点编号）
    self.chapter = nil              --章节ID
    self.type = nil              --事件点类型（0无，1初始点，2关卡，3终点，4状态）
    self.wave = nil              --对应波数（0为默认存在）
    self.ceilId = nil              --所在单元格
    self.clearTag = nil              --完成标志（1记录）
    self.unlockNode = nil              --前置解锁事件点
    self.unlockDesc = nil              --未解锁提示
    self.regionId = nil              --对应关卡ID（无则表示非战斗点）
    self.addStatus = nil              --完成事件点获得的状态
    self.addWave = nil              --完成事件点后增加的波数计数值
    self.model = nil              --单元格对应地表物件
    self.tips = nil              --弹窗提示文本
    self.tigger = nil              --完成事件点后触发的事件
    self.openConfirm = nil              --是否需要二次确认（0不需要，1需要）
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function BambooEventVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.chapter = parse_number(row, "chapter")
    self.type = parse_number(row, "type")
    self.wave = parse_number(row, "wave")
    self.clearTag = parse_number(row, "clearTag")
    self.ceilId = parse_string(row, "ceilId")
    self.unlockNode = parse_string(row, "unlockNode")
    self.addStatus = parse_string(row, "addStatus")
    self.regionId = parse_number(row, "regionId")
    self.addWave = parse_number(row, "addWave")
    self.tigger = parse_number(row, "tigger")
    self.model = parse_number(row, "model")
    self.tips = ParseUtils.ParseLocalizeText(row, "tips")
    self.unlockDesc = ParseUtils.ParseLocalizeText(row, "unlockDesc")
    self.openConfirm = parse_number(row, "openConfirm")
    self.status = parse_number(row, "status")
end

function BambooEventVo:__delete()
    self.id = nil
    self.chapter = nil
    self.type = nil
    self.wave = nil
    self.ceilId = nil
    self.regionId = nil
    self.addWave = nil
    self.model = nil
    self.tips = nil
    self.openConfirm = nil
    self.status = nil
    self.clearTag = nil    
    self.unlockNode = nil     
    self.unlockDesc = nil              --未解锁提示   
    self.addStatus = nil              --完成事件点获得的状态       
end

return BambooEventVo
