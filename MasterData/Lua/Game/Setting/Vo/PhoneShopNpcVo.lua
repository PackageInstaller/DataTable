---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2020-10-26 14:09:24
--- NPC配置表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class PhoneShopNpcVo
local PhoneShopNpcVo = Class("PhoneShopNpcVo", SettingRefBase)

---__int
function PhoneShopNpcVo:__init()
    self.id = nil              --编号
    self.npcId = nil              --npc编号
    self.text = nil              --台词文本
    self.title = nil              --台词文本
    self.scene = nil              --对应场景：1开场白，2选择送礼对象，3选择礼物档次，4确认礼物，5选择礼物包装，6确认订单，7派送订单，8签收订单
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function PhoneShopNpcVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.npcId = parse_number(row, "npcId")
    self.text = ParseUtils.ParseLocalizeText(row, "text")
    self.title = ParseUtils.ParseLocalizeText(row, "title")
    self.scene = parse_number(row, "scene")
    self.status = parse_number(row, "status")
end

function PhoneShopNpcVo:__delete()
    self.id = nil
    self.npcId = nil
    self.text = nil
    self.title = nil              --台词文本
    self.scene = nil
    self.status = nil
end

return PhoneShopNpcVo
