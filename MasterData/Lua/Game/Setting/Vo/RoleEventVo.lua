---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2021-08-30 17:01:19
--- 看板娘事件表

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class RoleEventVo
local RoleEventVo = Class("RoleEventVo", SettingRefBase)

---__int
function RoleEventVo:__init()
    self.id = nil              --事件ID
    self.roleId = nil              --对应角色编号
    self.type = nil              --事件类型（见枚举）
    self.shape = nil              --角色外形（0人形，1猫形）
    self.unlockType = nil              --开启条件（对应解锁条件表里的数值）
    self.unlockId = nil              --开启条件ID
    self.unlockNum = nil              --开启条件数值
    self.position = nil              --事件发生的位置
    self.timelinePath = nil              --timelinePath
    self.cardId = nil              --卡牌ID（不填即为通用）
    self.targetNum = nil              --好感等级
    self.given = nil              --特定日期（配置节日的日期，不配为不走节日）1.七夕 2.春节 3.端午 4.中秋 5.玩家生日
    self.weight = nil              --事件权重
    self.endCall = nil              --结束时调用事件ID
    self.scEndCall = nil              --辅助角色结束后衔接事件
    self.endTime = nil              --事件倒计时（仅类型8有效）
    self.targetPosition = nil              --目标地点（type9有效）
    self.bubbleBox = nil              --事件对应气泡框类型
    self.ifEnd = nil              --事件对应气泡框类型
    self.eventText = nil              --事件文本（只有事件类型3需要度这个字段，并且出现文字）
    self.voiceId = nil              --事件对应气泡框类型
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function RoleEventVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.roleId = parse_number(row, "roleId")
    --self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.type = parse_number(row, "type")
    self.shape = parse_number(row, "shape")
    self.unlockType = parse_string(row, "unlockType")
    self.unlockId = parse_number(row, "unlockId")
    self.unlockNum = parse_string(row, "unlockNum")
    self.position = parse_number(row, "position")
    self.timelinePath = ParseUtils.ParsePathName(row, "timelinePath")
    self.cardId = parse_number(row, "cardId")
    self.targetNum = parse_string(row, "targetNum")
    self.given = parse_string(row, "given")
    self.weight = parse_number(row, "weight")
    self.endCall = parse_string(row, "endCall")
    self.scEndCall = parse_string(row, "scEndCall")
    self.endTime = parse_number(row, "endTime")
    self.targetPosition = parse_number(row, "targetPosition")
    self.bubbleBox = parse_number(row, "bubbleBox")
    self.ifEnd = parse_number(row, "ifEnd")
    self.eventText = parse_localizeText(row, "eventText")
    self.voiceId = parse_string(row, "voiceId")
    self.status = parse_number(row, "status")
end

function RoleEventVo:__delete()
    self.id = nil
    self.roleId = nil
    self.name = nil
    self.type = nil
    self.shape = nil
    self.unlockType = nil
    self.unlockId = nil
    self.unlockNum = nil
    self.position = nil
    self.timelinePath = nil
    self.cardId = nil
    self.targetNum = nil
    self.given = nil
    self.weight = nil
    self.endCall = nil
    self.scEndCall = nil
    self.endTime = nil
    self.targetPosition = nil
    self.bubbleBox = nil        
    self.ifEnd = nil
    self.eventText = nil   
    self.voiceId = nil                      
    self.status = nil
end

return RoleEventVo
