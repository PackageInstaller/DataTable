---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2024-03-22 11:36:03
--- 战略强化属性枚举表
---

local parse_number = parse_number
local parse_localizeText = parse_localizeText

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class HomelandTalentEnumVo
local HomelandTalentEnumVo = Class("HomelandTalentEnumVo", SettingRefBase)

---__int
function HomelandTalentEnumVo:__init()
    self.attAddTypeId = nil              --效果枚举id
    self.attributeId = nil              --对应的属性加成定义表的id
    self.isPercentage = nil              --1为百分比
    self.attAddType = nil              --属性加成类型(1、固定值加减；2、百分比加减  )
    self.showNumType = nil              --客户端显示类型（1，固定值显示；2，百分比显示）
    self.showNumType2 = nil              --客户端计算正负值（1，正，2，负）
end

---FillVo
---@param row TableML.TableFileRow
function HomelandTalentEnumVo:FillVo(row)
    base.FillVo(self,row)
    self.attAddTypeId = parse_number(row, "attAddTypeId")
    self.attributeId = parse_number(row, "attributeId")
    self.isPercentage = parse_number(row, "isPercentage")
    self.attAddType   = parse_number(row, "attAddType")
    self.showNumType  = parse_number(row, "shownumType")
    self.showNumType2  = parse_number(row, "shownumType2")
end

function HomelandTalentEnumVo:__delete()
    self.attAddTypeId = nil
    self.attributeId = nil
    self.isPercentage = nil
    self.attAddType = nil
    self.showNumType = nil              
    self.showNumType2 = nil              
end

return HomelandTalentEnumVo
