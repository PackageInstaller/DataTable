---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by gaole.
--- DateTime: 2022-06-13 14:05:45
---

local SettingRefBase = require('Game.Setting.SettingRefBase')
local base = SettingRefBase
local ParseUtils = CS.ParseUtils

---@class MediumPTPlotVo
local MediumPTPlotVo = Class("MediumPTPlotVo", SettingRefBase)

---__int
function MediumPTPlotVo:__init()
    self.id = nil              --ID
    self.activityId = nil              --活动ID
    self.name = nil              --副本名
    self.regionLevel = nil              --难度等级
    self.type = nil              --剧情类型（1活动开始2活动中3结局）
    self.unlockItem = nil              --解锁道具（0为不消耗）
    self.unlockNum = nil              --解锁道具需求数量
    self.unlockPlot = nil              --前置剧情ID
    self.pos = nil              --入口位置（0为序章入口9为终章入口）
    self.endFlag = nil              --结束标志（0非1是）
    self.plotId = nil              --剧情ID（对应剧情梗概表）
    self.plotDesc = nil              --剧情入口台词
    --self.plotIcon = nil              --剧情入口小头像
    self.plotImage = nil              --剧情人物立绘
    self.status = nil              --状态
end

---FillVo
---@param row TableML.TableFileRow
function MediumPTPlotVo:FillVo(row)
    base.FillVo(self,row)
    self.id = parse_number(row, "id")
    self.activityId = parse_number(row, "activetyId")
    self.name = ParseUtils.ParseLocalizeText(row, "name")
    self.regionLevel = parse_number(row, "regionLevel")
    self.type = parse_number(row, "type")
    self.unlockItem = parse_number(row, "unlockItem")
    self.unlockNum = parse_number(row, "unlockNum")
    self.unlockPlot = parse_string(row, "unlockPlot")
    self.pos = parse_number(row, "pos")
    self.endFlag = parse_number(row, "endFlag")
    self.plotId = parse_number(row, "plotId")
    self.plotDesc = ParseUtils.ParseLocalizeText(row, "plotDesc")
    --self.plotIcon = ParseUtils.ParsePathName(row, "plotIcon")
    self.plotImage = ParseUtils.ParsePathName(row, "plotImage")
    self.status = parse_number(row, "status")
end

function MediumPTPlotVo:__delete()
    self.id = nil
    self.activityId = nil
    self.name = nil
    self.regionLevel = nil
    self.type = nil
    self.unlockItem = nil
    self.unlockNum = nil
    self.unlockPlot = nil
    self.pos = nil
    self.endFlag = nil
    self.plotId = nil
    self.plotDesc = nil
    --self.plotIcon = nil
    self.plotImage = nil
    self.status = nil
end

return MediumPTPlotVo
