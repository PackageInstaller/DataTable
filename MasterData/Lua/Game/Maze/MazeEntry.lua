---
--- Generated by gaole
--- Created by gaole.
--- DateTime: 2020/03/30 10:52 AM
---
---进入场景

---{
--- luaPath:string  必传 scene 入口脚本路径
---}

local util = require 'XLua.util'
local CoWait = CS.Engine.Lib.CoWait

------------ import ------------
------------ import ------------

------------ define ------------
------------ define ------------

---@class MazeEntry
local MazeEntry = Class("MazeEntry")

function MazeEntry:__init()
    
end

function MazeEntry:__delete()
    self._sceneEntry:__delete()
    self.controller = nil
end

function MazeEntry:Awake()
    local parameters = self.controller.Argument.parameters
    local luaPath    = parameters.luaPath
    self._sceneEntry = import(luaPath).New(parameters)
    --- New 会掉 __init
    if self._sceneEntry.Awake then
        self._sceneEntry:Awake()
    end
    
end

function MazeEntry:OnInitialize()
    ---@type Engine.Lib.CoWait
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        
        if self._sceneEntry.OnInitialize then
            self._sceneEntry:OnInitialize()
        end
        
    end))
    return coWait
end

function MazeEntry:OnShow()
    --- @type Engine.Lib.CoWait
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        if self._sceneEntry.OnShow then
            self._sceneEntry:OnShow()
        end

    end))
    return coWait
end

function MazeEntry:OnFocus(focus)
    if self._sceneEntry.OnFocus then
        self._sceneEntry:OnFocus(focus)
    end
end


function MazeEntry:OnHide()
    ---@type Engine.Lib.CoWait
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        if self._sceneEntry.OnHide then
            self._sceneEntry:OnHide()
        end
        
    end))
    return coWait
end

function MazeEntry:OnFinalize()
    local coWait = CoWait.Start():Coroutine(util.cs_generator(function()
        if self._sceneEntry.OnFinalize then
            self._sceneEntry:OnFinalize()
        end

        self.controller = nil --删除lua层引用到的c#对象 否则不会产生c# gc回收到对象
        self:Delete()
        self = nil --lua层的gc需要
    end))
    return coWait
end

return MazeEntry