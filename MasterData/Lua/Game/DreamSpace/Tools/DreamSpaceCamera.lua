---
--- Generated by zou hanjie
--- DateTime: 2023/06/30 09:28
---


-- 在以下两种 camera 中选择一个作为 默认的 mCamera 使用:
-- (1) 场景本地 main camera
-- (2) 游戏全局 main camera


---@class DreamSpaceCamera   
---@field localCamera       UnityEngine.Camera
local DreamSpaceCamera = Class('DreamSpaceCamera')
-- 外部访问时使用:
-- local DreamSpaceCamera = import('Game.DreamSpace.Tools.DreamSpaceCamera')

-- ========================================================


--- lua:
local cs_coroutine = require 'XLua.cs_coroutine' -- 协程
local Yielders = CS.Engine.Lib.Yielders -- 协程

---@type LuaParametersReader
local LuaParametersReader = import('Game.Utils.LuaParametersReader')

---:
local Object = CS.UnityEngine.Object
local GameObject = CS.UnityEngine.GameObject
local Transform = CS.UnityEngine.Transform
local Time = CS.UnityEngine.Time
local Camera = CS.UnityEngine.Camera


--- funtoy:
local BehaviourAction = CS.Engine.Lib.BehaviourAction
local KTool = CS.Engine.Lib.KTool
local LuaParameters = CS.Engine.Modules.LuaParameters
local LuaBehaviour = CS.Engine.Modules.LuaBehaviour

local DreamSpaceUtils = CS.DreamSpace.DreamSpaceUtils

local URPCameraController = CS.Game.Native.URP.URPCameraController


-- ========================================================


local LocalCamera = nil -- Camera


-- ========================================================


function DreamSpaceCamera:__init()
    self.controller = nil
end


function DreamSpaceCamera:__delete()
end



function DreamSpaceCamera:Awake()
    assert( self.localCamera )
    if LocalCamera == nil then 
        LocalCamera = self.localCamera
    else 
        assert( false, "不允许存在 第二个 DreamSpaceCamera 实例" )
    end 
end



function DreamSpaceCamera:OnDestroy()
    LocalCamera = nil
    self.controller = nil --删除lua层引用到的c#对象 否则不会产生c# gc回收到对象
    self:Delete()
end

-- =========================================== Self ================================================ --



---@return UnityEngine.Camera
function DreamSpaceCamera.GetMainCamera()

    -- local isUseGlobal = true
    -- if isUseGlobal == true then 
    --     -- ===== 方案-1-, 使用全局 main camera ======
    --     return DreamSpaceUtils.GetMainCamera()
    -- else 
    --     -- ===== 方案-2-, 使用本场景的 main camera ======
    --     assert( LocalCamera )
    --     return LocalCamera
    -- end

    if isNotNull(URPCameraController.Instance) and isNotNull(URPCameraController.Instance.mainCamera) then 
        if URPCameraController.Instance.mainCamera.isActiveAndEnabled == false then 
            KTool.SetActive(URPCameraController.Instance.mainCamera, true)
        end 
        return URPCameraController.Instance.mainCamera
    end 
    printError("没找到 URPCameraController mainCamera ")

    return Camera.main
end





return DreamSpaceCamera

