---
--- Generated by zou hanjie
--- DateTime: 2024/02/22 09:28
---

-- 有些 ui 界面先被放在场景中, 然后它们和 "场景功能" 相互引用, 这些 ui界面 被临时成为 "黏着canvas"
-- 等 RoleControllerRootCanvasMgr 初始化后, 本代码再把这些 ui界面 移动到 "RoleControllerRootCanvas" 中去



---@class StickyCanvasMgr
---@field tgtCanvas       UnityEngine.Canvas
local StickyCanvasMgr = Class('StickyCanvasMgr')
-- 外部访问时使用:
-- local StickyCanvasMgr = import('Game.DreamSpace.UI.StickyCanvasMgr')


-- ========================================================

--- lua:

local cs_coroutine = require 'XLua.cs_coroutine' -- 协程
local Yielders = CS.Engine.Lib.Yielders -- 协程

local DreamSpaceLuaUtils = import('Game.DreamSpace.Tools.DreamSpaceLuaUtils')
---:
local Object = CS.UnityEngine.Object
local GameObject = CS.UnityEngine.GameObject
local Canvas = CS.UnityEngine.Canvas
local Transform = CS.UnityEngine.Transform
local Button = CS.UnityEngine.UI.Button
local RectTransform = CS.UnityEngine.RectTransform
local Vector3 = CS.UnityEngine.Vector3
local Vector2 = CS.UnityEngine.Vector2


--- funtoy:
local BehaviourAction = CS.Engine.Lib.BehaviourAction
local KTool = CS.Engine.Lib.KTool


local DreamSpaceUtils = CS.DreamSpace.DreamSpaceUtils
local UIRootCanvas = CS.Engine.UI.UIRootCanvas

local ETCJoystick = CS.ETCJoystick



-- ========================================================


function StickyCanvasMgr:__init()
    self.controller = nil
    self.co_1 = nil   -- 发送携程请求

    self.isInit = false
end


function StickyCanvasMgr:__delete()
    self.co_1 = nil
end





function StickyCanvasMgr:Start()
    KTool.SetActive(self.controller.gameObject, true)

    assert( self.tgtCanvas  )

    DreamSpaceLuaUtils.StopCoroutine( self.co_1 )
    self.co_1 = cs_coroutine.start(self.DelayInit, self)
end


function StickyCanvasMgr:DelayInit() 

    local RoleControllerRootCanvasMgr = import('Game.DreamSpace.UI.RoleControllerRootCanvasMgr'):GetInstance()
    RoleControllerRootCanvasMgr:CheckOrInitSync()
    local parentRectTF = RoleControllerRootCanvasMgr.rootCanvas:rectTransform()
    ---
    self.tgtCanvas.name = "DreamSpaceUI"

    DreamSpaceLuaUtils.AlignRectTransform( self.tgtCanvas:rectTransform(), parentRectTF )
    ---
    self.isInit = true
end


function StickyCanvasMgr:OnDestroy()
    ---
    DreamSpaceLuaUtils.StopCoroutine( self.co_1 )
    self.co_1 = nil
    ---
    self.controller = nil --删除lua层引用到的c#对象 否则不会产生c# gc回收到对象
    self:Delete()
end

-- =========================================== Self ================================================ --









-- =========================================== Static ================================================ --







return StickyCanvasMgr
