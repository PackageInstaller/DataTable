---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by liuzhipeng.
--- DateTime: 2024/7/23 18:09
--- Description: 
---

---@class JapaneseStoryHomeDojo
local JapaneseStoryHomeDojo = Class("JapaneseStoryHomeDojo")

function JapaneseStoryHomeDojo:__init()
    self.entranceConf   = {} -- 功能入口配置
    self.scheduleConf   = {} -- 排期配置
    self.mainQuestMap   = {} -- 主线关卡地图
    self.mainQuestId    = 0  -- 最新通关的主线关卡ID
    self.taskTips       = -1 -- 任务小红点
    self.bossTips       = -1 -- BOSS小红点
    self.achievementTaskTips = -1 -- 成就小红点
    self.lastQuestId    = 0
    self.battleCardTips = -1 -- 战牌小红点
end

function JapaneseStoryHomeDojo:__delete()
    self.entranceConf   = nil
    self.scheduleConf   = nil
    self.mainQuestMap   = nil
    self.mainQuestId    = nil
    self.taskTips       = nil
    self.bossTips       = nil
    self.lastQuestId    = nil
    self.battleCardTips = nil
    self.achievementTaskTips = nil
end

---Update
---更新数据
---@param jsonData table
function JapaneseStoryHomeDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData,key) and table.haskey(self,key) then
            self[key] = value
        end
    end

    -- 更新战牌入口红点
    local hasBattleCardTips = checkInt(jsonData.battleCardTips) == 0
    GameUtils.SetRedPointNum(Constants.RedPointConst.BattleCardAlbum, hasBattleCardTips and 1 or 0)
end

return JapaneseStoryHomeDojo