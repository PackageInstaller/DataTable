---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by liuzhipeng.
--- DateTime: 2022/8/30 14:50
---

---@class ActivityNightCatHomeDojo
local ActivityNightCatHomeDojo = Class("ActivityNightCatHomeDojo")

function ActivityNightCatHomeDojo:__init()
    self.activityConf     = {} -- 活动配置信息
    self.plotInfo         = {} -- key 章节id value 通关的剧情关卡id
    self.unlockPlotInfo   = {} -- key plotId value 1 已解锁 2 已通关
    self.clearanceRewards = 0  -- 通关奖励是否领取 1 已领取 0 未领取
    self.climbingResetTtl = 0  -- 爬塔倒计时
    self.climbingInfo     = {} -- 爬塔信息
    self.startTime        = 0  -- 活动开启时间
    self.endTime          = 0  -- 活动结束时间
    self.isBossUnlock     = false  -- BOSS是否开启
    self.climbingResetNum = 0   --- 爬塔重置次数
    self.limit            = 0   --- 上限次数
    self.nextHpSeconds    = 0   --- 下次恢复秒数
    self.recoverSeconds   = 0   --- 总恢复需要秒数
end

function ActivityNightCatHomeDojo:__delete()
    self.activityConf     = nil
    self.plotInfo         = nil
    self.unlockPlotInfo   = nil
    self.clearanceRewards = nil
    self.climbingResetTtl = nil
    self.climbingInfo     = nil
    self.startTime        = nil
    self.endTime          = nil
    self.isBossUnlock     = nil
    self.climbingResetNum = nil
    self.limit            = nil
    self.nextHpSeconds    = nil
    self.recoverSeconds   = nil
end

---Update
---更新数据
---@param jsonData table
function ActivityNightCatHomeDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData,key) and table.haskey(self,key) then
            self[key] = value
        end
    end
end

return ActivityNightCatHomeDojo