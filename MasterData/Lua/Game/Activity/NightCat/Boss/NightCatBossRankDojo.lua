---
--- Generated by shentayibu
--- Created by shentayibu.
--- DateTime: 2023/11/1 14:36
---

---@class NightCatBossRankDojo
local NightCatBossRankDojo = Class("NightCatBossRankDojo")

function NightCatBossRankDojo:__init()
    self.playerRank = {
        hurt = 0,
        ranking = 0
    }
    self.rankInfo = {}
    self.percentageRank = 0
end

function NightCatBossRankDojo:__delete()
    self.playerRank = nil
    self.rankInfo = nil
    self.percentageRank = nil
end

---Update
---更新数据
---@param jsonData table
function NightCatBossRankDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData, key) and table.haskey(self, key) then
            self[key] = value
        end
    end
    for i, rankInfo in ipairs(self.rankInfo) do
        rankInfo.rankNum = i
    end
end

return NightCatBossRankDojo