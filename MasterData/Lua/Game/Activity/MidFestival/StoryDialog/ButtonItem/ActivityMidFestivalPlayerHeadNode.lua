---
--- Generated by shentayibu
--- Created by shentayibu.
--- DateTime: 2023/11/28 14:32
---

--region import
local util = require 'XLua.util'
local cs_coroutine = require 'XLua.cs_coroutine'
local CriWareUtils = import('Game.Entry.CriWareUtilsFix')
--endregion import

--region define
local KTool = CS.Engine.Lib.KTool
local CoWait = CS.Engine.Lib.CoWait
local Animator = CS.UnityEngine.Animator
local UIModule = CS.Engine.UI.UIModule
local Yielders = CS.Engine.Lib.Yielders
local WaitForSeconds = CS.UnityEngine.WaitForSeconds
local Debug = CS.UnityEngine.Debug
local UIRaycastBlocker = CS.Engine.UI.UIRaycastBlocker.Instance
local CDTimerModule = CS.Engine.Modules.CDTimerModule
--endregion define


--- Behavior生命周期：__init -> Awake -> Start -> OnDestroy -> __delete


--- from: Assets/BundleResources/Prefabs/ActivityMidFestival/12000201/ActivityMidFestivalStory12000201PlayerHeadNode.prefab > name: ActivityMidFestivalStory12000201PlayerHeadNode
---@class ActivityMidFestivalPlayerHeadNode
---@field Env                           	ActivityMidFestivalPlayerHeadNode       
---@field controller                    	Engine.Modules.LuaBehaviour             
---@field ImgIcon                       	UnityEngine.RectTransform               
local ActivityMidFestivalPlayerHeadNode = Class('ActivityMidFestivalPlayerHeadNode')

--region lifecycle

function ActivityMidFestivalPlayerHeadNode:__init()
    self.events = {}
    self.Constants = {
        Gender = {
            MALE = 1,
            FEMALE = 2
        }
    }
end

function ActivityMidFestivalPlayerHeadNode:__delete()
    self.events = nil
end

function ActivityMidFestivalPlayerHeadNode:Awake()
end

function ActivityMidFestivalPlayerHeadNode:Start()

end

function ActivityMidFestivalPlayerHeadNode:OnDestroy()
    for k, v in pairs(self.events) do
        Events.RemoveListener(k, v)
    end

    self.controller = nil --删除lua层引⽤到的c#对象否则不会产⽣c# gc回收到对象
    self:Delete()
    self = nil --lua层的gc需要
end
--endregion lifecycle

--region init
--endregion init

--region handler
--endregion handler

--region public
function ActivityMidFestivalPlayerHeadNode:Refresh()
end

function ActivityMidFestivalPlayerHeadNode:SetGender(gender)
    local flag = self.Constants.Gender.FEMALE
    if checkNumber(gender) == Constants.PLAYER_GENDER.MALE then
        flag = self.Constants.Gender.MALE
    end
    CfUtils.SetUISwitchImage(self.ImgIcon, flag)
end
--endregion public

--region private
--endregion private

--region get / set
--endregion get / set

return ActivityMidFestivalPlayerHeadNode
