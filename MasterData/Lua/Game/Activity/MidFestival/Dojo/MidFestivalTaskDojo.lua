---
--- Generated by shentayibu
--- Created by shentayibu.
--- DateTime: 2023/11/15 13:56
---

---@type MidFestivalTaskStruct
local MidFestivalTaskStruct = import("Game.Activity.MidFestival.TaskDialog.Struct.MidFestivalTaskStruct")

---@class MidFestivalTaskDojo
local MidFestivalTaskDojo = Class("MidFestivalTaskDojo")

function MidFestivalTaskDojo:__init()
    self.task = {}
    self.taskTimeLeft = 0
end

function MidFestivalTaskDojo:__delete()
    self.task = nil
    self.taskTimeLeft = nil
end

---Update
---更新数据
---@param jsonData table
function MidFestivalTaskDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData, key) and table.haskey(self, key) then
            if key == "task" then
                local tasks = {}
                for i, task in ipairs(value) do
                    ---@type MidFestivalTaskStruct
                    local taskStruct = MidFestivalTaskStruct.New()
                    taskStruct:Fill(task)
                    table.insert(tasks, taskStruct)
                end
                self[key] = tasks
            else
                self[key] = value
            end
        end
    end
end

return MidFestivalTaskDojo