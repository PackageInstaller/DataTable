---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by liuzhipeng.
--- DateTime: 2024/4/19 16:47
--- Description: 
---

--region import
---@type DreamAdventuresMgr
local DreamAdventuresMgr = import("Game.Activity.DreamAdventures.DreamAdventuresMgr"):GetInstance()
--endregion import

--region define
--endregion define

--- from: Assets/BundleResources/Prefabs/ActivityDreamAdventures12000801/ActivityDreamAdventuresPrepareMain.prefab > name: BtnDish
---@class DreamAdventuresPreparationMaterialNode
---@field Env                           	DreamAdventuresPreparationMaterialNode  
---@field controller                    	Engine.Modules.LuaBehaviour             
---@field ImgLight                      	UnityEngine.RectTransform               	@ 0    
---@field ImgIcon                       	UnityEngine.RectTransform               	@ 1    
---@field TitleLayout                   	UnityEngine.RectTransform               	@ 2    
---@field ItemImgIcon                   	UnityEngine.RectTransform               	@ 3    
---@field TextName                      	UnityEngine.RectTransform               	@ 4    
---@field ImgGet                        	UnityEngine.RectTransform               	@ 5    
---@field lock                          	UnityEngine.RectTransform               	@ 6    
local DreamAdventuresPreparationMaterialNode = Class("DreamAdventuresPreparationMaterialNode")

--region lifecycle
function DreamAdventuresPreparationMaterialNode:__init()

end

function DreamAdventuresPreparationMaterialNode:__delete()

end

function DreamAdventuresPreparationMaterialNode:Awake()
    SetButtonAction(self.controller, Bind(self, self.OnClickHandler))
end

function DreamAdventuresPreparationMaterialNode:Start()

end

function DreamAdventuresPreparationMaterialNode:OnDestroy()
    self.controller = nil --删除lua层引用到的c#对象 否则不会产生c# gc回收到对象
    self:Delete()
    self = nil --lua层的gc需要
end
--endregion lifecycle

--region handler
function DreamAdventuresPreparationMaterialNode:OnClickHandler()
    if self._cb then
        self._cb(self._index)
    end
end
--endregion handler

--region public
---刷新node
---@param conf table
---@param index number
---@param cb function
function DreamAdventuresPreparationMaterialNode:RefreshNode(conf, index, cb)
    self._conf = conf
    self._index = index
    self._cb = cb
    
    if DreamAdventuresMgr:CheckMaterialUnlock(conf.id) then
        CfUtils.SetActive(self.lock, false)
        CfUtils.SetActive(self.ImgLight, true)
        CfUtils.FillImage(self.ImgIcon, conf.icon)
        CfUtils.FillText(self.TextName, conf.name)
        CfUtils.SetActive(self.ItemImgIcon, false)
    else
        CfUtils.SetActive(self.lock, true)
        CfUtils.SetActive(self.ImgLight, false)
        CfUtils.FillText(self.TextName, conf.point)
        CfUtils.SetActive(self.ItemImgIcon, true)
    end
end

---设置选中状态
function DreamAdventuresPreparationMaterialNode:SetSelected(isSelected)
    CfUtils.SetActive(self.ImgGet, isSelected)
end

--endregion public

--region private

--endregion private

return DreamAdventuresPreparationMaterialNode