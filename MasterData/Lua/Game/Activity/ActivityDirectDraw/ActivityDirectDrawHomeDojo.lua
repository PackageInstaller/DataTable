---
--- Generated by shentayibu
--- Created by shentayibu.
--- DateTime: 2023/10/23 10:07
---



---@class ActivityDirectDrawHomeDojo
local ActivityDirectDrawHomeDojo = Class("ActivityDirectDrawHomeDojo")

function ActivityDirectDrawHomeDojo:__init()
    self.point = 0              --- 抽卡积分
    self.poolInfo = {}          --- 奖池信息
    self.playerChoseCardId = 0  --- 已经选择的卡牌id
    self.choseCardIds = {}      --- 可以选择的卡牌id
    self.exchange = {}          --- 兑换道具
    self.bgImage = nil          --- 背景图
end

function ActivityDirectDrawHomeDojo:__delete()
    self.point = nil
    self.poolInfo = nil
    self.playerChoseCardId = nil
    self.choseCardIds = nil
    self.exchange = nil
    self.bgImage = nil
end

---Update
---更新数据
---@param jsonData table
function ActivityDirectDrawHomeDojo:Fill(jsonData)
    jsonData = checkTable(jsonData)
    for key, value in pairs(jsonData) do
        --只更新自己关心的数据
        if isSet(jsonData, key) and table.haskey(self, key) then
            self[key] = value
        end
    end
end

return ActivityDirectDrawHomeDojo