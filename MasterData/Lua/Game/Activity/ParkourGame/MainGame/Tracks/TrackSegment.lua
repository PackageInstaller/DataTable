---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yunchuan.
--- DateTime: 2024/6/6 17:21
---

---@class TrackSegment
local TrackSegment = Class("TrackSegment")


local Vector3 = CS.UnityEngine.Vector3
local Mathf = CS.UnityEngine.Mathf


--[[
    在 track local coord 中, 每个 trackSegment 都是 静态的, pos对齐的, 详见 Track:_InitSegments();  (当然, 在 pool 里这些实例会被不停复用)
    TrackSegment.localPosition 就是 track local coord 中的 pos;

    创建一开始, 第一个 TrackSegment localpos.x 为 0, 第二个为 16, 第三个为 32 依次排列

    tracks[3] 的 length 为 16, 恰好是一个屏幕的宽度;

]]--

function TrackSegment:__init( gameobject_, length_ )
    -- 1 based; 随着地图的展开(不断向右), 每个 idx 都是最新值(一个更大的值)
    self.segmentIndex = 0 
    self.length = length_
    self.gameobject = gameobject_
    self.transform = gameobject_.transform
end

function TrackSegment:__delete()
    self.gameobject = nil
    self.transform = nil
end

function TrackSegment:Reset()
    self.segmentIndex = 0
end

function TrackSegment:GetStartPosition()
    return self.transform.localPosition.x
end

function TrackSegment:GetEndPosition()
    return self.transform.localPosition.x + self.length
end

function TrackSegment:SetPos(x_)
    self.transform.localPosition = Vector3(x_,0,0)
end

function TrackSegment:Move(x_)
    self.transform.localPosition = self.transform.localPosition + Vector3(x_,0,0)
end

function TrackSegment:RefreshContent()

end

function TrackSegment:OnOutRange()

end

return TrackSegment