---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by yunchuan.
--- DateTime: 2024/6/12 14:33
---

---@type ParkourGameUtil
local ParkourGameUtil = import("Game.Activity.ParkourGame.ParkourGameUtil")
---@type ParkourGameMgr
local Mgr = import("Game.Activity.ParkourGame.ParkourGameMgr"):GetInstance()

local LuaParameters = CS.Engine.Modules.LuaParameters
---@type LuaParametersReader
local LuaParametersReader = import('Game.Utils.LuaParametersReader')

---@class SpikeTrap:TrackTriggerItem
local SpikeTrap = Class("SpikeTrap", import("Game.Activity.ParkourGame.MainGame.TrackItems.TrackTriggerItem"))


-- ==============================


function SpikeTrap:Start()
    self.super.Start(self)

    local luaParams = CfUtils.GetOrAddComponent(self.controller.gameObject, typeof(LuaParameters))
    self.id = LuaParametersReader.Read(luaParams, "id", LuaParametersReader.Type.Int) -- 81001 这种

end


function SpikeTrap:Type()
    return ParkourGameUtil.TrackItemType.SpikeTrap
end

function SpikeTrap:PlayLogic(other)
    local isPlayer = other.name == Mgr.player.controller.gameObject.name
    if isPlayer == false then 
        return
    end 
    --printError("尖刺 = " .. tostring(self.id) .. "; pos = " .. tostring(self.transform.position:ToString())) 
    local player = Mgr.player
    if player:OnHit(self.Type()) then
        player:SetRoleFX( "fx_role_injured_jingji", true )
    end
end

function SpikeTrap:PlayParticle()
    --Mgr.gameRunner.particleMgr:PlayParticle(ParkourGameUtil.VfxPath.LevelUp, Mgr.player.controller.transform,1)
end

return SpikeTrap