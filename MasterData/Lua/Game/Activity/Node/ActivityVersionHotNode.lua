---
--- Generated by shentayibu
--- Created by shentayibu.
--- DateTime: 2023/12/5 17:43
---

--region import
local util = require 'XLua.util'
local cs_coroutine = require 'XLua.cs_coroutine'
local CriWareUtils = import('Game.Entry.CriWareUtilsFix')
--endregion import

--region define
local KTool = CS.Engine.Lib.KTool
local CoWait = CS.Engine.Lib.CoWait
local Animator = CS.UnityEngine.Animator
local UIModule = CS.Engine.UI.UIModule
local Yielders = CS.Engine.Lib.Yielders
local WaitForSeconds = CS.UnityEngine.WaitForSeconds
local Debug = CS.UnityEngine.Debug
local UIRaycastBlocker = CS.Engine.UI.UIRaycastBlocker.Instance
local CDTimerModule = CS.Engine.Modules.CDTimerModule
--endregion define


--- Behavior生命周期：__init -> Awake -> Start -> OnDestroy -> __delete


--- from: Assets/BundleResources/Prefabs/Activity/ActivityVersionDialog11000301.prefab > name: ActivityVersionDialog11000301
---@class ActivityVersionHotNode
---@field Env                           	ActivityVersionHotNode                  
---@field controller                    	Engine.Modules.LuaBehaviour             
---@field BtnMovie                      	UnityEngine.RectTransform               
local ActivityVersionHotNode = Class('ActivityVersionHotNode')

--region lifecycle

function ActivityVersionHotNode:__init()
    self.events = {}
end

function ActivityVersionHotNode:__delete()
    self.events = nil
end

function ActivityVersionHotNode:Awake()
    SetButtonAction(self.BtnMovie, Bind(self, self.OnBtnMovieClickHandler))
end

function ActivityVersionHotNode:Start()

end

function ActivityVersionHotNode:OnDestroy()
    for k, v in pairs(self.events) do
        Events.RemoveListener(k, v)
    end

    self.controller = nil --删除lua层引⽤到的c#对象否则不会产⽣c# gc回收到对象
    self:Delete()
    self = nil --lua层的gc需要
end
--endregion lifecycle

--region init
--endregion init

--region handler
function ActivityVersionHotNode:OnBtnMovieClickHandler()
    if string.isEmpty(self.activityDojo.link) then
        printError("URL为空：" .. checkString(self.activityDojo.link))
        return
    end
    ---@type SDKService
    local SDKService = import('Game.Entry.SDKService')
    if SDKService:GetInstance():IsCommonSDK() then
        ---@type UserComponent
        local UserComponent =  GameUtils.GetEntityCompByType(ECSComponentType.Constants.UserComponent)
        if  isNotNull(UserComponent) then
            CfUtils.OpenURL(string.format("%s=%s", self.activityDojo.link, UserComponent.userInfo.access_token))
        end
    else
        CfUtils.OpenURL(self.activityDojo.link)
    end
end
--endregion handler

--region public
---@param activityDojo ActivityDojo
function ActivityVersionHotNode:Refresh(activityDojo)
    self.activityDojo = activityDojo
end
--endregion public

--region private
--endregion private

--region get / set
--endregion get / set

return ActivityVersionHotNode
