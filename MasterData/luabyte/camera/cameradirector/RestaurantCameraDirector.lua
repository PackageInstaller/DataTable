local baseDirector = require "Camera.CameraDirector.BaseCameraDirector"
RestaurantCameraDirector = BaseClass("RestaurantCameraDirector", baseDirector)
local M = RestaurantCameraDirector

return RestaurantCameraDirector

