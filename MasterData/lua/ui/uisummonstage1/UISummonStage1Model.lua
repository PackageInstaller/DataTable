---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by win 10.
--- DateTime: 2019/5/24 16:58
---
---@class UISummonStage1Model
---@field resultList {reward:protocol.Reward, rewardResult:protocol.rewardResult[], isNew:boolean}[]
---@field otherRewards protocol.RewardResult[]
local UISummonStage1Model = BaseClass("UISummonStage1Model",UIBaseModel);
local base = UIBaseModel
local this = UISummonStage1Model

function this.OnCreate(self)
    base.OnCreate(self);
end

function this:OnEnable(info)
    if info == nil then info = {} end
    base.OnEnable(self)
    self.resultList = info.resultList or {}
    self.poolId = info.poolId
    self.briefItem = info.briefItem
    self.isConfirm = info.isConfirm
    self.skip = info.skip
    self.hideStage2 = info.hideStage2 -- 是否隐藏第二阶段
    self.otherRewards = info.otherRewards
    self:OnRefresh();
end

function this.OnRefresh(self)

end

function this.OnAddListener(self)
    base.OnAddListener(self);
end

function this.OnRemoveListener(self)
    base.OnRemoveListener(self);
end

function this.OnDisable(self)
    base.OnDisable(self);
    
end

function this.OnDestroy(self)
    base.OnDestroy(self);
end

return this;
