---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by win 10.
--- DateTime: 2019/4/22 9:52
---

local UIFightSoulIntensModel = BaseClass("UIFightSoulIntensModel",UIBaseModel);
local this = UIFightSoulIntensModel;
local base = UIBaseModel

function this.OnCreate(self)
    base.OnCreate(self);
end

function this.OnEnable(self,data)
    base.OnEnable(self);
    self:OnRefresh(data);
end

function this.OnRefresh(self,data)
    self.panel_id = (data ~= nil and {data.panel_id} or {self.panel_id})[1];
    --local card =  table.first(Game.Scene.Player:GetComponent("CardComponent").Cards);

    if data ~= nil then
        self.panel_id = data.panel_id;
        self.cur_card = data.cur_card or nil;
    else
        self.cur_card = self.cur_card or nil;
    end

    self.fight_soul_list = Game.Scene.Player:GetComponent("FightSoulComponent").FightSouls;
end

local function OnFightSoulChg(self)
    self:OnRefresh();
    self:UIBroadcast(UIMessageNames.ON_FIGHTSOUL_INFO_CHG);
end

--local function ON_Guide_Click(self,cid,num,isrun,minStep)
--    self:UIBroadcast(UIMessageNames.ON_Guide_Click,cid,num,isrun,minStep)
--end

function this.OnAddListener(self)
    base.OnAddListener(self);
    self:AddDataListener(DataMessageNames.ON_FIGHTSOUL_INFO_CHG,OnFightSoulChg);
    --self:AddDataListener(DataMessageNames.ON_Guide_Click,ON_Guide_Click)
end

function this.OnRemoveListener(self)
    base.OnRemoveListener(self);
    self:RemoveDataListener(DataMessageNames.ON_FIGHTSOUL_INFO_CHG);
    --self:RemoveDataListener(DataMessageNames.ON_Guide_Click)
end

function this.OnDisable(self)
    base.OnDisable(self);
end

function this.OnDestroy(self)
    base.OnDestroy(self);
end

return this