local ChangeTrapStateEffect = require("logic.scene.luaevent.effect.composed.changetrapstateeffect")
local effects = {
  [1] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "SwitchOff", 0.25)
        effect:SetCfgID(10100)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(1010000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [2] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "SwitchOn", 0.25)
        effect:SetCfgID(20200)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(2020000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [3] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(30300)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 9 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(30300)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 9 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
          effect:SetCfgID(3030000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (1)", false)
        effect:SetCfgID(30300)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (1)", false)
          effect:SetCfgID(3030000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (10)", false)
        effect:SetCfgID(30300)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (10)", false)
          effect:SetCfgID(3030000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (11)", false)
        effect:SetCfgID(30300)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (11)", false)
          effect:SetCfgID(3030000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(30303)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [4] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(40400)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 9 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(40400)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 9 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
          effect:SetCfgID(4040000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (1)", true)
        effect:SetCfgID(40400)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (1)", true)
          effect:SetCfgID(4040000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(40404)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [5] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(50500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(50500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(5050000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(50500)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(50505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [6] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(60600)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(60600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(6060000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(60600)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(60606)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [7] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(70700)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(70700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(7070000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(70700)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(70707)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [8] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(80800)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(80800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(8080000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(80800)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(80808)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [9] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(90900)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(90900)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
          effect:SetCfgID(9090000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (3)", false)
        effect:SetCfgID(90900)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (3)", false)
          effect:SetCfgID(9090000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (12)", false)
        effect:SetCfgID(90900)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (12)", false)
          effect:SetCfgID(9090000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (13)", false)
        effect:SetCfgID(90900)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (13)", false)
          effect:SetCfgID(9090000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(90909)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [10] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1001000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(1001000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
          effect:SetCfgID(100100000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (3)", true)
        effect:SetCfgID(1001000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (3)", true)
          effect:SetCfgID(100100000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(10010010)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [11] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1101100)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(1101100)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(110110000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (2)", false)
        effect:SetCfgID(1101100)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (2)", false)
          effect:SetCfgID(110110000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(11011011)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [12] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1201200)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(1201200)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(120120000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (2)", true)
        effect:SetCfgID(1201200)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (2)", true)
          effect:SetCfgID(120120000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (10)", false)
        effect:SetCfgID(1201200)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (10)", false)
          effect:SetCfgID(120120000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (1)", true)
        effect:SetCfgID(1201200)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (1)", true)
          effect:SetCfgID(120120000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (11)", false)
        effect:SetCfgID(1201200)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (11)", false)
          effect:SetCfgID(120120000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(12012012)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [13] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1301300)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(1301300)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(130130000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (4)", false)
        effect:SetCfgID(1301300)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (4)", false)
          effect:SetCfgID(130130000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(1301300)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(130130000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (9)", false)
        effect:SetCfgID(1301300)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (9)", false)
          effect:SetCfgID(130130000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(13013013)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [14] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1401400)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(1401400)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(140140000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (4)", true)
        effect:SetCfgID(1401400)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (4)", true)
          effect:SetCfgID(140140000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (9)", false)
        effect:SetCfgID(1401400)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (9)", false)
          effect:SetCfgID(140140000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (12)", false)
        effect:SetCfgID(1401400)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (12)", false)
          effect:SetCfgID(140140000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (13)", false)
        effect:SetCfgID(1401400)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (13)", false)
          effect:SetCfgID(140140000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(14014014)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [15] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1501500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(1501500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(150150000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (5)", false)
        effect:SetCfgID(1501500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (5)", false)
          effect:SetCfgID(150150000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(15015015)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [16] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1601600)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(1601600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(160160000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (5)", true)
        effect:SetCfgID(1601600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (5)", true)
          effect:SetCfgID(160160000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1601600)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [17] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1701700)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(1701700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(170170000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (6)", false)
        effect:SetCfgID(1701700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (6)", false)
          effect:SetCfgID(170170000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(1701700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(170170000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (8)", false)
        effect:SetCfgID(1701700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (8)", false)
          effect:SetCfgID(170170000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(17017017)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [18] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1801800)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(1801800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(180180000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (8)", false)
        effect:SetCfgID(1801800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (8)", false)
          effect:SetCfgID(180180000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (10)", false)
        effect:SetCfgID(1801800)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (10)", false)
          effect:SetCfgID(180180000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (1)", true)
        effect:SetCfgID(1801800)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (1)", true)
          effect:SetCfgID(180180000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (11)", false)
        effect:SetCfgID(1801800)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (11)", false)
          effect:SetCfgID(180180000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (14)", false)
        effect:SetCfgID(1801800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (14)", false)
          effect:SetCfgID(180180000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (15)", false)
        effect:SetCfgID(1801800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (15)", false)
          effect:SetCfgID(180180000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (6)", true)
        effect:SetCfgID(18018018)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (6)", true)
          effect:SetCfgID(1801801800)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(18018018)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [19] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1901900)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(1901900)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(190190000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (7)", false)
        effect:SetCfgID(1901900)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (7)", false)
          effect:SetCfgID(190190000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (14)", false)
        effect:SetCfgID(1901900)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (14)", false)
          effect:SetCfgID(190190000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (15)", false)
        effect:SetCfgID(1901900)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (15)", false)
          effect:SetCfgID(190190000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(19019019)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [20] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2002000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2002000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(200200000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (7)", true)
        effect:SetCfgID(2002000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (7)", true)
          effect:SetCfgID(200200000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(20020020)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [21] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2102100)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(2102100)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(210210000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(2102100)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 3 4]")
          root:Add("[trap 3 2]")
          root:Add("[trap 3 3]")
          root:Add("[trap 3 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2102100)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(21021021)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [22] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2202200)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(2202200)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(220220000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(2202200)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 1 2]")
          root:Add("[trap 1 3]")
          root:Add("[trap 1 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2202200)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(22022022)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [23] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2302300)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(2302300)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(230230000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(2302300)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 1 2]")
          root:Add("[trap 1 3]")
          root:Add("[trap 1 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateRemoveEventEffect(100092)
        effect:SetCfgID(2302300)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateRemoveEventEffect(100092)
          effect:SetCfgID(230230000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2302300)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(23023023)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [24] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2402400)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(2402400)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(240240000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(2402400)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 2 2]")
          root:Add("[trap 2 3]")
          root:Add("[trap 2 4]")
          root:Add("[trap 2 5]")
          root:Add("[trap 2 6]")
          root:Add("[trap 2 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2402400)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(24024024)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [25] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2502500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(2502500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(250250000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(2502500)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 2 2]")
          root:Add("[trap 2 3]")
          root:Add("[trap 2 4]")
          root:Add("[trap 2 5]")
          root:Add("[trap 2 6]")
          root:Add("[trap 2 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2502500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(25025025)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [26] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2602600)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(2602600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(260260000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 4 2]")
          root:Add("[trap 4 3]")
          root:Add("[trap 4 4]")
          root:Add("[trap 4 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(26026026)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [27] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2702700)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(2702700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(270270000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 4 2]")
          root:Add("[trap 4 3]")
          root:Add("[trap 4 4]")
          root:Add("[trap 4 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(27027027)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [28] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2802800)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(2802800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(280280000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 5 2]")
          root:Add("[trap 5 3]")
          root:Add("[trap 5 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(28028028)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [29] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2902900)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(2902900)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(290290000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 5 2]")
          root:Add("[trap 5 3]")
          root:Add("[trap 5 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(29029029)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [30] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3003000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3003000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(300300000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3003000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 6 2]")
          root:Add("[trap 6 3]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3003000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(30030030)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [31] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3103100)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3103100)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(310310000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3103100)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 6 2]")
          root:Add("[trap 6 3]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3103100)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(31031030)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [32] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3203200)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3203200)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(320320000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBGMEffect("play", 92)
        effect:SetCfgID(3203200)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("EventItem/t1_m_obj_22", true)
        effect:SetCfgID(3203200)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("EventItem/t1_m_obj_22", true)
          effect:SetCfgID(320320000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("EventItem/01", false)
        effect:SetCfgID(3203200)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("EventItem/01", false)
          effect:SetCfgID(320320000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3203200)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 7 2]")
          root:Add("[trap 7 3]")
          root:Add("[trap 7 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3203200)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(32032030)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [33] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3303300)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3303300)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(330330000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBGMEffect("play", 92)
        effect:SetCfgID(3303300)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("EventItem/t1_m_obj_22", false)
        effect:SetCfgID(3303300)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("EventItem/t1_m_obj_22", false)
          effect:SetCfgID(330330000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("EventItem/01", true)
        effect:SetCfgID(3303300)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("EventItem/01", true)
          effect:SetCfgID(330330000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3303300)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 7 2]")
          root:Add("[trap 7 3]")
          root:Add("[trap 7 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3303300)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(33033033)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [34] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3403400)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3403400)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(340340000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBGMEffect("play", 92)
        effect:SetCfgID(3403400)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("EventItem/t1_m_obj_23", true)
        effect:SetCfgID(3403400)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("EventItem/t1_m_obj_23", true)
          effect:SetCfgID(340340000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("EventItem/02", false)
        effect:SetCfgID(3403400)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("EventItem/02", false)
          effect:SetCfgID(340340000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3403400)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 8 2]")
          root:Add("[trap 8 3]")
          root:Add("[trap 8 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3403400)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(34034034)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [35] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3503500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3503500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(350350000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBGMEffect("play", 92)
        effect:SetCfgID(3503500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("EventItem/t1_m_obj_23", false)
        effect:SetCfgID(3503500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("EventItem/t1_m_obj_23", false)
          effect:SetCfgID(350350000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("EventItem/02", true)
        effect:SetCfgID(3503500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("EventItem/02", true)
          effect:SetCfgID(350350000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3503500)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 8 2]")
          root:Add("[trap 8 3]")
          root:Add("[trap 8 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3503500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(35035035)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [36] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3603600)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 9 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(3603600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 9 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
          effect:SetCfgID(360360000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (1)", true)
        effect:SetCfgID(3603600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (1)", true)
          effect:SetCfgID(360360000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(36036036)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [37] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3703700)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(3703700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
          effect:SetCfgID(370370000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (3)", true)
        effect:SetCfgID(3703700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (3)", true)
          effect:SetCfgID(370370000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(37037037)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [38] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3803800)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(3803800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(380380000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (7)", false)
        effect:SetCfgID(3803800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (7)", false)
          effect:SetCfgID(380380000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(38038038)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [39] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "GoUp", 0.25)
        effect:SetCfgID(3903900)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(390390000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [40] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "GoDown", 0.25)
        effect:SetCfgID(4004000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(400400000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [42] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "DoorOpen", 0.1)
        effect:SetCfgID(4204200)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(420420000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(3, 15, 1, 1, false)
        effect:SetCfgID(4204200)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(3, 15, 1, 1, false)
          effect:SetCfgID(420420000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateRemoveEventEffect(100627)
        effect:SetCfgID(4204200)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [43] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "DoorOpen", 0.1)
        effect:SetCfgID(4304300)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(430430000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(14, 11, 1, 1, false)
        effect:SetCfgID(4304300)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(14, 11, 1, 1, false)
          effect:SetCfgID(430430000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [44] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "DoorClose", 0.1)
        effect:SetCfgID(4404400)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(440440000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(3, 15, 1, 1, true)
        effect:SetCfgID(4404400)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(3, 15, 1, 1, true)
          effect:SetCfgID(440440000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [46] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "DoorClose", 0.1)
        effect:SetCfgID(4604600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(460460000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(14, 11, 1, 1, true)
        effect:SetCfgID(4604600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(14, 11, 1, 1, true)
          effect:SetCfgID(460460000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [47] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4704700)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(4704700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(470470000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(4704700)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 3 4]")
          root:Add("[trap 3 2]")
          root:Add("[trap 3 3]")
          root:Add("[trap 3 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(4704700)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4704700)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [48] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4804800)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(4804801)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(480480100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(4804802)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 11 2]")
          root:Add("[trap 11 3]")
          root:Add("[trap 11 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(4804804)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4804805)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [49] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4904900)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4904901)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(490490100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(4904902)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 11 2]")
          root:Add("[trap 11 3]")
          root:Add("[trap 11 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(4904904)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4904905)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [50] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5005000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(5005001)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(500500100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(5005002)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 12 2]")
          root:Add("[trap 12 3]")
          root:Add("[trap 12 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(5005004)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 12 5]")
          root:Add("[trap 12 6]")
          root:Add("[trap 12 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(5005006)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 12 8]")
          root:Add("[trap 12 9]")
          root:Add("[trap 12 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5005008)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5005009)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [51] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5105100)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(5105101)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(510510100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(5105102)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 12 2]")
          root:Add("[trap 12 3]")
          root:Add("[trap 12 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(5105104)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 12 5]")
          root:Add("[trap 12 6]")
          root:Add("[trap 12 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(5105106)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 12 8]")
          root:Add("[trap 12 9]")
          root:Add("[trap 12 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5105108)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5105109)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [52] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5205200)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(5205201)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(520520100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5205202)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5205203)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [53] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5305300)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(5305301)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(530530100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5305302)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5305303)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [54] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5405400)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(5405401)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(540540100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(5405402)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 17 3]")
          root:Add("[trap 17 4]")
          root:Add("[trap 17 5]")
          root:Add("[trap 17 7]")
          root:Add("[trap 17 8]")
          root:Add("[trap 17 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5405404)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5405405)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [55] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5505500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(5505501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(550550100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(5505502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 17 2]")
          root:Add("[trap 17 3]")
          root:Add("[trap 17 5]")
          root:Add("[trap 17 7]")
          root:Add("[trap 17 9]")
          root:Add("[trap 17 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5505504)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5505505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [56] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5605600)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 2, "", 0)
        effect:SetCfgID(5605601)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 2, "", 0)
          effect:SetCfgID(560560100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(5605602)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 17 3]")
          root:Add("[trap 17 4]")
          root:Add("[trap 17 5]")
          root:Add("[trap 17 7]")
          root:Add("[trap 17 8]")
          root:Add("[trap 17 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5605604)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5605605)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [57] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5705700)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 3, "", 0)
        effect:SetCfgID(5705701)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 3, "", 0)
          effect:SetCfgID(570570100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(5705702)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 17 2]")
          root:Add("[trap 17 3]")
          root:Add("[trap 17 5]")
          root:Add("[trap 17 7]")
          root:Add("[trap 17 9]")
          root:Add("[trap 17 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5705704)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5705705)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [62] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6206200)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(6206201)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(620620100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(6206202)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 20 2]")
          root:Add("[trap 20 3]")
          root:Add("[trap 20 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(6206204)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6206205)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [63] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6306300)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(6306301)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(630630100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(6306302)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 20 2]")
          root:Add("[trap 20 3]")
          root:Add("[trap 20 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(6306304)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6306305)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [64] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6406400)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(6406401)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(640640100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(6406402)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 21 2]")
          root:Add("[trap 21 3]")
          root:Add("[trap 21 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(6406404)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6406405)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [65] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6506500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(6506501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(650650100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(6506502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 21 2]")
          root:Add("[trap 21 3]")
          root:Add("[trap 21 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(6506504)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6506505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [66] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6606600)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(6606601)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(660660100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(6606602)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 22 2]")
          root:Add("[trap 22 3]")
          root:Add("[trap 22 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(6606604)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6606605)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [67] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6706700)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(6706701)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(670670100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(6706702)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 22 2]")
          root:Add("[trap 22 3]")
          root:Add("[trap 22 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(6706704)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6706705)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [68] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6806800)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(6806801)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(680680100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(6806802)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 23 2]")
          root:Add("[trap 23 3]")
          root:Add("[trap 23 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(6806804)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6806805)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [69] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6906900)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(6906901)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(690690100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(6906902)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 23 2]")
          root:Add("[trap 23 3]")
          root:Add("[trap 23 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(6906904)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6906905)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [70] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(7007000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(7007001)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(700700100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(7007002)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 24 3]")
          root:Add("[trap 24 4]")
          root:Add("[trap 24 5]")
          root:Add("[trap 24 7]")
          root:Add("[trap 24 8]")
          root:Add("[trap 24 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(7007004)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(7007005)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [71] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(7107100)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(7107101)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(710710100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(7107102)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 24 2]")
          root:Add("[trap 24 3]")
          root:Add("[trap 24 5]")
          root:Add("[trap 24 7]")
          root:Add("[trap 24 9]")
          root:Add("[trap 24 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(7107104)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(7107105)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [72] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(7207200)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 2, "", 0)
        effect:SetCfgID(7207201)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 2, "", 0)
          effect:SetCfgID(720720100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(7207202)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 24 3]")
          root:Add("[trap 24 4]")
          root:Add("[trap 24 5]")
          root:Add("[trap 24 7]")
          root:Add("[trap 24 8]")
          root:Add("[trap 24 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(7207204)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(7207205)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [73] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(7307300)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 3, "", 0)
        effect:SetCfgID(7307301)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 3, "", 0)
          effect:SetCfgID(730730100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(7307302)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 24 2]")
          root:Add("[trap 24 3]")
          root:Add("[trap 24 5]")
          root:Add("[trap 24 7]")
          root:Add("[trap 24 9]")
          root:Add("[trap 24 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(7307304)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(7307305)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [74] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(7407400)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(7407401)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(740740100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(7407402)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 25 2]")
          root:Add("[trap 25 3]")
          root:Add("[trap 25 4]")
          root:Add("[trap 25 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(7407404)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(7407405)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [75] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(7507500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(7507501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(750750100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(7507502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 25 2]")
          root:Add("[trap 25 3]")
          root:Add("[trap 25 4]")
          root:Add("[trap 25 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(7507504)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(7507505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [76] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(7607600)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(7607601)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(760760100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(7607602)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 26 2]")
          root:Add("[trap 26 3]")
          root:Add("[trap 26 4]")
          root:Add("[trap 26 5]")
          root:Add("[trap 26 6]")
          root:Add("[trap 26 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(7607604)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 26 8]")
          root:Add("[trap 26 9]")
          root:Add("[trap 26 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(7607606)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(7607607)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [77] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(7707700)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(7707701)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(770770100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(7707702)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 26 2]")
          root:Add("[trap 26 3]")
          root:Add("[trap 26 4]")
          root:Add("[trap 26 5]")
          root:Add("[trap 26 6]")
          root:Add("[trap 26 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(7707704)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 26 8]")
          root:Add("[trap 26 9]")
          root:Add("[trap 26 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(7707706)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(7707707)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [78] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(7807800)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(7807801)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(780780100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(7807802)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 27 3]")
          root:Add("[trap 27 4]")
          root:Add("[trap 27 5]")
          root:Add("[trap 27 7]")
          root:Add("[trap 27 8]")
          root:Add("[trap 27 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(7807804)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(7807805)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [79] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(7907900)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(7907901)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(790790100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(7907902)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 27 2]")
          root:Add("[trap 27 3]")
          root:Add("[trap 27 5]")
          root:Add("[trap 27 7]")
          root:Add("[trap 27 9]")
          root:Add("[trap 27 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(7907904)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(7907905)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [80] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(8008000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(8008001)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(800800100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(8008002)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 28 2]")
          root:Add("[trap 28 3]")
          root:Add("[trap 28 4]")
          root:Add("[trap 28 5]")
          root:Add("[trap 28 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(8008004)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 28 7]")
          root:Add("[trap 28 8]")
          root:Add("[trap 28 9]")
          root:Add("[trap 28 10]")
          root:Add("[trap 28 11]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(8008006)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(8008007)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [81] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(8108100)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(8108101)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(810810100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(8108102)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 28 2]")
          root:Add("[trap 28 3]")
          root:Add("[trap 28 4]")
          root:Add("[trap 28 5]")
          root:Add("[trap 28 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(8108104)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 28 7]")
          root:Add("[trap 28 8]")
          root:Add("[trap 28 9]")
          root:Add("[trap 28 10]")
          root:Add("[trap 28 11]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(8108106)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(8108107)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [82] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(8208200)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(8208201)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(820820100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(8208202)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 29 2]")
          root:Add("[trap 29 3]")
          root:Add("[trap 29 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(8208204)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 29 5]")
          root:Add("[trap 29 6]")
          root:Add("[trap 29 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(8208206)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(8208207)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [83] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(8308300)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(8308301)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(830830100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(8308302)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 29 2]")
          root:Add("[trap 29 3]")
          root:Add("[trap 29 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(8308304)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 29 5]")
          root:Add("[trap 29 6]")
          root:Add("[trap 29 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(8308306)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(8308307)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [84] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(8408400)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(8408401)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(840840100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(8408402)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 32 2]")
          root:Add("[trap 32 3]")
          root:Add("[trap 32 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(8408404)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(8408405)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [85] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(8508500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(8508501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(850850100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(8508502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 32 2]")
          root:Add("[trap 32 3]")
          root:Add("[trap 32 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(8508504)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(8508505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [86] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(8608600)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(8608601)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(860860100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(8608602)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 33 2]")
          root:Add("[trap 33 3]")
          root:Add("[trap 33 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(8608604)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 33 5]")
          root:Add("[trap 33 6]")
          root:Add("[trap 33 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(8608606)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(8608607)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [87] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(8708700)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(8708701)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(870870100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(8708702)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 33 2]")
          root:Add("[trap 33 3]")
          root:Add("[trap 33 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(8708704)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 33 5]")
          root:Add("[trap 33 6]")
          root:Add("[trap 33 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(8708706)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(8708707)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [88] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(8808800)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(8808801)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(880880100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(8808802)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 34 2]")
          root:Add("[trap 34 3]")
          root:Add("[trap 34 4]")
          root:Add("[trap 34 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(8808804)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(8808805)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [89] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(8908900)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(8908901)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(890890100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(8908902)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 34 2]")
          root:Add("[trap 34 3]")
          root:Add("[trap 34 4]")
          root:Add("[trap 34 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(8908904)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(8908905)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [90] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(9009000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(9009001)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(900900100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(9009002)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(9009003)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(9009004)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(9009005)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [91] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(9109100)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(9109101)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(910910100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(9109102)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(9109103)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(9109104)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(9109105)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [92] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(9209201)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [93] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(9309301)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [94] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(9409400)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 2, "", 0)
        effect:SetCfgID(9409401)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 2, "", 0)
          effect:SetCfgID(940940100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(9409402)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 27 3]")
          root:Add("[trap 27 4]")
          root:Add("[trap 27 5]")
          root:Add("[trap 27 7]")
          root:Add("[trap 27 8]")
          root:Add("[trap 27 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(9409404)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(9409405)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [95] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(9509500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 3, "", 0)
        effect:SetCfgID(9509501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 3, "", 0)
          effect:SetCfgID(950950100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(9509502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 27 2]")
          root:Add("[trap 27 3]")
          root:Add("[trap 27 5]")
          root:Add("[trap 27 7]")
          root:Add("[trap 27 9]")
          root:Add("[trap 27 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(9509504)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(9509505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [96] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(9609600)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("1", "SwitchState", 1, "", 0)
        effect:SetCfgID(9609600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("1", "loadState", 1, "", 0)
          effect:SetCfgID(960960000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(9609600)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(9609600)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(9609600)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(9609600)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [97] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(9709700)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("1", "SwitchState", 0, "", 0)
        effect:SetCfgID(9709700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("1", "loadState", 0, "", 0)
          effect:SetCfgID(970970000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(9709700)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(9709700)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(9709700)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(9709700)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [98] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(9809800)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("1", "SwitchState", 1, "", 0)
        effect:SetCfgID(9809800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("1", "loadState", 1, "", 0)
          effect:SetCfgID(980980000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(9809800)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(9809800)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(9809800)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(9809800)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [99] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(9909900)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("1", "SwitchState", 0, "", 0)
        effect:SetCfgID(9909900)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("1", "loadState", 0, "", 0)
          effect:SetCfgID(990990000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(9909900)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(9909900)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(9909900)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(9909900)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [100] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1000000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1000000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(100000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1000000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1000000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1000000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1000000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [101] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1010000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1010000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(101000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1010000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1010000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1010000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1010000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [102] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1020000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1020000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(102000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(1020000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 53 2]")
          root:Add("[trap 53 3]")
          root:Add("[trap 53 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1020000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 53 5]")
          root:Add("[trap 53 6]")
          root:Add("[trap 53 7]")
          root:Add("[trap 53 8]")
          root:Add("[trap 53 9]")
          root:Add("[trap 53 10]")
          root:Add("[trap 53 11]")
          root:Add("[trap 53 12]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1020000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1020000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [103] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1030000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1030000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(103000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(1030000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 53 2]")
          root:Add("[trap 53 3]")
          root:Add("[trap 53 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1030000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 53 5]")
          root:Add("[trap 53 6]")
          root:Add("[trap 53 7]")
          root:Add("[trap 53 8]")
          root:Add("[trap 53 9]")
          root:Add("[trap 53 10]")
          root:Add("[trap 53 11]")
          root:Add("[trap 53 12]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1030000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1030000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [104] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1040000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1040000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(104000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(1040000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1040000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1040000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1040000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [105] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1050000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1050000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(105000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(1050000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1050000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1050000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1050000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [106] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1060000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1060000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(106000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1060000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 55 2]")
          root:Add("[trap 55 3]")
          root:Add("[trap 55 4]")
          root:Add("[trap 55 5]")
          root:Add("[trap 55 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1060000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 55 7]")
          root:Add("[trap 55 8]")
          root:Add("[trap 55 9]")
          root:Add("[trap 55 10]")
          root:Add("[trap 55 11]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1060000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1060000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [107] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1070000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1070000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(107000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1070000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 55 2]")
          root:Add("[trap 55 3]")
          root:Add("[trap 55 4]")
          root:Add("[trap 55 5]")
          root:Add("[trap 55 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1070000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 55 7]")
          root:Add("[trap 55 8]")
          root:Add("[trap 55 9]")
          root:Add("[trap 55 10]")
          root:Add("[trap 55 11]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1070000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1070000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [108] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1080000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1080000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(108000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(1080000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 56 2]")
          root:Add("[trap 56 3]")
          root:Add("[trap 56 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1080000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 56 5]")
          root:Add("[trap 56 6]")
          root:Add("[trap 56 7]")
          root:Add("[trap 56 8]")
          root:Add("[trap 56 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1080000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1080000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [109] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1090000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1090000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(109000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(1090000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 56 2]")
          root:Add("[trap 56 3]")
          root:Add("[trap 56 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1090000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 56 5]")
          root:Add("[trap 56 6]")
          root:Add("[trap 56 7]")
          root:Add("[trap 56 8]")
          root:Add("[trap 56 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1090000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1090000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [110] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1100000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1100001)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(110000100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1100002)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1100003)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1100004)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1100005)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [111] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1110000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1110001)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(111000100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1110002)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1110003)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1110004)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1110005)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [112] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1120000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1120001)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(112000100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(1120004)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 58 5]")
          root:Add("[trap 58 6]")
          root:Add("[trap 58 7]")
          root:Add("[trap 58 8]")
          root:Add("[trap 58 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM08")
        effect:SetCfgID(1120006)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 58 10]")
          root:Add("[trap 58 11]")
          root:Add("[trap 58 12]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1120008)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1120009)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [113] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1130000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1130001)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(113000100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(1130004)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 58 5]")
          root:Add("[trap 58 6]")
          root:Add("[trap 58 7]")
          root:Add("[trap 58 8]")
          root:Add("[trap 58 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM08")
        effect:SetCfgID(1130006)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 58 10]")
          root:Add("[trap 58 11]")
          root:Add("[trap 58 12]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1130008)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1130009)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [114] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1140000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1140001)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(114000100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(1140002)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 59 2]")
          root:Add("[trap 59 3]")
          root:Add("[trap 59 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(1140004)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 59 5]")
          root:Add("[trap 59 6]")
          root:Add("[trap 59 7]")
          root:Add("[trap 59 8]")
          root:Add("[trap 59 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1140008)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1140009)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [115] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1150000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1150001)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(115000100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(1150002)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 59 2]")
          root:Add("[trap 59 3]")
          root:Add("[trap 59 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(1150004)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 59 5]")
          root:Add("[trap 59 6]")
          root:Add("[trap 59 7]")
          root:Add("[trap 59 8]")
          root:Add("[trap 59 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1150008)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1150009)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [116] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1160000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1160000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(116000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1160000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1160000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1160000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1160000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [117] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1170000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1170000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(117000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1170000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1170000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1170000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1170000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [118] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1180000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1180000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(118000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1180000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 61 2]")
          root:Add("[trap 61 3]")
          root:Add("[trap 61 4]")
          root:Add("[trap 61 5]")
          root:Add("[trap 61 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(1180000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 61 7]")
          root:Add("[trap 61 8]")
          root:Add("[trap 61 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1180000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1180000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [119] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1190000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1190000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(119000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1190000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 61 2]")
          root:Add("[trap 61 3]")
          root:Add("[trap 61 4]")
          root:Add("[trap 61 5]")
          root:Add("[trap 61 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(1190000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 61 7]")
          root:Add("[trap 61 8]")
          root:Add("[trap 61 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1190000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1190000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [120] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1200000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1200000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(120000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(1200000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1200000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1200000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1200000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [121] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1210000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1210000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(121000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(1210000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1210000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1210000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1210000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [122] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1220000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1220000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0.25)
          effect:SetCfgID(122000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1220000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 63 3]")
          root:Add("[trap 63 4]")
          root:Add("[trap 63 5]")
          root:Add("[trap 63 7]")
          root:Add("[trap 63 8]")
          root:Add("[trap 63 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1220000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1220000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [123] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1230000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(1230000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0.25)
          effect:SetCfgID(123000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1230000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 63 2]")
          root:Add("[trap 63 3]")
          root:Add("[trap 63 5]")
          root:Add("[trap 63 7]")
          root:Add("[trap 63 9]")
          root:Add("[trap 63 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1230000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1230000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [124] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1240000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 2, "", 0)
        effect:SetCfgID(1240000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 2, "", 0)
          effect:SetCfgID(124000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1240000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 63 3]")
          root:Add("[trap 63 4]")
          root:Add("[trap 63 5]")
          root:Add("[trap 63 7]")
          root:Add("[trap 63 8]")
          root:Add("[trap 63 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1240000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1240000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [125] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1250000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 3, "", 0)
        effect:SetCfgID(1250000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 3, "", 0)
          effect:SetCfgID(125000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1250000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 63 2]")
          root:Add("[trap 63 3]")
          root:Add("[trap 63 5]")
          root:Add("[trap 63 7]")
          root:Add("[trap 63 9]")
          root:Add("[trap 63 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1250000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1250000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [126] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1260000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1260000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(126000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1260000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1260000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1260000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1260000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [127] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1270000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1270000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(127000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1270000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1270000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1270000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1270000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [128] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1280000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1280000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(128000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1280000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1280000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1280000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1280000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [129] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1290000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1290000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(129000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1290000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1290000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1290000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1290000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [130] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1300000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1300000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0.25)
          effect:SetCfgID(130000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(1300000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 66 3]")
          root:Add("[trap 66 4]")
          root:Add("[trap 66 5]")
          root:Add("[trap 66 7]")
          root:Add("[trap 66 8]")
          root:Add("[trap 66 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1300000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1300000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [131] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1310000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(1310000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0.25)
          effect:SetCfgID(131000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(1310000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 66 2]")
          root:Add("[trap 66 3]")
          root:Add("[trap 66 5]")
          root:Add("[trap 66 7]")
          root:Add("[trap 66 9]")
          root:Add("[trap 66 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1310000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1310000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [132] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1320000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 2, "", 0.25)
        effect:SetCfgID(1320000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 2, "", 0.25)
          effect:SetCfgID(132000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(1320000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 66 3]")
          root:Add("[trap 66 4]")
          root:Add("[trap 66 5]")
          root:Add("[trap 66 7]")
          root:Add("[trap 66 8]")
          root:Add("[trap 66 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1320000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1320000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [133] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1330000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 3, "", 0.25)
        effect:SetCfgID(1330000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 3, "", 0.25)
          effect:SetCfgID(133000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(1330000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 66 2]")
          root:Add("[trap 66 3]")
          root:Add("[trap 66 5]")
          root:Add("[trap 66 7]")
          root:Add("[trap 66 9]")
          root:Add("[trap 66 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1330000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1330000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [134] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1340000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1340000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(134000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1340000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1340000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1340000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1340000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [135] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1350000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1350000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(135000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1350000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1350000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1350000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1350000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [136] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1360000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1360000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(136000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1360000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 68 2]")
          root:Add("[trap 68 3]")
          root:Add("[trap 68 4]")
          root:Add("[trap 68 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(1360000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 68 6]")
          root:Add("[trap 68 7]")
          root:Add("[trap 68 8]")
          root:Add("[trap 68 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1360000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1360000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [137] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1370000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1370000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(137000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1370000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 68 2]")
          root:Add("[trap 68 3]")
          root:Add("[trap 68 4]")
          root:Add("[trap 68 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(1370000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 68 6]")
          root:Add("[trap 68 7]")
          root:Add("[trap 68 8]")
          root:Add("[trap 68 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1370000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1370000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [138] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1380000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1380000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(138000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1380000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1380000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1380000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1380000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [139] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1390000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1390000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(139000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1390000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1390000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1390000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1390000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [140] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1400000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1400000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(140000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1400000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1400000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1400000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1400000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [141] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1410000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1410000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(141000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1410000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1410000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1410000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1410000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [142] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1420000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1420000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(142000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 71 5]")
          root:Add("[trap 71 6]")
          root:Add("[trap 71 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1420000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 71 2]")
          root:Add("[trap 71 3]")
          root:Add("[trap 71 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1420000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1420000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [143] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1430000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1430000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(143000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 71 5]")
          root:Add("[trap 71 6]")
          root:Add("[trap 71 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1430000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 71 2]")
          root:Add("[trap 71 3]")
          root:Add("[trap 71 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1430000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1430000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [144] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1440000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1440000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(144000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1440000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1440000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1440000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1440000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [145] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1450000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1450000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(145000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1450000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1450000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1450000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1450000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [146] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1460000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1460000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(146000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1460000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 73 2]")
          root:Add("[trap 73 3]")
          root:Add("[trap 73 4]")
          root:Add("[trap 73 8]")
          root:Add("[trap 73 9]")
          root:Add("[trap 73 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(1460000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 73 5]")
          root:Add("[trap 73 6]")
          root:Add("[trap 73 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1460000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1460000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [147] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1470000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1470000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(147000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1470000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 73 2]")
          root:Add("[trap 73 3]")
          root:Add("[trap 73 4]")
          root:Add("[trap 73 8]")
          root:Add("[trap 73 9]")
          root:Add("[trap 73 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(1470000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 73 5]")
          root:Add("[trap 73 6]")
          root:Add("[trap 73 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1470000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1470000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [148] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1480000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1480000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(148000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1480000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 74 2]")
          root:Add("[trap 74 3]")
          root:Add("[trap 74 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1480000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 74 5]")
          root:Add("[trap 74 6]")
          root:Add("[trap 74 7]")
          root:Add("[trap 74 8]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1480000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1480000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [149] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1490000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1490000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(149000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1490000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 74 2]")
          root:Add("[trap 74 3]")
          root:Add("[trap 74 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1490000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 74 5]")
          root:Add("[trap 74 6]")
          root:Add("[trap 74 7]")
          root:Add("[trap 74 8]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1490000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1490000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [150] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1500000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1500000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(150000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1500000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1500000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1500000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1500000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [151] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1510000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1510000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(151000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1510000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1510000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1510000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1510000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [152] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "SkateRecover", 0.25)
        effect:SetCfgID(1520001)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(152000100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [153] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "SkateBreak", 0.25)
        effect:SetCfgID(1530001)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(153000100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [154] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1540000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1540000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(154000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1540000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1540000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1540000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1540000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [155] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1550000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1550000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(155000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1550000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1550000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1550000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1550000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [156] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1560000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1560000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(156000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(1560000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1560000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1560000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1560000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [157] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1570000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1570000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(157000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(1570000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1570000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1570000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1570000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [158] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1580000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1580000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(158000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(1580000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1580000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1580000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1580000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [159] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1590000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1590000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(159000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(1590000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1590000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1590000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1590000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [160] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1600100)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1600100)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(160010000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1600100)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1600100)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [161] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1610200)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1610200)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(161020000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1610200)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1610200)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [162] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1620000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1620000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(162000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1620000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1620000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1620000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1620000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [163] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1630000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1630000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(163000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1630000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1630000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1630000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1630000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [164] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "SwitchOff", 0.25)
        effect:SetCfgID(1640100)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(164010000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [165] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "SwitchOn", 0.25)
        effect:SetCfgID(1650200)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(165020000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [166] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1660000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1660000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(166000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1660000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1660000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1660000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1660000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [167] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1670000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1670000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(167000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1670000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1670000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1670000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1670000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [168] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1680000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1680000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(168000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1680000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1680000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1680000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1680000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [169] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1690000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1690000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(169000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1690000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1690000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1690000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1690000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [170] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1700000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1700000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(170000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1700000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 98 2]")
          root:Add("[trap 98 3]")
          root:Add("[trap 98 4]")
          root:Add("[trap 98 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1700000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 98 6]")
          root:Add("[trap 98 7]")
          root:Add("[trap 98 8]")
          root:Add("[trap 98 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1700000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1700000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [171] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1710000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1710000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(171000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1710000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 98 2]")
          root:Add("[trap 98 3]")
          root:Add("[trap 98 4]")
          root:Add("[trap 98 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1710000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 98 6]")
          root:Add("[trap 98 7]")
          root:Add("[trap 98 8]")
          root:Add("[trap 98 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1710000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1710000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [172] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1720000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1720000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(172000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1720000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1720000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1720000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1720000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [173] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1730000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1730000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(173000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1730000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1730000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1730000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1730000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [174] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1740000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1740000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(174000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(1740000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1740000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1740000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1740000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [175] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1750000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1750000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(175000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(1750000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1750000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1750000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1750000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [176] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1760000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1760000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(176000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1760000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1760000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1760000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1760000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [177] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1770000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1770000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(177000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1770000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1770000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1770000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1770000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [178] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1780000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1780000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(178000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1780000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1780000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1780000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1780000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [179] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1790000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1790000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(179000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1790000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1790000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1790000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1790000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [180] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1800000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1800000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(180000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1800000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1800000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1800000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1800000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [181] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1810000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1810000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(181000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1810000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1810000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1810000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1810000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [182] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1820000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(1820000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(182000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1820000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1820000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1820000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1820000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [183] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1830000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1830000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(183000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1830000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1830000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1830000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1830000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [184] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1840000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "switchState", 0, "", 0)
        effect:SetCfgID(1840000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1840000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [185] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1850000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "switchState", 1, "", 0)
        effect:SetCfgID(1850000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1850000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1850000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [186] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 1, "TowerOn1", 1)
        effect:SetCfgID(1860000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [187] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 1, "TowerOn2", 1)
        effect:SetCfgID(1870000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [188] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 1, "TowerOn3", 1)
        effect:SetCfgID(1880000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [189] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 1, "TowerOn4", 1)
        effect:SetCfgID(1890000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [190] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(15, 15, 3, 1, true)
        effect:SetCfgID(1900000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(15, 15, 3, 1, true)
          effect:SetCfgID(190000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
        effect:SetCfgID(1900000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(190000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
        effect:SetCfgID(1900000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(190000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
        effect:SetCfgID(1900000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
          effect:SetCfgID(190000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
        effect:SetCfgID(1900000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
          effect:SetCfgID(190000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [191] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1910000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1910000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(191000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1910000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 112 2]")
          root:Add("[trap 112 3]")
          root:Add("[trap 112 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1910000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 112 5]")
          root:Add("[trap 112 6]")
          root:Add("[trap 112 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(1910000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 112 8]")
          root:Add("[trap 112 9]")
          root:Add("[trap 112 10]")
          root:Add("[trap 112 11]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(1910000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 112 12]")
          root:Add("[trap 112 13]")
          root:Add("[trap 112 14]")
          root:Add("[trap 112 15]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1910000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1910000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [192] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1920000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1920000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(192000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(1920000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 112 2]")
          root:Add("[trap 112 3]")
          root:Add("[trap 112 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1920000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 112 5]")
          root:Add("[trap 112 6]")
          root:Add("[trap 112 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(1920000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 112 8]")
          root:Add("[trap 112 9]")
          root:Add("[trap 112 10]")
          root:Add("[trap 112 11]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(1920000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 112 12]")
          root:Add("[trap 112 13]")
          root:Add("[trap 112 14]")
          root:Add("[trap 112 15]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1920000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1920000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [193] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1930000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1930000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(193000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(1930000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1930000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1930000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1930000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [194] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1940000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1940000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(194000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(1940000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1940000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1940000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1940000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [195] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1950000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1950000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(195000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM08")
        effect:SetCfgID(1950000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1950000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1950000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1950000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [196] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1960000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1960000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(196000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM08")
        effect:SetCfgID(1960000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1960000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1960000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1960000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [197] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1970000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1970000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(197000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1970000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1970000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1970000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1970000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [198] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1980000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(1980000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(198000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(1980000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1980000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1980000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1980000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [199] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(1990000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(1990000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(199000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(1990000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(1990000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(1990000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(1990000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [200] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2000000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(2000000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(200000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(2000000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(2000000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2000000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2000000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [201] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2010000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "switchState", 0, "", 0)
        effect:SetCfgID(2010000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2010000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [202] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2020000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "switchState", 1, "", 0)
        effect:SetCfgID(2020000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(2020000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(2020000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2020000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2020000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [203] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2030000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "switchState", 1, "", 0)
        effect:SetCfgID(2030000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(2030000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(2030000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2030000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2030000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [204] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(15, 15, 3, 1, false)
        effect:SetCfgID(2040000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(15, 15, 3, 1, false)
          effect:SetCfgID(204000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateDestroyEffect("[switch 120 9]")
        effect:SetCfgID(2040000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateDestroyEffect("[sceneobject 21]")
        effect:SetCfgID(2040000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [205] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(2, 26, 3, 1, false)
        effect:SetCfgID(2050000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(2, 26, 3, 1, false)
          effect:SetCfgID(205000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateDestroyEffect("[switch 111 9]")
        effect:SetCfgID(2050000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateDestroyEffect("[sceneobject 22]")
        effect:SetCfgID(2050000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [206] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(4, 31, 3, 1, false)
        effect:SetCfgID(2060000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(4, 31, 3, 1, false)
          effect:SetCfgID(206000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateDestroyEffect("[switch 121 9]")
        effect:SetCfgID(2060000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateDestroyEffect("[sceneobject 23]")
        effect:SetCfgID(2060000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [207] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2070000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTimelineEffect(1172, false, false)
        effect:SetCfgID(2070000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2070000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateJumpEffect(10021, 1)
        effect:SetCfgID(2070000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [208] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2080000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2080000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [209] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(2, 26, 3, 1, true)
        effect:SetCfgID(2090000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(2, 26, 3, 1, true)
          effect:SetCfgID(209000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
        effect:SetCfgID(2090000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(209000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
        effect:SetCfgID(2090000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(209000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
        effect:SetCfgID(2090000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
          effect:SetCfgID(209000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
        effect:SetCfgID(2090000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
          effect:SetCfgID(209000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [210] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(4, 31, 3, 1, true)
        effect:SetCfgID(2100000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(4, 31, 3, 1, true)
          effect:SetCfgID(210000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
        effect:SetCfgID(2100000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(210000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
        effect:SetCfgID(2100000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(210000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
        effect:SetCfgID(2100000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
          effect:SetCfgID(210000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
        effect:SetCfgID(2100000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
          effect:SetCfgID(210000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [211] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2110000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(2110000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
          effect:SetCfgID(211000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang", false)
        effect:SetCfgID(2110000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang", false)
          effect:SetCfgID(211000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2110000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [212] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2120000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(2120000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
          effect:SetCfgID(212000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang", true)
        effect:SetCfgID(2120000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang", true)
          effect:SetCfgID(212000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2120000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [213] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2130000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2130000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(213000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
        effect:SetCfgID(2130000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
          effect:SetCfgID(213000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2130000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(213000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (17)", false)
        effect:SetCfgID(2130000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (17)", false)
          effect:SetCfgID(213000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (23)", false)
        effect:SetCfgID(2130000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (23)", false)
          effect:SetCfgID(213000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (24)", false)
        effect:SetCfgID(2130000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (24)", false)
          effect:SetCfgID(213000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2130000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [214] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2140000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(2140000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(214000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", true)
        effect:SetCfgID(2140000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", true)
          effect:SetCfgID(214000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (17)", false)
        effect:SetCfgID(2140000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (17)", false)
          effect:SetCfgID(214000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (23)", false)
        effect:SetCfgID(2140000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (23)", false)
          effect:SetCfgID(214000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (24)", false)
        effect:SetCfgID(2140000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (24)", false)
          effect:SetCfgID(214000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2140000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [215] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2150000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2150000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(215000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
        effect:SetCfgID(2150000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
          effect:SetCfgID(215000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (23)", false)
        effect:SetCfgID(2150000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (23)", false)
          effect:SetCfgID(215000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (24)", false)
        effect:SetCfgID(2150000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (24)", false)
          effect:SetCfgID(215000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2150000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [216] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2160000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2160000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(216000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", true)
        effect:SetCfgID(2160000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", true)
          effect:SetCfgID(216000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (23)", false)
        effect:SetCfgID(2160000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (23)", false)
          effect:SetCfgID(216000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (24)", false)
        effect:SetCfgID(2160000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (24)", false)
          effect:SetCfgID(216000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2160000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [217] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2170000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(2170000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
          effect:SetCfgID(217000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang", true)
        effect:SetCfgID(2170000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang", true)
          effect:SetCfgID(217000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2170000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [218] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2180000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(2180000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
          effect:SetCfgID(218000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
        effect:SetCfgID(2180000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
          effect:SetCfgID(218000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2180000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [219] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2190000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(2190000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
          effect:SetCfgID(219000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
        effect:SetCfgID(2190000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
          effect:SetCfgID(219000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2190000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [220] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2200000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(2200000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
          effect:SetCfgID(220000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
        effect:SetCfgID(2200000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
          effect:SetCfgID(220000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2200000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [221] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2210000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2210000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(221000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
        effect:SetCfgID(2210000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
          effect:SetCfgID(221000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (25)", false)
        effect:SetCfgID(2210000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (25)", false)
          effect:SetCfgID(221000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (26)", false)
        effect:SetCfgID(2210000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (26)", false)
          effect:SetCfgID(221000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2210000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [222] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2220000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2220000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(222000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
        effect:SetCfgID(2220000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
          effect:SetCfgID(222000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", true)
        effect:SetCfgID(2220000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", true)
          effect:SetCfgID(222000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (25)", false)
        effect:SetCfgID(2220000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (25)", false)
          effect:SetCfgID(222000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (26)", false)
        effect:SetCfgID(2220000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (26)", false)
          effect:SetCfgID(222000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2220000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [223] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2230000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2230000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(223000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
        effect:SetCfgID(2230000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
          effect:SetCfgID(223000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2230000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(223000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (18)", false)
        effect:SetCfgID(2230000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (18)", false)
          effect:SetCfgID(223000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2230000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [224] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2240000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(2240000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(224000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", true)
        effect:SetCfgID(2240000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", true)
          effect:SetCfgID(224000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (18)", false)
        effect:SetCfgID(2240000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (18)", false)
          effect:SetCfgID(224000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2240000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [225] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2250000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 136 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(2250000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 136 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
          effect:SetCfgID(225000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (6)", false)
        effect:SetCfgID(2250000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (6)", false)
          effect:SetCfgID(225000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2250000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [226] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2260000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 136 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(2260000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 136 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
          effect:SetCfgID(226000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (6)", true)
        effect:SetCfgID(2260000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (6)", true)
          effect:SetCfgID(226000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2260000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [227] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2270000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 136 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(2270000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 136 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
          effect:SetCfgID(227000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (6)", true)
        effect:SetCfgID(2270000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (6)", true)
          effect:SetCfgID(227000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2270000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [228] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2280000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2280000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(228000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (7)", false)
        effect:SetCfgID(2280000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (7)", false)
          effect:SetCfgID(228000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (16)", false)
        effect:SetCfgID(2280000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (16)", false)
          effect:SetCfgID(228000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2280000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [229] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2290000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2290000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(229000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (7)", true)
        effect:SetCfgID(2290000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (7)", true)
          effect:SetCfgID(229000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (16)", false)
        effect:SetCfgID(2290000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (16)", false)
          effect:SetCfgID(229000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2290000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [230] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2300000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(2300000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(230000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (7)", false)
        effect:SetCfgID(2300000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (7)", false)
          effect:SetCfgID(230000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (16)", true)
        effect:SetCfgID(2300000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (16)", true)
          effect:SetCfgID(230000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2300000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [231] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "", 0)
        effect:SetCfgID(2310000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(231000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(16, 6, 1, 1, true)
        effect:SetCfgID(2310000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(16, 6, 1, 1, true)
          effect:SetCfgID(231000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [232] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(2320000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(232000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(16, 6, 1, 1, false)
        effect:SetCfgID(2320000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(16, 6, 1, 1, false)
          effect:SetCfgID(232000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [233] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2330000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(2330000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(233000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM09")
        effect:SetCfgID(2330000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(2330000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2330000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2330000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [234] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2340000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(2340000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(234000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM09")
        effect:SetCfgID(2340000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(2340000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2340000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2340000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [235] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2350000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 139 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(2350000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 139 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
          effect:SetCfgID(235000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (8)", false)
        effect:SetCfgID(2350000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (8)", false)
          effect:SetCfgID(235000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2350000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [236] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2360000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 139 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(2360000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 139 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
          effect:SetCfgID(236000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
        effect:SetCfgID(2360000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
          effect:SetCfgID(236000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2360000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [237] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2370000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 139 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(2370000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 139 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
          effect:SetCfgID(237000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
        effect:SetCfgID(2370000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
          effect:SetCfgID(237000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2370000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [238] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2380000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 139 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2380000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 139 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(238000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
        effect:SetCfgID(2380000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
          effect:SetCfgID(238000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2380000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [239] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2390000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 139 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2390000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 139 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(239000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (9)", true)
        effect:SetCfgID(2390000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (9)", true)
          effect:SetCfgID(239000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2390000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [240] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2400000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 139 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2400000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 139 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(240000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (12)", false)
        effect:SetCfgID(2400000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (12)", false)
          effect:SetCfgID(240000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (11)", false)
        effect:SetCfgID(2400000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (11)", false)
          effect:SetCfgID(240000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2400000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [241] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2410000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 139 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(2410000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 139 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(241000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (12)", false)
        effect:SetCfgID(2410000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (12)", false)
          effect:SetCfgID(241000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (11)", true)
        effect:SetCfgID(2410000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (11)", true)
          effect:SetCfgID(241000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2410000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [242] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2420000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 139 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2420000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 139 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(242000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (12)", true)
        effect:SetCfgID(2420000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (12)", true)
          effect:SetCfgID(242000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (11)", false)
        effect:SetCfgID(2420000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (11)", false)
          effect:SetCfgID(242000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2420000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [243] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "", 0)
        effect:SetCfgID(2430000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(243000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(26, 20, 1, 1, true)
        effect:SetCfgID(2430000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(26, 20, 1, 1, true)
          effect:SetCfgID(243000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [244] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(2440000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(244000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(26, 20, 1, 1, false)
        effect:SetCfgID(2440000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(26, 20, 1, 1, false)
          effect:SetCfgID(244000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [245] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2450000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(2450000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
          effect:SetCfgID(245000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (8)", false)
        effect:SetCfgID(2450000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (8)", false)
          effect:SetCfgID(245000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2450000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [246] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2460000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(2460000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
          effect:SetCfgID(246000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
        effect:SetCfgID(2460000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
          effect:SetCfgID(246000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2460000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [247] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2470000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(2470000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
          effect:SetCfgID(247000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
        effect:SetCfgID(2470000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
          effect:SetCfgID(247000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2470000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [248] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2480000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2480000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(248000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
        effect:SetCfgID(2480000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
          effect:SetCfgID(248000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2480000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(248000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (19)", false)
        effect:SetCfgID(2480000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (19)", false)
          effect:SetCfgID(248000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (27)", false)
        effect:SetCfgID(2480000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (27)", false)
          effect:SetCfgID(248000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2480000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [249] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2490000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2490000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(249000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (9)", true)
        effect:SetCfgID(2490000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (9)", true)
          effect:SetCfgID(249000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (19)", false)
        effect:SetCfgID(2490000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (19)", false)
          effect:SetCfgID(249000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (27)", false)
        effect:SetCfgID(2490000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (27)", false)
          effect:SetCfgID(249000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2490000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [250] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2500000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2500000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(250000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (12)", false)
        effect:SetCfgID(2500000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (12)", false)
          effect:SetCfgID(250000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (11)", false)
        effect:SetCfgID(2500000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (11)", false)
          effect:SetCfgID(250000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2500000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [251] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2510000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(2510000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(251000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (12)", false)
        effect:SetCfgID(2510000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (12)", false)
          effect:SetCfgID(251000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (11)", true)
        effect:SetCfgID(2510000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (11)", true)
          effect:SetCfgID(251000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
        effect:SetCfgID(2510000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
          effect:SetCfgID(251000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (27)", true)
        effect:SetCfgID(2510000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (27)", true)
          effect:SetCfgID(251000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2510000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [252] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2520000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2520000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(252000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (12)", true)
        effect:SetCfgID(2520000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (12)", true)
          effect:SetCfgID(252000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (11)", false)
        effect:SetCfgID(2520000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (11)", false)
          effect:SetCfgID(252000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (27)", false)
        effect:SetCfgID(2520000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (27)", false)
          effect:SetCfgID(252000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2520000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [253] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2530000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2530000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(253000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (13)", false)
        effect:SetCfgID(2530000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (13)", false)
          effect:SetCfgID(253000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2530000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(253000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (20)", false)
        effect:SetCfgID(2530000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (20)", false)
          effect:SetCfgID(253000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (28)", false)
        effect:SetCfgID(2530000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (28)", false)
          effect:SetCfgID(253000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2530000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [254] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2540000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(2540000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(254000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (13)", true)
        effect:SetCfgID(2540000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (13)", true)
          effect:SetCfgID(254000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (20)", false)
        effect:SetCfgID(2540000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (20)", false)
          effect:SetCfgID(254000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (28)", true)
        effect:SetCfgID(2540000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (28)", true)
          effect:SetCfgID(254000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2540000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [255] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2550000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2550000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(255000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (14)", false)
        effect:SetCfgID(2550000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (14)", false)
          effect:SetCfgID(255000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2550000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(255000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (21)", false)
        effect:SetCfgID(2550000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (21)", false)
          effect:SetCfgID(255000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (28)", false)
        effect:SetCfgID(2550000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (28)", false)
          effect:SetCfgID(255000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2550000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [256] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2560000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(2560000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(256000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (13)", false)
        effect:SetCfgID(2560000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (13)", false)
          effect:SetCfgID(256000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (14)", true)
        effect:SetCfgID(2560000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (14)", true)
          effect:SetCfgID(256000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (21)", false)
        effect:SetCfgID(2560000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (21)", false)
          effect:SetCfgID(256000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (28)", true)
        effect:SetCfgID(2560000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (28)", true)
          effect:SetCfgID(256000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2560000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [257] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2570000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2570000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(257000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (15)", false)
        effect:SetCfgID(2570000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (15)", false)
          effect:SetCfgID(257000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2570000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(257000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (22)", false)
        effect:SetCfgID(2570000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (22)", false)
          effect:SetCfgID(257000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2570000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [258] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2580000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(2580000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(258000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (15)", true)
        effect:SetCfgID(2580000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (15)", true)
          effect:SetCfgID(258000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (22)", false)
        effect:SetCfgID(2580000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (22)", false)
          effect:SetCfgID(258000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2580000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [259] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2590000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(2590000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(259000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(2590000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(2590000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2590000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2590000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [260] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2600000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(2600000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(260000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(2600000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(2600000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2600000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2600000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [261] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2610000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(2610000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(261000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(2610000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 142 2]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(2610000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 142 3]")
          root:Add("[trap 142 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2610000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2610000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [262] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2620000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(2620000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(262000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(2620000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 142 2]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(2620000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 142 3]")
          root:Add("[trap 142 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2620000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2620000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [263] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2630000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(2630000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(263000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(2630000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 143 2]")
          root:Add("[trap 143 3]")
          root:Add("[trap 143 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(2630000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 143 5]")
          root:Add("[trap 143 6]")
          root:Add("[trap 143 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2630000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2630000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [264] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2640000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(2640000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(264000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(2640000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 143 2]")
          root:Add("[trap 143 3]")
          root:Add("[trap 143 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(2640000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 143 5]")
          root:Add("[trap 143 6]")
          root:Add("[trap 143 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2640000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2640000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [265] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2650000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(2650000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(265000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(2650000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 144 2]")
          root:Add("[trap 144 3]")
          root:Add("[trap 144 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(2650000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 144 5]")
          root:Add("[trap 144 6]")
          root:Add("[trap 144 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2650000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2650000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [266] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2660000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(2660000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(266000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(2660000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 144 2]")
          root:Add("[trap 144 3]")
          root:Add("[trap 144 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(2660000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 144 5]")
          root:Add("[trap 144 6]")
          root:Add("[trap 144 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2660000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2660000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [267] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2670000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(2670000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(267000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(2670000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(2670000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2670000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2670000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [268] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2680000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(2680000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(268000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(2680000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(2680000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2680000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2680000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [269] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2690000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(2690000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(269000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(2690000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(2690000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2690000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2690000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [270] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2700000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(2700000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(270000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(2700000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(2700000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2700000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2700000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [271] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2710000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(2710000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(271000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(2710000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(2710000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2710000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2710000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [272] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2720000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(2720000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(272000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(2720000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(2720000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2720000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2720000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [273] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2730000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateEventEffect(100402)
        effect:SetCfgID(2730000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2730000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [274] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[switch 148 1]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [275] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[switch 148 2]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [276] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[switch 148 3]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [277] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[switch 148 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [278] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(15, 15, 3, 1, true)
        effect:SetCfgID(2780000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(15, 15, 3, 1, true)
          effect:SetCfgID(278000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [279] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(2, 26, 3, 1, true)
        effect:SetCfgID(2790000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(2, 26, 3, 1, true)
          effect:SetCfgID(279000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [280] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2800000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(2800000)
        root:AddEffect(effect)
      end
      do
        local upper = root or composed
        local root
        if not composed then
          composed = EffectFactory.CreateEffectList()
          root = composed
        else
          root = EffectFactory.CreateEffectList()
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(280000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(280000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
          effect:SetCfgID(280000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
          effect:SetCfgID(280000000)
          root:AddEffect(effect)
        end
        if upper then
          upper:AddEffect(root)
        end
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trigger", 1, "TowerIdle", 1)
        effect:SetCfgID(2800000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2800000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2800000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [281] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2810000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(2810000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 1, "TowerOn1", 1)
        effect:SetCfgID(2810000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 1, "TowerOn1", 1)
          effect:SetCfgID(281000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2810000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2810000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [282] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2820000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(2820000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 1, "TowerOn2", 1)
        effect:SetCfgID(2820000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 1, "TowerOn2", 1)
          effect:SetCfgID(282000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2820000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2820000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [283] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2830000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(2830000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 1, "TowerOn3", 1)
        effect:SetCfgID(2830000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 1, "TowerOn3", 1)
          effect:SetCfgID(283000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2830000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2830000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [284] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2840000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(2840000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 1, "TowerOn4", 1)
        effect:SetCfgID(2840000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 1, "", 1)
          effect:SetCfgID(284000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2840000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2840000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [285] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2850000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(2850000)
        root:AddEffect(effect)
      end
      do
        local upper = root or composed
        local root
        if not composed then
          composed = EffectFactory.CreateEffectList()
          root = composed
        else
          root = EffectFactory.CreateEffectList()
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(285000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(285000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
          effect:SetCfgID(285000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
          effect:SetCfgID(285000000)
          root:AddEffect(effect)
        end
        if upper then
          upper:AddEffect(root)
        end
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trigger", 1, "TowerIdle", 1)
        effect:SetCfgID(2850000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2850000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2850000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [286] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2860000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(2860000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 1, "TowerOn1", 1)
        effect:SetCfgID(2860000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 1, "TowerOn1", 1)
          effect:SetCfgID(286000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2860000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2860000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [287] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2870000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(2870000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 1, "TowerOn2", 1)
        effect:SetCfgID(2870000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 1, "TowerOn2", 1)
          effect:SetCfgID(287000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2870000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2870000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [288] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2880000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(2880000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 1, "TowerOn3", 1)
        effect:SetCfgID(2880000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 1, "TowerOn3", 1)
          effect:SetCfgID(288000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2880000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2880000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [289] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2890000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(2890000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 1, "TowerOn4", 1)
        effect:SetCfgID(2890000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 1, "", 1)
          effect:SetCfgID(289000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2890000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2890000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [290] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(4, 31, 3, 1, true)
        effect:SetCfgID(2900000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(4, 31, 3, 1, true)
          effect:SetCfgID(290000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [291] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2910000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(2910000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(291000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(2910000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(2910000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2910000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2910000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [292] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2920000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(2920000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(292000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(2920000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(2920000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2920000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2920000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [293] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2930000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(2930000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(293000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(2930000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(2930000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2930000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2930000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [294] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2940000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(2940000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(294000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(2940000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(2940000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2940000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2940000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [295] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(2950000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [296] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(2960000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(296000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [297] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(2970000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(297000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [298] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2980000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [299] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(2990000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(2990000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(299000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(2990000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 153 2]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(2990000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 153 3]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(2990000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(2990000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [300] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3000000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3000000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(300000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3000000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 153 2]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3000000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 153 3]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3000000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3000000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [301] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3010000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3010000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(301000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(3010000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3010000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3010000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3010000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [302] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3020000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3020000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(302000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(3020000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3020000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3020000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3020000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [303] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3030000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3030000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(303000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3030000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 155 2]")
          root:Add("[trap 155 3]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(3030000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 155 4]")
          root:Add("[trap 155 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3030000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3030000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [304] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3040000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3040000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(304000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3040000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 155 2]")
          root:Add("[trap 155 3]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(3040000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 155 4]")
          root:Add("[trap 155 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3040000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3040000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [305] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3050000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3050000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(305000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3050000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 156 2]")
          root:Add("[trap 156 3]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3050000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 156 4]")
          root:Add("[trap 156 5]")
          root:Add("[trap 156 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3050000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3050000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [306] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3060000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3060000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(306000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3060000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 156 2]")
          root:Add("[trap 156 3]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3060000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 156 4]")
          root:Add("[trap 156 5]")
          root:Add("[trap 156 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3060000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3060000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [307] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3070000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 157 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(3070000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 157 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
          effect:SetCfgID(307000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang", false)
        effect:SetCfgID(3070000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang", false)
          effect:SetCfgID(307000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3070000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [308] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3080000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 157 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(3080000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 157 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
          effect:SetCfgID(308000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang", true)
        effect:SetCfgID(3080000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang", true)
          effect:SetCfgID(308000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3080000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [309] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3090000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 157 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(3090000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 157 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
          effect:SetCfgID(309000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang", true)
        effect:SetCfgID(3090000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang", true)
          effect:SetCfgID(309000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3090000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [310] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "DoorClose", 0.1)
        effect:SetCfgID(3100000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(310000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(13, 8, 1, 1, true)
        effect:SetCfgID(3100000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(13, 8, 1, 1, true)
          effect:SetCfgID(310000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [311] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "DoorOpen", 0.1)
        effect:SetCfgID(311031100)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(31103110000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(13, 8, 1, 1, false)
        effect:SetCfgID(311031100)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(13, 8, 1, 1, false)
          effect:SetCfgID(31103110000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [312] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3120000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 157 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(3120000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 157 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(312000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", true)
        effect:SetCfgID(3120000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", true)
          effect:SetCfgID(312000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
        effect:SetCfgID(3120000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
          effect:SetCfgID(312000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3120000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [313] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3130000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 157 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(3130000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 157 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(313000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
        effect:SetCfgID(3130000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
          effect:SetCfgID(313000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 157 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(3130000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 157 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(313000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
        effect:SetCfgID(3130000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
          effect:SetCfgID(313000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3130000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [314] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3140000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 157 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(3140000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 157 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(314000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", true)
        effect:SetCfgID(3140000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", true)
          effect:SetCfgID(314000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", true)
        effect:SetCfgID(3140000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", true)
          effect:SetCfgID(314000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (6)", false)
        effect:SetCfgID(3140000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (6)", false)
          effect:SetCfgID(314000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (7)", false)
        effect:SetCfgID(3140000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (7)", true)
          effect:SetCfgID(314000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (8)", false)
        effect:SetCfgID(3140000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
          effect:SetCfgID(314000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3140000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [315] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3150000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 157 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(3150000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 157 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(315000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
        effect:SetCfgID(3150000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
          effect:SetCfgID(315000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (7)", false)
        effect:SetCfgID(3150000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (7)", true)
          effect:SetCfgID(315000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (8)", false)
        effect:SetCfgID(3150000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
          effect:SetCfgID(315000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3150000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [316] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3160000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 157 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(3160000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 157 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(316000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
        effect:SetCfgID(3160000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
          effect:SetCfgID(316000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (6)", false)
        effect:SetCfgID(3160000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (6)", false)
          effect:SetCfgID(316000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (9)", true)
        effect:SetCfgID(3160000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (9)", true)
          effect:SetCfgID(316000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3160000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [317] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3170000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 157 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(3170000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 157 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(317000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
        effect:SetCfgID(3170000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
          effect:SetCfgID(317000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3170000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 157 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(3170000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 157 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(317000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (6)", false)
        effect:SetCfgID(3170000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (6)", false)
          effect:SetCfgID(317000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
        effect:SetCfgID(3170000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
          effect:SetCfgID(317000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [318] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3180000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 157 5]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(3180000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 157 5]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(318000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
        effect:SetCfgID(3180000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
          effect:SetCfgID(318000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", true)
        effect:SetCfgID(3180000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", true)
          effect:SetCfgID(318000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
        effect:SetCfgID(3180000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
          effect:SetCfgID(318000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (10)", false)
        effect:SetCfgID(3180000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (10)", false)
          effect:SetCfgID(318000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3180000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [319] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3190000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 157 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(3190000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 157 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(319000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
        effect:SetCfgID(3190000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
          effect:SetCfgID(319000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
        effect:SetCfgID(3190000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
          effect:SetCfgID(319000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (10)", false)
        effect:SetCfgID(3190000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (10)", false)
          effect:SetCfgID(319000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3190000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [320] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3200000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3200000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(320000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3200000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3200000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3200000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3200000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [321] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3210000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3210000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(321000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3210000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3210000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3210000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3210000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [322] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3220000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3220000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(322000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3220000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3220000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3220000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3220000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [323] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3230000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3230000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(323000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3230000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3230000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3230000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3230000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [324] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3240000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3240000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(324000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3240000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3240000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3240000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3240000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [325] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3250000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3250000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(325000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3250000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3250000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3250000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3250000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [326] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3260000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3260000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(326000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3260000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3260000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3260000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3260000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [327] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3270000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3270000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(327000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3270000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3270000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3270000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3270000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [328] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3280000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3280000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(328000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(3280000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3280000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3280000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3280000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [329] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3290000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3290000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(329000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(3290000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3290000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3290000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3290000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [330] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3300000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3300000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(330000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(3300000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3300000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3300000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3300000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [331] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3310000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3310000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(331000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(3310000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3310000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3310000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3310000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [332] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3320000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3320000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(332000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(3320000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3320000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3320000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3320000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [333] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3330000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3330000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(333000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(3330000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3330000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3330000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3330000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [334] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3340000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3340000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(334000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3340000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 162 3]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3340000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 162 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3340000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3340000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [335] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3350000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3350000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(335000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3350000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 162 3]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3350000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 162 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3350000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3350000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [336] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3360000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3360000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(336000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(3360000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 162 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(3360000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 162 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3360000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 162 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3360000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3360000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [337] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3370000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3370000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(337000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(3370000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 162 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(3370000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 162 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3370000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 162 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3370000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3370000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [338] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3380000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(3380000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
          effect:SetCfgID(338000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang", false)
        effect:SetCfgID(3380000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang", false)
          effect:SetCfgID(338000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3380000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [339] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3390000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(3390000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
          effect:SetCfgID(339000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang", true)
        effect:SetCfgID(3390000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang", true)
          effect:SetCfgID(339000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3390000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [340] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3400000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(3400000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(340000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
        effect:SetCfgID(3400000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
          effect:SetCfgID(340000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(3400000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(340000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
        effect:SetCfgID(3400000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
          effect:SetCfgID(340000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3400000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [341] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3410000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(3410000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(341000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", true)
        effect:SetCfgID(3410000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", true)
          effect:SetCfgID(341000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
        effect:SetCfgID(3410000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
          effect:SetCfgID(341000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3410000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [342] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3420000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(3420000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(342000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
        effect:SetCfgID(3420000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
          effect:SetCfgID(342000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
        effect:SetCfgID(3420000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
          effect:SetCfgID(342000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
        effect:SetCfgID(3420000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
          effect:SetCfgID(342000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3420000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [343] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3430000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(3430000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(343000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", true)
        effect:SetCfgID(3430000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", true)
          effect:SetCfgID(343000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
        effect:SetCfgID(3430000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
          effect:SetCfgID(343000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
        effect:SetCfgID(3430000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
          effect:SetCfgID(343000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3430000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [344] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "DoorClose", 0.1)
        effect:SetCfgID(3440000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(344000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(5, 12, 1, 1, true)
        effect:SetCfgID(3440000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(5, 12, 1, 1, true)
          effect:SetCfgID(344000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [345] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "DoorOpen", 0.1)
        effect:SetCfgID(345031100)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(34503110000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(5, 12, 1, 1, false)
        effect:SetCfgID(345031100)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(5, 12, 1, 1, false)
          effect:SetCfgID(34503110000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [346] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3460000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3460000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(346000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(3460000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3460000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3460000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3460000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [347] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3470000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3470000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(347000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(3470000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3470000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3470000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3470000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [348] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3480000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3480000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(348000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3480000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3480000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3480000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3480000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [349] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3490000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3490000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(349000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3490000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3490000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3490000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3490000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [350] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3500000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3500000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(350000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3500000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3500000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3500000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3500000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [351] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3510000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3510000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(351000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3510000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3510000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3510000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3510000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [352] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3520000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3520000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(352000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(3520000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3520000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3520000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3520000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [353] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3530000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3530000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(353000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(3530000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3530000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3530000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3530000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [354] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3540000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3540000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3540000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [355] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3550000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(3550000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3550000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [356] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3560000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(3560000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 168 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(3560000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3560000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [357] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "", 0)
        effect:SetCfgID(3570000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "", 0)
          effect:SetCfgID(357000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(34, 12, 3, 1, true)
        effect:SetCfgID(3570000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(34, 12, 3, 1, true)
          effect:SetCfgID(357000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [358] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(3580000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
          effect:SetCfgID(358000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(34, 12, 3, 1, false)
        effect:SetCfgID(3580000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(34, 12, 3, 1, false)
          effect:SetCfgID(358000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [359] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3590000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(3590000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3590000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [360] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3600000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(3600000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 171 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(3600000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3600000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [361] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "", 0)
        effect:SetCfgID(3610000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "", 0)
          effect:SetCfgID(361000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(24, 10, 3, 1, true)
        effect:SetCfgID(3610000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(24, 10, 3, 1, true)
          effect:SetCfgID(361000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [362] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(3620000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
          effect:SetCfgID(362000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(24, 10, 3, 1, false)
        effect:SetCfgID(3620000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(24, 10, 3, 1, false)
          effect:SetCfgID(362000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [363] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3630000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3630000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(363000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3630000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3630000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3630000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3630000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [364] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3640000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3640000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(364000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3640000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3640000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3640000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3640000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [365] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3650000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3650000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(365000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(3650000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3650000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3650000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3650000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [366] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3660000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3660000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(366000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(3660000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3660000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3660000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3660000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [367] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3670000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3670000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(367000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(3670000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3670000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3670000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3670000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [368] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3680000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3680000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(368000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(3680000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3680000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3680000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3680000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [369] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3690000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3690000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(369000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3690000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3690000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3690000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3690000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [370] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3700000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3700000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(370000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3700000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3700000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3700000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3700000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [371] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3710000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3710000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(371000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3710000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3710000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3710000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3710000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [372] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3720000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3720000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(372000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3720000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3720000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3720000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3720000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [373] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3730000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3730000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(373000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3730000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3730000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3730000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3730000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [374] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3740000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3740000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(374000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3740000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3740000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3740000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3740000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [375] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3750000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3750000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(375000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(3750000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3750000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3750000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3750000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [376] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3760000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3760000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(376000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(3760000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3760000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3760000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3760000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [377] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3770000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3770000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3770000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [378] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3780000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3780000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(378000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3780000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [379] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3790000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3790000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(379000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3790000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3790000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3790000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3790000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [380] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "", 0)
        effect:SetCfgID(3800000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "", 0)
          effect:SetCfgID(380000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(5, 26, 3, 1, true)
        effect:SetCfgID(3800000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(5, 26, 3, 1, true)
          effect:SetCfgID(380000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [381] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(3810000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
          effect:SetCfgID(381000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(5, 26, 3, 1, false)
        effect:SetCfgID(3810000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(5, 26, 3, 1, false)
          effect:SetCfgID(381000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [382] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3820000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3820000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(382000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3820000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3820000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3820000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3820000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [383] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3830000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3830000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(383000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3830000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3830000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3830000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3830000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [384] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3840000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3840000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(384000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(3840000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 188 2]")
          root:Add("[trap 188 3]")
          root:Add("[trap 188 4]")
          root:Add("[trap 188 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3840000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 188 6]")
          root:Add("[trap 188 7]")
          root:Add("[trap 188 8]")
          root:Add("[trap 188 9]")
          root:Add("[trap 188 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3840000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3840000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [385] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3850000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3850000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(385000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(3850000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 188 2]")
          root:Add("[trap 188 3]")
          root:Add("[trap 188 4]")
          root:Add("[trap 188 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3850000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 188 6]")
          root:Add("[trap 188 7]")
          root:Add("[trap 188 8]")
          root:Add("[trap 188 9]")
          root:Add("[trap 188 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3850000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3850000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [386] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3860000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3860000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(386000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(3860000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3860000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3860000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3860000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [387] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3870000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3870000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(387000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(3870000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3870000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3870000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3870000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [388] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3880000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3880000)
        root:AddEffect(effect)
      end
      do
        local upper = root or composed
        local root
        if not composed then
          composed = EffectFactory.CreateEffectList()
          root = composed
        else
          root = EffectFactory.CreateEffectList()
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(388000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(388000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
          effect:SetCfgID(388000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
          effect:SetCfgID(388000000)
          root:AddEffect(effect)
        end
        if upper then
          upper:AddEffect(root)
        end
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trigger", 1, "TowerIdle", 1)
        effect:SetCfgID(3880000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3880000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3880000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [389] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(11, 18, 3, 1, true)
        effect:SetCfgID(3890000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(11, 18, 3, 1, true)
          effect:SetCfgID(389000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
        effect:SetCfgID(3890000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(389000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
        effect:SetCfgID(3890000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(389000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
        effect:SetCfgID(3890000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
          effect:SetCfgID(389000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
        effect:SetCfgID(3890000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
          effect:SetCfgID(389000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [390] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(11, 18, 3, 1, true)
        effect:SetCfgID(3900000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(11, 18, 3, 1, true)
          effect:SetCfgID(390000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [391] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(11, 18, 3, 1, false)
        effect:SetCfgID(3910000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(11, 18, 3, 1, false)
          effect:SetCfgID(391000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateDestroyEffect("[switch 190 9]")
        effect:SetCfgID(3910000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateDestroyEffect("[sceneobject 25]")
        effect:SetCfgID(3910000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [392] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3920000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3920000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(392000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3920000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3920000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3920000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3920000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [393] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3930000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3930000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(393000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(3930000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3930000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3930000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3930000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [394] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3940000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3940000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(394000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3940000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3940000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3940000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3940000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [395] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3950000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3950000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(395000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(3950000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3950000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3950000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3950000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [396] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3960000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3960000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(396000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(3960000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3960000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3960000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3960000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [397] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3970000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3970000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(397000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(3970000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3970000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3970000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3970000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [398] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3980000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(3980000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(398000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(3980000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3980000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3980000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3980000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [399] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(3990000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(3990000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(399000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(3990000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(3990000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(3990000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(3990000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [400] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4000000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(4000000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(400000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(4000000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(4000000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(4000000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4000000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [401] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4010000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4010000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(401000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(4010000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(4010000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(4010000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4010000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [402] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4020000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(4020000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(402000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(4020000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(4020000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(4020000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4020000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [403] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4030000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4030000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(403000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(4030000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(4030000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(4030000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4030000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [404] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4040000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(4040000)
        root:AddEffect(effect)
      end
      do
        local upper = root or composed
        local root
        if not composed then
          composed = EffectFactory.CreateEffectList()
          root = composed
        else
          root = EffectFactory.CreateEffectList()
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(404000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(404000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
          effect:SetCfgID(404000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
          effect:SetCfgID(404000000)
          root:AddEffect(effect)
        end
        if upper then
          upper:AddEffect(root)
        end
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trigger", 1, "TowerIdle", 1)
        effect:SetCfgID(4040000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(4040000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4040000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [405] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [406] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4060000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4060000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(406000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 208 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4060000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [407] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(22, 9, 3, 1, true)
        effect:SetCfgID(4070000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(22, 9, 3, 1, true)
          effect:SetCfgID(407000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
        effect:SetCfgID(4070000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(407000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
        effect:SetCfgID(4070000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(407000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
        effect:SetCfgID(4070000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
          effect:SetCfgID(407000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
        effect:SetCfgID(4070000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
          effect:SetCfgID(407000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [408] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(22, 9, 3, 1, true)
        effect:SetCfgID(4080000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(22, 9, 3, 1, true)
          effect:SetCfgID(408000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [409] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateEffectList()
        root = composed
      else
        root = EffectFactory.CreateEffectList()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(22, 9, 3, 1, false)
        effect:SetCfgID(4090000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(22, 9, 3, 1, false)
          effect:SetCfgID(409000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateDestroyEffect("[switch 208 13]")
        effect:SetCfgID(4090000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [410] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4100000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(4100000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 1, "TowerOn1", 1)
        effect:SetCfgID(4100000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 1, "TowerOn1", 1)
          effect:SetCfgID(410000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(4100000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4100000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [411] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4110000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(4110000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 1, "TowerOn2", 1)
        effect:SetCfgID(4110000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 1, "TowerOn2", 1)
          effect:SetCfgID(411000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(4110000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4110000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [412] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4120000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(4120000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 1, "TowerOn3", 1)
        effect:SetCfgID(4120000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 1, "TowerOn3", 1)
          effect:SetCfgID(412000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(4120000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4120000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [413] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4130000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(4130000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 1, "TowerOn4", 1)
        effect:SetCfgID(4130000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 1, "", 1)
          effect:SetCfgID(413000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(4130000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4130000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [414] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4140000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(4140000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(4140000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4140000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [415] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4150000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(4150000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4150000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4150000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [416] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4160000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(4160000)
        root:AddEffect(effect)
      end
      do
        local upper = root or composed
        local root
        if not composed then
          composed = EffectFactory.CreateEffectList()
          root = composed
        else
          root = EffectFactory.CreateEffectList()
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(416000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(416000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
          effect:SetCfgID(416000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
          effect:SetCfgID(416000000)
          root:AddEffect(effect)
        end
        if upper then
          upper:AddEffect(root)
        end
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trigger", 1, "TowerIdle", 1)
        effect:SetCfgID(4160000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(4160000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4160000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [417] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(5, 26, 3, 1, true)
        effect:SetCfgID(4170000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(5, 26, 3, 1, true)
          effect:SetCfgID(417000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
        effect:SetCfgID(4170000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(417000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
        effect:SetCfgID(4170000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(417000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
        effect:SetCfgID(4170000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
          effect:SetCfgID(417000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
        effect:SetCfgID(4170000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
          effect:SetCfgID(417000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [418] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(5, 26, 3, 1, true)
        effect:SetCfgID(4180000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(5, 26, 3, 1, true)
          effect:SetCfgID(418000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [419] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateEffectList()
        root = composed
      else
        root = EffectFactory.CreateEffectList()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(5, 26, 3, 1, false)
        effect:SetCfgID(4190000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(5, 26, 3, 1, false)
          effect:SetCfgID(419000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateDestroyEffect("[switch 209 13]")
        effect:SetCfgID(4190000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [420] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4200000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(4200000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(4200000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4200000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [421] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4210000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(4210000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4210000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4210000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [422] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4220000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(4220000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 1, "TowerOn1", 1)
        effect:SetCfgID(4220000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 1, "TowerOn1", 1)
          effect:SetCfgID(422000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(4220000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4220000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [423] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4230000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(4230000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 1, "TowerOn2", 1)
        effect:SetCfgID(4230000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 1, "TowerOn2", 1)
          effect:SetCfgID(423000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(4230000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4230000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [424] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4240000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(4240000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 1, "TowerOn3", 1)
        effect:SetCfgID(4240000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 1, "TowerOn3", 1)
          effect:SetCfgID(424000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(4240000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4240000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [425] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4250000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(4250000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 1, "TowerOn4", 1)
        effect:SetCfgID(4250000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 1, "", 1)
          effect:SetCfgID(425000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(4250000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4250000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [426] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4260000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(4260000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(426000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(4260000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 210 7]")
          root:Add("[trap 210 8]")
          root:Add("[trap 210 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(4260000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 210 2]")
          root:Add("[trap 210 3]")
          root:Add("[trap 210 4]")
          root:Add("[trap 210 5]")
          root:Add("[trap 210 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(4260000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4260000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [427] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4270000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4270000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(427000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(4270000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 210 7]")
          root:Add("[trap 210 8]")
          root:Add("[trap 210 9]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(4270000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 210 2]")
          root:Add("[trap 210 3]")
          root:Add("[trap 210 4]")
          root:Add("[trap 210 5]")
          root:Add("[trap 210 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(4270000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4270000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [428] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4280000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(4280000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(4280000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4280000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [429] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4290000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(4290000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4290000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4290000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [430] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(28, 6, 3, 1, true)
        effect:SetCfgID(4300000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(28, 6, 3, 1, true)
          effect:SetCfgID(430000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
        effect:SetCfgID(4300000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(430000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
        effect:SetCfgID(4300000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(430000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
        effect:SetCfgID(4300000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
          effect:SetCfgID(430000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
        effect:SetCfgID(4300000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
          effect:SetCfgID(430000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [431] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(28, 6, 3, 1, true)
        effect:SetCfgID(4310000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(28, 6, 3, 1, true)
          effect:SetCfgID(431000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [432] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateEffectList()
        root = composed
      else
        root = EffectFactory.CreateEffectList()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(28, 6, 3, 1, false)
        effect:SetCfgID(4320000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(28, 6, 3, 1, false)
          effect:SetCfgID(432000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateDestroyEffect("[switch 211 13]")
        effect:SetCfgID(4320000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [433] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4330000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(4330000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 1, "TowerOn1", 1)
        effect:SetCfgID(4330000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 1, "TowerOn1", 1)
          effect:SetCfgID(433000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(4330000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4330000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [434] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4340000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(4340000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 1, "TowerOn2", 1)
        effect:SetCfgID(4340000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 1, "TowerOn2", 1)
          effect:SetCfgID(434000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(4340000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4340000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [435] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4350000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(4350000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 1, "TowerOn3", 1)
        effect:SetCfgID(4350000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 1, "TowerOn3", 1)
          effect:SetCfgID(435000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(4350000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4350000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [436] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4360000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(4360000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 1, "TowerOn4", 1)
        effect:SetCfgID(4360000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 1, "", 1)
          effect:SetCfgID(436000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(4360000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4360000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [437] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(4370000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [438] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4380000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4380000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(438000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 209 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4380000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [439] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4390000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4390000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(439000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 209 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4390000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [440] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4400000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4400000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(440000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 209 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4400000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [441] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4410000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4410000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(441000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 209 8]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4410000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [442] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4420000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4420000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(442000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 208 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4420000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [443] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4430000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4430000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(443000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 208 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4430000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [444] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4440000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4440000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(444000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 208 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4440000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [445] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4450000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4450000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(445000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 208 8]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4450000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [446] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4460000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4460000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(446000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 211 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4460000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [447] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4470000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4470000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(447000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 211 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4470000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [448] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4480000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4480000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(448000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 211 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4480000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [449] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4490000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4490000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(449000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 211 8]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4490000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [450] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4500000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(4500000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(450000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(4500000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(4500000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(4500000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4500000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [451] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4510000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4510000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(451000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(4510000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(4510000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(4510000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4510000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [452] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4520000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(4520000)
        root:AddEffect(effect)
      end
      do
        local upper = root or composed
        local root
        if not composed then
          composed = EffectFactory.CreateEffectList()
          root = composed
        else
          root = EffectFactory.CreateEffectList()
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(452000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(452000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
          effect:SetCfgID(452000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
          effect:SetCfgID(452000000)
          root:AddEffect(effect)
        end
        if upper then
          upper:AddEffect(root)
        end
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trigger", 1, "TowerIdle", 1)
        effect:SetCfgID(4520000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(4520000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4520000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [453] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4530000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4530000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(453000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 171 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4530000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [454] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4540000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4540000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(454000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 171 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4540000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [455] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4550000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4550000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(455000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 171 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4550000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [456] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4560000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4560000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(456000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 171 8]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4560000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [457] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(24, 10, 3, 1, true)
        effect:SetCfgID(4570000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(24, 10, 3, 1, true)
          effect:SetCfgID(457000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
        effect:SetCfgID(4570000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(457000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
        effect:SetCfgID(4570000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(457000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
        effect:SetCfgID(4570000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
          effect:SetCfgID(457000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
        effect:SetCfgID(4570000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
          effect:SetCfgID(457000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [458] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(24, 10, 3, 1, true)
        effect:SetCfgID(4580000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(24, 10, 3, 1, true)
          effect:SetCfgID(458000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [459] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateEffectList()
        root = composed
      else
        root = EffectFactory.CreateEffectList()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(24, 10, 3, 1, false)
        effect:SetCfgID(4590000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(24, 10, 3, 1, false)
          effect:SetCfgID(459000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateDestroyEffect("[switch 171 13]")
        effect:SetCfgID(4590000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [460] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4600000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(4600000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(4600000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4600000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [461] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4610000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(4610000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4610000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4610000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [462] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4620000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(4620000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 1, "TowerOn1", 1)
        effect:SetCfgID(4620000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 1, "TowerOn1", 1)
          effect:SetCfgID(462000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(4620000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4620000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [463] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4630000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(4630000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 1, "TowerOn2", 1)
        effect:SetCfgID(4630000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 1, "TowerOn2", 1)
          effect:SetCfgID(463000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(4630000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4630000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [464] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4640000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(4640000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 1, "TowerOn3", 1)
        effect:SetCfgID(4640000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 1, "TowerOn3", 1)
          effect:SetCfgID(464000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(4640000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4640000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [465] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4650000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(4650000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 1, "TowerOn4", 1)
        effect:SetCfgID(4650000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 1, "", 1)
          effect:SetCfgID(465000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(4650000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4650000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [466] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4660000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(4660000)
        root:AddEffect(effect)
      end
      do
        local upper = root or composed
        local root
        if not composed then
          composed = EffectFactory.CreateEffectList()
          root = composed
        else
          root = EffectFactory.CreateEffectList()
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(466000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(466000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
          effect:SetCfgID(466000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
          effect:SetCfgID(466000000)
          root:AddEffect(effect)
        end
        if upper then
          upper:AddEffect(root)
        end
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trigger", 1, "TowerIdle", 1)
        effect:SetCfgID(4660000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(4660000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4660000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [467] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4670000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4670000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(467000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 168 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4670000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [468] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4680000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4680000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(468000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 168 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4680000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [469] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4690000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4690000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(469000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 168 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4690000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [470] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4700000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4700000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(470000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[switch 168 8]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4700000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [471] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(34, 12, 3, 1, true)
        effect:SetCfgID(4710000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(34, 12, 3, 1, true)
          effect:SetCfgID(471000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
        effect:SetCfgID(4710000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(471000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
        effect:SetCfgID(4710000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(471000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
        effect:SetCfgID(4710000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
          effect:SetCfgID(471000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
        effect:SetCfgID(4710000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
          effect:SetCfgID(471000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [472] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(34, 12, 3, 1, true)
        effect:SetCfgID(4720000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(34, 12, 3, 1, true)
          effect:SetCfgID(472000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [473] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateEffectList()
        root = composed
      else
        root = EffectFactory.CreateEffectList()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(34, 12, 3, 1, false)
        effect:SetCfgID(4730000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(34, 12, 3, 1, false)
          effect:SetCfgID(473000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateDestroyEffect("[switch 168 13]")
        effect:SetCfgID(4730000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [474] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4740000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(4740000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(4740000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4740000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [475] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4750000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(4750000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(4750000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4750000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [476] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4760000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(4760000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 1, "TowerOn1", 1)
        effect:SetCfgID(4760000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 1, "TowerOn1", 1)
          effect:SetCfgID(476000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(4760000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4760000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [477] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4770000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(4770000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 1, "TowerOn2", 1)
        effect:SetCfgID(4770000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 1, "TowerOn2", 1)
          effect:SetCfgID(477000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(4770000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4770000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [478] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4780000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(4780000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 1, "TowerOn3", 1)
        effect:SetCfgID(4780000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 1, "TowerOn3", 1)
          effect:SetCfgID(478000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(4780000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4780000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [479] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4790000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(4790000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 1, "TowerOn4", 1)
        effect:SetCfgID(4790000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 1, "", 1)
          effect:SetCfgID(479000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(4790000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4790000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [480] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(4800000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(4800000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(480000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (8)", true)
        effect:SetCfgID(4800000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (8)", true)
          effect:SetCfgID(480000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(4800000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [481] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(4810000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(4810000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(481000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (9)", true)
        effect:SetCfgID(4810000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (9)", true)
          effect:SetCfgID(481000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (4)", false)
        effect:SetCfgID(4810000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (4)", false)
          effect:SetCfgID(481000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(4810000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [482] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(4820000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(4820000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(482000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (17)", true)
        effect:SetCfgID(4820000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (17)", true)
          effect:SetCfgID(482000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
        effect:SetCfgID(4820000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
          effect:SetCfgID(482000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(4820000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [483] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(4830000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(4830000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(483000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (18)", true)
        effect:SetCfgID(4830000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (18)", true)
          effect:SetCfgID(483000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(4830000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [484] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(4840000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(4840000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(484000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (19)", true)
        effect:SetCfgID(4840000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (19)", true)
          effect:SetCfgID(484000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (27)", false)
        effect:SetCfgID(4840000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (27)", false)
          effect:SetCfgID(484000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(4840000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [485] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(4850000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(4850000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(485000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (20)", true)
        effect:SetCfgID(4850000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (20)", true)
          effect:SetCfgID(485000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (13)", false)
        effect:SetCfgID(4850000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (13)", false)
          effect:SetCfgID(485000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (28)", false)
        effect:SetCfgID(4850000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (28)", false)
          effect:SetCfgID(485000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(4850000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [486] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(4860000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(4860000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(486000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (21)", true)
        effect:SetCfgID(4860000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (21)", true)
          effect:SetCfgID(486000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (14)", false)
        effect:SetCfgID(4860000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (14)", false)
          effect:SetCfgID(486000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (13)", true)
        effect:SetCfgID(4860000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (13)", true)
          effect:SetCfgID(486000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (28)", false)
        effect:SetCfgID(4860000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (28)", false)
          effect:SetCfgID(486000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(4860000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [487] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(4870000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(4870000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(487000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (22)", true)
        effect:SetCfgID(4870000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (22)", true)
          effect:SetCfgID(487000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(4870000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [488] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(4880000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 157 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(4880000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 157 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(488000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", true)
        effect:SetCfgID(4880000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", true)
          effect:SetCfgID(488000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
        effect:SetCfgID(4880000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
          effect:SetCfgID(488000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(4880000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [489] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(4890000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 157 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(4890000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 157 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(489000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (6)", true)
        effect:SetCfgID(4890000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (6)", true)
          effect:SetCfgID(489000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
        effect:SetCfgID(4890000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (9)", false)
          effect:SetCfgID(489000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(4890000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [490] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(4900000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(4900000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(490000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
        effect:SetCfgID(4900000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
          effect:SetCfgID(490000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
        effect:SetCfgID(4900000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
          effect:SetCfgID(490000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(4900000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [491] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(4910000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(4910000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(491000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (10)", true)
        effect:SetCfgID(4910000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (10)", true)
          effect:SetCfgID(491000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (1)", false)
        effect:SetCfgID(4910000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (1)", false)
          effect:SetCfgID(491000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (11)", true)
        effect:SetCfgID(4910000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (11)", true)
          effect:SetCfgID(491000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(4910000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [492] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(4920000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(4920000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(492000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (12)", true)
        effect:SetCfgID(4920000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (12)", true)
          effect:SetCfgID(492000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (13)", true)
        effect:SetCfgID(4920000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (13)", true)
          effect:SetCfgID(492000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (5)", false)
        effect:SetCfgID(4920000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (5)", false)
          effect:SetCfgID(492000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (4)", false)
        effect:SetCfgID(4920000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (4)", false)
          effect:SetCfgID(492000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(4920000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [493] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(4930000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(4930000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(493000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (14)", true)
        effect:SetCfgID(4930000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (14)", true)
          effect:SetCfgID(493000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (15)", true)
        effect:SetCfgID(4930000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (15)", true)
          effect:SetCfgID(493000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (6)", false)
        effect:SetCfgID(4930000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (6)", false)
          effect:SetCfgID(493000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (7)", false)
        effect:SetCfgID(4930000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("Particles/Sence_TX_Prefab_JiGuang (7)", false)
          effect:SetCfgID(493000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(4930000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [494] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(4940000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(4940000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(494000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (23)", true)
        effect:SetCfgID(4940000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (23)", true)
          effect:SetCfgID(494000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (24)", true)
        effect:SetCfgID(4940000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (24)", true)
          effect:SetCfgID(494000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
        effect:SetCfgID(4940000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
          effect:SetCfgID(494000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
        effect:SetCfgID(4940000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
          effect:SetCfgID(494000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(4940000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [495] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(4950000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(4950000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(495000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (25)", true)
        effect:SetCfgID(4950000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (25)", true)
          effect:SetCfgID(495000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (26)", true)
        effect:SetCfgID(4950000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (26)", true)
          effect:SetCfgID(495000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
        effect:SetCfgID(4950000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
          effect:SetCfgID(495000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
        effect:SetCfgID(4950000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
          effect:SetCfgID(495000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(4950000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [496] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4960000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 157 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(4960000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 157 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(496000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
        effect:SetCfgID(4960000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
          effect:SetCfgID(496000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
        effect:SetCfgID(4960000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
          effect:SetCfgID(496000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (7)", true)
        effect:SetCfgID(4960000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (7)", true)
          effect:SetCfgID(496000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
        effect:SetCfgID(4960000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (8)", true)
          effect:SetCfgID(496000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4960000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [497] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4970000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 157 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(4970000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 157 5]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(497000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
        effect:SetCfgID(4970000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
          effect:SetCfgID(497000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (9)", true)
        effect:SetCfgID(4970000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (9)", true)
          effect:SetCfgID(497000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (10)", true)
        effect:SetCfgID(4970000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (10)", true)
          effect:SetCfgID(497000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4970000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [498] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(4980000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(4980000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(498000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
        effect:SetCfgID(4980000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
          effect:SetCfgID(498000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", true)
        effect:SetCfgID(4980000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", true)
          effect:SetCfgID(498000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", true)
        effect:SetCfgID(4980000)
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", true)
          effect:SetCfgID(498000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(4980000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [499] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(4990000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(4990000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(499000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(4990000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(4990000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(4990000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(4990000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [500] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(5000000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(5000000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(500000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(5000000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5000000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5000000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(5000000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [501] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(5010000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(5010000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(501000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(5010000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5010000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5010000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(5010000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [502] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(5020000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(5020000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(502000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(5020000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5020000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5020000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(5020000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [503] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(5030000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(5030000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(503000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(5030000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5030000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5030000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(5030000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [504] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(5040000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(5040000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(504000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(5040000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5040000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5040000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(5040000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [505] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(5050000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(5050000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(505000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(5050000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5050000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5050000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(5050000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [506] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(5060000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(5060000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(506000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(5060000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5060000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5060000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(5060000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [507] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(5070000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(5070000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(507000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(5070000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5070000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5070000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(5070000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [508] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(5080000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(5080000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(508000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(5080000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5080000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5080000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(5080000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [509] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(5090000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(5090000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(509000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(5090000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5090000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5090000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(5090000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [510] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect(false)
        effect:SetCfgID(5100000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(5100000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(510000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(5100000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5100000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5100000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect(false)
        effect:SetCfgID(5100000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [511] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5110000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
        effect:SetCfgID(5110000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 1]/Sence_TX_Prefab_JiGuang_fasheqi", false)
          effect:SetCfgID(511000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
        effect:SetCfgID(5110000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", false)
          effect:SetCfgID(511000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5110000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [512] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5120000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
        effect:SetCfgID(5120000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 1]/Sence_TX_Prefab_JiGuang_fasheqi", true)
          effect:SetCfgID(512000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", true)
        effect:SetCfgID(5120000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (1)", true)
          effect:SetCfgID(512000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5120000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [513] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5130000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(5130000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(513000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", true)
        effect:SetCfgID(5130000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", true)
          effect:SetCfgID(513000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
        effect:SetCfgID(5130000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
          effect:SetCfgID(513000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5130000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [514] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5140000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(5140000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(514000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
        effect:SetCfgID(5140000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
          effect:SetCfgID(514000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
        effect:SetCfgID(5140000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", true)
          effect:SetCfgID(514000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5140000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [515] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5150000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(5150000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(515000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
        effect:SetCfgID(5150000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (2)", false)
          effect:SetCfgID(515000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
        effect:SetCfgID(5150000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (3)", false)
          effect:SetCfgID(515000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5150000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [516] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5160000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(5160000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(516000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", true)
        effect:SetCfgID(5160000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", true)
          effect:SetCfgID(516000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (7)", false)
        effect:SetCfgID(5160000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (7)", false)
          effect:SetCfgID(516000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5160000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [517] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5170000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(5170000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(517000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
        effect:SetCfgID(5170000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
          effect:SetCfgID(517000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (7)", true)
        effect:SetCfgID(5170000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (7)", true)
          effect:SetCfgID(517000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5170000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [518] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5180000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(5180000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(518000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
        effect:SetCfgID(5180000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (4)", false)
          effect:SetCfgID(518000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (7)", false)
        effect:SetCfgID(5180000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (7)", false)
          effect:SetCfgID(518000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5180000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [519] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5190000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
        effect:SetCfgID(5190000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_jingmian", true)
          effect:SetCfgID(519000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", true)
        effect:SetCfgID(5190000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", true)
          effect:SetCfgID(519000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (6)", false)
        effect:SetCfgID(5190000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (6)", false)
          effect:SetCfgID(519000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5190000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [520] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5200000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(5200000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(520000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
        effect:SetCfgID(5200000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
          effect:SetCfgID(520000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (6)", true)
        effect:SetCfgID(5200000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (6)", true)
          effect:SetCfgID(520000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5200000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [521] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5210000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
        effect:SetCfgID(5210000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_jingmian", false)
          effect:SetCfgID(521000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
        effect:SetCfgID(5210000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (5)", false)
          effect:SetCfgID(521000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (6)", false)
        effect:SetCfgID(5210000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("SceneParticles/Sence_TX_Prefab_JiGuang (6)", false)
          effect:SetCfgID(521000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5210000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [522] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "", 0)
        effect:SetCfgID(5220000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(522000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(13, 26, 1, 1, true)
        effect:SetCfgID(5220000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(13, 26, 1, 1, true)
          effect:SetCfgID(522000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [523] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(5230000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(523000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(13, 26, 1, 1, false)
        effect:SetCfgID(5230000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(13, 26, 1, 1, false)
          effect:SetCfgID(523000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [524] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5240000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(5240000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "", 0)
        effect:SetCfgID(5240000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(524000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(6, 16, 1, 1, true)
        effect:SetCfgID(5240000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(6, 16, 1, 1, true)
          effect:SetCfgID(524000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5240000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5240000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [525] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5250000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(5250000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(5250000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(525000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(6, 16, 1, 1, false)
        effect:SetCfgID(5250000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(6, 16, 1, 1, false)
          effect:SetCfgID(525000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5250000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5250000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [526] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5260000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(5260000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "", 0)
        effect:SetCfgID(5260000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(526000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(6, 6, 1, 1, true)
        effect:SetCfgID(5260000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(6, 6, 1, 1, true)
          effect:SetCfgID(526000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5260000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5260000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [527] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5270000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(5270000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(5270000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(527000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(6, 6, 1, 1, false)
        effect:SetCfgID(5270000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(6, 6, 1, 1, false)
          effect:SetCfgID(527000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5270000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5270000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [528] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 223 1]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [529] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 223 2]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [530] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 223 3]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [531] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5310000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(5310000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(531000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(16, 15, 1, 1, true)
        effect:SetCfgID(5310000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(16, 15, 1, 1, true)
          effect:SetCfgID(531000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(17, 15, 1, 1, false)
        effect:SetCfgID(5310000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(17, 15, 1, 1, false)
          effect:SetCfgID(531000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(5310000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5310000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5310000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5310000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [532] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5320000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(5320000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(532000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(16, 15, 1, 1, false)
        effect:SetCfgID(5320000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(16, 15, 1, 1, false)
          effect:SetCfgID(532000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(17, 15, 1, 1, true)
        effect:SetCfgID(5320000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(17, 15, 1, 1, true)
          effect:SetCfgID(532000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(5320000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5320000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5320000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5320000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [533] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5330000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(5330000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(533000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(9, 11, 1, 1, true)
        effect:SetCfgID(5330000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(9, 11, 1, 1, true)
          effect:SetCfgID(533000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(11, 11, 1, 1, false)
        effect:SetCfgID(5330000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(11, 11, 1, 1, false)
          effect:SetCfgID(533000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(10, 12, 1, 1, true)
        effect:SetCfgID(5330000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(10, 12, 1, 1, true)
          effect:SetCfgID(533000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(10, 10, 1, 1, false)
        effect:SetCfgID(5330000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(10, 10, 1, 1, false)
          effect:SetCfgID(533000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(5330000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 253 2]")
          root:Add("[trap 253 3]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5330000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5330000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [534] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5340000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(5340000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(534000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(9, 11, 1, 1, false)
        effect:SetCfgID(5340000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(9, 11, 1, 1, false)
          effect:SetCfgID(534000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(11, 11, 1, 1, true)
        effect:SetCfgID(5340000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(11, 11, 1, 1, true)
          effect:SetCfgID(534000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(10, 12, 1, 1, false)
        effect:SetCfgID(5340000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(10, 12, 1, 1, false)
          effect:SetCfgID(534000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(10, 10, 1, 1, true)
        effect:SetCfgID(5340000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(10, 10, 1, 1, true)
          effect:SetCfgID(534000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(5340000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 253 2]")
          root:Add("[trap 253 3]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5340000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5340000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [535] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5350000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(5350000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(535000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(10, 9, 1, 1, true)
        effect:SetCfgID(5350000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(10, 9, 1, 1, true)
          effect:SetCfgID(535000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(10, 7, 1, 1, false)
        effect:SetCfgID(5350000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(10, 7, 1, 1, false)
          effect:SetCfgID(535000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(5350000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5350000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5350000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5350000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [536] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5360000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(5360000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(536000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(10, 9, 1, 1, false)
        effect:SetCfgID(5360000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(10, 9, 1, 1, false)
          effect:SetCfgID(536000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(10, 7, 1, 1, true)
        effect:SetCfgID(5360000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(10, 7, 1, 1, true)
          effect:SetCfgID(536000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(5360000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5360000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5360000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5360000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [537] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5370000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(5370000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(537000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(3, 15, 1, 1, false)
        effect:SetCfgID(5370000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(3, 15, 1, 1, false)
          effect:SetCfgID(537000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(4, 15, 1, 1, false)
        effect:SetCfgID(5370000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(4, 15, 1, 1, false)
          effect:SetCfgID(537000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(5, 15, 1, 1, false)
        effect:SetCfgID(5370000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(5, 15, 1, 1, false)
          effect:SetCfgID(537000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(9, 10, 1, 1, false)
        effect:SetCfgID(5370000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(9, 10, 1, 1, false)
          effect:SetCfgID(537000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(10, 10, 1, 1, false)
        effect:SetCfgID(5370000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(10, 10, 1, 1, false)
          effect:SetCfgID(537000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(3, 17, 1, 1, true)
        effect:SetCfgID(5370000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(3, 17, 1, 1, true)
          effect:SetCfgID(537000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(4, 17, 1, 1, true)
        effect:SetCfgID(5370000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(4, 17, 1, 1, true)
          effect:SetCfgID(537000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(5, 17, 1, 1, true)
        effect:SetCfgID(5370000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(5, 17, 1, 1, true)
          effect:SetCfgID(537000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(9, 12, 1, 1, true)
        effect:SetCfgID(5370000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(9, 12, 1, 1, true)
          effect:SetCfgID(537000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(10, 12, 1, 1, true)
        effect:SetCfgID(5370000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(10, 12, 1, 1, true)
          effect:SetCfgID(537000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(5370000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5370000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5370000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5370000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [538] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5380000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(5380000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(538000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(3, 15, 1, 1, true)
        effect:SetCfgID(5380000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(3, 15, 1, 1, true)
          effect:SetCfgID(538000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(4, 15, 1, 1, true)
        effect:SetCfgID(5380000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(4, 15, 1, 1, true)
          effect:SetCfgID(538000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(5, 15, 1, 1, true)
        effect:SetCfgID(5380000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(5, 15, 1, 1, true)
          effect:SetCfgID(538000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(9, 10, 1, 1, true)
        effect:SetCfgID(5380000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(9, 10, 1, 1, true)
          effect:SetCfgID(538000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(10, 10, 1, 1, true)
        effect:SetCfgID(5380000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(10, 10, 1, 1, true)
          effect:SetCfgID(538000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(3, 17, 1, 1, false)
        effect:SetCfgID(5380000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(3, 17, 1, 1, false)
          effect:SetCfgID(538000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(4, 17, 1, 1, false)
        effect:SetCfgID(5380000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(4, 17, 1, 1, false)
          effect:SetCfgID(538000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(5, 17, 1, 1, false)
        effect:SetCfgID(5380000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(5, 17, 1, 1, false)
          effect:SetCfgID(538000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(9, 12, 1, 1, false)
        effect:SetCfgID(5380000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(9, 12, 1, 1, false)
          effect:SetCfgID(538000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(10, 12, 1, 1, false)
        effect:SetCfgID(5380000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(10, 12, 1, 1, false)
          effect:SetCfgID(538000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(5380000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5380000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5380000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5380000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [539] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5390000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(5390000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(539000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(5390000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5390000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5390000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5390000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [540] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5400000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(5400000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(540000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(5400000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5400000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5400000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5400000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [541] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5410000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(5410000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(541000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(14, 12, 1, 1, true)
        effect:SetCfgID(5410000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(14, 12, 1, 1, true)
          effect:SetCfgID(541000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(15, 12, 1, 1, true)
        effect:SetCfgID(5410000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(15, 12, 1, 1, true)
          effect:SetCfgID(541000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(14, 10, 1, 1, false)
        effect:SetCfgID(5410000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(14, 10, 1, 1, false)
          effect:SetCfgID(541000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(15, 10, 1, 1, false)
        effect:SetCfgID(5410000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(15, 10, 1, 1, false)
          effect:SetCfgID(541000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(5410000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5410000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5410000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5410000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [542] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5420000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(5420000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(542000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(14, 12, 1, 1, false)
        effect:SetCfgID(5420000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(14, 12, 1, 1, false)
          effect:SetCfgID(542000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(15, 12, 1, 1, false)
        effect:SetCfgID(5420000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(15, 12, 1, 1, false)
          effect:SetCfgID(542000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(14, 10, 1, 1, true)
        effect:SetCfgID(5420000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(14, 10, 1, 1, true)
          effect:SetCfgID(542000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(15, 10, 1, 1, true)
        effect:SetCfgID(5420000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(15, 10, 1, 1, true)
          effect:SetCfgID(542000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(5420000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5420000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5420000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5420000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [543] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5430000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(5430000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(543000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(12, 19, 1, 1, true)
        effect:SetCfgID(5430000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(12, 19, 1, 1, true)
          effect:SetCfgID(543000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(12, 16, 1, 1, false)
        effect:SetCfgID(5430000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(12, 16, 1, 1, false)
          effect:SetCfgID(543000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(5430000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5430000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5430000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5430000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [544] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5440000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(5440000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(544000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(12, 19, 1, 1, false)
        effect:SetCfgID(5440000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(12, 19, 1, 1, false)
          effect:SetCfgID(544000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(12, 16, 1, 1, true)
        effect:SetCfgID(5440000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(12, 16, 1, 1, true)
          effect:SetCfgID(544000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(5440000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5440000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5440000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5440000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [545] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5450000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(5450000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(545000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(11, 20, 1, 1, true)
        effect:SetCfgID(5450000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(11, 20, 1, 1, true)
          effect:SetCfgID(545000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(13, 20, 1, 1, true)
        effect:SetCfgID(5450000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(13, 20, 1, 1, true)
          effect:SetCfgID(545000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(11, 18, 1, 1, false)
        effect:SetCfgID(5450000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(11, 18, 1, 1, false)
          effect:SetCfgID(545000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(13, 18, 1, 1, false)
        effect:SetCfgID(5450000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(13, 18, 1, 1, false)
          effect:SetCfgID(545000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(5450000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5450000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5450000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5450000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [546] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5460000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(5460000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(546000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(11, 20, 1, 1, false)
        effect:SetCfgID(5460000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(11, 20, 1, 1, false)
          effect:SetCfgID(546000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(13, 20, 1, 1, false)
        effect:SetCfgID(5460000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(13, 20, 1, 1, false)
          effect:SetCfgID(546000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(11, 18, 1, 1, true)
        effect:SetCfgID(5460000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(11, 18, 1, 1, true)
          effect:SetCfgID(546000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(13, 18, 1, 1, true)
        effect:SetCfgID(5460000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(13, 18, 1, 1, true)
          effect:SetCfgID(546000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(5460000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5460000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5460000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5460000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [547] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5470000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(5470000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(547000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(5470000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 260 5]")
          root:Add("[trap 260 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5470000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5470000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [548] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5480000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(5480000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(548000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(5480000)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 260 5]")
          root:Add("[trap 260 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5480000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5480000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [549] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5490000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(5490000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(549000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(5490000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5490000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5490000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5490000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [550] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5500000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(5500000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(550000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(5500000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5500000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5500000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5500000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [551] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5510000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(5510000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(551000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(5510000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5510000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5510000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5510000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [552] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5520000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(5520000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(552000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(5520000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5520000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5520000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5520000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [553] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5530000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0)
        effect:SetCfgID(5530000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(553000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(5530000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5530000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5530000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5530000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [554] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5540000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0)
        effect:SetCfgID(5540000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(554000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(5540000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5540000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5540000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5540000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [555] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "GoLeft", 0.25)
        effect:SetCfgID(5550000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(555000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(11, 25, 1, 1, true)
        effect:SetCfgID(5550000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(11, 25, 1, 1, true)
          effect:SetCfgID(555000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(13, 25, 1, 1, false)
        effect:SetCfgID(5550000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(13, 25, 1, 1, false)
          effect:SetCfgID(555000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [556] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "GoRight", 0.25)
        effect:SetCfgID(5560000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(556000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(11, 25, 1, 1, false)
        effect:SetCfgID(5560000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(11, 25, 1, 1, false)
          effect:SetCfgID(556000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(13, 25, 1, 1, true)
        effect:SetCfgID(5560000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(13, 25, 1, 1, true)
          effect:SetCfgID(556000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [557] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "GoDown", 0.25)
        effect:SetCfgID(5570000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(557000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(12, 24, 1, 1, false)
        effect:SetCfgID(5570000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(12, 24, 1, 1, false)
          effect:SetCfgID(557000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(12, 26, 1, 1, true)
        effect:SetCfgID(5570000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(12, 26, 1, 1, true)
          effect:SetCfgID(557000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [558] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "GoUp", 0.25)
        effect:SetCfgID(5580000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(558000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(12, 24, 1, 1, true)
        effect:SetCfgID(5580000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(12, 24, 1, 1, true)
          effect:SetCfgID(558000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(12, 26, 1, 1, false)
        effect:SetCfgID(5580000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(12, 26, 1, 1, false)
          effect:SetCfgID(558000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [559] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5590000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(5590000)
        root:AddEffect(effect)
      end
      do
        local upper = root or composed
        local root
        if not composed then
          composed = EffectFactory.CreateEffectList()
          root = composed
        else
          root = EffectFactory.CreateEffectList()
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(559000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(559000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
          effect:SetCfgID(559000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
          effect:SetCfgID(559000000)
          root:AddEffect(effect)
        end
        if upper then
          upper:AddEffect(root)
        end
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trigger", 1, "TowerIdle", 1)
        effect:SetCfgID(5590000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5590000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5590000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [560] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(34, 13, 3, 1, true)
        effect:SetCfgID(5600000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(34, 13, 3, 1, true)
          effect:SetCfgID(560000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
        effect:SetCfgID(5600000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(560000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
        effect:SetCfgID(5600000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(560000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
        effect:SetCfgID(5600000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
          effect:SetCfgID(560000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
        effect:SetCfgID(5600000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
          effect:SetCfgID(560000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [561] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(34, 13, 3, 1, true)
        effect:SetCfgID(5610000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(34, 13, 3, 1, true)
          effect:SetCfgID(561000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [562] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(34, 13, 3, 1, false)
        effect:SetCfgID(5620000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(34, 13, 3, 1, false)
          effect:SetCfgID(562000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateDestroyEffect("[switch 264 9]")
        effect:SetCfgID(5620000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateDestroyEffect("[sceneobject 42]")
        effect:SetCfgID(5620000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [563] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5630000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(5630000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 1, "TowerOn1", 1)
        effect:SetCfgID(5630000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 1, "TowerOn1", 1)
          effect:SetCfgID(563000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5630000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5630000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [564] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5640000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(5640000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 1, "TowerOn2", 1)
        effect:SetCfgID(5640000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 1, "TowerOn2", 1)
          effect:SetCfgID(564000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5640000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5640000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [565] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5650000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(5650000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 1, "TowerOn3", 1)
        effect:SetCfgID(5650000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 1, "TowerOn3", 1)
          effect:SetCfgID(565000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5650000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5650000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [566] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5660000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(5660000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 1, "TowerOn4", 1)
        effect:SetCfgID(5660000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 1, "", 1)
          effect:SetCfgID(566000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5660000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5660000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [567] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5670000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(5670000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(567000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(5670000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5670000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5670000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5670000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [568] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5680000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(5680000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(568000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(5680000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(5680000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5680000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5680000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [569] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5690000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(5690000)
        root:AddEffect(effect)
      end
      do
        local upper = root or composed
        local root
        if not composed then
          composed = EffectFactory.CreateEffectList()
          root = composed
        else
          root = EffectFactory.CreateEffectList()
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(569000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(569000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
          effect:SetCfgID(569000000)
          root:AddEffect(effect)
        end
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
          effect:SetCfgID(569000000)
          root:AddEffect(effect)
        end
        if upper then
          upper:AddEffect(root)
        end
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trigger", 1, "TowerIdle", 1)
        effect:SetCfgID(5690000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5690000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5690000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [570] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(22, 9, 3, 1, true)
        effect:SetCfgID(5700000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(22, 9, 3, 1, true)
          effect:SetCfgID(570000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
        effect:SetCfgID(5700000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 0, "TowerOff1", 1)
          effect:SetCfgID(570000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
        effect:SetCfgID(5700000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 0, "TowerOff2", 1)
          effect:SetCfgID(570000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
        effect:SetCfgID(5700000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 0, "TowerOff3", 1)
          effect:SetCfgID(570000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
        effect:SetCfgID(5700000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 0, "TowerOff4", 1)
          effect:SetCfgID(570000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [571] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(22, 9, 3, 1, true)
        effect:SetCfgID(5710000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(22, 9, 3, 1, true)
          effect:SetCfgID(571000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [572] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(22, 9, 3, 1, false)
        effect:SetCfgID(5720000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(22, 9, 3, 1, false)
          effect:SetCfgID(572000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateDestroyEffect("[switch 266 9]")
        effect:SetCfgID(5720000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateDestroyEffect("[sceneobject 43]")
        effect:SetCfgID(5720000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [573] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5730000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(5730000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 1, "TowerOn1", 1)
        effect:SetCfgID(5730000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal1", 1, "TowerOn1", 1)
          effect:SetCfgID(573000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5730000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5730000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [574] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5740000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(5740000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 1, "TowerOn2", 1)
        effect:SetCfgID(5740000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal2", 1, "TowerOn2", 1)
          effect:SetCfgID(574000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5740000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5740000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [575] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5750000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(5750000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 1, "TowerOn3", 1)
        effect:SetCfgID(5750000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal3", 1, "TowerOn3", 1)
          effect:SetCfgID(575000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5750000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5750000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [576] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5760000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(5760000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 1, "TowerOn4", 1)
        effect:SetCfgID(5760000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "crystal4", 1, "", 1)
          effect:SetCfgID(576000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(5760000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5760000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [577] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "GoUp", 0.25)
        effect:SetCfgID(5770000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(577000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(12, 24, 1, 1, false)
        effect:SetCfgID(5770000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(12, 24, 1, 1, false)
          effect:SetCfgID(577000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(12, 26, 1, 1, true)
        effect:SetCfgID(5770000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(12, 26, 1, 1, true)
          effect:SetCfgID(577000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [578] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "GoUp", 0.25)
        effect:SetCfgID(5780000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(578000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(12, 24, 1, 1, true)
        effect:SetCfgID(5780000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(12, 24, 1, 1, true)
          effect:SetCfgID(578000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateBlockChangeEffect(12, 26, 1, 1, false)
        effect:SetCfgID(5780000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateBlockChangeEffect(12, 26, 1, 1, false)
          effect:SetCfgID(578000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [579] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(579057900)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(579057901)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(57905790100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(579057902)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(579057903)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(579057904)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(579057905)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [580] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(580058000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(580058001)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(58005800100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(580058002)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapStateChangeEffect(args.traps, args.switches)
        effect:SetCfgID(580058003)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(580058004)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(580058005)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [581] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5810500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(5810500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(581050000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(5810500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(581050000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(5810500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(581050000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(5810500)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5810505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [582] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5820600)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(5820600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(582060000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(5820600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(582060000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(5820600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(582060000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(5820600)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5820606)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [583] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5830700)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(5830700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(583070000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(5830700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(583070000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(5830700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(583070000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(5830700)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5830707)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [584] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5840800)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(5840800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(584080000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(5840800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(584080000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(5840800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 9 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(584080000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(5840800)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5840808)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [585] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5850500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(5850500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(585050000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(5850500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(585050000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(5850500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(585050000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(5850500)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5850505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [586] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5860600)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(5860600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(586060000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(5860600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(586060000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(5860600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(586060000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(5860600)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5860606)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [587] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5870700)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(5870700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(587070000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(5870700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(587070000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(5870700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(587070000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(5870700)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5870707)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [588] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5880800)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(5880800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(588080000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(5880800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(588080000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(5880800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(588080000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(5880800)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5880808)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [589] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5890500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(5890500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(589050000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(5890500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(589050000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(5890500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(589050000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(5890500)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5890505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [590] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5900600)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(5900600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(590060000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(5900600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(590060000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(5900600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(590060000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(5900600)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5900606)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [591] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5910700)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(5910700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(591070000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(5910700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(591070000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(5910700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(591070000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(5910700)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5910707)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [592] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5920800)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(5920800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(592080000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(5920800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(592080000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(5920800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 3]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(592080000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(5920800)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5920808)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [593] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5930500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(5930500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(593050000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(5930500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(593050000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(5930500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(593050000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(5930500)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5930505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [594] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5940600)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(5940600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(594060000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(5940600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(594060000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(5940600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(594060000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(5940600)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5940606)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [595] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5950700)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(5950700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(595070000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(5950700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(595070000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(5950700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(595070000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(5950700)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5950707)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [596] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5960800)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(5960800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(596080000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(5960800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(596080000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(5960800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 4]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(596080000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(5960800)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5960808)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [597] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5970500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(5970500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(597050000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(5970500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(597050000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(5970500)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(597050000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(5970500)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5970505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [598] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5980600)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(5980600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(598060000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(5980600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(598060000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(5980600)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(598060000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(5980600)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5980606)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [599] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(5990700)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(5990700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(599070000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(5990700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(599070000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(5990700)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(599070000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(5990700)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(5990707)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [600] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6000800)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(6000800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(600080000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6000800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(600080000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6000800)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 10 5]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(600080000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6000800)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6000808)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [601] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6010000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(6010000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(601000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6010000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(601000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6010000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(601000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6010000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6010000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [602] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6020000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(6020000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(602000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6020000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(602000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6020000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(602000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6020000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6020000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [603] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6030000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(6030000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(603000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(6030000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(603000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(6030000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(603000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6030000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6030000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [604] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6040000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(6040000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(604000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6040000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(604000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6040000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(604000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6040000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6040000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [605] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6050000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(6050000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(605000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(6050000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(605000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(6050000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(605000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6050000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6050000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [606] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6060000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(6060000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(606000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6060000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(606000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6060000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(606000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6060000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6060000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [607] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6070000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(6070000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(607000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6070000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(607000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6070000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(607000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6070000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6070000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [608] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6080000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(6080000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(608000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6080000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(608000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6080000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 122 3]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(608000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6080000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6080000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [609] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6090000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(6090000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(609000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(6090000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(609000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(6090000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(609000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6090000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6090000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [610] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6100000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(6100000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(610000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6100000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(610000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6100000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(610000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6100000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6100000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [611] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6110000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(6110000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(611000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6110000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(611000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6110000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(611000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6110000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6110000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [612] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6120000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(6120000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(612000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6120000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(612000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6120000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(612000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6120000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6120000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [613] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6130000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(6130000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(613000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6130000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(613000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6130000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(613000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6130000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6130000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [614] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6140000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(6140000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(614000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6140000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(614000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6140000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(614000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6140000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6140000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [615] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6150000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(6150000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(615000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6150000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(615000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6150000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(615000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6150000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6150000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [616] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6160000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(6160000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(616000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(6160000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(616000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(6160000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 135 3]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(616000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6160000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6160000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [617] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6170000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(6170000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(617000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6170000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(617000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6170000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(617000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6170000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6170000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [618] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6180000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(6180000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(618000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(6180000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(618000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(6180000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(618000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6180000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6180000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [619] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6190000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(6190000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(619000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(6190000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(619000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(6190000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(619000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6190000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6190000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [620] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6200000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(6200000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(620000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6200000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(620000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6200000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 136 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(620000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6200000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6200000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [621] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6210000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(6210000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(621000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6210000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(621000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6210000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(621000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6210000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6210000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [622] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6220000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(6220000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(622000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(6220000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(622000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(6220000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(622000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6220000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6220000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [623] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6230000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(6230000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(623000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6230000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(623000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6230000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(623000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6230000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6230000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [624] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6240000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(6240000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(624000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6240000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(624000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6240000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(624000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6240000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6240000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [625] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6250000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(6250000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(625000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(6250000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(625000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(6250000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(625000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6250000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6250000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [626] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6260000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(6260000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(626000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6260000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(626000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6260000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(626000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6260000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6260000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [627] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6270000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(6270000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(627000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6270000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(627000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6270000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(627000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6270000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6270000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [628] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6280000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(6280000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(628000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(6280000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(628000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(6280000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 3]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(628000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6280000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6280000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [629] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6290000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(6290000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(629000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6290000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(629000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6290000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(629000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6290000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6290000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [630] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6300000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(6300000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(630000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6300000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(630000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6300000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(630000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6300000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6300000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [631] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6310000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(6310000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(631000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(6310000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(631000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(6310000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(631000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6310000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6310000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [632] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6320000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(6320000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(632000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6320000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(632000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6320000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 4]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(632000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6320000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6320000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [633] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6330000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(6330000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(633000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6330000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(633000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6330000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(633000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6330000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6330000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [634] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6340000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(6340000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(634000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6340000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(634000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6340000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(634000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6340000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6340000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [635] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6350000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(6350000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(635000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6350000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(635000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6350000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(635000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6350000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6350000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [636] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6360000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(6360000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(636000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(6360000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(636000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(6360000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 5]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(636000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6360000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6360000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [637] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6370000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(6370000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(637000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6370000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(637000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6370000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(637000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6370000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6370000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [638] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6380000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(6380000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(638000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(6380000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(638000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(6380000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(638000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6380000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6380000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [639] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6390000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(6390000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(639000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6390000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(639000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6390000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(639000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6390000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6390000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [640] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6400000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(6400000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(640000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6400000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(640000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6400000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 140 6]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(640000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6400000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6400000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [641] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6410000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(6410000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(641000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6410000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(641000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6410000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(641000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6410000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6410000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [642] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6420000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(6420000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(642000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6420000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(642000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6420000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(642000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6420000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6420000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [643] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6430000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(6430000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(643000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(6430000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(643000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(6430000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(643000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6430000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6430000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [644] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6440000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(6440000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(644000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6440000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(644000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6440000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(644000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6440000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6440000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [645] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6450000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(6450000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(645000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(6450000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(645000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(6450000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(645000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6450000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6450000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [646] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6460000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(6460000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(646000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6460000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(646000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6460000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(646000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6460000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6460000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [647] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6470000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(6470000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(647000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6470000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(647000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6470000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(647000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6470000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6470000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [648] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6480000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(6480000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(648000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6480000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(648000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6480000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 163 3]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(648000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6480000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6480000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [649] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6490000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(6490000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(649000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6490000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(649000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6490000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(649000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6490000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6490000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [650] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6500000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(6500000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(650000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(6500000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(650000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(6500000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(650000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6500000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6500000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [651] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6510000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(6510000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(651000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(6510000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(651000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(6510000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(651000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6510000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6510000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [652] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6520000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(6520000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(652000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6520000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(652000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6520000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 2]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(652000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6520000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6520000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [653] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6530000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(6530000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(653000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6530000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(653000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6530000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(653000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6530000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6530000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [654] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6540000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(6540000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(654000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6540000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(654000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6540000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(654000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6540000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6540000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [655] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6550000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(6550000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(655000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(6550000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(655000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(6550000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(655000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6550000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6550000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [656] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6560000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(6560000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(656000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6560000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(656000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6560000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 3]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(656000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6560000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6560000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [657] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6570000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "", 0)
        effect:SetCfgID(6570000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(657000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(6570000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(657000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(6570000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(657000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6570000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6570000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [658] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6580000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 2, "", 0)
        effect:SetCfgID(6580000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 2, "", 0)
          effect:SetCfgID(658000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6580000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(658000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6580000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(658000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6580000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6580000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [659] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6590000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 3, "", 0)
        effect:SetCfgID(6590000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 3, "", 0)
          effect:SetCfgID(659000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_right", false)
        effect:SetCfgID(6590000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_right", false)
          effect:SetCfgID(659000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_wrong", true)
        effect:SetCfgID(6590000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_wrong", true)
          effect:SetCfgID(659000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6590000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6590000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [660] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(6600000)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 4, "", 0)
        effect:SetCfgID(6600000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 4, "", 0)
          effect:SetCfgID(660000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_right", true)
        effect:SetCfgID(6600000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_right", true)
          effect:SetCfgID(660000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_wrong", false)
        effect:SetCfgID(6600000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateVisualEffect("[switch 222 4]/Sence_TX_Prefab_JiGuang_wrong", false)
          effect:SetCfgID(660000000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateTrapCheckEffect(args.traps, args.switches)
        effect:SetCfgID(6600000)
        effect:SetParam("runoninit")
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(6600000)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [665] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(66502500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(66502501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(6650250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 269 3]")
          root:Add("[trap 269 4]")
          root:Add("[trap 269 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(66502503)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 269 6]")
          root:Add("[trap 269 7]")
          root:Add("[trap 269 8]")
          root:Add("[trap 269 9]")
          root:Add("[trap 269 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(66502505)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(66502506)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [666] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 0, "GoUp", 1)
        effect:SetCfgID(66603900)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 0, "", 0)
          effect:SetCfgID(6660390000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [667] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "trapState", 1, "GoDown", 1)
        effect:SetCfgID(66704000)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("nil", "loadState", 1, "", 0)
          effect:SetCfgID(6670400000)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [668] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(66802500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(66802501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(6680250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 269 3]")
          root:Add("[trap 269 4]")
          root:Add("[trap 269 5]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(66802503)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 269 6]")
          root:Add("[trap 269 7]")
          root:Add("[trap 269 8]")
          root:Add("[trap 269 9]")
          root:Add("[trap 269 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(66802505)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(66802506)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [669] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(66902500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(66902501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(6690250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(66902503)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 269 6]")
          root:Add("[trap 269 7]")
          root:Add("[trap 269 8]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(66902505)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(66902506)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [670] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(67002500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(67002501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(6700250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(67002503)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 269 6]")
          root:Add("[trap 269 7]")
          root:Add("[trap 269 8]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(67002505)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(67002506)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [671] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(67102500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(67102501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(6710250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 271 2]")
          root:Add("[trap 271 3]")
          root:Add("[trap 271 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(67102503)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 271 5]")
          root:Add("[trap 271 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(67102505)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(67102506)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [672] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(67202500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(67202501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(6720250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 271 2]")
          root:Add("[trap 271 3]")
          root:Add("[trap 271 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(67202503)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 271 5]")
          root:Add("[trap 271 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(67202505)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(67202506)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [673] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(67302500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(67302501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(6730250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(67302502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 272 2]")
          root:Add("[trap 272 3]")
          root:Add("[trap 272 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(67302504)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 272 5]")
          root:Add("[trap 272 6]")
          root:Add("[trap 272 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(67302506)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(67302507)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [674] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(67402500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(67402501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(6740250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(67402502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 272 2]")
          root:Add("[trap 272 3]")
          root:Add("[trap 272 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(67402504)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 272 5]")
          root:Add("[trap 272 6]")
          root:Add("[trap 272 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(67402506)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(67402507)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [675] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(67502500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(67502501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(6750250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(67502502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 273 2]")
          root:Add("[trap 273 3]")
          root:Add("[trap 273 4]")
          root:Add("[trap 273 5]")
          root:Add("[trap 273 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(67502504)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(67502505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [676] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(67602500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(67602501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(6760250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(67602502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 273 2]")
          root:Add("[trap 273 3]")
          root:Add("[trap 273 4]")
          root:Add("[trap 273 5]")
          root:Add("[trap 273 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(67602504)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(67602505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [677] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(67702500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(67702501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(6770250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(67702502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 274 2]")
          root:Add("[trap 274 3]")
          root:Add("[trap 274 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(67702504)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(67702505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [678] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(67802500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(67802501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(6780250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(67802502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 274 2]")
          root:Add("[trap 274 3]")
          root:Add("[trap 274 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(67802504)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(67802505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [679] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(67902500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(67902501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(6790250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(67902502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 275 2]")
          root:Add("[trap 275 3]")
          root:Add("[trap 275 4]")
          root:Add("[trap 275 5]")
          root:Add("[trap 275 6]")
          root:Add("[trap 275 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(67902504)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(67902505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [680] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(68002500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(68002501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(6800250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(68002502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 275 2]")
          root:Add("[trap 275 3]")
          root:Add("[trap 275 4]")
          root:Add("[trap 275 5]")
          root:Add("[trap 275 6]")
          root:Add("[trap 275 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(68002504)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(68002505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [681] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(68102500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(68102501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(6810250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(68102502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 276 2]")
          root:Add("[trap 276 3]")
          root:Add("[trap 276 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(68102504)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(68102505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [682] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(68202500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(68202501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(6820250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(68202502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 276 2]")
          root:Add("[trap 276 3]")
          root:Add("[trap 276 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(68202504)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(68202505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [683] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(68302500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(68302501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(6830250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(68302502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 277 2]")
          root:Add("[trap 277 3]")
          root:Add("[trap 277 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(68302504)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(68302505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [684] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(68402500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(68402501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(6840250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM02")
        effect:SetCfgID(68402502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 277 2]")
          root:Add("[trap 277 3]")
          root:Add("[trap 277 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(68402504)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(68402505)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [685] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(68502500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(68502501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(6850250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(68502502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 278 2]")
          root:Add("[trap 278 3]")
          root:Add("[trap 278 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM08")
        effect:SetCfgID(68502504)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 278 5]")
          root:Add("[trap 278 6]")
          root:Add("[trap 278 7]")
          root:Add("[trap 278 8]")
          root:Add("[trap 278 9]")
          root:Add("[trap 278 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(68502506)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(68502507)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [686] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(68602500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(68602501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(6860250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(68602502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 278 2]")
          root:Add("[trap 278 3]")
          root:Add("[trap 278 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM08")
        effect:SetCfgID(68602504)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 278 5]")
          root:Add("[trap 278 6]")
          root:Add("[trap 278 7]")
          root:Add("[trap 278 8]")
          root:Add("[trap 278 9]")
          root:Add("[trap 278 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(68602506)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(68602507)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [687] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(68702500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(68702501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(6870250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(68702502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 279 4]")
          root:Add("[trap 279 5]")
          root:Add("[trap 279 6]")
          root:Add("[trap 279 7]")
          root:Add("[trap 279 8]")
          root:Add("[trap 279 9]")
          root:Add("[trap 279 10]")
          root:Add("[trap 279 11]")
          root:Add("[trap 279 12]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(68702504)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 279 13]")
          root:Add("[trap 279 14]")
          root:Add("[trap 279 15]")
          root:Add("[trap 279 16]")
          root:Add("[trap 279 17]")
          root:Add("[trap 279 18]")
          root:Add("[trap 279 19]")
          root:Add("[trap 279 20]")
          root:Add("[trap 279 21]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(68702506)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 279 22]")
          root:Add("[trap 279 23]")
          root:Add("[trap 279 24]")
          root:Add("[trap 279 25]")
          root:Add("[trap 279 26]")
          root:Add("[trap 279 27]")
          root:Add("[trap 279 28]")
          root:Add("[trap 279 29]")
          root:Add("[trap 279 30]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(68702508)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(68702509)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [688] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(68802500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(68802501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(6880250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(68802502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 279 4]")
          root:Add("[trap 279 5]")
          root:Add("[trap 279 6]")
          root:Add("[trap 279 7]")
          root:Add("[trap 279 8]")
          root:Add("[trap 279 9]")
          root:Add("[trap 279 10]")
          root:Add("[trap 279 11]")
          root:Add("[trap 279 12]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(68802504)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 279 13]")
          root:Add("[trap 279 14]")
          root:Add("[trap 279 15]")
          root:Add("[trap 279 16]")
          root:Add("[trap 279 17]")
          root:Add("[trap 279 18]")
          root:Add("[trap 279 19]")
          root:Add("[trap 279 20]")
          root:Add("[trap 279 21]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(68802506)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 279 22]")
          root:Add("[trap 279 23]")
          root:Add("[trap 279 24]")
          root:Add("[trap 279 25]")
          root:Add("[trap 279 26]")
          root:Add("[trap 279 27]")
          root:Add("[trap 279 28]")
          root:Add("[trap 279 29]")
          root:Add("[trap 279 30]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(68802508)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(68802509)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [689] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(68902500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(68902501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(6890250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(68902502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 279 4]")
          root:Add("[trap 279 8]")
          root:Add("[trap 279 12]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(68902504)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 279 13]")
          root:Add("[trap 279 17]")
          root:Add("[trap 279 21]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(68902506)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 279 22]")
          root:Add("[trap 279 26]")
          root:Add("[trap 279 30]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(68902508)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(68902509)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [690] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(69002500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(69002501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(6900250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(69002502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 279 4]")
          root:Add("[trap 279 8]")
          root:Add("[trap 279 12]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(69002504)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 279 13]")
          root:Add("[trap 279 17]")
          root:Add("[trap 279 21]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(69002506)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 279 22]")
          root:Add("[trap 279 26]")
          root:Add("[trap 279 30]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(69002508)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(69002509)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [691] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(69102500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(69102501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(6910250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(69102502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 279 6]")
          root:Add("[trap 279 8]")
          root:Add("[trap 279 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(69102504)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 279 15]")
          root:Add("[trap 279 17]")
          root:Add("[trap 279 19]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(69102506)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 279 24]")
          root:Add("[trap 279 26]")
          root:Add("[trap 279 28]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(69102508)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(69102509)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [692] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(69202500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(69202501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(6920250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM07")
        effect:SetCfgID(69202502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 279 6]")
          root:Add("[trap 279 8]")
          root:Add("[trap 279 10]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(69202504)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 279 15]")
          root:Add("[trap 279 17]")
          root:Add("[trap 279 19]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(69202506)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Sequence.Create()
          root:Add("[trap 279 24]")
          root:Add("[trap 279 26]")
          root:Add("[trap 279 28]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(69202508)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(69202509)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [693] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(69302500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(69302501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(6930250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(69302502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 282 2]")
          root:Add("[trap 282 3]")
          root:Add("[trap 282 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 282 5]")
          root:Add("[trap 282 6]")
          root:Add("[trap 282 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(69302506)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(69302507)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [694] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(69402500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(69402501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(6940250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(69402502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 282 2]")
          root:Add("[trap 282 3]")
          root:Add("[trap 282 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 282 5]")
          root:Add("[trap 282 6]")
          root:Add("[trap 282 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(69402506)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(69402507)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [695] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(69502500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(69502501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(6950250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(69502502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 283 2]")
          root:Add("[trap 283 3]")
          root:Add("[trap 283 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(69502504)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 283 5]")
          root:Add("[trap 283 6]")
          root:Add("[trap 283 7]")
          root:Add("[trap 283 8]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(69502506)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(69502507)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [696] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(69602500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(69602501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(6960250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM05")
        effect:SetCfgID(69602502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 283 2]")
          root:Add("[trap 283 3]")
          root:Add("[trap 283 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM06")
        effect:SetCfgID(69602504)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 283 5]")
          root:Add("[trap 283 6]")
          root:Add("[trap 283 7]")
          root:Add("[trap 283 8]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(69602506)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(69602507)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [697] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(69702500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(69702501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(6970250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(69702502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 284 2]")
          root:Add("[trap 284 3]")
          root:Add("[trap 284 4]")
          root:Add("[trap 284 5]")
          root:Add("[trap 284 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(69702506)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(69702507)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [698] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(69802500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(69802501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(6980250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(69802502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 284 2]")
          root:Add("[trap 284 3]")
          root:Add("[trap 284 4]")
          root:Add("[trap 284 5]")
          root:Add("[trap 284 6]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(69802506)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(69802507)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [699] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(69902500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(69902501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(6990250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 285 2]")
          root:Add("[trap 285 3]")
          root:Add("[trap 285 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(69902507)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [700] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(70002500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(70002501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(7000250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 285 2]")
          root:Add("[trap 285 3]")
          root:Add("[trap 285 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(70002507)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [701] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(70102500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(70102501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(7010250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(70102502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 286 2]")
          root:Add("[trap 286 3]")
          root:Add("[trap 286 4]")
          root:Add("[trap 286 5]")
          root:Add("[trap 286 6]")
          root:Add("[trap 286 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(70102506)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(70102507)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [702] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(70202500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(70202501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(7020250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM03")
        effect:SetCfgID(70202502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 286 2]")
          root:Add("[trap 286 3]")
          root:Add("[trap 286 4]")
          root:Add("[trap 286 5]")
          root:Add("[trap 286 6]")
          root:Add("[trap 286 7]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(70202506)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(70202507)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [703] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(70302500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 0, "", 0.25)
        effect:SetCfgID(70302501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 0, "", 0)
          effect:SetCfgID(7030250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(70302502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 287 2]")
          root:Add("[trap 287 3]")
          root:Add("[trap 287 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(70302506)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(70302507)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end,
  [704] = function(args)
    local composed
    do
      local upper = root or composed
      local root
      if not composed then
        composed = EffectFactory.CreateComposedEffect()
        root = composed
      else
        root = EffectFactory.CreateComposedEffect()
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateFreezeEffect()
        effect:SetCfgID(70402500)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "SwitchState", 1, "", 0.25)
        effect:SetCfgID(70402501)
        effect:SetParam("runoninit")
        do
          local upper = effect
          local effect = EffectFactory.CreateAnimatorStateChangeEffect("", "loadState", 1, "", 0)
          effect:SetCfgID(7040250100)
          upper:SetParam("alternative", effect)
        end
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM04")
        effect:SetCfgID(70402502)
        root:AddEffect(effect)
      end
      do
        local upper = root
        local root = ChangeTrapStateEffect.Create()
        do
          local upper = root
          local root = ChangeTrapStateEffect.Parallel.Create()
          root:Add("[trap 287 2]")
          root:Add("[trap 287 3]")
          root:Add("[trap 287 4]")
          upper:Add(root)
        end
        upper:AddEffect(root)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateAnimationEffect("CM StateDrivenCamera1", "CM01")
        effect:SetCfgID(70402506)
        root:AddEffect(effect)
      end
      do
        local upper = effect
        local effect = EffectFactory.CreateThawEffect()
        effect:SetCfgID(70402507)
        root:AddEffect(effect)
      end
      if upper then
        upper:AddEffect(root)
      end
    end
    return composed
  end
}
return effects
