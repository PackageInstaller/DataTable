---自动生成代码----start----
local LotteryStartView = BaseClass( 'LotteryStartView' , BaseView )
local M = LotteryStartView 
--desc

function M:__init(go)
--require

--declare
  local trans = go.transform 
  self.trans_On =   self._binder:GetValue('trans_On')  
end
return LotteryStartView
---自动生成代码----end----