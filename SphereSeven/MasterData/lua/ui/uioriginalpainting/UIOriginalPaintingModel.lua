---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by win 10.
--- DateTime: 2019/5/24 16:58
---

local UIOriginalPaintingModel = BaseClass("UIOriginalPaintingModel",UIBaseModel);
local base = UIBaseModel
local this = UIOriginalPaintingModel



function this.OnCreate(self)
    base.OnCreate(self);
   
end

function this.OnEnable(self,info)
    base.OnEnable(self)
    self.image = info.image
    self.callBack = info.callBack
    self.scale = info.scale
    self.scaleRot = info.scaleRot
    self.state = info.state
    self.subImage = info.subImage
    self.subImages = info.subImages
    self.cardOffset = info.cardOffset
    self.bgOffset = info.bgOffset
    self.bgCard = info.bgCard
    self.id = info.id
    self.stage = info.stage
    self:OnRefresh();
end

function this.OnRefresh(self)

end


function this.OnAddListener(self)
    base.OnAddListener(self);
end

function this.OnRemoveListener(self)
    base.OnRemoveListener(self);
end

function this.OnDisable(self)
    base.OnDisable(self);
    
end

function this.OnDestroy(self)
    base.OnDestroy(self);
end

return this;
