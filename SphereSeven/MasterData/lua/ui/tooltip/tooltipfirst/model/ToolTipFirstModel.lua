---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by win 10.
--- DateTime: 2019/4/12 17:40
---

---@class ToolTipFirstData
---@field hideCloseBtn boolean
---@field hideBackBtn boolean
---@field hideConfirmBtn boolean
---@field callBack function @ 确认回调 (先默认关闭此页面 再调用回调)
---@field cancelCallBack function @ 取消回调 (先默认关闭此页面 再调用回调)
---@field closeCallBack function @ 关闭回调 (先默认关闭此页面 再调用回调)
---@field cancenSysLangKsy number @ 取消按钮的系统语言key 默认507(取消)
---@field alignment number @ 对齐
---@field message string @ 提示文本
---@field title string @ 标题文本

---@class ToolTipFirstModel : UIBaseModel
---@field data ToolTipFirstData
local ToolTipFirstModel = BaseClass("ToolTipFirstModel",UIBaseModel)
local base = UIBaseModel
local this = ToolTipFirstModel

function this.OnCreate(self)
    base.OnCreate(self);
    self.data = nil;
end

function this.OnEnable(self,data)
    base.OnEnable(self);
    self:OnRefresh(data);
end

function this.OnRefresh(self,data)
    self.data = data;
end

function this.OnAddListener(self)
    base.OnAddListener(self);
end

function this.OnRemoveListener(self)
    base.OnRemoveListener(self);
end

function this.OnDisable(self)
    base.OnDisable(self);
    
end

function this.OnDestroy(self)
    self.data = nil;
    base.OnDestroy(self);
end

return this

