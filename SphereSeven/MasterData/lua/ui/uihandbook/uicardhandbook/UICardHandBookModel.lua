---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by win 10.
--- DateTime: 2019/7/1 16:21
---

local UICardHandBookModel = BaseClass("UICardHandBookModel",UIBaseModel)
local base = UIBaseModel;
local this = UICardHandBookModel;

function this.OnCreate(self)
    base.OnCreate(self);
end

function this.OnEnable(self)
    base.OnEnable(self);
    self:OnRefresh();
end

function this.OnRefresh(self)
    self.cardIds = {}
    self.cardIds[0] = {} -- 全部
    for k,v in table.pairsByKeys(Z_Character) do
        if v.Enable == 1 then
            if self.cardIds[v.Class] == nil then
                self.cardIds[v.Class] = {}
            end
            table.insert(self.cardIds[v.Class], v.Id)
            table.insert(self.cardIds[0], v.Id)
        end
    end
end

function this.OnAddListener(self)
    base.OnAddListener(self);
end

function this.OnRemoveListener(self)
    base.OnRemoveListener(self);
end

function this.OnDisable(self)
    base.OnDisable(self);
end

function this.OnDestroy(self)
    base.OnDestroy(self);
end

return this;