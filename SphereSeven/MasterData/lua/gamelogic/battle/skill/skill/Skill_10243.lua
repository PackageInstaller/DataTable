---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by win 10.
--- DateTime: 2019/5/28 11:12
--- 君临天下
---每回合开始时自身攻击力、暴击伤害＋10%，最多叠加50%"
--自身成功击杀敌人时重置自身所有技能的冷却时间",
--自身使用觉醒技（技能3）后自身获得神行、无双效果持续2回合

local Skill_10243 = BaseClass("Skill_10243",Skill)
local base = Skill

local function __init(self)
end

local function OnUseActiveSkill(self, targetUnit)
    local count = targetUnit:GetComponent("BuffComponent"):BuffTypeCount(true)
    if count > 5 then
        count = 5
    end

    local result = ActiveSkillResult.New()
    local damageScale = self.Params[1] / 100
    local extraScale = count / 5 * self.Params[2] / 100
    local scale = damageScale + extraScale
    local atk = self.__snc:Get(NumericType.Atk)
    local damageNum = atk * scale / self.MultistageNum
    result.FeixiaoDamage = {
        Type = BattleDamageType.Physical,
        Value = damageNum
    }    

    return result
end

Skill_10243.__init = __init
Skill_10243.OnUseActiveSkill = OnUseActiveSkill

return Skill_10243