---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by daiyi.
--- DateTime: 2019/4/18 21:59
---
local ActiveSkillBuff = {
    BuffId = 0,
    TurnCount = 0,
    Hit = 0,
    IsCannotDispelled = false,
    NumericType = NumericType.Max,
    NumericVal = 0,
    
    __init = function(self,buffId,TurnCount,hit,isCannotDispelled,numericType,numericVal, data)
        self.BuffId = buffId
        self.TurnCount = TurnCount or 0
        self.Hit = hit or 1
        self.IsCannotDispelled = isCannotDispelled or false
        self.NumericType = numericType or NumericType.Max
        self.NumericVal = numericVal or 0
        self.Data = data
    end
}

return DataClass("ActiveSkillBuff",ActiveSkillBuff)